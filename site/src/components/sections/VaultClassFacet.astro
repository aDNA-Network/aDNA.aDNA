---
// P1.3: the parent passes a public display label + an attribute-safe slug — raw class enums
// (org_graph, tbd_at_p0) never reach the anchor href/class hooks (gate-27 scans attributes).
interface Props { slug: string; label: string; count: number; }
const { slug, label, count } = Astro.props;
---

<a href={`#class-${slug}`} class={`facet-chip facet-${slug}`}>
  {label} <span class="facet-count">{count}</span>
</a>

<style>
  /* P4 cohesion fix: was hardcoded #f0f0f0/#333 (light-only — broke the dark-first default) plus a
     rogue #4a86e8 blue hover (a third accent). Tokenized to the theme-aware brand surface + the cyan
     --color-link accent (no new hue), matching the site's bordered-pill convention. */
  .facet-chip {
    display: inline-block;
    padding: 0.4rem 0.8rem;
    border-radius: 1rem;
    background: var(--color-bg-alt);
    border: 1px solid var(--color-border);
    text-decoration: none;
    color: var(--color-text);
    font-size: 0.9em;
    transition: background var(--transition-fast), border-color var(--transition-fast), color var(--transition-fast);
  }
  .facet-chip:hover { background: var(--color-surface); border-color: var(--color-link); color: var(--color-link); }
  .facet-count { opacity: 0.7; margin-left: 0.25rem; }
</style>
