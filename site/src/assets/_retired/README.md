# Retired site assets (archive, not delete — Project SO #6)

Assets no longer referenced by the site but preserved for audit / revert.

| File | Retired | Reason |
|------|---------|--------|
| `banner.jpg` | 2026-07-02 (Champollion M4.2) | Superseded by `../aDNABanner.png` (landed M3.2 S3, 2026-05-22; stable in production since). Zero active references in `site/src` verified at archival. Original archival deferral: `how/backlog/idea_banner_asset_cleanup.md`. |

> Assets here are unimported, so Astro does not process them into the build (`dist/`). Kept in tree for the revert path and audit trail. The canonical mirror `what/assets/banner.jpg` is outside the site tree; its disposition was flagged to the orchestrator at M4.2 (same-commit archive recommended).
