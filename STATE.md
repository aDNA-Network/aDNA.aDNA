---
type: state
created: 2026-04-13
updated: 2026-07-14  # 2026-07-14 ✅ **v8.8 SHIPPED** — `campaign_v8_8_release` (Operation Distillery) CLOSED; v8.8 LIVE on aDNA-Network/aDNA (`a32724b` + tag `v8.8`). The accumulated inline PRIOR-chain this key carried (~29.7KB) is archive-shifted **verbatim** → [[STATE_archive]] §Shifted-2026-07-17 (Clear Hearth W-B slice 3; never deleted).
status: active
last_edited_by: agent_rosetta
_state_router_version: "1.0"
tags: [state, governance, router]
state_history: STATE_archive.md   # Clear Hearth graduation 2026-07-17 rides the vault's own immortal-spine (§Shifted-2026-07-17)
---
<!-- Router shape — split from monolithic STATE.md at M2.1 S2 2026-05-19. Historical session prose at STATE_archive.md. -->


# Operational State

Dynamic operational snapshot for cold-start orientation. Updated each session.

> **State router** (split from monolithic STATE.md at M2.1 S2 2026-05-19; pre-split SHA `1e337db`). For historical session prose (19 DEPRECATED-marker `## Last Session` blocks + retired Next Session Prompts) see [[STATE_archive.md|STATE_archive.md]]. Most-recent live session block + most-recent Next Session Prompt stay here.

## ⏭ QUEUED — Next Live Session (READ THIS FIRST)

> ▶ **2026-07-14 — ✅ OPERATION DISTILLERY — v8.8 SHIPPED + LIVE on `aDNA-Network/aDNA`; `campaign_v8_8_release` CLOSED → `completed`.** P3 fired at operator GO (the push IS the release): assembled the P2-ratified ship-set into a fresh image clone → bumped the 5 version surfaces → dry-run gates green (gitleaks **no leaks** w/ new `.gitleaks.toml` `.obsidian/plugins` rider [8 pre-existing minified-JS FPs, **0 in changed files**]; `adna_validate --governance` **Zero drift**; section-header integrity diff = only reference tables left CLAUDE.md) → **path-scoped commit `a32724b` + annotated tag `v8.8`** pushed (main `30f6862..a32724b`, tags-only) → local `~/aDNA/.adna` synced (`3051a2d`) → **7/7 fresh-clone smoke green** (redirect 301 · embed-note · role:template · `.aDNA` ignore all pass). Governance **8.7→8.8**; standard stays **v2.5**; counts hold **30 templates / 32 skills**. **P3 FINDING (operator-ruled at fire-time re-verify):** the P2-frozen **E2** skills-table extraction pointed to `how/skills/AGENTS.md` claiming "the full catalogue" — but that file is a `directory_index` protocol guide, not a catalogue (claim was FALSE) → operator ruled **REVERT E2 (keep the 19-skill table inline)** → final = **aggressive-minus-E3-minus-E2** (CLAUDE.md ~7,720→~5,820 tok, −24%; §7.7 supplement in `ratification_record_v8_8_p2.md`). E1/E4/E5 shipped (`adna_reference.md` + `example_personalities.md`); E3 + Skills table + 16-entity triad inline. **Do NOT re-open — Distillery is COMPLETE.** Follow-ups: dev-vault record-keeping push (operator-elected, SO-9); site install-truth fixture reflects v8.7 sha → regen at next site touch. AAR `aar_v8_8_p3_fire.md`; session `session_stanley_20260714T200550Z_v8_8_p3_fire`.

> ▶ **2026-07-13 — ✅ OPERATION CLEANROOM — v8.7 SHIPPED + LIVE on `aDNA-Network/aDNA`; `campaign_v8_7_release` GRADUATED → `completed`.** Cold-start: Storyweave graduated (below) → no active campaign; operator "continue the campaign" → surfaced the choice → picked the **v8.7 release queue** (Ouroboros's designated successor), scope = **all 4 low-risk riders** (item 2 STATE `phase:`/`campaigns:` keys · item 3 image `how/templates/AGENTS.md` index completion · item 4 `skill_git_remote_setup` fleet-name genericize · item 5 visual-inspection doctrine fold). Governance **8.6 → 8.7**; **standard stays v2.5**; **no count bump** (no new skills/templates). Items 1 (node_manifest — ADR-gated, Hestia-led) + 6 (Batch B — own sub-campaign) held OUT. **P0 done** — chartered `campaign_v8_7_release` (charter + CLAUDE.md + `mission_v8_7_p1_rider_authoring` + session). Shipped commit `1c30f3b` + annotated tag `v8.7` (main `32d4dd5..1c30f3b`, tags-only); local `~/aDNA/.adna` synced (`14a1d43`); 7-row fresh-clone smoke green; `adna_validate --governance` Zero drift. Governance **8.6→8.7**; standard stays **v2.5**; no count bump. Badge-completion follow-up `30f6862` (README governance v8.5→v8.7; tag unmoved). Follow-ups: gitleaks `.obsidian/plugins` allowlist; held items 1 (node_manifest→Hestia) + Batch B. **Do NOT re-open** — next release = v8.8 queue. **P2 ratify + P3 fire = operator gates** (`skill_template_release`, tags-only, dry-run-then-pause). Plan: `please-read-teh-claude-md-playful-valiant.md`.

> ▶ **2026-07-13 — ✅ Storyweave P5 M5.3 (a11y/perf/reach — THE CAPSTONE) SHIPPED + LIVE + `campaign_storyweave` GRADUATED → `completed`.** The DE-RISKED "hold, don't fix" capstone, built + shipped [[mission_p5_3_a11y_perf_reach]] (O0–O8) in one session; deploy `dpl_HeNviXYNLo3EeX68HuAbnGop3FVo` (adna.network `/`·`/privacy`·`/get-started` 200, live T0 axe-0). **Folded O4:** a visible hero keyboard-focus ring (`.hero-graph-nodelist a:focus-visible`, system `.btn` treatment) + graph-cohesion verified (NetworkDiagram = one shared tokenized component, home §1 ≡ /network; canvas-vs-SVG data-drift documented). **Perf hold:** gate-19 kept hard + sweep extended to `/get-started` + `/learn/what-is-adna` (both Perf 100 / LCP ~446 ms). **Reach:** OG share-card dims+alt (gate-6-locked) · an **HONEST zero-tracking `/privacy` notice** (operator-confirmed: no analytics/cookies/tracking, self-hosted fonts, static-on-Vercel with honest server-log disclosure, footer-linked, gate-4-locked) · i18n/low-bandwidth PLAN (deferred). **Long-tail axe = 0** both themes (~33 routes; gate-4 broadened 26→36). **371 gates green · home Perf 100/CLS0/TBT0.** Capstone 4-lens ranker **4.89** (Design-Critic 4.75 [hero-letterbox] · Newcomer 4.90 [no live demo] · Adopter 4.95 · Movement-Skeptic 4.95) — **honest, short of ≥4.95, gated ENTIRELY by 2 operator-deferred in-person items**; operator GO'd **"ship + graduate now at 4.89."** **⏭ Storyweave is COMPLETED — no in-vault work remains.** Follow-ups (IN-PERSON dev-rel): hero-letterbox full-bleed re-cut (imagen; 4 doc-heroes incl /get-started) + R1 demo screencast (M5.1b) → landing both **re-ranks ≥4.95**. **GOTCHAs:** the T0 axe harness analyzes only `themes[0]` (put **light first**) · home LH needs `--preset=desktop` (default mobile-throttle = a 97/2.26 s artifact) · redact with `${VAR:+SET}` ONLY (a `${VAR:-…}` fallback **leaked the throwaway SS_VERCEL_TOKEN** this session) · **no clean mechanical letterbox fix** (auto-crop = variable + retina-upscale + a compositional call → it's genuine imagen work). Ranker: [[m5_3_capstone_ranker]]. Commits `e45e9c1..` (this session; push operator-gated, SO-11). Session `session_stanley_20260713_124816_storyweave_p5_m5_3`.

> ▶ **2026-07-12 — ✅ Storyweave P5 M5.2 (Craft + design-system) SHIPPED + LIVE on adna.network.** Operator chose **M5.2** over the in-person-deferred M5.1b. Built + shipped [[mission_p5_2_craft_design_system]] (O0–O3 build · O4 folded→M5.3 · O5 gate+ship · O6 AAR) in one session. **B9 craft:** a word-safe `excerpt()` util (`site/src/utils/text.ts` — word-boundary-only + idempotent, so clean cards stay byte-identical + `AGENTS.md`/`3.8+` internal dots survive) applied in `CardGrid` → the ~11 index pages' card excerpts go word-safe (Unicode `…`, never ASCII `...`; use-cases 6/6 + reference mid-word cuts fixed, live-verified) · `CardGrid` `minmax(min(track,100%),1fr)` → no 320px card clip · copy fixes (commons "share back" clause · `tool-setup.mdx` list→prose · `design-rationale.mdx` artifact) · **new gate-24-copy-craft** + **gate-9 extended** (5 index pages + a per-card overflow check catching the `overflow-x:hidden`-masked clip). **B11 hardening:** `--font-weight-*` tokens · VaultCard↔RegistryCard harmonized (padding→`--space-4`, shared `--color-primary` hover; density kept) · **`site/src/styles/palette.ts`** single-sources the Mermaid Tokyo-Night palette (byte-identical, both themes T0-verified) · **new gate-25-token-discipline** (inventories + LOCKS every deliberate hardcoded hex behind a dated allowlist — *finding: the residual hex is all deliberate [terminal chrome · macOS traffic-lights · AA-tuned status/btn · hero scrim · B1 orphan], not drift → a non-regression fence*). **NEW `/design-system` page** (`site/src/pages/design-system.astro`) — a token-driven living reference (swatches · type scale · fonts+weights · spacing · radius/shadow · component samples), footer-linked, noindex, passes its own gate-25 + gate-4 both themes, self-referential (SO#8). **Latent AA bug FIXED**: CardGrid badges (`updated/deprecated/experimental`) used raw `--color-info/warning/accent` as text on a 15% tint → **1.9–2.45:1 in light mode** (live on the 2 index pages that wire `badge:`); the `/design-system` showcase surfaced it → darkened via the proven status-pill `color-mix(…, --color-text)` pattern → AA both modes. **Verify:** full `npm run test:gates` **359 passed** (315 M5.1a baseline + 44 new) · T0 both themes axe-0 on all changed surfaces · **home Lighthouse Perf 100 / CLS 0 / TBT 0 / LCP 0.5s** (home structurally untouched) · **ranker 4.5** (Design-Critic/Newcomer/Adopter/Movement-Skeptic all 4.5, source-verified) · deployed `dpl_HicFkr9DmPPynDX1r1LzADxdD5Co` → adna.network 200, `/design-system` live, B9 fix live (ASCII `...`=0, `…`=6), live T0 axe-0. Commits **`b3a396d..2c93864` PUSHED → origin/main** (gitleaks clean, SO-11). **Residuals (documented, non-blocking):** hero-letterbox PNG re-cut (4 doc-heroes; asset/imagen work) · 51-file frontmatter `<meta>` regen (`scripts/transform-content.mjs`) · HomeHero canvas/scrim palette + stability-color dedup (gate-25 allowlist) · image-palette build check · O4 folded (graph cohesion + hero keyboard-focus). **⏭ NEXT = M5.3** (a11y/perf/reach — DE-RISKED "hold, don't fix": hold gate-19 LH budget · OG imagery · i18n/low-bandwidth *plan only* · legal/privacy notice · long-tail axe · + the folded O4 items land here under its home-Lighthouse gate); then the **capstone ranker ≥ 4.95** graduates the campaign to `completed`. **⚠ token-redaction note:** a shell `${VAR:-…}` fallback echoed the throwaway `SS_VERCEL_TOKEN` once this session — use `${VAR:+SET}` only. Session `session_stanley_20260712_205433_storyweave_p5_m5_2_craft`.

> ▶ **2026-07-12 — ✅ Storyweave P5: O3 PUSHED + M5.1b (R1 demo) prepped for the in-person dev-rel build.** Pushed O3 (`8e7f67c` → origin/main, gitleaks clean, SO-11; front_page_doctrine §10 durable). **The M5.1b demo asset (the real recording) + its final format are operator-DEFERRED to an in-person session with the new dev-rel contributors (week of 2026-07-13)** — the recording IS the deliverable, so M5.1b's shippable core waits. Authored a **turnkey build brief + recording runbook** [[m5_1b_demo_build_brief]]: placement = the "How it Works" section + `/get-started` (**NOT** the hero — the P1.6 living-network graph stays the single hero focus); mirror the **P1.6 Canvas-over-SVG** progressive-enhancement pattern (ASCII mock = the no-JS/a11y floor sizing the box → CLS 0; player = the enhancement — opacity cross-fade + `requestIdleCallback` + `IntersectionObserver`, off LCP, reduced-motion → poster); the full **gate/CSP/perf checklist** (gate-4 axe `video-caption` trap · gate-11 `hero-stage-img` count · CSP `default-src 'self'` + the **asciinema `blob:`-worker preflight** · **manual** home Lighthouse Perf≥99/CLS0/TBT0); the recommended demo script (clone→`claude`→orient-from-`STATE.md`→one task); and **BOTH recording paths** (A asciinema cast — Rosetta's lean, lightest + terminal-native + no `<video>` a11y risk; B muted `<video>` — richest real TUI, CSP-clean). Docs only — **zero `site/` edits, nothing shipped.** **⏭ NEXT = the in-person M5.1b build (opus): pick the format [asciinema lean, after the CSP-worker preflight] → record a REAL session → build the player + poster + gates → hold home CLS0/TBT0/Perf≥99 → ranker ≥4.0 (Movement-Skeptic honesty) → operator ship-gate → deploy. Then M5.1c (R4/R5) / M5.2 (craft); exit ranker ≥4.95.** Session `session_stanley_20260712_195714_m5_1b_prep`.

> ▶ **2026-07-12 — ✅ Storyweave P5 O3 (the R1 ≥6-exemplar install-forward reference pass) DELIVERED + §10 RATIFIED durable (operator, ⛩ O3 gate 2026-07-12).** A DOCTRINE/RESEARCH pass ([[skill_reference_inspection]]) — **zero `site/` edits, no build, no deploy, no push.** Inspected 6 exemplars through an install-forward / demo-as-proof lens (Hermes·Val Town·Raycast·Vercel·Bun·Astro — both tonal poles [Hermes 65 ≥60; Astro/Vercel 35, Bun 38 ≤40]; adds the CLI/clone-and-run pole the June hero-tonal set lacked; headless captures all 200, dark+light, in `onboarding_references/{name}_capture/`). **Outputs:** 2 new durable artifacts [[site_bun]] + [[site_astro]]; install-forward addenda on [[site_valtown]]·[[site_raycast]]·[[site_vercel]] (Hermes already re-lensed 2026-07-11); refreshed durable [[_reference_set]] §Install-forward synthesis (site_count 10→12); and **R1–R5 promoted candidate → durable [[front_page_doctrine]] §10 (marked PROPOSED)** with per-rule provenance. **The gate's whole point — the R1 demo *format* — is de-risked:** across all 6, *no one autoplays video-with-sound*; the CLI peers Bun/Astro hero **no video at all** (benchmark + copy-pasteable code), and only **Hermes** (whose product IS an agent doing work) heroes a "video of usage" → **R1 = the lightest honest product-in-action: an `asciinema`-style terminal cast or an optimized muted-lazy `<video>`/GIF, poster = the existing ASCII mock, lazy-loaded below LCP — never a cinematic autoplay** (holds the hard home budget CLS 0 / TBT 0 / Perf ≥99; Movement-Skeptic gate: a real recording, never staged). R2/R3 recorded as already-shipped (M5.1a); currency finding — Val Town + Vercel homepages drifted since June (Vercel's install is now demoted below the fold). Skill advance gate MET (≥6 Tier-A-full · both tonal poles · functional spread · every rule provenance'd). **⛩ RATIFIED (operator, 2026-07-12): §10 is durable.** **⏭ NEXT = O4 = M5.1b** — build the R1 demo-as-proof in the ratified format (`asciinema`-style cast or muted-lazy `<video>`/GIF, poster=ASCII, below LCP; a **real** recording of clone→`claude`→the agent orients from `STATE.md` + does one real task), holding home CLS0/TBT0/Perf≥99, no-JS/a11y baseline, per-increment ranker ≥4.0 (Movement-Skeptic gates honesty). M5.1c (R4/R5 adjacents) / M5.2 (craft) also available at operator direction. **O3 committed LOCAL; push operator-gated (SO-11).** Session `session_stanley_20260712_183151_storyweave_p5_o3_reference_pass`.

> ▶ **2026-07-12 — ✅ Storyweave P5 re-plan RATIFIED — the capstone is authorized (onboarding leads; R1 gated behind a ≥6-exemplar pass).** The charter's **measure-before-re-plan** gate, delivered in one session ([[mission_p5_replan]] COMPLETED + AAR — a PLANNING mission, **zero `site/` edits, no deploy, no push**). **Measured live** ([[measure_summary]], 2026-07-12): **T0** 6 surfaces ×2vp ×2themes +`--axe` — all **axe-0**, 0 console errors, loads < 1 s → **P4 held + landed** (`/learn` 798/0h2 → **2526/5h2** ordered path · `what-is-adna` **8492→6411** · `/reference` spec-led + version stamp); **T1 21/21** (`scripts/p3_interactions.mjs` — registry search/filter · 68===68 · hero+graph deep-link twins · on-ramps; the 1/68 no-purpose = `zeta.aDNA`, the known Hestia thread); **Lighthouse** home **Perf 99 / CLS 0 / TBT 0** (the budget R1's demo must hold). **Re-plan authored + ratified** ([[p5_replan]], `accepted` §7.7 · stanley · 2026-07-12): the **capstone** = **M5.1 Onboarding** (5.1a R2 hero-install + R3 MIT eyebrow ship **FIRST** on the current basis, single-sourced [gate-12/`standard.ts`]; **5.1b R1 demo-as-proof gated behind a ≥6-exemplar install-forward reference pass** [Val Town·Raycast·Bun·Vercel·Astro] to de-risk format + promote to durable doctrine — the operator's sub-decision at the gate; 5.1c R4/R5 adjacents) · **M5.2 craft/design-system** (B9 truncation · B11 card-tier harmonize · `/design-system` · kill hardcoded hex · P1.6/P3 held follow-ups) · **M5.3 a11y/perf/reach** (DE-RISKED: axe already 100 + perf tight-green → hold-budget/OG/i18n-plan/legal); exit = **capstone ranker ≥ 4.95** → campaign `completed`. Fresh LH for `/get-started`+`what-is-adna` deferred (intermittent node-shell PATH flake → exit 127 after 3 tries; non-blocking — P3 baseline 99–100 + T0 loads <1s cover them). **⏭ NEXT = author `mission_p5_1_onboarding` (a fresh session; M5.1a first) — do NOT auto-advance into building at this gate.** Residuals unchanged/non-blocking: untracked P4 evidence · the parked Exchange→Rosetta coord memo · a candidate upstream `.prose`-table a11y idea · Hestia data-currency (`headline_mission` 0/68, `zeta.aDNA`). This close (`6f35fbb`) is **PUSHED → `origin/main`** (gitleaks pre-push clean; operator-authorized SO-11). Session `session_rosetta_20260711_235750_p5_replan`.

> ▶ **2026-07-11 — 🧭 Storyweave WIND-DOWN — Hermes onboarding reference captured; next = the P5 re-plan (on Opus).** A reference + planning turn (no `site/` code, no deploy). The operator surfaced the **Nous "Hermes agent"** landing page (`hermes-agent.nousresearch.com`) — "a nice video of usage, direct install links" — as a design reference. Reviewed it **headless** (`scripts/visual_capture.mjs` + WebFetch): the "video of usage" is a large **"HERMES DESKTOP — BETA PREVIEW"** product panel (the agent visibly drafting release notes → committing → opening a PR), and **install is hero-level** (an "Install desktop app" button **+** an "Install via terminal" `curl … \| bash` one-liner). **The gap on adna.network:** zero video/GIF anywhere (proof-of-life = the constellation hero + three *static ASCII terminal mocks*), and its real install one-liner (`git clone …/aDNA.git ~/aDNA && cd ~/aDNA && claude`, single-sourced from `install_truth.json`) lives **only in `/get-started`**, never the home hero. Crucially this is **not new scope to invent** — [[front_page_doctrine]] already names a **"one demo"** pattern + cites Hermes, but no backlog item (B1–B13) ever built it. **Captured** into the campaign's reference machinery (per [[skill_reference_inspection]]): `how/campaigns/campaign_storyweave/artifacts/onboarding_references/{_reference_set.md, synthesis_onboarding_guidance.md}` — 5 provenance-tagged **candidate** rules, led by **R1** (a real ~30–60s screencast of the agent navigating a vault) + **R2** (surface the one-liner install + copy-button on the home hero); durable exemplar [[site_hermes]] got an onboarding-UX pointer. **Folded** into [[p4p5_replan]] §Phase 5 as a **prioritized candidate thread** with the guardrails a demo video must respect — the CWV budget (lazy-load + poster + no autoplay-sound, hold `/` Perf 1.00 / LCP), the no-JS/a11y baseline (captions/controls; the ASCII mock is the honest poster), and a **real recording, not staged** (Movement-Skeptic). **⏭ NEXT = the Storyweave P5 re-plan (a fresh session, on Opus)** — the charter's *measure-before-re-plan* gate: measure the shipped P4 surfaces live, fold in this onboarding reference, then re-plan + ratify P5 (which now leads with the demo-as-proof + hero-install thread). **Planning only — do NOT build.** Session `session_rosetta_20260711_233758_hermes_ref_windown`.

> ▶ **2026-07-11 — ✅ Storyweave P4 (Docs IA) M4.2 SHIPPED + LIVE on adna.network — the docs IA is reconciled; P4 CLOSED.** Built + shipped **M4.2** (O3+O4) in one session against the operator-approved plan. **`/learn`** is now an **ordered path** (was a near-empty 4-card hub, 0 h2 → 5 h2: Start here → 1·Understand → 2·Practice → 3·Compare → Where to next, + a sticky TOC). **`/reference`** **leads with the spec** — a "The specification / `Standard v2.5`" callout (EV6, from `STANDARD_VERSION`) — then **groups by genre** (Rationale & guides · Governance & quality · Craft · Resources); the **8-vs-10 drift is resolved** (the 2 orphaned docs surface under **Craft**) via a new single-source `site/src/data/reference-ia.ts` shared by the index + sidebar. **The two docs-shell navs are reconciled:** `Header.astro` now single-sources a `topNav` model in `navigation.ts`, and the flat "Reference" link became a **no-JS "More ▾" disclosure** (operator-chosen at an AskUserQuestion gate) surfacing Reference/Glossary/Guides/For-you — closing the gap without overflowing the 1024px row (the panel is `position:absolute`; the disclosure sits OUTSIDE `.nav-desktop` so gate-13's `.nav-desktop a` selector is unaffected — the real trap of this change). **`/compliance`** gains an **honest named-regime bridge** (EV7): a bold "aDNA is an open documentation standard, not a certified product… certification is granted by accredited assessors" caveat above a SOC 2 / ISO 27001 / EU AI Act mapping table. **Verify:** `npm run test:gates` **313/313** (twice) · **full-axe 0** across 5 surfaces × 2 vp × 2 themes — the T0 full ruleset caught a `scrollable-region-focusable` on the compliance table at 375px that the gates' WCAG-AA filter misses (fixed `tabindex="0"`+`aria-label` on the table, the `<pre>` precedent) · **Lighthouse 100/100/100/100** on `/learn`+`/reference`+`/compliance` (LCP ≤0.5s, CLS 0 — budget held, no JS island) · ranker **4.33** (Newcomer 4.4 · Educator 4.3 · Framework-Docs 4.3). Deployed `npx astro build` → `vercel --prebuilt --prod` → **live-verified** (3 surfaces 200, markers present, More menu live; **live-axe 0 across 12 runs**). Commit **LOCAL on main — push operator-gated (SO-11)**. Mission [[mission_p4_docs_ia]] **COMPLETED + AAR (O5)**; **P4 CLOSED**. **⏭ NEXT = P5 re-plan (provisional baton) — the charter's measure-before-re-plan gate runs after a P4 live-measure; do NOT auto-advance into P5.** Evidence: `how/missions/artifacts/storyweave_p4_docs_ia/{measure_m42.md, m42_capture/, lighthouse/}`. Session `session_rosetta_20260711_215636_p4_m42_docs_ia`.

> ▶ **2026-07-11 — ✅ Storyweave P4 (Docs IA) M4.1 SHIPPED + LIVE on adna.network — `/learn/what-is-adna` reframed for comprehension.** Authored [[mission_p4_docs_ia]] (P4 build; O0–O5 with ⛩ gates at O2/O4; `executor_tier: opus`) then built + shipped **M4.1** in one session. The flagship explainer now **teaches the north-star fast:** lead reframed to the payoff (build · operate · understand — without re-explaining every session) · **rendered density 8492→6416 (−24%)** · **prose reading grade FKGL 12.9→10.5, passive 6.5%→0%, avg sentence 26→20.7 w** · **sticky "On this page" TOC wired** — the page authored inline HTML so it *never passed `headings` to `DocumentationLayout`* → the TOC was **never rendered** (not styling-broken); fixed via the **existing optional prop, no layout fork** (a clean P3 "verify-before-scoping" catch) · the **16-entity table + the CLAUDE.md example progressively disclosed** (native `<details>`, no-JS-safe; a visible "4 WHO · 5 WHAT · 7 HOW" line keeps the claim on-page). **Gates:** `npm run test:gates` **313/313 green** (axe-0 both themes · LH budget held · responsive · no-JS · credibility/meta/claim-trace intact) · persona ranker **4.23 ≥ 4.0** (Newcomer/Educator/IA). Deployed `npx astro build` → `vercel --prebuilt --prod` → **verified live** (north-star lead + TOC + 2 disclosures present; live T0 status 200 · bodyLen 6416 · axe-0). Commit `8fe4eee`, **PUSHED** (`3bc87ca..8fe4eee` — also synced the 6 previously-held commits; gitleaks clean). **Honest flag:** hit **−24%** (not the guideline "~35%") by *disclosing, not deleting* — every artifact preserved; **operator accepted the comprehension-first result at the O2 gate.** **⏭ NEXT = M4.2** (`/reference` + `/learn` IA — a fresh session, O3–O4): `/learn` ordered path · `/reference` lead-with-spec + version stamp (EV6) · group-by-genre · **reconcile the two docs-shell navs** (flat `Header.astro` vs unified `navigation.ts`) · `/compliance` regime bridge (EV7); **then P4 close (O5) + P5 re-plan** (still provisional). Evidence: `how/missions/artifacts/storyweave_p4_docs_ia/`. Session `session_rosetta_20260711_203748_p4_docs_ia`.

> ▶ **2026-07-11 — ✅ Storyweave P3 MEASURED live → P4/P5 re-plan RATIFIED; P4 (Docs IA) AUTHORIZED, P5 provisional.** The charter's **measure-before-re-plan gate**, delivered in one session ([[mission_p4p5_replan]] COMPLETED + AAR — a PLANNING mission, **zero `site/` edits, no deploy, no push**). Three headless passes against **live adna.network**: **T0** static (15 surfaces ×2vp ×2themes +`--axe`), **T1** interactive **21/21 pass** (`scripts/p3_interactions.mjs` — drove search · the 15 class-facet filters · adopt/publish rail · hero + graph deep-link twins · **68 rendered === 68 declared** · illustrative labels · on-ramps), **Lighthouse** 6 surfaces (**Perf 99–100 · A11y/BP/SEO 100 · LCP 1.4–1.8s · CLS ≤0.001 · TBT 0**). **P3 verified live + clean:** the nested-`<main>` axe bug is **cleared both themes** on `/vaults` + `/vaults/[slug]`; the only live gap is `zeta.aDNA`'s missing purpose line (the known, non-blocking Hestia `headline_mission` thread). **The measure sharpened the arc** ([[p4p5_replan]], `accepted` §7.7 · stanley · 2026-07-11): **P4 = comprehension/IA, not perf** (`what-is-adna` scores Perf 100 / LCP 1.4 s despite 8492 chars → the "trim ~35%" is cognitive) · **P5 materially de-risked** (axe cleared + perf best-in-class → M5.2 shrinks to hold-the-budget + OG imagery + i18n *plan* + legal/privacy; craft debt B9/B11 + hero focus polish stay in M5.1). **Folded:** the untracked Berthier→Rosetta graph-exchange memo answered as a **draft** spec-placement ruling ([[coord_2026_07_11_rosetta_to_berthier_exchange_graph_spec_placement]] — proposes `spec_graph_exchange.md` in `what/specs/` citing Exchange's `adna-manifest/v0` + M-S6; publishable-graph minimum; ownership split confirmed) — **operator held it as draft** (non-blocking, at-cadence). Evidence: `how/missions/artifacts/storyweave_p3_measure/` (`measure_and_review.md` · `capture_report.json` · `lighthouse/` · 5 cited screens · `p3_interactions_result.txt`). **⏭ NEXT = author `mission_p4_docs_ia`** (P4 build — a **separate, fresh session**); **P5 stays provisional** (re-plan after P4 ships + is measured — same gate). This close is the 6th local commit on main; **nothing pushed** (SO-11). Session `session_2026_07_11_storyweave_p4p5_measure_replan`.

> ▶ **2026-07-11 — ✅ Storyweave P3 (Actionability + Registry) SHIPPED + LIVE on adna.network — both increments (M3.1 + M3.2) in one session; `mission_p3_actionability` COMPLETED + AAR.** The registry is no longer browse-only: `/vaults` now shows a **one-line purpose per card**, a working **client search + 15 class-facet filters** (no-JS → the SSR grouped list), and an **adopt/publish rail**; every on-ramp (`/get-started`, `/community` ladder, `/use-cases`, `/commons`, home §1's S7 4-pillar payoff) now **lands somewhere real — no browse-only dead-ends**. **Key finding:** the mission's "single highest-value fix" assumed `vaults.json.headline_mission` was populated — it is **0/68** (never authored in Home.aDNA; `tagline` also 0/68). Shipped `headline_mission || note` (`note` 67/68); **operator ruled keep the fallback** + a Hestia memo routes the enrichment ([[coord_2026_07_11_rosetta_to_hestia_headline_mission]]). Also **un-dropped 4 `tbd_at_p0` genesis vaults** the registry silently hid while claiming 68 (→ all 68 render); nested-`<main>` axe fix was in **3** pages (not the measure's 2); fixed one dark-mode AA miss (`filter-count` 4.35:1) at the gate. Both increments: **313/313 `test:gates` green · live axe-0 both themes**; deployed `vercel --prebuilt --prod`, verified live. Commits `5b9be4c` (M3.1) + `fb5676f` (M3.2), **local on main — push operator-gated (SO-11)**. **⏭ NEXT = MEASURE P3 live, THEN re-plan P4/P5** (the charter's measure-before-re-plan gate; P4/P5 stay provisional — do NOT auto-advance into P4). Deferred to P5: B9 craft (the `/use-cases` mid-word `CardGrid` truncation, still live) · B11 (VaultCard/RegistryCard tier harmonization) · hero nav-twin sighted-keyboard-focus polish. Session `session_2026_07_11_storyweave_p3`.

> *(QUEUED banners 2026-07-08 → 2026-07-05 — Storyweave P0–P1.6 · Ouroboros · Meridian · Concord · Fleet-Re-Seed, all closed — archive-shifted → [[STATE_archive]] §Shifted-2026-07-17 [Clear Hearth W-B slice 3]; never deleted. Kept live: the 07-11+ Storyweave P3→P5 + v8.7/v8.8 release arc.)*

> *(Older QUEUED banners archive-shifted → [[STATE_archive]] §Shifted-2026-07-06; never deleted per SO-6.)*

## 🌐 adna.network — LIVE on Cloudflare (2026-05-31)

**The aDNA site is live at https://adna.network** (canonical public face; adna.dev abandoned). **First executed ADR-031 Cloudflare migration** — API-driven, single operator-present session (operator hands-on = 1 token + 1 nameserver change). CF zone `667a2d5e…` (account `e048a126…`; NS `keanu`/`sierra.ns.cloudflare.com`); apex + www → Vercel `adna-docs` (`prj_SBKnZf…`) via **CNAME flattening**, DNS-only; **Mailgun email preserved** (MX×2 / SPF / `smtp._domainkey` DKIM / strict `_dmarc p=reject`). Canonical flipped: `site/astro.config.mjs` `site:` → adna.network + Vercel `SITE_URL` env + **prebuilt** redeploy (`dpl_AxbEXjuF…`; local build required — `prebuild` reaches `../scripts`); live `<link rel=canonical>` + 159-URL sitemap emit adna.network. **CF token onboarded to Home.aDNA broker** (Keychain `CLOUDFLARE_API_TOKEN` + zshrc + inventory C47; 1P backup = operator Touch-ID one-liner pending). **Skill graduated to SiteForge** — `skill_cloudflare_dns_cutover` v1.0.0 (ADR-031 successor-ownership relocated per operator directive; SiteForge owns build+deploy+DNS). **Operator override:** M5.7 O6 domain-cutover executed early — DNS/canonical only; **renamed no content** (honors the aDNALabs "nothing renames before the brief" broadcast). WARN: `SS_VERCEL_TOKEN` leaked again in a Vercel-CLI error (recurring incident) -> rotate. Seeded turnkey for next in-vault session: `worldgeno.me` (wga.aDNA M02, unblocked) + `stanley.science` (ScienceStanley M14). Doctrine: [[adr_031_cloudflare_dns_site_publishing_standard]].

## Current Phase

> *(Current-Phase rows 2026-07-01 → 2026-07-03 — the Champollion G0→G5 ladder + STR Track-C close — archive-shifted → [[STATE_archive]] §Shifted-2026-07-17 [Clear Hearth W-B slice 3]; never deleted.)*

> *(Current-Phase activity older than 2026-07-01 archive-shifted → [[STATE_archive]] §Shifted-2026-07-02 (Champollion M1.5); 48 bullets, never deleted per SO-6. This router keeps the recent live arc (Champollion 2026-07-02 + STR close 2026-07-01); older bullets archive-shifted, trim to the next diet.)*

## Active Campaigns

### `campaign_v8_8_release` (Operation Distillery — **⏳ ACTIVE**; chartered 2026-07-13; **P1 ✅ + P2 ✅ ratified → awaiting P3 fire [operator GO]**)

`how/campaigns/campaign_v8_8_release/` — the **v8.8 template release** (governance `8.7 → 8.8`; **standard stays v2.5**): **Batch B** template-quality work deferred from v8.6+v8.7 — prune the base `.adna/CLAUDE.md` (~7.7K tok / 447 ln; P1 produces conservative + aggressive diffs, **operator rules depth per-section at P2 [DP1]**) + a re-scoped III pass on the real inner (157 ln) + root (88 ln) READMEs. 4 phases (P0 Charter ✅ · P1 Author ✅ · P2 Ratify ✅ [aggressive-minus-E3, 2026-07-13] · P3 Fire ⏳ [operator GO]) / 1 mission / 3-5 sessions; `executor_tier: opus`. **P1 staged the full evidence package** in `artifacts/`; **P2 ratified** (§7.7 `ratification_record_v8_8_p2.md`) → frozen ship candidate `staged_claude_md_ratified.md` (**~5,335 tok / −31%**; E3 Visual-inspection kept inline). Ship-set = ratified CLAUDE.md + `what/docs/adna_reference.md` + `how/templates/example_personalities.md` + inner-README III + root badge bump + gitleaks rider; counts 30/32 hold. `.adna/` untouched. OUT: node_manifest (→ Hestia). Ideas: `idea_upstream_claude_md_prune` (M01 lessons) + `idea_inner_readme_iii` (re-scoped premise). Plan: `please-read-teh-claude-md-playful-valiant.md`.

> *(Completed-campaign entries — Cleanroom v8.7 · Meridian · operation_adna · feedback_loop · keystone · looking_glass · STR · network_audit · v2_infrastructure · Completed Mini-Campaigns — archive-shifted → [[STATE_archive]] §Shifted-2026-07-17; never deleted. Kept live: ACTIVE Distillery + Pending/seeded below.)*

### Pending Campaigns (seeded but not yet open)

#### `campaign_obsidian_deployment_stabilization` (NEW successor — seeded 2026-05-13 at M-LWX-03 S2 Phase L; **ABSORBED by `campaign_adna_serious_tool_readiness` 2026-05-17**)

`how/campaigns/campaign_obsidian_deployment_stabilization/` — implementation-focused successor to `campaign_lattice_workspace_ux`. Owner: Rosetta. Strategic intent: make the Obsidian deployment of every aDNA vault stable, standard, and self-stabilizing. **ABSORBED into `campaign_adna_serious_tool_readiness` Phase 3** (Forge Ecosystem Hardening) 2026-05-17 — 8 tracks T1-T8 distributed across v8 missions M3.1-M3.4. Stub directory preserved for audit (per Standing Order #6 archive-not-delete); status `planned` stays in stub frontmatter; effective status `absorbed_by: campaign_adna_serious_tool_readiness`. 7 backlog files F-S2-1..8 (in `aDNA.aDNA/how/backlog/`) source the v8 Phase 3 work directly.

#### `campaign_validation_node_adna_lwx_outputs` (NEW successor — seeded 2026-05-13 at M-LWX-03 S2 Phase L; lives in `lattice-labs/`)

`lattice-labs/how/campaigns/campaign_validation_node_adna_lwx_outputs/` — validation-focused successor; dispatched to Carly + Herb. Owner: Berthier (lattice-labs). Strategic intent: validate M-LWX-01/02/03 outputs on operator-owned machines via Carly+Herb dispatch. Phase 1 narrow: M-VNAL-01 covers outstanding O4 (wikilinks), O5 (cross-vault links), O6 (marketplace), O3-extended (full vault tables). Phase 2+ broader: recurring "Carly+Herb validate-all-aDNA-features" pattern — the FIRST instance of an explicit validation-dispatch campaign for aDNA work. Coord memo at `lattice-labs/who/coordination/coord_2026_05_13_carly_herb_node_adna_validation_dispatch.md`. Status: `planned`; opens when Carly + Herb each acknowledge the coord memo.

#### `campaign_adna_v3_ecosystem_compliance` (planned successor — seeded 2026-05-08)

`how/campaigns/campaign_adna_v3_ecosystem_compliance/` — applies v7.0 changes per-vault to the 19 active aDNA ecosystem vaults. Strategic intent: bring the lattice into full v7.0 compliance after the standard codifies it. Preliminary phase structure: P0 planning + P1 audit + P2 bulk skill upgrade + P3 git remote setup + GitHub naming standardization + P4 airlock adoption + workspace router resync + P5 final ecosystem audit + AAR. Preliminary mission outline: M01-EC (per-vault audit) → M02-EC (bulk skill upgrade) → M03-EC (git remote setup) → M04-EC (GitHub repo rename) → M05-EC (airlock adoption) → M06-EC (workspace router resync) → M07-EC (final audit + AAR). Estimated 12–20 sessions (recalibrated by M01-EC). Persona: Rosetta continues. **Opens at v2 P3 phase gate** (post-M03 flatten + M08a/M08b shipped); M11 of v2 finalizes the mission tree before this campaign opens.

> *(Operation Rosetta [absorbed by STR] + its phase table archive-shifted → [[STATE_archive]] §Shifted-2026-07-17; never deleted.)*

## Phase 7 Progress → archived
> *(Operation Rosetta Phase-7 100-cycle III loop + Persona Ranker Summary (done 2026-04-26; ranker 5.00) archive-shifted → [[STATE_archive]] §Shifted-2026-07-02, Champollion M1.5. Historical; live handoff = ⏭ QUEUED above.)*

## What's Working → archived
> *(the Phase-7-era site snapshot (117pp / 47 gates / "5 reviewer personas" — superseded: site 179pp, 16 personas, 281+ gates) archive-shifted → [[STATE_archive]] §Shifted-2026-07-02, Champollion M1.5. Historical; live handoff = ⏭ QUEUED above.)*

## Active Blockers

None.


## Next Steps → archived
> *(the 2026-06-24 keystone-DP2 Next-Steps snapshot (superseded by the ⏭ QUEUED banner above) archive-shifted → [[STATE_archive]] §Shifted-2026-07-02, Champollion M1.5. Historical; live handoff = ⏭ QUEUED above.)*

## Pending Manual Actions

- **✅ 2026-06-10 RESOLVED — upstream PR #8** (`docs/install-root-flip`): **PR #7 merged** the same flip that morning; **PR #8 CLOSED-AS-SUPERSEDED** (residual stale-slug fixes folded into the same-day public-face docs pass). The template repo was then **archived as `aDNA-Network/adna-legacy`** (frozen `74cb761`; old URLs redirect) and **`aDNA-Network/aDNA` stood up as the canonical clone-and-run workspace image** (tag `v7.1` @ `e0fbb4c`). Local `.adna` at the frozen final; fixture regenerated (`template_sha 74cb761`, schema 0.2, 1-command flow). Record: `what/decisions/adr_034_public_face_repo_release_architecture.md` + `how/skills/skill_template_release.md`.
- **ADR-010 Wilhelm co-sign** (carry) — gates the `/commons` un-embargo; first inclusion = the E5-close coordinated deploy (c169).
- **Hestia: vault-card public fields** — ack `coord_2026_06_10_rosetta_to_hestia_vault_card_public_fields.md` (Harness `display_name` split + optional taglines) → Rosetta regen + next deploy.
- **Hero eyeball (2 promoted candidates, 2026-06-10)** — `/get-started` doc-hero (`hero_get_started.png` ← `helix_r1_H2_v2`) + `/vaults/graph` band (`hero_vaults_graph.png` ← `sec_network_r1_N2_v2`); both LIVE; swap-and-redeploy is cheap if either misses.
- **GitHub social preview**: Upload `aDNABanner.png` (repo root; new banner from M3.2 S3 close 2026-05-22) at github.com/**aDNA-Network**/aDNA.aDNA > Settings > Social preview *(repo slug updated 2026-06-10 — org migration; old slug redirects)*. Supersedes prior recommendation to upload `site/public/images/og-default.png`; new banner is the canonical first-contact visual across all 3 surfaces (Astro hero + README + social preview).
- **✅ 2026-05-22: `.adna/` upstream README flatten-residue fixed + new banner asset landed** at `LatticeProtocol/aDNA` HEAD `5861133` (operator-authorized override of Workspace SR #1 + v8 P2-P5 "zero `.adna/` touches" hard constraint per Campaign SO #14 exception clause; v8 P6 propagation queue item #1 of 10-11 fired early; v7.0 tag at `27e6395`/annotated `3681f76` NOT moved; 6 path corrections to README lines 2/12/144/145/146 + line 143 self-reference removed + new `what/assets/aDNABanner.png` 1288×512 pixel-art wordmark; existing `banner.jpg` retained as one-release-cycle fallback). github.com render verified via `gh api`. **Operator follow-up: upload same `aDNABanner.png` to the public face's Settings → Social preview** (NEW manual step — separate from aDNA.aDNA social preview above) *(retargeted 2026-06-10: the original target `LatticeProtocol/aDNA` is now the ARCHIVED `adna-legacy` — settings frozen; the live target is `github.com/aDNA-Network/aDNA`, ADR-034)*.
- **Vercel Git integration**: Connect repo at vercel.com > adna-docs > Settings > Git for auto-deploy (currently manual via `vercel --prod`)
- **Vercel deploy**: D7+D8 deployed to production 2026-04-24 ✅
- **Vercel deploy**: D9 deployed to production 2026-04-26 ✅
- **Vercel deploy**: D10 deployed to production 2026-04-26 ✅
- **Vercel deploy**: Wind-down (M35 AAR, campaign closeout) deployed to production 2026-04-26 ✅
- **Google Search Console**: Register adna-docs.vercel.app, obtain verification code, add `<meta name="google-site-verification" content="...">` to `SEOHead.astro`
- **Bing Webmaster Tools**: Register adna-docs.vercel.app, obtain verification code, add `<meta name="msvalidate.01" content="...">` to `SEOHead.astro`
- **Delete M05 S2 scratch GitHub repo**: `github.com/ScienceStanley/m05-test` (private; ~10KB; verification scratch from 2026-05-18). Delete via GitHub UI > Settings > Delete, or refresh gh auth scope with `gh auth refresh -h github.com -s delete_repo` then `gh repo delete ScienceStanley/m05-test --yes`. Repo lingers because gh token at S2 only had `repo` scope (not `delete_repo`).


> *(Historical session log — ~20 `## Last Session (…)` blocks (2026-05→06-30) + the accumulated `## Next Session Prompt` stack (superseded 2026-06-27) — archive-shifted → [[STATE_archive]] §Shifted-2026-07-02 (Champollion M1.5). Never deleted (SO-6); the live handoff is the ⏭ QUEUED banner at the top.)*
