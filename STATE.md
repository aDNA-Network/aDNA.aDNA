---
type: state
created: 2026-04-13
updated: 2026-08-18  # 2026-08-18 🚀 P2.2 SHIPPED (deployed tree=301daef, live probe 121/0) · ✅ P2.3 COMPLETE 5/5 (446→460 gates, axe 0, undeployed) · closed a 2-day CI outage nobody had noticed. Prior same-day: ✅ P2.2 COMPLETE — ADR-049 Option A implemented (nav 7 · 11 redirects · 4 link-set copies → 1 · 0 duplicate titles · 446/446 gates); BUILT+GATED, NOT DEPLOYED (⛩ next). Guard diff caught a false premise in the ratified ADR → 9 folds, zero content discarded. Prior same-day: P2.1 shipped + live-proven (162/0), DP5 ratified.
status: active
phase: "P2/6 — P2.1 ✅ · P2.2 ✅ SHIPPED+PROVEN · P2.3 ✅ COMPLETE (undeployed) · ⛩ DEPLOY GATE NEXT → P2.4"                  # HAUSSMANN: P1 closed 2026-08-18 on P1.2's deploy; P0.4 still awaits Aspasia's ack (her lane). P2 does not open until the operator signs the phase gate (SO-1). P<n>[/<count>] grammar — see what/doctrine/doctrine_state_conventions.md §2
campaigns: [campaign_haussmann]                      # RATIFIED §7.7 2026-08-16 (Gate C, operator) — 6 phases / 27 missions; charter: how/campaigns/campaign_haussmann/campaign_haussmann.md
last_edited_by: agent_rosetta
_state_router_version: "1.0"
tags: [state, governance, router]
state_history: STATE_archive.md   # Clear Hearth graduation 2026-07-17 rides the vault's own immortal-spine (§Shifted-2026-07-17)
---
<!-- Router shape — split from monolithic STATE.md at M2.1 S2 2026-05-19. Historical session prose at STATE_archive.md. -->


# Operational State

Dynamic operational snapshot for cold-start orientation. Updated each session.

> **State router** (split from monolithic STATE.md at M2.1 S2 2026-05-19; pre-split SHA `1e337db`). For historical session prose (19 DEPRECATED-marker `## Last Session` blocks + retired Next Session Prompts) see [[STATE_archive.md|STATE_archive.md]]. Most-recent live session block + most-recent Next Session Prompt stay here.

## ⏭ QUEUED — Next Live Session (READ THIS FIRST)

> ▶ **2026-08-18 — 🚀 HAUSSMANN P2.2 SHIPPED (121/0 live) · ✅ P2.3 COMPLETE (5/5 objectives, undeployed) · CI HAD BEEN RED FOR TWO DAYS AND IS FIXED.** Session `session_stanley_20260818_212246_haussmann_p2_2_deploy_p2_3_docs`, commits `0a472f3` → `29313e8`. **P2.2 finally shipped** — pushed `356b33b..301daef`, deployed `deploy_record: 2026-08-19T04:24:08Z mode=prod tree=301daef`, pre-flight fetch-and-compare clean. Production **before-state captured first** so the change is provable, not asserted: `/provenance-audit/` was **404** and is now 200; `/compliance/` and `/adopters/*` were 200 and now 301. Live probe **121 assertions, 0 failures** (42 redirects × both slash forms + all 37 destinations at 200); nav **7** with `nav-more` absent from the page; `/reference` titled "The Standard"; `/install.html` still 200; 4/4 headers. **P2.3 ran end-to-end in the same session, all five objectives.** **O0** — the 29/11 broken-link figure was re-measured rather than trusted (P2.1/P2.2 had moved routes underneath it); **26 stale targets rewritten across 5 files**, four to real pages, four to the public repo *each verified 200 before being linked*, and `/patterns/content-as-code` — a page that never existed — to spec §14. Bare code-span mentions of the same filenames are **prose about the upstream file and were deliberately left alone**; a blanket sweep would have corrupted 6 correct sentences. **gate-31** is purpose-built rather than lychee, deviation recorded with its reason: URL correctness here lives in `dist/` **and** the adapter's redirect config, so a generic crawler reports the 13 mixed-case vault links as broken when they 301 correctly — it classifies those as hops and prints them (P2.4's lane). External links get a **scheduled non-blocking** workflow instead; all 31 currently reachable. **O1** — ⛩ operator ruled **hub + sections + full**: `/reference/specification/` is now a contents page, 20 numbered sections each have a URL with prev/next, and the whole document stays at `/full/`. **Default URL 74,067 → 2,244 px at 375px (33×).** Generated, never hand-split — `specification.mdx` is *itself* a projection, so splitting the file would be reverted by the next transform (the `subnetworks.yaml` clobber class). **Two traps avoided by measuring**: the split is on *numbered* headings because `## SITREP` lives inside a fenced code block in §8 and a heading-level split would **cut through the fence**; and the anchor map is derived from *rendered* ids because rehype strips the dot, making §1.4 `#14-normative-language` collide head-on with §14's own `#14-content-as-code-pipelines`. **O2** — **113 pages** now carry a last-updated date and an edit link. Dates come from **git, not frontmatter** (only 3 of 92 files declare one) and the helper **refuses to guess**: a shallow clone makes every file resolve to one commit, so it omits rather than print a confident wrong date; both workflows now set `fetch-depth: 0`. Spec pages point at the **upstream standard** with the label "Edit the standard", because an edit to the generated copy would be overwritten. **O3** — changelog **1 → 4 entries**, RSS carries them, valid XML; every claim probed live before it was written, and **this session's own work is deliberately absent until it deploys**. Cadence rule wired into `deploy_adna.sh`, non-blocking on purpose. **O4** — F11 fixed (`AGENTS.md — AGENTS.` and two more degenerates); the `excerpt()` docblock had *predicted this exact bug* and filed the consolidation as a follow-up — now closed. **Suite 446 → 460, zero xfail; axe 0 across 8 surfaces × 2 themes; 36 T0 captures.** **14 new assertions across 4 gates, every one red-proven.** ⚠ **THE BIG UNPLANNED FINDING: the standing-watch CI had been failing for three consecutive runs** (`b9d510a`, `356b33b`, `301daef` — every run since P2.1's redirect work) **and nobody had noticed.** `gate-30` asserts the redirect widening, but the widening is done by `inject_redirects.mjs`, which runs **only inside `deploy_adna.sh`**; CI ran `astro build` → `test:gates` with no injection, so it failed every time while the same suite passed locally — because local sessions had deployed first and left an injected `config.json` on disk. `gates.yml` now assembles the real deploy artifact before testing, proven by reproducing the CI sequence from a clean tree. ⚠ **A REGRESSION THIS SESSION INTRODUCED, caught by evidence**: the 20 section pages went h1→h3 (axe `heading-order`, both themes); fixed by promoting body headings one level, **skipping fenced blocks**. It was nearly missed because the capture-report parser looked for a `violations` key that does not exist (it is `axeViolations`), summed nothing, and printed a clean **0** over a real violation — the **fourth instrument-lies-green incident of the session**, and the only one that would have shipped a defect. LESSON: *the verification instrument is as likely to be wrong as the thing it verifies, and a red result deserves the same scrutiny as a green one.* ⚠ **Self-inflicted, recovered**: `node -e "import('./transform-content.mjs')"` as a parse check **executed** the script and regenerated 37 content files over committed work; fully restored via `git checkout HEAD --`, one intentional change re-applied, verified by diff. The ESM syntax check is `node --check`. **⛩ NEXT = A DEPLOY GATE for P2.3** — fetch + diff `deploy_log` FIRST, then `npx astro build` → `deploy_adna.sh prod`; a push GO is per-action and does not carry forward. HEAD is **6 commits ahead of origin**. Then **P2.4** (registry redesign), which also owns the 13 mixed-case vault links and the **77-vs-74 registry admission ruling**. **Owed, still unanswered:** Pythia's **DP-16** ruling and Venus's `adna.network.invite/v1` standard-side review (**Gangway Phase A's exit gate is waiting on it**). New follow-up: the `src/content/docs/` files have **drifted from `transform-content.mjs`** — it is no longer authoritative for hand-edited projections, and that needs an explicit ruling. Token: ~250 kT.
>
> ▶ **2026-08-18 — ✅ HAUSSMANN P2.2 COMPLETE: ADR-049 Option A IMPLEMENTED (nav 7 · 11 redirects · 446/446) — BUILT + GATED, NOT DEPLOYED.** Session `session_stanley_20260818_183026_haussmann_p2_2_ia_implementation`. **The mission is done end-to-end**: DP5 was signed last session and the site was still unchanged; O2 implemented it and O3 proved it. **Nav is 7 with no disclosure** — `Standard(/reference) · Learn · Vaults · Network · Commons · Use Cases · Community`; verified in built markup (7 links, `nav-more` absent from the page entirely). `/reference` is **retitled "The Standard"** so the click does not land on a differently-named page — a `/reference`→`/standard` rename was outside the ratified 11-redirect budget, so the label moved to the page instead of the page to the label. **The four copies of the audience link set are one**: the "For you" group deleted, `home.ts` `audiences` **derived from `personas`** rather than deleted (deleting it would have broken gate-13's import; deriving keeps the test deriving, KW-8/FR-K), `pathCards` gone with its page, `Breadcrumb` `terminalRoutes` down to one row. The persona double-listing ended, dissolving the `navigation.ts:10-13` ordering constraint. **11 redirects**, all widened to both slash forms (42/42). **`/compliance` → `/provenance-audit`** is a real page move paying **ADR-048's owed rename**, and it closes the charter's **Enterprise Architect routing gap** — that page was reachable from neither disclosure surface and now sits in Guides, crumbing under HOW. **⚠ THE RATIFIED ADR CARRIED A FALSE PREMISE, AND THE GUARD DIFF CAUGHT IT.** ADR-049 called the `/adopters/*` prose "near-paraphrase" and Option A "redirect-only, zero content rewritten". Measured on disk it is not: each adopter doc carries a **`## Typical Ontology Extensions` table — 13 unique entity-type rows** absent from its twin, plus a self-reference block and unique glossary links; the four segment landings are **curated reading-path decks** (~1,690w — a 3-week course structure, a procurement checklist, a 60-minute quickstart), not narratives. Under the operator's fold-before-retire ruling this became **9 folds, not 4**; five destinations grew (483→690, 467→762, 441→917, 474→826, 536→644 words). Redirect count, nav, and audience surface are **exactly as ratified** — only the cost line changes, to **zero content *discarded*; 7 destinations gained folded sections**. ADR-049 carries an implementation note; the charter DP5 row is corrected. *DP5 settled which branch survives — a judgment call, and it held. It could not settle what is in the files.* **VERIFIED**: build clean 195 pages · **446/446 gates incl. `@audit`** (444 at P2.1 + 2 new gate-30 tests) · re-crawl of 194 built pages = **0 orphans · 0 duplicate `<title>` · 10/10 ≤2-click** · 36 T0 captures · 11/11 redirects correct in `.vercel/output/config.json`. **The new assertions were proven by making them fail** — both mutations applied, observed failing, reverted (`gate-7:68` had been asserting against `/adopters/solo-developer`, **a route that never existed**, passing vacuously its whole life). **One trap avoided**: the redirect assertions were first drafted into gate-13, which runs against `npm run preview` — per P2.1 doctrine §3.2 preview does not serve the adapter layer at all, so they would have failed in a way indistinguishable from a real bug; they live in gate-30 reading the emitted config. **4 FINDINGS OUT**: (1) **13 mixed-case vault links are still emitted in markup** — `commons.astro:196` renders `/vaults/${row.slug}/` from `subnetworks.json`'s raw values, bypassing P2.1's accessor; they 301 so they work, but every click is a hop, and **gate-30's accessor test has a `subnetworks.json` blind spot** → P2.4 · (2) duplicate `<h1>` "aDNA" on `/glossary/glossary-adna/` + `/vaults/adna/` → P2.3 · (3) `/patterns/content-as-code/` still dangles → P2.3 · (4) stray gitignored `dist/` + `node_modules/` inside `site/src/pages/` (Astro skips both; debris, flagged not touched). **The B1 crawl instrument had evaporated** — it ran from a session scratchpad, so an inventory four missions cite could not be regenerated; **re-authored at `artifacts/p2_2/crawl_local_inventory.mjs`**, in the campaign dir, pointed at the local build (crawling production would have measured the OLD IA and called it healthy). **⛩ NEXT = A DEPLOY GATE** — fetch+diff `deploy_log` FIRST, then `npx astro build` → `deploy_adna.sh`; a push GO is per-action and does not carry forward. Then **P2.3**. **Owed, unanswered:** Pythia's **DP-16** ruling (`ack_required` — an OpenWebUI instance has persisted conversations on this node since 2026-04-10, colliding with the control-plane classification held in THIS vault's `keystone_cohort_manifest.md`; her 08-07 question memo sat undelivered 11 days and both memos were **untracked in git**, invisible until this session committed them) and Venus's `adna.network.invite/v1` standard-side review (Gangway Phase A exit gate waits on it). Token: ~105 kT.
>
> ▶ **2026-08-18 — 🚀 HAUSSMANN P2.1 SHIPPED + PROVEN LIVE (162/0) · ADR-051 ACCEPTED · P2.2 O0+O1 DONE — ⛩ HALTED AT DP5.** Session `session_stanley_20260818_164055_haussmann_p2_1_deploy_p2_2_open`, commits `7b9956e` + `5112884`. **P2.1 finally shipped** — it had been completed-but-undeployed, two commits local-only while production still 404'd. Pushed `070f104..b9d510a` (gitleaks clean), deployed `deploy_record: 2026-08-18T23:42:11Z mode=prod tree=b9d510a`; `inject_redirects` **widened 31 of 31 routes — its first run ever against production**; headers 4/4 no drift. **THE OWED PROBE RAN: 162 assertions, 0 failures** — 31 legacy sources × *both* slash forms (62), all 74 canonical vault URLs at 200, the 24 vault redirects followed end-to-end to a live page, `/org-context-graphs` in both forms. The headline repair is confirmed: `/org-context-graphs/` **with** the trailing slash was a 404 and is now a 301, and trailing-slash is the shape every canonical URL here emits. **⚠ THE INSTRUMENT REPRODUCED THE BUG IT WAS TESTING FOR** — the probe's first draft reported a green "64 PASS, 0 FAIL" while testing *nothing at all* in its canonical third: it guessed field `slug` where the registry carries `vault_slug`, got an empty array, and iterated it happily. That is P2.1's own silent-drop class recurring **inside the verification instrument within a day**. It now derives from the build snapshot and **throws** on an empty derivation. A claim also needed retracting: `/vaults/ScienceStanley` was never a real URL (its card already declares a canonical slug) — the broken class was the 24 cards declaring a *non-lowercase* slug. **ADR-051 → accepted** (operator, at the deploy ⛩ — the earlier of its two permitted gates; ratification followed the probe, not the intent). **Doctrine §3.2 added** (the P2.1 follow-up that had no owner): local preview does not serve the adapter layer — `astro preview` cannot test redirects **at all**; the trap is that the failure is indistinguishable from a real bug, and the control that disambiguates is to probe a redirect known to work in production. **P2.2 OPENED**: ADR-049 authored with exact derived redirect counts (A=11 · B=12 · C=18); **B cut as dominated** (more redirects *and* a rewrite, no home for `open-source-project`, insider vocabulary) with the reasoning recorded not the option dropped. Comps at `artifacts/p2_2/ia_comps.html` headless-verified — each pane renders exactly the nav count it claims (Current 8 + load-bearing More · A 7 · C 6), zero console errors. **Ranker A 4.03 · C 4.17, both ≥4.0** — and the record **declines to name a winner**: 0.14 from a single synthetic rater is one persona moving one cell, and the two are near-perfect mirrors (A Relevance 4.4/Actionability 3.4; C Actionability 4.8/Relevance 3.4). **Conflict declared**: the agent that built the comps also scored them — against convention 4 — so the score is a `[D-syn]` **pre-screen**, not a verdict. **4 FINDINGS → O2**: `gate-7-interaction.spec.ts:68` asserts against `/adopters/solo-developer`, **a route that has never existed** — `page.goto` doesn't throw on a 404, so it has been **passing vacuously** its whole life · 2 nav-orphaned routes · `question-test` at 3 routes · the audience link set in **4 copies**, personas nav-listed **twice**. Charter's `nav ≤8` row corrected to **≤7** (operator ruling). **⛩ DP5 RATIFIED same-session — Option A** (consolidate to `/use-cases/`; nav 7; 11 redirects; **zero content rewritten**). The ranker had cleared both and **declined to separate them**, so the call was judgment: A is reversible and generates evidence cheaply, and **P2.6 is already the recalibration point** where C gets reconsidered on data — C is deferred, not rejected. Cost accepted openly: A keeps an identity taxonomy (7.7 only partly answered) and a later C means a second redirect generation. ADR-049 `accepted`; charter DP5 row + status board updated; **next substantive DP = ⛩ DP6 at P2.6**. **Pushed** `b9d510a..19950c5`, gitleaks clean. **⏭ Next: P2.2 O2** — implement A (nav 7 · fold `/adopters/*` → `/use-cases/*` · retire 4 segment landings · `/compliance/`→`/provenance-audit/` **owed by ADR-048** · kill 4 duplicate titles · collapse the **4 copies** of the audience link set · fix the vacuous gate-7 assertion), **same-diff in the same commit** (`gate-13-nav-surfacing` is the primary blocker — its test *name* hardcodes "8-item desktop row"; `Footer.astro` is NOT nav-derived and needs its own edit), then O3 (re-crawl · T0 captures · AAR). Deploy is a separate ⛩. **Flagged, deferred by operator ruling:** **a concurrent lane committed `cac43e1` into this tree mid-session** — an inbound Venus memo, `ack_required: true`, standard-side review of `adna.network.invite/v1` **before Gangway Phase A's exit gate closes** (medium severity, not this mission's lane, unanswered). Token: ~110 kT vs 200–300 est.
>
> ▶ **2026-08-18 — ⛩ P1→P2 GATE SIGNED · P2 OPEN · ✅ P2.1 URL NORMALIZATION COMPLETE (not deployed).** Session `session_stanley_20260818_150215_haussmann_p2_1_url_normalization`, commits `070f104` + `10de74c`. **The gate:** signed by the operator with the record stating what the signature covers — P1 closed 4/4 on *missions*, **3-of-4 on *exit conditions***, D6/D7 re-score **deferred to P2.6, recorded as deferred not satisfied**. Next substantive DP = **⛩ DP5 at P2.2**. **Also cleared, both found by reading the tree rather than the record:** an **overdue Vitruvius ack** (his 08-16 reply carried `ack_required: true` and had been held on this vault's own non-empty lease — the lease cleared at P1's close) — answered with the craft-floor graduation cite **and its true weight**: an *agent-authored* mission-close ruling whose artifact is still `status: draft` with the operator countermand window open, explicitly **not** labelled ratified; §A8 got an owner + a dated slot (after Decade 1, via `skill_template_release`, since it touches a base-template skill) with the honest caveat that no view on the merits is formed yet. And **four governance artifacts authored 08-17 that had never been committed** + ~15 untracked inbound peer memos. **Pushed** `a37b40a..070f104`, gitleaks clean. **P2.1 (ADR-051, proposed):** root cause was **not data** — `card.vault_slug || slugOf(slug)` let a vault_card beat the canonicalizer, and 24 cards declare the raw name. Law decided by **census**: both candidate laws are collision-free across all 74, so incumbency decided it (50 of 74 already route drop-suffix; keeping the suffix breaks 50 to fix 24). **The registry was deliberately NOT regenerated** — `Home.aDNA` now carries **77** vaults to the committed **74**, so a `sync:vaults` would have silently published `Bearly` · `RareGraph` · `StrongerWithScience` to a public page, two of them *data-bearing, local-only, NO remote*. That is a **DP4-class admission ruling**, not a side effect of a casing fix → flagged to **P2.4 / Hestia's B7**. Instead: generator fixed *and* a new read-side accessor `site/src/data/vaults.ts` (11 importers repointed), so `vaults.json` is byte-untouched (pt19 absolute) and a future card **cannot** reintroduce a mixed-case URL. **TWO FINDINGS:** (1) the site's **two shipped redirects were half-broken in production** — `/org-context-graphs` 301s, `/org-context-graphs/` **404s**, and trailing-slash is the shape every canonical URL here uses; Astro's `redirects:` **cannot** express the slash form (it normalises keys) and Vercel matches redirects *before* its filesystem phase → repair = `inject_redirects.mjs`, widening each `$` to `/?$`, carrying no list of its own so nothing can drift, wired into `deploy_adna.sh`; (2) the **Wayback CDX sweep returns ZERO project URLs** — all 47 captures across both hosts belong to **prior owners** (an evolution-biology group; a portfolio template) → ruling recorded: **prior-owner URLs are not redirected**, because pointing `/the-team` at aDNA content claims another org's URL space. **Same-diff (ADR-057) cost two SILENT-DROP sites** — canonicalization would have removed 4 of 6 NetworkDiagram nodes and 6 of 8 homepage registry cards **with no error**, because a missed lookup was filtered away rather than raised; only one had a gate. Curated slice now **throws** on a miss. **gate-30** added (6 assertions, red-proven ×3, all derived from the build snapshot — never a typed list). Suite **444 → 450 green, zero xfail**. LESSON: *a silent-drop lookup is a same-diff hazard no route grep finds* — same-diff looks for hardcoded **routes**; these were hardcoded **keys**, which do not look like URLs. **⛩ NOT DEPLOYED** — and verification is honest about its limit: `astro preview` cannot test redirects at all (they live only in the adapter config, never in `dist/`), proven by the control that the pre-existing *production* redirect also 404s locally; config-level + regex simulation pass, **live probe owed at the deploy ⛩**. **⏭ Next: ⛩ deploy GO for P2.1** (fetch + diff `deploy_log.txt` first) → then **P2.2 IA consolidation (⛩ DP5)**. **Not done, flagged:** the P2.1 commit `10de74c` is **unpushed** — the earlier push GO was consumed by the P1 record and an outward act is per-action (Git-Ops rule 3). Token: ~135 kT vs 120–200 est.
>
> ▶ **2026-08-18 — ✅ HAUSSMANN P1 CLOSED 4/4 — P1.2 RE-RANKED (4.22, CRITERION MET), DEPLOYED + LIVE-VERIFIED. NEXT = THE ⛩ P1→P2 PHASE GATE.** Close session (`session_stanley_20260818_143557_haussmann_p1_2_close`, commits `84dd3bd` + close cascade) executed the operator's three rulings: **re-rank first → one deploy → close P1 and halt**. **The owed measurement ran.** P1.2's `verification_method` demanded ranker ≥4.0; O3 measured **3.61** and never re-scored after its own remediation. Re-run: **4.11 pre-fix → 4.22 post-fix — MET**, and met *before* the fixes as well as after. **Actionability, the dimension the remediation targeted, moved 2.50 → 4.00**; Credibility + Tone held 4.50. ⚠ **Finding before the finding: the 3.61 was NOT REPRODUCIBLE** — it logged neither its persona set nor 3 of its 6 dimensions, and 2 it *did* name (Credibility, Tone) are not in the canonical `skill_decadal_aar` six. Only 3 rows are a true comparison; the aggregate delta is *indicative, not measured*. This run records its instrument in full (`artifacts/p1_2/rerank_record.md`; register **§7.6**) so the next is comparable. **The re-rank caught 2 S2s (no S1), both fixed before the deploy + gate-pinned + RED-PROVEN:** **R-112** `/about` — the closing band re-asserted *"a real Founding Architect, a real anchor partner, real public-good work"* **fifty lines below the band this same mission rebuilt to stop asserting it (R-62)** — the R-28 family's *second* recurrence, on the sibling surface never adjudicated; the trailing *"Real stewards grow the network"* asserted stewards the page elsewhere says don't exist → **cut, not softened**. **R-113** `/canonical-properties` — dead-ended at the footer while its twin, built in the same mission on the same scaffold, closed with five exits; **both reviewer lenses found it independently**. Suite **442 → 444 green, zero xfail**. **⛩ DEPLOYED** `deploy_record: 2026-08-18T21:45:51Z mode=prod tree=84dd3bd` (4/4 headers at deploy *and* re-checked on the apex) → **live-verified on adna.network**: both surfaces 200 + both footer-linked, disclosure sentence above the stat strip, `og:site_name`="aDNA Network", 5/5 load-bearing P1.2 sentences intact, 3/3 retired phrasings absent, `/install.html` still 200 (the 08-16 collision class). **Title-alignment memo DELIVERED** to ScienceStanley + WilhelmAI under GO. **Charter corrected:** DP2/DP3/DP4 all read `pending` while their missions were closed — status labels lag truth. DP7 annotated with its P1.1 early-fire deviation. **P1 exit-gate evidence recorded honestly: 3 of 4 conditions met, D6/D7 re-score DEFERRED to P2.6 by the instrument's own cadence** — not claimed as met. LESSONS (both generalize): **a rebuilt band does not rebuild the page — grep the claim family, not the component**; and **paired pages need paired reviews** — when two surfaces ship together on one scaffold, a fix to one is a hypothesis about both. Plus: **record the instrument with the score, always** — an unrecorded measurement decays into a claim. **⏭ Next: ⛩ the P1→P2 phase gate (operator, SO-1)** → then P2.1 URL normalization. **Not done, flagged:** HEAD is now **14 commits ahead of `origin/main`** — pushing is an outward act needing its own GO (Git-Ops rule 3). Token: ~65 kT actual vs 80–140 est.
>
> ▶ *(superseded same-day by the banner above)* **2026-08-18 — 🏛 HAUSSMANN P1.2 CODE-COMPLETE + VERIFIED — ⛩ AWAITING DEPLOY GO (nothing shipped).** Single session (`session_stanley_20260818_125835_haussmann_p1_2_state_of_network`), six commits `462ac2e` → `eff6670`. **All five acceptance criteria met.** **`/state-of-the-network/`** ships the §8.3 move — the four strata (what runs · operator-operated · not ours · planned), three file-sourced dates, and the sentence the campaign turns on: *"All of them run on one computer, operated by one person"* (**H5, the trust stratum's only S1, resolved; anti-pattern 7.3 inverted**), plus *"It is not evidence of adoption, and this site will not present it as any"* — a forward commitment, not a confession. **The fold carries the disclosure SENTENCE, not a link to it** (a band that only links is the footnote §7.3 rules out), and that same edit is the **Berthier proof-of-life re-placement** — one move, two criteria. **`/canonical-properties/`** ships the §7.1 clone-site defense (every property + probe date; `adna.dev` named as retired; *"aDNA runs no social accounts"*). **Named humans (H12)**: Stanley Bishop + three affiliations + one outbound link, **O1 consent satisfied in advance by operator ruling** — incl. the ship-and-stage call on the Wilhelm title conflict (stanley.science still says "Lead AI Architect"; memo staged) and the **"UCLA Anderson GSB" → "Anderson Venture Accelerator" correction** (GSB is Stanford's naming, zero vault support). **R-62's structural debt — the one §6 deferred to this mission — DISCHARGED.** Identity coherence: **13 hardcoded `adna.dev` fallbacks → 0**, `og:site_name` reconciled, `sameAs` created, `PUBLISHER_URL` off the GitHub repo, publisher attached to the two builders that emitted none. **Suite 407 → 442, zero xfail; axe 0 × 4 surfaces × both themes; 13/13 properties re-probed logged-out; the rare-archive sole-contributor claim re-verified against the GitHub API; R-58 byte-intact; 4/4 new gates RED-PROVEN.** Register **97 → 111 rows, zero FALSE, zero unsupported**. ⚠ **TWO INDEPENDENT REVIEWS FORCED 11 CLAIM CORRECTIONS** (register §7.5): a hostile cold-read and a ranker **converged on the same two S1s** — the verification checklist verified nothing (all three checks are artifacts a clone copies; check 3 was additionally *false* because the O2a `sameAs` narrowing made it so), and *"Every entry was opened from outside"* was false for 4 of 13. Also: a wrong data path **inside "How to check this page"**, and the largest omission — `/about` names the operator as Head of AI at the Wilhelm Foundation while the other page called the Foundation independent. **Ranker 3.61 vs ≥4.0 — NOT MET** (Credibility 4.50 · Tone 4.50 validated the thesis; Actionability 2.50 was the miss; remediation targets it but was not re-scored — **re-rank owed**). GOTCHA (new): **a Playwright gate cannot import site source that transitively imports JSON** — data moved to `canonical_properties.json` with the `.ts` as a typed wrapper + a build-time drift assertion. LESSON: **the author of a checkability claim is structurally the worst auditor of it.** **⏭ Next: ⛩ operator deploy GO** (`site/scripts/deploy_adna.sh prod`; **fetch + diff `deploy_log.txt` first** — a parallel lane committed `ab841c2` into this tree mid-session) → then P1 closes and **P2 opens**. Token: ~430 kT actual vs 200–300 est.

> ▶ **2026-08-17 — 🚀 HAUSSMANN P1.1 CLOSED: CLAIM PURGE — ZERO FALSE, ZERO XFAIL, DEPLOYED.** Single session (`session_stanley_20260817_185706_haussmann_p1_1_claim_purge`, commit `0f7cca0`). **Hero ships ADR-048 candidate-A VERBATIM** (definition-as-hero + audience sub + NOT-line; lyric demoted INTACT to a movement band; the embargo pair is now the fold's only protocol phrasing — gate-23 re-anchored + guards the FALSE gloss's return). **All 8 FALSE resolved** (R-14/20/23/23b/46/47/61/84/90): quantifiers down · **/community funnel REAL** — issue templates SHIPPED to `aDNA-Network/aDNA` (`fd32fc7..d4742db`, ⛩ GO; questions-link → community.adna.network per operator) · **⚠ Fluxer link-GO fired EARLY by operator OVERRIDE** (P0.4/ADR-054 prerequisites unmet — live probe: `legal.*` null + unbranded; deviation recorded ADR-054 §Status; honest-state + dated + human-only framing shipped, R-95/R-96 register rows guard it) · R-84 truth-copy ("attributed and dated") · **R-90 verified-links projection gate** (`verified_links.json`, empty=healthy; dead Videos URL 0-in-dist; Hestia memo staged w/ 3 data rows). 19 unsupported adjudicated (memo §2). **Hostile cold-read (fresh agent, 204pp, live channel probes): 18 findings ALL dispositioned** — 14 fixed · **SECURITY.md SHIPPED (`b64b81e`) + private vulnerability reporting ENABLED** (the security-disclosure channel was DEAD and is now real — worst find) · 2 → Hestia memo (pt19) · 3 adjudicated-keep. **Suite 407/407 green ZERO xfail** (was 405+9xf; gate-26 fixture 21→14, R-94/R-95 honesty guards added). **DEPLOYED prod** `2026-08-18T03:01:11Z tree=0f7cca0` (4/4 headers script-verified) → **live-verified on adna.network**: new hero + movement band + zero retired phrases + Fluxer disclosure + attributed-and-dated. GOTCHA (new, KW-14 class): **`subnetworks.yaml` is the SOURCE, json is projected** — a json-only edit clobbers at next `sync:vaults`; trace data files to their generator before editing. Still pending: P0.4 Aspasia ack (now MORE urgent — the site links the policy-naked instance) · evidence-retention ⛩ · `VERCEL_TOKEN_ADNA` shell-export. **⏭ Next: P1.2 state-of-network** (last P1 mission; ADR-048 dependency satisfied) — then P2. Token note: ~460 kT actual vs 150–250 est (hostile burn-down unscoped; AAR follow-up 4).

> ▶ **2026-08-16 — 🚀 HAUSSMANN P1 WAVE: P0.1 CLOSED at DP2 (panel waived) · P1.3 ✅ REGISTRY TRUTH · P1.4 ✅ MOBILE INTEGRITY — ALL LIVE.** Operator waived the O4 human panel in-chat → **ADR-048 ACCEPTED at DP2** (deviation recorded in §Status; retro-validation duty → P5.1) → **P1.1 + P1.2 UNBLOCK**. **P1.3 ✅** (`c61a544`+`df0b30b`+`0fa7b3d`) — projection sanitizer v2 (sentence-granular, fed by gate-27's own `leak_patterns.json` — the projection can no longer emit what the gate forbids), persona placeholders → null (F15 "— —" titles fixed), shared public-label util (raw enums out of built HTML incl. attributes), ⛩ **DP4 RULED minimal-card ×3** (aiLP-Dataroom · CakeHealth · PercySleep; suppression at the generator; ADR-052 §admission), **operator-GO'd projection regen** (55 notes sanitized, 28→honest-null, ZERO other fields; counts hold 74/14), graph svg 68-era→74 (59-orphan label consistent sitewide), **LEAK BASELINE RETIRED — 86 rows/563 occ → 0; empty-state promoted to a HARD gate, red-proven**; Hestia backfill memo **DELIVERED** (29 vaults await taglines — the honest cost of sanitization is sparseness). **P1.4 ✅** (`3e53637`) — F1 docs dead-column (base grid lacked its named areas — article sat in an implicit column; gate-9-invisible by construction), F2 /network reflow (minmax + soft-wrap, WCAG 1.4.10), F3 portrait diagram twin (labels ≥13px at 320), F12 copy-button `:global` overlay; **gate-29 reflow guard red-proven** (10 assertions); `visual_capture.mjs` now `reducedMotion`-deterministic (armed-animation captures were shooting blanks — part of the F3 evidence was the instrument). Suite **404→414 green, axe 0 both themes** (9 claim xf remain = P1.1's lane). **DEPLOYED prod** via `deploy_adna.sh`: record `2026-08-18T01:33:57Z mode=prod tree=3e53637` (UTC; operator-frame 08-16 evening), **4/4 headers + leak-free registry + minimal cards + graph counts live-verified on adna.network**. Unplanned find (structural fix): the gate suite silently adopted a **foreign port-4321 dev server** (`reuseExistingServer: true` — 304 bogus failures against stanley.science) → fail-loud + `GATE_PORT` env. Still pending: P0.4 Aspasia ack · `VERCEL_TOKEN_ADNA` unbrokered · ⛩ evidence-retention ruling (72M capture sets on disk, cited subset committed). **⚠ CONCURRENT-DEPLOY COLLISION, CAUGHT + REPAIRED at close:** a parallel session shipped the **installer (ADR-058)** to origin+prod (12 commits, `f38f93e..7c7eedb`, prod 19:54Z tree `e8b4540`) while this wave ran — the 01:33Z deploy above was built pre-installer and **knocked the installer routes to 404**; caught at push-time divergence, repaired by rebase (only conflict: the append-only deploy log, unioned) → suite 414 green on the merged tree → **redeploy `2026-08-18T01:39:25Z tree=62e7388`** → live-verified installer 200×3 + registry/mobile fixes + 4/4 headers. Lesson (the unrecorded-deploy class, sequel): **fetch-and-compare deploy_log BEFORE any prod deploy** — a deploy built behind origin un-ships the other lane's work; candidate guard for `deploy_adna.sh`. **⏭ Next: P1.1 claim purge** (ships the ADR-048 hero verbatim; clears the 9 xf) **or P1.2 state-of-network — both now open.** Session `session_stanley_20260816_204351_haussmann_p1_wave` (history/2026-08).

> ▶ **2026-08-16 — 🚀 HAUSSMANN P0 WAVE: 3/5 missions closed; PRODUCTION HARDENED + DEPLOYED.** All five P0 missions ran (operator-ordered, session `haussmann_p0_wave`): **P0.2 ✅** — header-drift root-caused (prebuilt ignores root `vercel.json`) + fixed via WebForge's `inject_headers.mjs` (byte-identical adoption) inside the new sole-sanctioned `site/scripts/deploy_adna.sh`; **adna.network now serves 4/4 configured security headers; MDN Observatory C/50 → B+/80**; ADR-050 **RATIFIED (c)** at ⛩ DP3; deploys RECORDED (preview `2026-08-17T01:38Z`, **prod `2026-08-17T01:54Z`**, tree `d88b6ff`) — **the deploy freeze is LIFTED**; drift checker red-proven; Vitruvius token-migration ACK delivered (pending-token caveat: `VERCEL_TOKEN_ADNA` unbrokered). **P0.5 ✅** — the editorial gate: suite **371 → 404** (394 ✓ + 10 expected-failures = the 9 FALSE-claim rows [xf-until-P1.1] + the 86-row leak baseline [expires P1.3]; 0 real failures), 4 red-tests proven incl. the unexpected-pass ratchet; **5,748 dev comments stripped per build incl. the deployed artifact**. **P0.3 ✅** — `how/federation/webforge/` wrapper live (pin @ WebForge `6096157a`), craft-floor **Tier-2 graduation ACCEPTED** (coverage declaration → P4.2). **P0.1 🟡 active** — candidates + unanimous 3/3 synthetic pre-screen + ⛩ operator picked **A (definition-as-hero)**; ADR-048 updated (`proposed`); **awaiting the O4 human panel** (kit ready: `artifacts/p0_1/panel_kit.md`) → DP2. **P0.4 🟡 active** — prerequisite register built (PR-1/2/3 × owners); **awaiting Aspasia's ack** (delivery verified 17:11). *(Out-of-wave note: Hestia's imagen-4.0 memo was **already resolved in a parallel session** — all 8 runners ported off the retiring family at `9534691` + lease closed `07423ad`, 2026-08-16 18:00; no action remains here.)*

> ▶ **2026-08-16 — ✅ HAUSSMANN GENESIS COMPLETE + CHARTER RATIFIED (Gate C §7.7, operator).** The full arc ran in one day: Phase A orientation (4 artifacts, `a1b586f`) → Phase B full assessment (12 evidence packets + 2-scorer baseline **51.6/100** vs MCP ≈83; 8 FALSE claims; H1–H15 resolved; `d58ea13`+`df3827c`) → ⛩ Gate B confirmed → Phase C genesis package (charter + 27 missions + ADR-048…057 `proposed` + 5 context graduations + 4 memos; cold-read test FAIL→FIXED; `30a9c3b`) → ⛩ **Gate C RATIFIED + both outward acts GO'd** (origin push; 4-memo delivery). **campaign_haussmann is ACTIVE**; next executable step = mission **P0.1** (or any P0 mission) via `missions/session_prompts_haussmann.md` — the activation gate in the campaign CLAUDE.md is now satisfied. Deploy-freeze request stands until P0.2 lands. Genesis session: `session_stanley_20260816_094350_haussmann_genesis` (history/2026-08/).

> ▶ *(superseded same-day by the banner above)* **2026-08-16 — 🏗 OPERATION HAUSSMANN GENESIS — IN FLIGHT.** Operator issued two directives (verbatim copies: `how/campaigns/campaign_haussmann/directives/`): a cowork directive to **design the campaign that rebuilds adna.network** (this session designs the campaign, never the website — zero `site/` edits) governed by the **VITRUVIUS review instrument** (D1–D12, S1–S4, provenance tags `[D]/[I]/[R]/[A]`, hypotheses H1–H15). Plan of record operator-approved 2026-08-16 (`~/.claude/plans/please-read-the-claude-md-fuzzy-sedgewick.md`); rulings: Gate A folded into plan approval · Phase B full fan-out · Gate C in-chat §7.7 · Flux assessment outside-only. Arc: **Phase A** orientation artifacts (committed) → **Phase B** full situation assessment (site + community.adna.network + cohort MCP/Mastra + craft dossier + claim register + VITRUVIUS baseline) → ⛩ **Gate B** (operator) → **Phase C** genesis package (charter `campaign_haussmann` + ~25 missions + 10 ADR stubs + 5 context artifacts + 4 staged memos) → ⛩ **Gate C** ratification. Key pre-findings: Berthier 08-11 wave memo absorbed-not-relitigated; Fluxer STATE stale-wrong (community live on third-party metal; ADR-025 human-only); WebForge substantive but **no `how/federation/webforge/` wrapper** (straggler); deployed site sends only HSTS vs `vercel.json` headers (deploy drift); llms-full.txt is an index not a corpus; 24/74 mixed-case vault URLs. Session `session_stanley_20260816_094350_haussmann_genesis`. **⏭ Next:** if this session is interrupted, resume from the session file's Activity Log + the plan of record.

> ▶ **2026-07-24 — ✅ REGISTRY REGEN 73→74 (RareAnthropic.aDNA) — the flagged Refit carry-forward, DONE.** Cold-start with **no active campaign** (Palimpsest + Refit both closed 2026-07-24) → operator elected the registry data-currency regen. **Full loop, operator-GO'd:** **(A)** committed Home.aDNA's deliberately-held **RareAnthropic** registration **locally** (`faf5eac`, `vault_count 73→74`; the row landed 2026-07-22 under Operation Portolan with the derived-projection regen deferred to Hestia — finalized here under GO; **NO push**, Home stays local-by-default) → **(B)** `npm run sync:vaults` regen `vaults.json` **73→74 / 14 edges** (sha `59058a4`→`536e9d62`; **org_graph 3→4**; RareAnthropic projected; **idempotent 2×**); subnetworks.json date-churn `git restore`d; `vaults_graph.mmd` +rareanthropic node (orphans 58→59); `.svg` left for a dedicated graph-currency pass (pre-existing 68-era stale) → **(C)** G20 `claim_trace_manifest` fixture **73→74** (edges stay 14) → **(D)** `npx astro build` (203pp) + **`npm run test:gates` 371/371 green** (gate-20 + gate-21 at 74) → **(E)** single-file commit `498f985`. Mirrors **Refit M2** (`75c6d42`). **✅ SHIPPED + DEPLOYED + PUSHED (wind-down 2026-07-24):** deployed live to **adna.network** via `npx astro build` → `VERCEL_TOKEN=$SS_VERCEL_TOKEN vercel --prebuilt --prod` (`dpl_2qAgWuYA9D38F9g8Xyw8EhU7J8eB`, `readyState READY`/`target production`; home renders **74**, HTTP 200 live-verified); aDNA.aDNA `main` **pushed → origin/main** (`498f985` + close-out `4d78a0d` + wind-down; gitleaks pre-push clean). **Home stays local — never pushed** (Rule 4). Session AAR appended. **⏭ Next:** no active campaign; pre-existing follow-ups only (`vaults_graph.svg` 68→74 currency · `install_truth` regen at next deploy · `.adna` doc-name-leak pass). *(Clock note: `generated_at` rolled to UTC `2026-07-25`; human records use operator-frame 2026-07-24.)* Session `session_stanley_20260724_200611_registry_regen_73_74`.

> *(QUEUED banners 2026-07-24 → 2026-07-11 — Palimpsest v8.9 · Refit · Distillery v8.8 · Cleanroom v8.7 · Storyweave P3–P5, all COMPLETED — archive-shifted → [[STATE_archive]] §Shifted-2026-08-03; never deleted.)*

> *(QUEUED banners 2026-07-08 → 2026-07-05 — Storyweave P0–P1.6 · Ouroboros · Meridian · Concord · Fleet-Re-Seed, all closed — archive-shifted → [[STATE_archive]] §Shifted-2026-07-17 [Clear Hearth W-B slice 3]; never deleted. Kept live: the 07-11+ Storyweave P3→P5 + v8.7/v8.8 release arc.)*

> *(Older QUEUED banners archive-shifted → [[STATE_archive]] §Shifted-2026-07-06; never deleted per SO-6.)*

## 🌐 adna.network — LIVE on Cloudflare (2026-05-31)

**The aDNA site is live at https://adna.network** (canonical public face; adna.dev abandoned). **First executed ADR-031 Cloudflare migration** — API-driven, single operator-present session (operator hands-on = 1 token + 1 nameserver change). CF zone `667a2d5e…` (account `e048a126…`; NS `keanu`/`sierra.ns.cloudflare.com`); apex + www → Vercel `adna-docs` (`prj_SBKnZf…`) via **CNAME flattening**, DNS-only; **Mailgun email preserved** (MX×2 / SPF / `smtp._domainkey` DKIM / strict `_dmarc p=reject`). Canonical flipped: `site/astro.config.mjs` `site:` → adna.network + Vercel `SITE_URL` env + **prebuilt** redeploy (`dpl_AxbEXjuF…`; local build required — `prebuild` reaches `../scripts`); live `<link rel=canonical>` + 159-URL sitemap emit adna.network. **CF token onboarded to Home.aDNA broker** (Keychain `CLOUDFLARE_API_TOKEN` + zshrc + inventory C47; 1P backup = operator Touch-ID one-liner pending). **Skill graduated to SiteForge** — `skill_cloudflare_dns_cutover` v1.0.0 (ADR-031 successor-ownership relocated per operator directive; SiteForge owns build+deploy+DNS). **Operator override:** M5.7 O6 domain-cutover executed early — DNS/canonical only; **renamed no content** (honors the aDNALabs "nothing renames before the brief" broadcast). WARN: `SS_VERCEL_TOKEN` leaked again in a Vercel-CLI error (recurring incident) -> rotate. Seeded turnkey for next in-vault session: `worldgeno.me` (wga.aDNA M02, unblocked) + `stanley.science` (ScienceStanley M14). Doctrine: [[adr_031_cloudflare_dns_site_publishing_standard]].

## Current Phase

> *(Current-Phase rows 2026-07-01 → 2026-07-03 — the Champollion G0→G5 ladder + STR Track-C close — archive-shifted → [[STATE_archive]] §Shifted-2026-07-17 [Clear Hearth W-B slice 3]; never deleted.)*

> *(Current-Phase activity older than 2026-07-01 archive-shifted → [[STATE_archive]] §Shifted-2026-07-02 (Champollion M1.5); 48 bullets, never deleted per SO-6. This router keeps the recent live arc (Champollion 2026-07-02 + STR close 2026-07-01); older bullets archive-shifted, trim to the next diet.)*

## Active Campaigns

### `campaign_v8_9_release` (Operation Palimpsest — ✅ **COMPLETED 2026-07-24**; v8.9 SHIPPED [commit c8e5427 + tag v8.9]; P0→P1→P2→P3 all done — **DO NOT re-open**)

> *(Body archive-shifted → [[STATE_archive]] §Shifted-2026-08-03; the ruling above is the live record.)*

### `campaign_refit` (Operation Refit — ✅ **COMPLETED 2026-07-24**; G1 07-21 · G2 07-24 · **G3 07-24** [DP10: accept + push all + deliver 5 memos]; 6/6 missions, 21/21 A–E rows, no normative change — **DO NOT re-open**)

> *(Body archive-shifted → [[STATE_archive]] §Shifted-2026-08-03; the ruling above is the live record.)*

### `campaign_v8_8_release` (Operation Distillery — ✅ **COMPLETED 2026-07-14**; v8.8 SHIPPED, `a32724b` + tag `v8.8`)

> *(Body archive-shifted → [[STATE_archive]] §Shifted-2026-08-03; the ruling above is the live record.)*

> *(Completed-campaign entries — Cleanroom v8.7 · Meridian · operation_adna · feedback_loop · keystone · looking_glass · STR · network_audit · v2_infrastructure · Completed Mini-Campaigns — archive-shifted → [[STATE_archive]] §Shifted-2026-07-17; never deleted. Kept live: ACTIVE Distillery + Pending/seeded below.)*

### Pending Campaigns (seeded but not yet open)

#### `campaign_obsidian_deployment_stabilization` (NEW successor — seeded 2026-05-13 at M-LWX-03 S2 Phase L; **ABSORBED by `campaign_adna_serious_tool_readiness` 2026-05-17**)

`how/campaigns/campaign_obsidian_deployment_stabilization/` — implementation-focused successor to `campaign_lattice_workspace_ux`. Owner: Rosetta. Strategic intent: make the Obsidian deployment of every aDNA vault stable, standard, and self-stabilizing. **ABSORBED into `campaign_adna_serious_tool_readiness` Phase 3** (Forge Ecosystem Hardening) 2026-05-17 — 8 tracks T1-T8 distributed across v8 missions M3.1-M3.4. Stub directory preserved for audit (per Standing Order #6 archive-not-delete); status `planned` stays in stub frontmatter; effective status `absorbed_by: campaign_adna_serious_tool_readiness`. 7 backlog files F-S2-1..8 (in `aDNA.aDNA/how/backlog/`) source the v8 Phase 3 work directly.

#### `campaign_validation_node_adna_lwx_outputs` (NEW successor — seeded 2026-05-13 at M-LWX-03 S2 Phase L; lives in `lattice-labs/`)

`lattice-labs/how/campaigns/campaign_validation_node_adna_lwx_outputs/` — validation-focused successor; dispatched to Carly + Herb. Owner: Berthier (lattice-labs). Strategic intent: validate M-LWX-01/02/03 outputs on operator-owned machines via Carly+Herb dispatch. Phase 1 narrow: M-VNAL-01 covers outstanding O4 (wikilinks), O5 (cross-vault links), O6 (marketplace), O3-extended (full vault tables). Phase 2+ broader: recurring "Carly+Herb validate-all-aDNA-features" pattern — the FIRST instance of an explicit validation-dispatch campaign for aDNA work. Coord memo at `lattice-labs/who/coordination/coord_2026_05_13_carly_herb_node_adna_validation_dispatch.md`. Status: `planned`; opens when Carly + Herb each acknowledge the coord memo.

#### `campaign_adna_v3_ecosystem_compliance` (planned successor — seeded 2026-05-08)

`how/campaigns/campaign_adna_v3_ecosystem_compliance/` — applies v7.0 changes per-vault to the 19 active aDNA ecosystem vaults. Strategic intent: bring the lattice into full v7.0 compliance after the standard codifies it. Preliminary phase structure: P0 planning + P1 audit + P2 bulk skill upgrade + P3 git remote setup + GitHub naming standardization + P4 airlock adoption + workspace router resync + P5 final ecosystem audit + AAR. Preliminary mission outline: M01-EC (per-vault audit) → M02-EC (bulk skill upgrade) → M03-EC (git remote setup) → M04-EC (GitHub repo rename) → M05-EC (airlock adoption) → M06-EC (workspace router resync) → M07-EC (final audit + AAR). Estimated 12–20 sessions (recalibrated by M01-EC). Persona: Rosetta continues. **Opens at v2 P3 phase gate** (post-M03 flatten + M08a/M08b shipped); M11 of v2 finalizes the mission tree before this campaign opens.

> *(Operation Rosetta [absorbed by STR] + its phase table archive-shifted → [[STATE_archive]] §Shifted-2026-07-17; never deleted.)*

## Phase 7 Progress → archived
> *(Operation Rosetta Phase-7 100-cycle III loop + Persona Ranker Summary (done 2026-04-26; ranker 5.00) archive-shifted → [[STATE_archive]] §Shifted-2026-07-02, Champollion M1.5. Historical; live handoff = ⏭ QUEUED above.)*

## What's Working → archived
> *(the Phase-7-era site snapshot (117pp / 47 gates / "5 reviewer personas" — superseded: site 179pp, 16 personas, 281+ gates) archive-shifted → [[STATE_archive]] §Shifted-2026-07-02, Champollion M1.5. Historical; live handoff = ⏭ QUEUED above.)*

## Active Blockers

None.


## Next Steps → archived
> *(the 2026-06-24 keystone-DP2 Next-Steps snapshot (superseded by the ⏭ QUEUED banner above) archive-shifted → [[STATE_archive]] §Shifted-2026-07-02, Champollion M1.5. Historical; live handoff = ⏭ QUEUED above.)*

## Pending Manual Actions

- **ADR-010 Wilhelm co-sign** (carry) — gates the `/commons` un-embargo; first inclusion = the E5-close coordinated deploy (c169).
- **Hestia: vault-card public fields** — ack `coord_2026_06_10_rosetta_to_hestia_vault_card_public_fields.md` (Harness `display_name` split + optional taglines) → Rosetta regen + next deploy.
- **Hero eyeball (2 promoted candidates, 2026-06-10)** — `/get-started` doc-hero (`hero_get_started.png` ← `helix_r1_H2_v2`) + `/vaults/graph` band (`hero_vaults_graph.png` ← `sec_network_r1_N2_v2`); both LIVE; swap-and-redeploy is cheap if either misses.
- **GitHub social preview**: Upload `aDNABanner.png` (repo root; new banner from M3.2 S3 close 2026-05-22) at github.com/**aDNA-Network**/aDNA.aDNA > Settings > Social preview *(repo slug updated 2026-06-10 — org migration; old slug redirects)*. Supersedes prior recommendation to upload `site/public/images/og-default.png`; new banner is the canonical first-contact visual across all 3 surfaces (Astro hero + README + social preview).
- **Vercel Git integration**: Connect repo at vercel.com > adna-docs > Settings > Git for auto-deploy (currently manual via `vercel --prod`)
- **Google Search Console**: Register adna-docs.vercel.app, obtain verification code, add `<meta name="google-site-verification" content="...">` to `SEOHead.astro`
- **Bing Webmaster Tools**: Register adna-docs.vercel.app, obtain verification code, add `<meta name="msvalidate.01" content="...">` to `SEOHead.astro`
- **Delete M05 S2 scratch GitHub repo**: `github.com/ScienceStanley/m05-test` (private; ~10KB; verification scratch from 2026-05-18). Delete via GitHub UI > Settings > Delete, or refresh gh auth scope with `gh auth refresh -h github.com -s delete_repo` then `gh repo delete ScienceStanley/m05-test --yes`. Repo lingers because gh token at S2 only had `repo` scope (not `delete_repo`).
> *(Manual-action rows already ✅ RESOLVED (2026-04 → 2026-06: upstream PR #8 · `.adna/` README flatten-residue + banner · four Vercel production deploys) archive-shifted → [[STATE_archive]] §Shifted-2026-08-03; never deleted.)*


> *(Historical session log — ~20 `## Last Session (…)` blocks (2026-05→06-30) + the accumulated `## Next Session Prompt` stack (superseded 2026-06-27) — archive-shifted → [[STATE_archive]] §Shifted-2026-07-02 (Champollion M1.5). Never deleted (SO-6); the live handoff is the ⏭ QUEUED banner at the top.)*
