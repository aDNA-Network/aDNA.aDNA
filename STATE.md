---
type: state
created: 2026-04-13
updated: 2026-09-04  # 2026-09-04 🚀 **`R-97` DEPLOYED — `tree=2a72efe`, no override flags; `/` reads *"aDNA itself sends nothing"* ×1 and *"nothing leaves your machine"* ×0, content-probed. The four-surface over-promise class (`R-64`/`R-161`/`R-167`/`R-97`) is CLOSED. ⛩ Scope ruled to R-97 ALONE — the blocking commit sat ABOVE the fix, so `/learn/course/` stayed unshipped and 404s live; push `2a72efe..c32a4b7` made it CI-visible instead. ⛔ `DP8` was ruled 2026-08-23 and the charter read `pending` for 12 days — corrected, no decision owed.** Earlier same day: ⛩✅ **`R-97` SCOPED AND BUILT — the homepage NOT-line, ADR-048 AMENDED.** *"…no signup, **nothing leaves your machine**"* → *"…no signup; **aDNA itself sends nothing**."* ⛩ Operator-ruled at the gate (candidate 1 of 4 measured; scope HELD to one prop, two offered widenings declined). Register **§22**; ADR-048 carries a §Amendment with its own 4-field block; counts **189/174/0**, derived after writing. ⭐ **The measurement disqualified the cheap candidate**: *"nothing **of yours**"* costs 0.00 FKGL and **fixes nothing** (the falsifying case IS "yours"), and the best-scoring one (9.84) drops `R-161`'s *"until you choose"*. Real build reproduced the twin-substitution prediction **exactly at 9.98** (target 10). ⛔⛔ **A provenance claim of this desk's was FALSE on five surfaces**: every draft said the string was *"already shipped on `/get-started` by GR-1"* — GR-1 shipped a **different** string, and *"itself sends nothing"* measured **0 occurrences site-wide** before this commit ⇒ **R-97 is the FIRST surface to ship it**. `F-u`'s class, **sixth sighting**; ⭐ caught by the **fixture**, not vigilance — adding the row meant reading `R-64`'s, four entries above, which holds the actual shipped text. ⭐ `R-97` was classed `verified` and **asserted by nothing** (absent from gate-26's fixture, from claim_trace_manifest, a comment only in gate-23) — now pinned, red-proven **32→1 failed at exactly R-97→32**. ⚠ **`F-ab` EXTENDED and MEASURED**: `gate-47`'s Shift+Tab fails **in isolation**, control **3/5 pass** vs changed **2/5** ⇒ **this increment is NOT implicated**; ⛔⛔ this desk concluded the opposite from **n=1** first — *a control is a rate, not a run*. DOM measured **stable at 640** ⇒ reordering, not injection (hypothesis: the hero rAF loop `gate-49` neutralises and `gate-47` does not). ⚠ Four instrument defects of mine, all caught by their own output — incl. a **false green** where an unquoted zsh glob made `grep` fail and `wc -l` print `0`, and a snapshot run against the **wrong surface** (host vs container). ⭐ `gate-49` re-baseline **predicted, confirmed red first (2 failed/24 passed), exactly 2 of 24 regenerated**. ⛔ **BUILT, NOT DEPLOYED** — ⛩ push and ⛩ deploy are separate GOs.  ·  2026-09-03 🚀🚀 **DEPLOYED — the five GR-4 increments are LIVE and `R-124` IS DISCHARGED** (`deploy_record: 2026-09-03T21:37:31Z mode=prod tree=7cef6e0`; no override flags, ancestry guard passed on its own terms; headers 4/4 by value; probe 38 PASS / 0 FAIL against 7cef6e0 beside 15/22 red on a852423). Register §21.1: `gap → open` → **`gap → fixed`**, counts 189/174/0 unchanged because §21 adds no row. ⛔⛔ The carried NEXT pointer `P4.4b B3` was **stale in three surfaces** — B3 closed 2026-09-02 (`4bbbe01`); ⭐ all three AGREED, so only opening the mission file caught it: *three agreeing indexes are one claim copied twice*. ⚠⚠ The post-deploy green is **weaker than GR-1's**: script modified between runs after a **FALSE RED** (Astro's scoped attribute emptied the strip extraction) ⇒ the four strip-content assertions **have never been demonstrated to fail for the reason they assert** (`F-z`), stated as a limit. ⚠ The build refused the changelog entry on `title` 70 vs 73 — GR-1's *schema-limit check that measured one field and assumed its sibling*. ✅ `F-aa` discharged. ⏭ NEXT = the endgame, **human-gated at P5.1**, whose `AC-P` is satisfiable for the first time.  ·  2026-09-03 ✅✅ **`GR-4` CLOSED — ALL EIGHT CRITERIA MET, AAR FILED (SO#5) — AND WITH IT THE GRANDE REVUE'S RATIFIED GATE-1 ORDER IS COMPLETE** (B → P4.4b B1+B2a → GR-1 → **Lane D**, all four lanes closed). **AC-1…AC-8 ✅ · V1–V7 ✅.** Chromium **679 → 684** (delta isolated by `--list`: gate-54's own file **19 → 24**, +5, removed nothing) · all-projects **710** · **snapshot 26 UNCHANGED** · run **683 passed / 1 skipped / 0 failed** · `check:markup` **0** control-checked · red-test **27/0**, every case red at exactly its declared set · gitleaks **1025 commits, no leaks**. ⛔ **Nothing deployed and nothing owed** — met on-build; prod serves `a852423`. ⭐⭐ **`AC-6`'s OWN LABEL RESOLVES TO NOTHING AND A GREP FOR IT WOULD HAVE PASSED**: `movement 3` is **0** in both campaign directives `[D]`, the whitepaper's 4 hits are ordinary English, and the Grande Revue order was **VERBAL** ⇒ a gate greping the label returns 0, goes green, and checks nothing. **O3's homonym finding entered from the OTHER SIDE, and the worse half** — there a notation resolved to the WRONG referent (a false positive, which announces itself); here to NO referent, yielding a false negative **indistinguishable from a met criterion**, because AC-6's met-state IS absence. ⇒ ***a grep cannot tell you that it found nothing because there was never anything to find.*** Subject re-derived from D9(e)'s body — the embargoed protocol-opening story, checkable because it **shipped and was retired three times** (R-14 · P1-2 · R-125), all **0** across every non-excluded twin, with the zero **controlled**. ⚠ **The absence is NOT total**: a candor-framed horizon passage is already live on `/learn/tutorials/exchange-adoption-path`, and the vendored `skill-onboarding.md` carries the one un-candid promise — **`F-w`, filed at GR-1 as a TRUST-PAGE defect and never connected to D6.** ⚠⚠ **AND THE CORRECTION OF THE REVUE'S FIGURE WAS ITSELF WRONG**: carried as *"2 render, 2 comments"* in four artifacts, it is **1 renders and 3 are comments** `[D]` — ***the total was derived and the breakdown was typed***, KW-14 one level down and the same shape as the error it corrected. The conclusion survives: D9(e) said 0, the truth is 1, the phrase is live. ⭐⭐ **The `AC-7` enumeration cut FOUR things from two paragraphs** — *"clinical"* (the `/` twin measures it **ZERO**), *"a processor"* (**a GDPR term of art**, inside the section whose purpose is to make no regulatory claim), *"moves no data anywhere"* (the R-64/R-97/R-161 over-promise class on a **fourth** surface, in the mission that narrowed it on the third one objective ago), and a definition competing with the site's ratified *"open specification"*. ⭐ **`AC-8` is the FIRST constraint limb in this mission that could have bitten**: `/privacy` **9.43 → 9.98** vs target 12, where AC-4 and AC-3 *improved* and AC-5 *held* — the before **re-derived on this instrument and reproduced EXACTLY**. ⚠ `R124_FLOOR` was **typed, wrong, and GREEN** (1113/292 vs measured 892/295) — B0's finding, **fourth sighting in this mission**. ⛔ **`R-124` is BUILT, NOT LIVE, and its row deliberately does NOT move** (register §20.4); register rows **189** · ids **174** · `R-11`…`R-170` · **0 gaps**, derived after §20 was written. ⏭ Grande Revue's order is complete and **Lane D was its last lane**; nothing in the ratified sequence is queued. Held: **P5.1** with the humans. · 2026-09-02(c) ⏸⛩ **`GR-4` (GRANDE REVUE Lane D — story coverage) OPEN AND HALTED AT ITS ⛩ CONVENTION-13 PRE-BUILD GATE.** Nothing built, no copy authored. Pass COMPLETE **63/63** with coverage recorded, both directions: **6 defective · 57 clean · 7 non-pair findings** — tenth consecutive mission where it paid for itself, and the **first where it paid in the RECON**. ⭐⭐ **Two of the revue's six premises FALSE, one materially narrower**: model routing occurs **once site-wide, inside a byte-vendored file we must not edit** ⇒ the obvious criterion is **already green against zero work** (convention 18's family, caught **before** its instrument existed); `/network` does **not** own the L0–L3 story (**0** hits); and *"opening progressively"* was **live and rendered ten days BEFORE the revue called it 0 hits** — a **typed** figure (KW-14) typed wrong. ⭐ **DEFECT-1**: `AC-4` is tested only by its **constraint**, never its **substance**, and **FKGL falls when the criterion is met**, so the limb moves *reassuringly* while asserting nothing. ⭐ **DEFECT-4 spends `F-z` forward** — one mutation, two assertions. ⛩ Three questions to the signature (R-124's audience call · D3's home · D4's surface, headroom **measured**: `/` **0.04** vs **`/commons` 3.39**), plus `mission_count` 30→31. ⛔ No deploy owed. · 2026-09-02(b) ⭐⭐ **THE FOUR `content_static` BARS ADOPTED (post-close addendum to P4.4 — NOT a reopening; they were filed at its close as an OWED item, the GR-2 precedent).** ⛩ Operator: *"Go and adopt."* Adoption turned out to be a **FIXTURE RE-BASELINE** — the committed fixtures carried **1 category and 2 audits**, and the regeneration script **did not exist** though every fixture's `_provenance` said *"regenerate…"*. Built `scripts/gen_lighthouse_fixtures.mjs`; measured first, then pinned, at `lighthouse@13.4.1 --preset=desktop`: **perf 1.0 · a11y 1.0 · bp 1.0 · seo 1.0 · TBT 0 ms · desktop**. `gate-19` **3 bars → 7** (`a11yMin` · `bestPracticesMin` · `seoMin` · `tbtMaxMs`; **`perfMin` HELD at 0.9**) · `gate-53` **6 → 7** (**G53g**: the committed fixture now asserts its own instrument) · red-test **15/15**, every case red at its **declared** set. Chromium **660** · fast **542** · snapshot **26** · all-projects **686**, each derived by its own command; html-validate **0**; vault gates 26·35·37·41 **68/68**. ⛔⛔ **AND THE ADDENDUM OPENS BY CORRECTING P4.4's OWN CLOSE.** Its AAR said *"the fixtures carry `configSettings` ABSENT ⇒ 'our 90 is a desktop bar' is `[I]`, not `[D]`"* — **FALSE, in seven surfaces.** Every fixture carries a top-level **`_provenance`** naming the instrument, and the **raw archived runs** carry hard `configSettings` `[D]`. The error was checking the *standard* field, finding it absent, and concluding no record existed ⇒ **convention 16 breached one sitting after this vault cited it, inside the artifact built to record provenance.** Corrected at `e60e7ad` **before** the re-baseline. ⭐ Residual real but **NARROWER** — the *committed* fixture had no **machine-readable** instrument field, so **no gate could assert it** (*a gate cannot assert prose*), and the evidence sat in a **gitignored** dir CI never sees. **G53g is that close.** ⚠ **TBT adopted but INERT** (0 ms vs a 200 ms ceiling) — a **regression floor**, not evidence of interactivity health, and it inherits `perfMin`'s mobile-vs-desktop caveat; **not** silently tightened, because inventing a bar is `ratchet_law`'s operator gate. ⚠ **`seoMin` ZERO headroom**; **a11y deliberate redundancy** under `gate-4`'s axe-at-zero. ⚠ **A re-baseline that also moved the instrument (13.4.0 → 13.4.1) CANNOT ATTRIBUTE what it measures** — one route read 0.99 before and 1.0 after; confounded, and recorded as confounded. ⭐ **Three harness defects caught by the harness's own discipline**: `failing_set()` matched `G53[a-f]` and **could not see `G53g`** (every new case would have read NO RED); `G53c`'s coverage floor was **`>= 2` against an actual 6**; two declared red-sets went stale and reported **HARNESS BUG** rather than passing. ⇒ ***a coverage floor goes stale the moment its subject grows.*** ✅ **PUSHED** `60d0120..0362a00` (gitleaks clean, verified at the remote, unpushed **0**) and **BOTH staged replies DELIVERED** — Hopper → `Git.aDNA`, babbage → `Hardware.aDNA`, byte-identical, stamped, **RESYNCed**. ⭐ **Hopper's memo was stale on its face and its own supersession clause caught it** — it said the literal stays public *"until our next ⛩ GO'd push"*, which had happened 30 minutes earlier; **updated at delivery** rather than delivered false. ⏭ NEXT unchanged: **Lane D**. · 2026-09-02 ✅✅ **`P4.4` IS CLOSED — BOTH HALVES — AND THE ANSWER THAT UNBLOCKED IT HAD BEEN IN OUR OWN INBOX FOR FOUR DAYS.** P4.4b B2b + B3 shipped (`1f495c6` · `4bbbe01`); **AC1 ✅ AC2 ✅ AC3 ✅ AC4 ✅ · V1–V4 ✅**, with P4.4a's AC0 ✅ + V5 ✅ ⇒ every criterion of P4.4 met; AAR filed (SO#5). ⛔ **Nothing deployed and nothing owed to production** — met ON-BUILD by design; prod serves `a852423`, an ancestor of `main`, diff carries no site bytes. Chromium **659** · fast **541** · snapshot **26** · all-projects **685**, each derived by its own command `[D]`; html-validate **0**. ⭐⭐ **`F-e` STRUCK, and the premise it rested on was a CATEGORY ERROR.** For three weeks this campaign said our `Perf ≥ 90` is *"LOOSER than `content_static`'s 95 — the direction their `ratchet_law` reserves for an operator gate."* `_meta.lh_settings` `[D]`: *"mobile emulation + simulated throttling… **Desktop pass: not wired**."* ⇒ **their 95 is a MOBILE bar, `gate-19`'s 90 is a DESKTOP bar, and no desktop counterpart exists to be looser than.** B2a had already measured that gap: mobile throttle on a desktop form factor scored **0.78** vs desktop **0.95–1.0** — **form factor ~17 points, bar 5**. ⛩ Ruled: **HOLD 0.9 and SOURCE it**, so `F-e` closed by recording where each number came from rather than by moving one. ⭐ **EQUALITY IS NOT PROVENANCE** — LCP 2500 and CLS 0.1 *equal* `content_static` and were never derived from it (both took the CWV *Good* band); a number matching its supposed source is the hardest un-sourced number to notice, because it looks like the check already passed. Built: `bar_provenance.json` + **`gate-53`** (G53a–G53f), red-proven **9/9** (7 mutations + 2 controls), **every case red at its DECLARED assertion set** (GR-3's attribution clause). Hash is over `classes.content_static`, **not the file** — MEASURED: the file moved **43,988 → 52,879 B across 5 commits** while the subtree did not move at all. ⚠ **THE DELIVERY FINDING:** Vitruvius's scope-B answer landed **2026-08-29**, sat **untracked** in `who/coordination/`, and this vault recorded B2b as *"no reply yet `[D]`"* **three times afterwards** — verified in the peer's *prose notice*, never at **our own inbox**. **`F-u`'s class, fourth sighting.** babbage's 08-29 memo **named the untracked memos explicitly** and *that* memo was untracked too. 5 inbound memos committed (`d2228b0`); babbage answered (staged, ⛩ send GO owed); Chronos's Keystone ledger row corrected. ⚠ **AND THIS FILE WAS ITSELF THE FINDING'S NEXT INSTANCE:** it carried **ZERO mentions of `GR-2` or `GR-3`** `[D]` — both missions closed 2026-09-01, both cascades updated the campaign file and **not** this one. Repaired here. · 2026-08-28(h) 2026-08-28(h) 🚀🚀 **DEPLOYED — GR-1 + P4.4b B1/B2a ARE LIVE at `adna.network`, `tree=d5ff043`, and the GR-1 probe reads 26 PASS / 0 FAIL** (the same unmodified script that read 13/13 against `51af717` four hours earlier — every assertion has now been demonstrated capable of failing on this surface). `deploy_record: 2026-08-29T03:14:32Z mode=prod url=https://adna-docs-483gwhhxc-science-stanleys-projects.vercel.app token=SS_VERCEL_TOKEN tree=d5ff043` — **no override flags**; ancestry guard passed on its own terms (`live 51af717 is an ancestor of HEAD d5ff043`); live headers 4/4 by name AND value. Records: `artifacts/gr_1/probe_postdeploy_green.md`. ⚠ **TWO CARRIED FACTS WERE WRONG AND BOTH WERE DERIVED**: the record said *unpushed 39* and *next = push GO then deploy GO* — derived, unpushed was **2** and the push GO **was already spent** (`f2d7324` on `origin/main`, checked with `git ls-remote` AT THE REMOTE, not at a tracking ref that can be stale by exactly that much) ⇒ the gate was **one act, not two**. ⭐⭐ **THE PUSH-BEFORE-DEPLOY RULE HAS A MECHANISM AND WE FOUND IT BY LOOKING FOR ONE**: the two unpushed commits were records-only (zero shipped bytes), so 'push first' looked like ceremony — but `inject_build_stamp.mjs:83` stamps `git rev-parse HEAD` and **nothing checks HEAD is public**, so deploying first would have published a build stamp naming a commit **no stranger could resolve — P1-3's exact defect class, reintroduced by the act of shipping P1-3's fix**. Verified at the object: `git branch -r --contains <stamped sha>` → `origin/main`. ⭐ **Convention 18 decided the CI question**: CI is red on `gate-33-freshness` but `deploy_adna.sh:157` builds LOCALLY and `:237` ships `--prebuilt` ⇒ **CI's artifact never reaches production**, and the gate's own claim is green on the surface that ships — the red is a statement about **CI's checkout**. Scoped out with the reason written down, not waved past. ⚠ **`F-x` added — TWO debts**: (a) the cause, unknown; (b) **the error message names a remedy already applied** (`gates.yml:51` is `fetch-depth: 0`). Lead filed **as a hypothesis, not a diagnosis**: `contentSource.ts:37` computes `isShallow` as `git(...) !== 'false'`, so **a git that fails for ANY reason is indistinguishable from a shallow clone** — which is what lets the message be wrong, and is fixable regardless of cause. Operator scoped it to its own sitting. Register **22 · 15 struck · 7 live**. ⏭ **NEXT = Lane D or P4.4b B3** (`F-x`'s sitting now competes with both). ⛔ Still held: **B2b** (⊳ D-E; Vitruvius reply staged ≠ delivered) · **Hopper reply** (separate ⛩) · **P5.1** with the humans. ⚠ B1's vitals emitter is live but **zero-network** — owed: ⛩ Speed Insights enable → transport → first p75. // PRIOR:  # 2026-08-28(g) ✅ **PUSHED — `22a4fa6..f2d7324`, gitleaks clean, unpushed 0 / behind 0.** ⛩ Push GO taken; **⛔ the DEPLOY GO is NOT taken** (separate act by the campaign's own rule). ⭐⭐ **The advice was REORDERED before it was executed, and the reorder was the point**: `gates.yml:147` runs `npm run test:visual` **BLOCKING, no continue-on-error**, so the visual lane gates the **PUSH's CI run**, not the deploy — running it after the push would have been running it after the thing it guards. ⇒ lanes first. ⭐⭐ **And the lane was BROKEN, which is the only reason we know**: the container died on `Rollup failed to resolve import "web-vitals"` because `[ -x node_modules/.bin/astro ] || npm ci` tests **one binary** and concludes **the dependencies are installed** — so the named volume (populated at B0 `1816993`) never saw `web-vitals` (landed at B1 `9c8d79b`). **A staleness guard that cannot detect staleness**, the same shape as the `assetsInlineLimit` grep guard corrected hours earlier. ⚠ **CI never hit it** (fresh runner, unconditional `npm ci`) — only the LOCAL lane rots, which is worse, because the local lane is the one meant to catch things BEFORE CI. Fixed at `f2d7324`, keyed to the lockfile hash. **MEASURED after the fix, both lanes never before run against this HEAD: gate-49 in-container zero-tolerance **26/26**; full suite incl. @audit **644 passed · 1 skipped · 0 failed**.** ⇒ the O1 font change is **pixel-neutral (measured, not predicted)** and the 24 baselines — **38 commits stale** — hold. **Redaction verified AT THE PUBLIC OBJECT**: only IPv4 on public HEAD's keystone manifest is `127.0.0.1`; ⚠ history unchanged (the fix-forward ruling). ⏭ **NEXT = the GR-1 deploy probe run RED against prod, then ⛩ the DEPLOY GO.** ⚠ Two corrections to the advice as given: the redaction was RFC1918 + already-public-in-history ⇒ hygiene not incident; and **`deploy_adna.sh` runs NO tests** and its clean-tree guard does not check that HEAD is pushed. // PRIOR:  # 2026-08-28(f) ✅ **GR-1 CLOSED — THE TRUST PATH IS REPAIRED.** All five criteria met, AAR filed (SO#5), mission `completed`. Fast lane **522 → 526/1skip** derived · html-validate **0**. Every new or repaired gate RED-PROVEN with controls (G42e 4/4 · G18 5/5 · gate-36 pin 5/5 · G19 5/5). ⭐⭐ **Convention 18 earned by three live instances, and all three fixes landed IN the blind gate**: gate-42 ran on a CSP-free preview · gate-36 resolved the pin in the checkout it came from AND skipped in CI (could only ever pass) · gate-17 G12 checked twin *shape*, never twin *content*. ⭐⭐ **A4's fix was a THIRD OPTION neither ratified disjunct contained** — the pin is now derived from `.adna/CLAUDE.md`'s own `version:` and published as the immutable release tag **v8.9**; **all 5 URLs 200** (were 404), **all 4 files byte-identical AT THE PIN**. ⭐ **AC-1's scope settled and it falsified the pass's own premise**: 50/50 pre-fix → 0/50 post-fix, so the revue was RIGHT and this desk's inference was wrong (`unicode-range` defers a *network fetch*; a `data:` face has none). The remedy survived because AC-1 was written against the ASSET, not a page count. ⭐⭐ **Second-order finding: R-64 had carried P2-1's diagnosis AND its remedy since P0.5**, rediscovered independently 11 missions later — *a caveat in the register is a finding with a home and no gate*. ⚠ **Five of my own instruments were wrong before their subjects**, incl. a corpus excision that silently excluded NOTHING while reading like a success. **F-w** added with a named destination (next `skill_template_release`, with F-k); register **21 · 15 struck · 6 live** derived. ⛔ **BUILT, NOT DEPLOYED** — met on-build by design. ⏭ **NEXT = ⛩ a push GO, then ⛩ a deploy GO** (in that order), which would put GR-1 **and** P4.4b B1+B2a live; then **Lane D** or **P4.4b B3**. ⛔ B2b HELD on ⊳ D-E; P5.1 with the humans. Unpushed **39** derived at close. // PRIOR:  # 2026-08-28(e) ⛩⛩ **GR-1 SIGNED — THE GATE IS PASSED AND THE TRUST-PATH REPAIRS ARE BUILDING.** `ac_amendment_proposal.md` **`accepted`**; §4 adopted as proposed; budget ⛩ ratified **~200–290 kT / 2 sessions**, `opus`; mission `queued` → **`in_progress`**. Three further rulings in the same act: ⛩ **`mission_count` 27 → **28**** — the first change to that field since DP6, with a new **`### GR — Grande Revue`** charter section, `estimated_sessions` **37-50 → 39-52** and `calibrated_sessions` **37-42 → 39-44** re-derived same-commit (KW-14), **`phase_count` HOLDS at 6** · ⛩ the shipped visual identity is **v2** and the **TITLE** is the wrong string (a one-word content fix, NOT a URL change ⇒ ADR-057 redirects do not fire; `HomeHero.astro:327` is the **evidence**, cited not edited) · ⛩ the local-proxy gate class is now **convention 18** — *state the surface an instrument runs against, and whether it is the surface the claim is about* — ⛔ **habit only, no checker**. ⚠ **AC-1's scope question is NOT closed by the signature**: V1's red run during O1 settles it, and the criterion is written so either answer gives the same fix. ⏭ **NEXT = O1** (fonts as files under an unchanged CSP), then O2→O5. // PRIOR:  # 2026-08-28(d) ⏸⛩ **GR-1 (GRANDE REVUE Lane A) IS OPEN AND HALTED AT ITS CONVENTION-13 PRE-BUILD GATE** — `missions/mission_haussmann_gr_1_trust_path.md` (`queued`), `artifacts/gr_1/ac_amendment_proposal.md` (**`proposed`**). **NOTHING BUILT; criteria and budget NOT ratified.** First mission in the campaign with **no phase number** (`phase: GR` — Grande Revue is the late-stage review-and-improve phase, not a P-slot); ⛩ **`mission_count: 27 → 28` PROPOSED, NOT EDITED** (a new mission is not a split increment, and the charter reserves that field). Pass ran **COMPLETE at 40/40 with coverage recorded**, both directions: **31 clean · 9 defective** — 1 failure · 2 structural gaps · 2 limb defects · 2 unstated constraints · 1 open operator choice · 1 control that passed. **Eighth consecutive mission where the pass paid for itself.** ⭐⭐ Unifying finding: **three of five objectives are guarded by gates that verify a LOCAL PROXY for a PUBLIC or RENDERED property** — gate-42 runs on `astro preview` (no CSP) · gate-36:73 resolves the pin in the checkout it came from AND skips in CI, so it can only ever pass · gate-17 G12 checks twin **shape**, never twin **content**. ⇒ A1/A3/A4 are one finding with three faces. ⭐⭐ **A4 worse than recorded**: the manifest pairs a SHA from the local `.adna` clone of the **archived** `adna-legacy` with the URL of `aDNA-Network/aDNA`, and the pin is on **no remote** — so the battle plan's *"push the release-sync commit"* is **not performable**; the fix is a third option, pin **upstream `v8.9` → `c8e5427`** (serves 200). ✅ **Control measured**: all four vendored files **byte-identical** local vs upstream v8.9 ⇒ the re-pin **moves no content**. ⭐ **This pass did NOT raise the band** (first of eight) — findings split between cheap remedies and scope reductions. Band **~200–290 kT / 2 sessions**, `opus`. ⛩ **NEXT = THE SIGNATURE** (3 questions: the v2/v3 content-truth ruling · mission_count 27→28 · whether the local-proxy gate class becomes a convention). ⛔ B2b still HELD on ⊳ D-E (no reply delivered, re-verified this sitting); P4.4b remainder = B3; P5.1 with the humans. Nothing deployed; unpushed **33** derived. // PRIOR:  # 2026-08-28(c) ✅ **P4.4b B1 + B2a BUILT** (`9c8d79b` · `f852060`; suite 659 → **667** derived, fast 522/1skip). B1/AC2 ✅ ON-BUILD: `web-vitals` emitter wired in BaseLayout, same-origin ZERO-NETWORK by construction (connect-src 'self' · static output · gate-42); emission observed live (TTFB+FCP → `window.__adnaVitals` + `adna:vital`); gate-50 red-proven 6/6; `/privacy` §performance updated in the same commit BEFORE ship per its own commitment; owed-list on the mission's face (⛩ dashboard enable → transport at its own gate → first p75 reading on traffic). B2a/AC3 ✅: weekly `unlighthouse-sweep.yml` over the CI-built artifact, no continue-on-error, JOINS `gates-${{ github.ref }}` per gates.yml's own contract (queues, never cancels); gate-51 red-proven 7/7 incl. a transcription-drift detector binding the bar to gate-19's perfMin×100; V3 demonstrated live (EXIT=1 at bar 100, EXIT=0 at 90). ⭐ Calibration finding: Unlighthouse's CI default = mobile-4G throttle on a desktop form factor, scored the site 0.78 vs the fixture instrument's 0.95–1.0 — sweep pinned to the bar's own instrument (`--preset=desktop` constants), measured not assumed. ⛔ B2b stays HELD (Vitruvius's 08-27 notice telegraphs a scope-B reply STAGED their side — staged ≠ delivered, gate unfired). Nothing deployed; first on-GitHub sweep run owed on the next ⛩ push GO. ⏭ NEXT = **GR-1 Lane A at its own conv-13 pre-build gate** (ratified order); P4.4b remainder = B3 (+ B2b on reply). // PRIOR:  # 2026-08-28(b) ⛩⛩ **GRANDE REVUE GATE 1 SIGNED AS PROPOSED** — battle plan `accepted` (order B → P4.4b B1+B2a → GR-1 Lane A → Lane D; Lane C GO'd stage-and-deliver). Lane B executed same sitting: P4.4's stale qualifier struck-and-corrected (P1-7) · **F-n struck** as discharged-in-fact by `6675442` (P1-8) · **F-m struck WITH its fix** (P1-6: adr_index backfilled 047–059, tally 41 → **54**, self-matching drift-check glob fixed) — register re-derived **20 total · 15 struck · 5 live** (F-d F-e F-j F-k F-v) · R-124's deferral gated to Lane D's scope gate (P2-5) · Hopper's forge-address measurement confirmed at the object and the mesh literal REDACTED at source in keystone_cohort_manifest (fix-forward; reply staged, ⛩ send GO pending) · Venus ADR-022 ack verified already delivered (Dynamo refit). Lane C: Hestia data-ask memo (worldgenome per Gaia's table · wga · freshness advisory, figures re-derived live) **DELIVERED** to Home.aDNA under the Gate 1 GO. MANIFEST genuinely reviewed same-commit (57 skills · 45 templates · 27 subtopics at the topic-dir predicate — a wider find says 34 and is the wrong instrument; no ADR-count claim in MANIFEST). ⏭ NEXT = P4.4b **B1** (web-vitals wired AND emitting) → **B2a**. // PRIOR:  # 2026-08-28 ⛩ **GRANDE REVUE Gate 0 ACKNOWLEDGED — GO Phase 1**, and STATE's three stale counts corrected in this edit (the campaign's own *index-vs-artifact* class at the vault root, found by Grande Revue P0): the `phase:` field's struck-P4.4b sentence · suite 633 → **659** (B0/gate-49, `1816993`) · the QUEUED banner predating the P4.4b signature. Since the prior entry: **P4.4b SIGNED 2026-08-26** (`ac_amendment_proposal_p4_4b.md` `accepted`; §4 ruled **(c)** — build B2a, HOLD B2b on ⊳ D-E) **+ B0 CLOSED** (`1816993`, suite 633 → 659) · **P5.1 SIGNED, O0 CLOSED, HANDED OFF** (humans only) · Dynamo P5 refit closed (`df46d5c`) · MANIFEST reviewed same-commit (57 skills · 45 templates · 27 subtopics re-derived, zero drift) per G41d's own instruction. // PRIOR:  # 2026-08-27 ✅ **HAUSSMANN P4.5b CLOSED AND DEPLOYED** (tree=51af717) — the whole P4.5 mission with it. Suite 633/633, gitleaks 938 clean, unpushed 0. Voice guide published at /design-system#voice. ⚠ V4 partial: /commons 3.77, routed to D-8 not rounded. Next = P5.1. // PRIOR:  # 2026-08-25 ⛩⛩ **THE DEPLOY FREEZE IS LIFTED, AND `P4.3` IS CLOSED.** The lift rests on a MEASUREMENT, not a lapse of the old condition, and the reasoning is the reusable part: ⭐ **the freeze's RELEASE CONDITION and its PROTECTIVE PURPOSE had come apart, and its own text merged them into one sentence.** The actual invariant — *never publish a tree that does not contain the commit currently serving the alias* — is **built, red-proven 13/13, and ALREADY ON `origin/main` at `4a9bc09`**, so lemur receives it **on pull**; the alias serves `922519c` and **HEAD contains it**, so the guard PASSES on ancestry; and lemur's v0.4.3 + the Arch repo were **already un-published** at the 08-24 restore, so deploying does not destroy them again. ⇒ *"lemur pushes both halves"* is what **RESTORES** lemur's work — it is no longer what **PROTECTS** it. Holding cost four missions of unshipped work and **hard-blocked P5.1**, whose human-evidence criteria go green under the freeze while producing evidence about the WRONG BUILD (**G-11**). ⛩ **A push is now a PRECONDITION of the deploy** — deploy an unpushed tree and the alias carries a commit absent from `origin`, so lemur's pull cannot satisfy the guard and they are permanently blocked; push and deploy each take their own GO. ⛔ **Unchanged:** v0.4.3 + the Arch `[adna]` repo stay un-published and still need lemur; live `install.sh` stays at `0.3.1`. ✅ **`P4.3` CLOSED** (AAR, SO#5) — AC1 ✅ AC2 ◐ AC3 ✅ AC4 ✅ AC5 ✅ AC6 ✅ AC7 ✅; **D11 re-scored 2 → 4**, binary gate CONDITIONAL PASS → **PASS**, ceiling 4 declared before the work (anchor 5 needs AT-**USER** testing). ⛩ **O2's human VoiceOver sitting DEFERRED to a follow-up campaign by operator ruling** — recorded as register row **F-v**, NOT prose (G-6/G-7's lesson applied to this mission's own close), and named **on `/accessibility` itself** so no automated pass implies a human one. ⭐⭐ **A 620-green suite knew NOTHING about the new route** (gate-4 + P1S3 lists are hardcoded) — and **the first same-diff fix was wrong too**: the P1S3 sweep is `@audit`-excluded and scoped to what gate-4 does NOT cover, so it would have duplicated axe twice while leaving overflow outside CI. ⇒ **ADR-057 is about WHERE an assertion belongs.** Suite **628/628** derived. ✅✅ **PUSHED AND DEPLOYED — `deploy_record: 2026-08-25T22:58:05Z mode=prod tree=6675442`.** ⭐⭐ **AND THE FREEZE'S ORIGINAL RELEASE CONDITION SATISFIED ITSELF MID-SESSION: lemur pushed.** The push was REJECTED (`fetch first`) — the remote had gained **21 commits**, including `30c8163` + `f4fa9c5` (the freeze's two named halves, re-verified ABSENT at this session's 11:03 open and PRESENT by 15:53), a merge of our own Haussmann work, installer releases to **v0.4.17**, and lemur's OWN bootstrap-stamped prod deploy at `89a7263`. ⇒ **convention 16 live and in our favour**: *a verification with no recurrence is a claim about the past wearing the grammar of the present* — our session-open reading was TRUE at 11:03 and FALSE by 15:53, and only re-probing found it. ⭐ **The ancestry guard then did the job the freeze had been doing by hand, and did it better**: `check_alias_ancestry.mjs` refused nothing because we MERGED FIRST (`89a7263` an ancestor of `6675442`), and **no `--bootstrap-stamp` was needed or used — lemur had already consumed the one-time exception**, so the alias was self-describing and the guard ran on real evidence rather than on a forgiveness branch. ⭐⭐⭐ **THE PROOF THAT F-s IS CLOSED, not merely fenced: after OUR prod deploy, live `install.sh` still reads `0.4.17` and `/repo/arch/adna.db` + `adna-installer-0.4.17.tar.gz` are still 200.** Both checkouts' work is live SIMULTANEOUSLY for the first time in this campaign — the mutual-clobbering that raised the freeze did not recur, because the guard made merge-before-deploy the only available path. Live re-probe `[D]`: stamp `6675442` · `/accessibility` **200** (new) · `/vaults/graph` twin **15 relationship rows live** · `/vaults.json` · `/api/registry.v1.json` · `/state-of-the-network` · `/canonical-properties` · `/security` · `/privacy` all **200** · live-headers **4/4 by value**. ⚠ **F-n discharged en route**: the merge tripped gate-41's ratchet (49 → 50 days), the MANIFEST was genuinely REVIEWED rather than date-bumped, and the review found **`skill_web_quality_sweep` active and UN-TABLED for 8 days** in an inventory whose own text promises the count is *"auditable by counting rows"* — MANIFEST and CLAUDE.md agreed with each other while both disagreed with the filesystem. Ratchet **49 → 0**. Suite **628/628** on the merged tree. ⏭ **NEXT = P5.1 (human evidence), NOW UNBLOCKED** — it was hard-blocked by G-11 (criteria that go green against the wrong build); production now carries P4.1 + P4.2 + P4.4a + P4.3 and stamps the commit each participant saw. ‖ PRIOR (2026-08-24, superseded but kept for the record): 2026-08-24 ⛔⛔ **F-s CAUSE FOUND — AND THE RESTORE FIRED THE SAME HAZARD BACKWARDS. STANDING DEPLOY FREEZE.** Venus relayed it (`severity: high`, delivered **03:28Z — 44 MINUTES AFTER my restore had already fired**): the ten deploys came from the **lemur checkout of `aDNA.aDNA`**, *a second clone of this repo on another machine*, riding unpushed local commits **`30c8163` + `f4fa9c5`** (v0.4.3 installer artifacts + the Arch `[adna]` repo) and **recorded in lemur's own `deploy_log.txt`**. ⇒ **BOTH CHECKOUTS HOLD WORK THE OTHER LACKS, AND EACH ONE'S `--prod` DEPLOY SILENTLY UN-PUBLISHES THE OTHER'S.** My restore un-published v0.4.3 + the Arch repo `[D]` — 3 × 404. ✅ **Not broken though**: live `install.sh` pins `0.3.1` and that payload serves **200**, so the site is internally consistent, just regressed to the older release — *checked before escalating, because “an older release is live” and “installs are broken” are very different alarms.* ⛔ **FREEZE: no `deploy_adna.sh prod` from ANY checkout until lemur pushes those two commits and ONE deploy runs from a tree holding both halves.** Not performable here: `git cat-file` says both commits are ABSENT and the v0.4.3 bytes have never reached this node — **fifth** instance of a required act whose prerequisite does not exist on the performing tree. ⭐ **THE REAL GAP IS NOT A ROGUE ACTOR — `deploy_adna.sh` has a clean-tree guard and NO SINGLE-WRITER LEASE FOR THE PRODUCTION ALIAS.** Both checkouts followed the discipline; both assumed they were the only one deploying → ~~routed **P4.4**~~ **LANDED as P4.4 row `F-u` at P4.1's close 2026-08-24 — this line said *routed* for four days while P4.4's register had no such row, and F-u IS the freeze's release condition.** "Routed" is a claim about the destination: verify it there. ⭐⭐ **AND I HAD TO STRIKE MY OWN `[D]`**: F-s said the deploys were in *“no `deploy_log*` anywhere in the fleet”* — **FALSE**; that grep covered `~/aDNA` on THIS node, and lemur is a peer machine. **A NEGATIVE RESULT IS ONLY AS WIDE AS THE COMMAND THAT PRODUCED IT; `[D]` marks what was OBSERVED, not what was SEARCHED** — sibling of *“a truncated command is a derived figure”*. Struck not deleted, in F-s + convention 16 + the Venus ack. ✅ **P4.1 O1 COMPLETE** — AC1/AC2/AC3 met, suite **555/555** (554 + G25b), no `site/src` touched. ⛩ **AC AMENDMENT signed off** (5 changes): ⭐ re-reading the ACs against the RULINGS found a **FIFTH** defect the DP8-day pass missed — **AC2 offered a binary and the operator had ruled a HYBRID neither branch described**, so O1 would have ticked AC2 on the wrong branch and the validator adoption would never have entered the acceptance record. **ADR-059 (c) all four limbs**: `token_aa_check.py` **imports** WebForge's pair table + contrast math by reference (AA PASS 0 fails, red-proven 3 ways vs a clean control); `conformance.py` KW-10 adopted **scoped** as gate-25 **G25b** (red-proven both directions); divergence **pinned** in the wrapper with a review condition; Vitruvius memo **delivered**. **AC3 has a real test for the first time** — `--validate-entry` → **`entry READY`**, after it rejected `mechanism: image`, a value I invented. ⭐⭐ **THE INSTRUMENT WAS WRONG BEFORE THE SUBJECT WAS, TWICE**: `token_aa_check`'s first run reported **4 failures, ALL FOUR pairs I fabricated** (`--color-warning`/`--color-info` are used as text **ZERO** times; `--color-border` got a 3:1 floor when WCAG 1.4.11 governs component boundaries, not decorative dividers — 103/105 usages). **And 2 of the 4 were manufactured by the CHECK ON THE CHECK**: `\bcolor\s*:` **also matches `border-color:`**, so a border rule read as a text usage and *appeared to confirm* the fabricated pair. ⇒ **a contrast pair asserts “this colour is rendered on that colour”; if nothing in the codebase does that, the pair tests nothing.** Every pair now carries its usage count. ⚠ **A third false finding was one sentence from a delivered memo** — `--validate-entry` looked like it exited 0 on NOT-READY (a check that cannot gate); it was `| head` swallowing `$?`. Unpiped it exits **1**. ⚠ **`--strict-leak` NOT adopted wholesale, measured not skipped**: ~400 fires here (308 SVG attrs, 64 token-based `color-mix`) for 3 predicted real items — **G25b found 7, in TWO files**, both the same shape: a **half-guarded pair** (dark hex fenced by G25, light `hsl()` twin fenced by nothing). ⏭ **NEXT = P4.1 O2** — first slot application, ⛩ ruled **`empty_state` on the 57 planned vault cards**.  ·  2026-08-23 ⛩ **HAUSSMANN P4.1 — DP8 RULED, O0 CLOSED; and a LIVE PRODUCTION REGRESSION found + restored mid-mission.** ⛔ **F-s (the headline): `adna.network` was serving a build that predated 2026-08-18.** `/api/registry.v1.json` + `/vaults.json` **404** (P3.2 verified them 200 on the alias on 08-22) · `/state-of-the-network` **404 in all three forms** while the footer, the homepage's own `proofHref` and `llms.txt` all link it · P2.1's + P2.2's redirects **un-fired** (`/adopters` and `/vaults/Astro.aDNA` served 200, not 301) · the P3.3 machine-door block **absent**. The alias pointed at a deployment created **1h before the probe**; `vercel ls` showed **TEN production deploys that day, NONE recorded in any `deploy_log*` fleet-wide** (this vault's log ended correctly at the P3.4 record, and this session had deployed nothing). Project has no git integration ⇒ CLI deploys by an actor outside this vault's discipline; **cause still UNKNOWN**. ⛩ Operator ruled restore-via-sanctioned-path: `deploy_record: 2026-08-24T02:44:59Z mode=prod url=…j2fq4vn44… tree=922519c`, build 225 pages / 222 twins, injectors 4+5+42+444, **red-proven 10/10** (every assertion failing 1h earlier, passing after). ⭐⭐ **THE LESSON — `check_live_headers` passed 4/4 on the alias BOTH BEFORE AND AFTER the restore**: the stale build carried the same four headers. **Headers were never what regressed; content was, and only a content probe can see content.** ⇒ **convention 16**: a live check that runs once at deploy time and is never asked again makes *“DEPLOYED + LIVE-VERIFIED”* **a claim about the past wearing the grammar of the present**. Four such verifications were **honest when they ran** and silently went false with no event to mark it. Habit adopted (re-probe your phase's surfaces before trusting a `completed`); ⛔ **no monitor built** — convention 15's ruling stands. ⚠ **It was found BY ACCIDENT**, by a capture harness pointed at prod for an unrelated mission — and **its first casualty was this session's own evidence**: 30 green T0 captures, of the wrong build (re-captured against a local preview). ⛩ **DP8 RULED**: **ADR-053 → `accepted` (a)** — the visual voice is a **governed, slot-contained illustration program**: normative 5-slot table (`hero_panel` live ×10 · `vault_card_mark` · `empty_state` · `category_mark` partly-live ×6 · `graph_frame`), containment rule holding all other chrome to Tokyo-Night type-and-colour in both themes, **credit NORMATIVE and currently UNMET**, pipeline **named as owed not claimed**. **ADR-059 → `accepted` (c)** — authored this session because the substrate call proved load-bearing, not mechanical: adopt WebForge's **validators** over the existing CSS, **pin the emission divergence**; ⛔ derive NO ceiling, regenerate NO token value. ⭐ **THE ADR'S OWN PREMISE WAS FALSE AND CORRECTING IT CHANGED THE RULING**: the stub read *“one excellent hero and little else”* — `[D]` there are **TEN illustrated routes in ONE render language** (pixel art, warm wood/brass/amber vs cool cyan/purple, DNA-helix motif; three assets viewed at full res), and the instrument's actual definition is *“nothing **else** carries the style”* = confinement to one slot type, not one image. So DP8 governed an **existing ungoverned program**, **3 of the dossier's 5 requirements were already met**, and option (b) *reduce to an accent* silently meant **removing nine live surfaces**. ⚠ **`tokyo_night` IS NOT OUR SEED** — the pattern register's *“convergence is natural”* is WITHDRAWN: it is filed under `ss_ceilings` as “SS app canon (dark-only)”, declares `appearances:[“dark”]`, ships only dark primitives, and is the **anti_signature** in WebForge's own fixture; adopting it **deletes this site's light mode** (a protected asset). ⚠ **Convention-13 pass COMPLETE (16/16 AC×V + 4 AC×AC) — 3 FAILURES + 1 STRUCTURAL GAP, its best yield yet**: AC2's verification **cannot see AC2** (gate-25 *excludes* `tokens.css`/`branding.css` by construction; 4d compares WebForge's source) · AC2's record limb named the **wrong ADR** (resolved by authoring 059) · **AC4 has no schema to instantiate** (`style_atmosphere` is declared *“not exercised at GA”* with no schema file — a **Pygmalion ask**, not a local fix) · **O2's slot applications are covered by NO acceptance criterion**, so all four ACs could pass with **zero slots built**. ⛩ Operator ruled **AMEND THE ACs BEFORE O1**. ⚠ **F-t — I leaked `SS_VERCEL_TOKEN` into the transcript**: the redaction was `${VAR:+SET}${VAR:-UNSET}` and **the second expansion prints the value precisely when the variable IS set**; **`${VAR:+SET}` alone is the safe form**. Operator ruled *record, no rotation* (known throwaway). ⭐ **Fifth wrong instrument in two weeks — authored inside the session auditing wrong instruments**, and the first whose failure mode is **disclosure** rather than a false green. A sixth was caught by validating rather than by eye: a comment after a YAML `>` indicator became block content and broke the parse. ⏭ **NEXT = P4.1 O1** (AC amendment first, then the pin + the two validators, both red-tested).  ·  2026-08-21 ⏸ P3.3 O3 SHIPPED **REDUCED**, MISSION STILL OPEN AT ⛩ O2 — and ✅ **P0.4 CLOSED**. ⛔ **O2 IS NOT PERFORMABLE ON THIS NODE, GO OR NO GO**: `npm whoami` → ENEEDAUTH, **no `~/.npmrc`**, every npm env var UNSET, **NO npm row in the Home.aDNA credential broker** `[D]`. There is no npm identity here for a GO to attach to — the publish needs an interactive operator `npm login` FIRST. ⭐ **FOURTH instance in this campaign of a gate asking for a GO on an outward act whose PREREQUISITE DOES NOT EXIST on the performing tree** (installer tarball · @adna scope · DEFECT 3 · now O2) — **and this very mission WROTE that rule at O0, naming the credential case explicitly.** Writing a rule down is not running it. ⛩ Operator ruled: DEFER O2, ship the pre-agreed AC2-deferred O3. **What shipped**: a homepage `machine-door` block naming `/llms.txt` + the `.md` twins + `/api/registry.v1.json` ONLY — all re-probed live that day — plus the self-conformance sentence ON THE HOMEPAGE for the first time (item 13's actual complaint was placement, not truth). Twin count **222 DERIVED** from `twin_manifest.json`. **What did NOT ship, deliberately**: no server, no install line, no `npx`, **no `/.well-known/mcp.json`** (still 404) — all pre-agreed conditional on the publish, because a descriptor naming an unpublished package is a false claim on a machine surface. **machine_eye item 11 UNMOVED**, and the conformance report LEADS with that instead of burying it under nine green rows. Gates **552/552**, axe **0 in BOTH themes**, 12 captures, 0 console errors. ✅ **DEPLOYED + LIVE-VERIFIED ON THE ALIAS** — `deploy_record: 2026-08-22T03:40:39Z mode=prod tree=43e0280 url=https://adna-docs-588oiskjw-science-stanleys-projects.vercel.app`; post-deploy `[D]`: `"itself an aDNA vault"` → **1** (was 0) · `"222 pages have one"` → **1** · three named surfaces **200/200/200** · twins **10/10** no regression · `/.well-known/mcp.json` + `/mcp` **404/404** correctly · `mcp`/`npx` on `/` → **0/0** · live headers **4/4 no drift**. Packet re-stamped `live_alias_verified`, local-build banner **struck not deleted**. ⇒ **machine_eye item 13 FLIPPED ON A LIVE PROBE, not a status field** — and only its **placement** half; the machine-checkable half (no `source_vault_path`, no JSON-LD page→source tie) stays open, so the row reads ◐ not ▲. **Item 11 remains ABSENT** and the conformance report leads with it. **Pushed** `58876e9..43e0280` (2 commits) — ⚠ the pre-push hook again printed `clean ✓` having scanned nothing; the by-hand `gitleaks detect --source .` (855 commits, 1 known FP) was the real check. ⭐⭐ **TOP LESSON — the register pass caught a FALSE CLAIM IN MY OWN NEW COPY before it shipped**: the block first read *"a by-product … not an add-on bolted on later"*, which this campaign's own mission files disprove (twins landed P3.1, endpoint P3.2, BOTH because an audit scored them absent). Cut, replaced with the narrower structural claim checkable in `src/utils/twin.ts`. Record: `claim_register` **§14.1**. ⇒ **The register pass is a STEP, not a review reflex** — and convention 1 bit its own author mid-sentence. ⚠ **CONVENTION 13 WAS MIS-DIAGNOSED BY P3.3 ITSELF, and the correction is the real upstream contribution.** The mission said *"convention 13 does not ask whether each method reaches the surface its test probes"* — **FALSE**: that IS its second sentence, and **P3.1 is already logged in it as the identical failure mode**. The real gap is **scope + visibility** — P3.3's pass checked AC2↔AC3, cleared AC4's count, stopped, and **recorded no coverage**, so a partial pass was indistinguishable from a complete one to the ratifying operator. ⇒ two obligations added to `idea_upstream_mission_ac_coherence_check` + convention 13: **run every (method × test) pair**, and **state which pairs were checked**. *A correct question applied partially reports exactly like a correct question applied fully.* ✅ **P0.4 CLOSED with AAR** — Aspasia's ack answered all ten §6 questions; PR-3 aliveness **CLEARED WITH EVIDENCE** (4 guilds/23 users/49 channels/264 messages), PR-1 owned+sequenced, PR-2 unblocked by the ⛩ venue ruling "aDNA Community", D-1 RULED, D-2 → Venus, D-3 harvest interim OFF. Register **0/3 → 1/3 green** — ⛔ **and then 2/3–3/3 WITHIN THE HOUR**: a SECOND Aspasia memo (`…adr054_prerequisites_green`) was sitting UNTRACKED in the drop-box while that AAR was being written, declaring all three GREEN. ⭐ **The close sweep the AAR had JUST adopted is what caught it** — the discipline found, within the hour, the very memo proving why it was needed; open-only sweeping would have closed the mission on an already-false follow-up (struck in-file, not deleted). Re-probed `[D]` not trusted: **PR-1 MET** (`legal.terms_url`+`privacy_url` non-null → `aDNA-Network/community-policies` 200) · **PR-3 MET** · **PR-2 OWNER-GREEN / METHOD-RED** — config is `product_name: "aDNA Community"` + `theme_color: #9d7cd8` + self-hosted icon, but the served HTML `<title>` is **STILL `Fluxer`** (upstream app-proxy binary; a fork is ruled out by Fluxer ADR-000). ⇒ **2/3 by the register's own stated methods, 3/3 by owner attestation.** ⛩ **DP7 is now a REAL question, not a formality**: does "minimal aDNA branding" mean what a CLIENT RENDERS or what an UNAUTHENTICATED FETCH SEES? ⭐ **FIFTH instance in this campaign of a verification method that does not reach the thing it verifies** (DEFECT 3 · convention 14 · PR-2 · …). ⇒ **P3.4's `depends_on` DISCHARGED and its likely answer has flipped from NO-GO to an open ⛩ call; DP7 still NOT fired** — P3.4 owes a live re-probe at its own O0. **P2.6 is now the ONLY Decade-1 leftover.** Trigger model POLL → **PUSH**, over a drop-box nobody watches — *a push trigger whose transport is unwatched is a poll with extra steps*. 📨 Venus memo intaken (`…neither_a_nor_b_the_bytes_exist`, `ack_required: false`): the signed v0.4.3 artifacts **EXIST on lemur** — a **HOP, not a re-cut**, and it is **Jake's lane (ADR-019)**, not ours; our GO survives it; nothing returns to us on a clock. Venus concedes `installer/DEPLOYMENT.md` is **wrong and would have taken the deploy down**. ⇒ **convention 15 gains a THIRD face — reachability**: a memo can be delivered, byte-identical, correctly stamped, current, **and still ask for an act the recipient cannot perform**, because *actionability is a property of the recipient's filesystem and every check we own measures the memo*. Habit adopted (state the artifact's path from the RECIPIENT's root); **checker deliberately NOT built** — Venus proposed then declined it, and convention 14 agrees. ⚠ **F-q — THE GITLEAKS BASELINE IS SELF-DEFEATING**: the by-hand scan read **855 commits / 1 leak** before this session's commits and **857 / 2** after, and the new finding is **in the session file that documented the first one** — the record quoted the FP's matched text to identify it. Both share ONE literal (`measured+gating`, prose about **design tokens**), so **no secret, and never was**. ⛔ **But "expect exactly 1 known FP" is used as a pre-push go/no-go, and it now ratchets up every time an agent honestly records why a scan was clean** — within a few sessions a REAL leak arrives as "3 instead of 2" and reads as more of the same. Fix = allowlist/`.gitleaksignore` that literal to **zero**, or record the FP by fingerprint never by matched text; **do NOT just raise the expected number.** ⚠ **PROCESS ERROR, MINE, stated plainly**: I ran the rescan and the second push **in one command block**, so the push was NOT gated on the scan result. Outcome was safe (both FPs benign) but the sequencing was wrong — **scan, READ the result, then push, as separate acts.** ⚠ **F-o + F-p + F-q routed to P4.4 (now 17 rows, a→q; derive with `grep -cE '^\| \*\*F-[a-z]\*\*'`, never type)** — both are instrument defects found BY USING the instruments: **F-o** item 11's text probe has gone NOISY (`mcp` now 5 incidental hits in the corpus P3.1 grew 2 KB→950 KB, was 0) so a future `grep -c` scores it MOVED when it has not; **F-p** gate-17 G15's skip guard tests for `config.json`'s EXISTENCE rather than the routes it asserts on, so convention 6's own out-of-deploy workflow leaves it unskipped and certain-red, with a skip message naming a remedy that does not inject at all.  ·  2026-08-21 ⏸ P3.3 O0+O1 DONE, HALTED AT ⛩ O2 — MCP server built at `mcp/` (`adna-mcp-server`, 26 smoke + 24 red-test green) but UNPUBLISHED; /.well-known/mcp.json still 404. DEFECT 3: an npx stdio server cannot move machine_eye item 11 (URL + text probes) ⇒ discoverability limb at O3, conditional on O2. ~~Convention 13 gap named.~~ ⛔ **that diagnosis was FALSE — struck at O3 the next day; see the newest entry above.** Budget re-ratified ~245–285 kT. F-m + F-n routed to P4.4 (~~now 14 rows~~ → 16 at O3).  ·  2026-08-20 ⚠ WIND-DOWN INTAKE — two inbound memos to Rosetta sat UNREAD+UNTRACKED since 08-19 and would have been lost on context clear. (1) **Hopper/Git.aDNA — the pre-push gitleaks hook v1 is a PROVEN NO-OP at push time** (F-S158-01: `--pre-commit` scans the STAGED diff, empty during a push; prints its success line regardless). **This vault runs v1** `[D]` md5 `216aaca254b97d69819562d506afca29` vs fail-closed v2 `a1288f7371afa187cb1cfd8b9810a669`. ⇒ **ERRATUM filed on the P3.5 session record**: "gitleaks clean on outgoing changes" overstated the hook; the real evidence was the MANUAL full-history `gitleaks detect` run before each push. **Until v2 lands, treat the hook as decoration and run `gitleaks detect` by hand before any push that matters.** v2 is the NINTH fix in the pending `.adna/` release batch — fires on the next `skill_template_release` (Git.aDNA's lane). (2) **Pandora/Container.aDNA — Keystone cohort manifest row 6's `State` column is stale**: reads "P5 open-partial (blocked on D-9 registry)", false since 2026-08-18 (P2 CLOSED, D-9 RULED-ADOPTED, P5 package v1 finalized-awaiting-gate). Offered text in the memo; our pen. Low, no ack. Target: `how/campaigns/campaign_keystone/artifacts/keystone_cohort_manifest.md`. **LESSON: untracked inbound memos are invisible to `git status` habits and to context clears — sweep `who/coordination/` for untracked files at every wind-down.** Concurrency note: Berthier had an ACTIVE session in Exchange.aDNA during the wind-down pushes; only LICENSE was committed there and their WIP was verified untouched.  ·  2026-08-20 ✅ HAUSSMANN WIND-DOWN — R-128 CLOSED + the handoff made cold-start-proof. ⛩ Operator: *"no latlabs at all"*, holder ruled **aDNA Labs** — REVERSING the "leave both, decide later" taken hours earlier at the P3.5 push gate (reversal stated in register §11.1, not silently applied). ⚠ THE SCOPE PUT TO THE OPERATOR WAS WRONG: it said "four live LICENSEs", derived from a `grep | head -20`; the real MIT-holder class is **SIX**. zeta.aDNA was missed (fixed under the ruled principle, flagged in-field); `.adna/LICENSE` NOT edited — Standing Rule 1 — and inherits from the image repo at the next template release. **FINDING: a truncated command is a derived figure.** §9.5 said *count last*; this is the same defect one step upstream — the ENUMERATION a count comes from can be silently partial, and `head -N` never announces itself. `grep | head` is fine for orientation, never for scoping a decision. 5 repos pushed on GO: image `b94ec45` · aDNA.aDNA `71efbce` · zeta `6e7eb2e` · Exchange `6527f57` · LAVentureGraph `3fe62d8`; all five LICENSEs byte-identical md5 `b189a96420df57c630764b57ba7ff2f4`, so ⊳ D-D's "matching the image repo" now holds on a CORRECT name. Verification asymmetry recorded: only 2 of 5 are publicly fetchable — for zeta (private) / Exchange (Codeberg) / LAVentureGraph (rd-forge) the live check is UNAVAILABLE, not passed. LEFT STANDING deliberately: the BSL Licensor *"Lat Labs, Inc."* (a party to a commercial licence, not a copyright line — memo routed to Galileo, `coord_2026_08_20_galileo_bsl_licensor.md`), package metadata in other vaults, and ~74 historical refs (SO-6). HANDOFF FIXES: **P4.4 now carries the FIVE follow-ups routed to it** — they existed only inside the P4.5a/P3.5 AARs, which is *"recorded in three places and implemented in none"* recurring; its off-site CTA-target gate also changed PURPOSE (R-122/R-123 closed → it guards a regression, not a discovery). **convention 11 + P3.1 now state `depends_on: p2_6_midscore` is discharged by the DP6 signature** — P2.6 stays `in_progress` indefinitely behind ⛩ O0b, and a cold agent must neither halt nor learn depends_on is decorative. EVIDENCE ESTATE CLOSED (open since P1): the ratified Refit policy applied in its DANGLE-SAFE ORDER — 61 cited PNGs committed FIRST, then 126 uncited frames gitignored; zero new dangles (the 4 found are pre-existing Storyweave/Refit ignores). All 10 remaining Decade-2 specs verified substantive (66–72 lines, 4–5 objectives). NEXT = P3.1.  ·  2026-08-20 ✅ HAUSSMANN P3.5 COMPLETE — the aDNA standard has a public proposal process, and D9 (baseline 2, untouched by nine missions) finally moved. ⛩ ADR-055 RATIFIED: AEP naming · numbers IMMUTABLE even when rejected · an 8-state machine whose terminal states are as visible as its successful ones · §4 `final` means a check in aDNA's own tooling FAILS when the rule is violated · agent authorship disclosed in a REQUIRED field, ratification human-only · surface at /community/proposals/ because ADR-049 caps the primary nav at 7 and it is at 7. AEP-1 IS the process (the PEP-1/EIP-1/TC39/W3C pattern) at `final`, naming gate-37 as its own conformance check — delete the gate and AEP-1's claim fails with it. AEP-2 (the URL-casing law) at `review`, ⛩ operator-sponsored in-session because a sponsor is a person. R-122 NARROWED at execution: the image repo's CONTRIBUTING was never missing — it sat at .adna/CONTRIBUTING.md, one directory below the three paths GitHub reads. Present and invisible; different fix. SECOND consecutive mission a live re-probe shrank an inherited row (R-111 at P4.5a). R-122/R-123/R-127 CLOSED by two ⛩ GO'd pushes (image b64b81e..8354bce; origin 2f3bf5d..16e2c34), verified 6/6 live + GitHub API license: MIT (was null). R-128 filed #needs-human — the LICENSE names 'Lat Labs', on no other surface; staged byte-identical per ⊳ D-D, md5 verified. gate-37 = 20 assertions, red-proven 16/17. Suite 495→521 zero xfail. Axe 0 (4 surfaces × 6 viewports × 2 themes) — but a T0-caught empty table header revealed gate-4 filters wcag2a/aa and is BLIND to axe best-practice rules → P4.4. Deployed tree=16e2c34 (deploy_record 2026-08-20T22:48:11Z); probe 3 PASS/26 FAIL pre-deploy → 29/0 GREEN post. KEY FINDING: a red run is the only moment an assertion's VACUOUS branch is exercised — the probe passed 2 checks against production before the feature existed (undefined===undefined; 'no median' on a 404). Green runs can never reveal it. gate-14's C-1 failed the first build and was RIGHT: it answered R-122 before the register did. Register 146 rows / 132 ids, derived by a script now saved. Open: 5. NEXT = P3.1. P4.5 in_progress until P4.5b; P2.6 in_progress behind ⛩ O0b.  ·  2026-08-20 ✅ HAUSSMANN P4.5a COMPLETE — Decade 2's first mission shipped + proven live (probe 12/14 RED pre-deploy → 26/0 GREEN post; deploy tree=bb00464; pushed a8cc707..e4f0d65; suite 487→495 zero xfail; axe 0). Four rows fixed: R-120 (the S2 homepage contradiction), R-125 (the ⊳ D-C hero cut — FOUR surfaces, not one), R-111 (narrowed to /canonical-properties alone by a live re-probe), R-121. R-124 DEFERRED with reasoning — no destination to route to, so a fix would author a positioning claim. P4.5a HAD NO SPEC (the ⊳ D-A split lived only in frontmatter comments) — authored in-file. gate-23 REQUIRED the sentence D-C cut; inverted, not deleted, because its second half guards R-14. R-126 found: every changelog entry rendered one day early, timezone-dependent on the build machine. Register 140 rows / 126 ids, re-derived twice. NEXT = P3.5. P4.5 stays in_progress until P4.5b; P2.6 still in_progress behind ⛩ O0b.  ·  2026-08-19 ⛩ HAUSSMANN DP6 RATIFIED  # 2026-08-19 ⛩ HAUSSMANN DP6 RATIFIED — DECADE 2 OPEN. p2_replan accepted; NINE ⊳ ruled (D-I was unlettered inside §2 prose and would have ridden the master signature silently); 12 missions queued-provisional → queued; mission_count HOLDS 27; calibrated_sessions 37-42 (moved 3× in one session, derivation in-field); ADR-057 proposed → accepted; 2 unfollowable standing rules marked as such. D-A split P4.5 — P4.5a runs FIRST, P4.5b still LAST; phase order is NO LONGER claim order. Zero site/ files. ⛩ O0b STILL OUTSTANDING — D3 unscored, no 12-dim composite, R-34/R-63 undischarged, P2.6 in_progress. · 2026-08-19 🚀 P2.5 SHIPPED tree=db4b34f, live probe 52/0 (red-proven 3/33 pre-deploy) · P2.5 CLOSED 3-of-4, criterion 4 (TTFS run) CARRIED to P2.6 O0b/O0c · context refreshed ×4 · TTFS kit + fresh-account runbook authored · earlier: ✅ P2.5 O1 BUILT — zero-install tour live in tree (4 files vendored verbatim at a VERIFIED pin), /get-started reworked (cost statement · R-118 cut to a labelled gap · R-119 found+fixed · first success published), gates 472→487/487 zero xfail, axe-0 both themes · NOT deployed, NOT pushed (5 commits) · earlier: 🚀 P2.4 SHIPPED tree=d42ee68, live probe 37/0 (red-proven 17/17 pre-deploy) · /commons stopped showing nothing · changelog backfilled (P2.3 had shipped with no entry) · F-P2.5-1 invented terminal output on /get-started, R-118 · ✅ P2.5 O0 designed, ⛩ HALTED at the 4-question pick · NOT PUSHED.
status: active
phase: "P4/6 — ✅✅ **THE GRANDE REVUE'S RATIFIED GATE-1 ORDER IS COMPLETE 2026-09-03: `GR-4` (Lane D) CLOSED, and it was the LAST LANE.** GR lane: **GR-1 ✅ (08-28, deployed 08-29) · GR-2 ✅ (09-01) · GR-3 ✅ (09-01) · GR-4 ✅ (09-03)** — order B → P4.4b B1+B2a → GR-1 → Lane D, all four closed. ⏭ **NEXT is not queued by anything**: the ratified sequence is spent. Candidates are **P4.4b B3** and the **endgame (P5.1 → P5.2)**, and P5.1 is held with the humans. ⛔ **Five increments of new public copy are BUILT AND UNSEEN** (two doctrine pages · `/commons` · `/network` · `/`'s strip · `/privacy`) — Lane D is met on-build and **no deploy has been GO'd**, which is also what `R-124`'s discharge waits on. ~~prior: ✅✅ `P4.4` CLOSED 2026-09-02 (both halves; AC0–AC4 + V1–V5 all met, AAR filed) — the last multi-increment P4 mission is done. ⏭ NEXT = Lane D (needs an ⛩ audience/scope decision first).~~ ⛔ Held: **P5.1** (with the humans) · the **Hopper reply** + the **babbage reply** (each its own ⛩ send GO) · B1's Speed-Insights → transport → first p75. ~~prior:~~ ✅ **P4.5b CLOSED + DEPLOYED 2026-08-27** (tree=51af717; the whole P4.5 mission with it). ⛩⛩ **P4.4b SIGNED 2026-08-26** (§4 ruled **(c)**: build B2a, **HOLD B2b** on ⊳ D-E) and ✅ **B0 CLOSED** (gate-49, `1816993`) — AC1 ✅; ~~next agent increments **B1 → B2a**~~ ✅ **B1 + B2a DONE 2026-08-28** (`9c8d79b` · `f852060` — AC2 ✅ on-build, AC3 ✅, AC4 ◐ owed to B2b; suite 659 → **667**); remainder **B3** (+ B2b at its own ⛩ gate on the Vitruvius reply). ⛩⛩ **P5.1 SIGNED, O0 CLOSED, HANDED OFF** — waits on humans only (run order AC-3 → AC-2; AC-3 halts before submitting). **GRANDE REVUE** (chief-of-staff order 08-27) executes inside the campaign: P0 closed at Gate 0; Phase 1 closed at Gate 1 (Mid-Campaign Review 0 P0 · 8 P1 · 7 P2); ⛩⛩ **Gate 1 SIGNED AS PROPOSED 2026-08-28** — battle plan `accepted`, Lanes B + C executed same sitting (register live count 5, derived); ~~**live line of advance: P4.4b B1 → B2a**, then GR-1 Lane A at its own conv-13 gate~~ ✅ **B1 + B2a BUILT 2026-08-28** (`9c8d79b` · `f852060`); ~~**live line of advance = `GR-1`, OPEN AND HALTED AT ITS ⛩ CONVENTION-13 PRE-BUILD GATE**~~ ✅ **GR-1 CLOSED 2026-08-28** (signed, built, AAR filed; 5/5 criteria, suite 526/1skip). **Live line of advance = ⛩ a push GO then a ⛩ deploy GO**, then **Lane D** (or P4.4b B3). Closed to date: P4.5a ✅ · P3.5 ✅ · P3.1 ✅ · P3.2 ✅ · P3.4 ✅ · P4.1 ✅ · P4.2 ✅ · P4.4a ✅ · P4.3 ✅ · P4.5b ✅. Open: P2.6 (⛩ O0b only) · P3.3 (⛩ npm login, not performable here) · P4.4b (B1/B2a; ~~every criterion waits on an actor outside the session~~ — struck 2026-08-26 as FALSE for AC1–AC3, FINDING 9 of the signed amendment; only AC4 branches on ⊳ D-E) · P5.1 (humans). Queued: P5.2. Suite: fast lane **526/1skip** derived (522 → 523 G42e → 524 G18 → 526 at G19 + gate-36's split limb); full suite **667 → 671**. ⚠ The prior **659** was true when written and went stale at `f852060` — the index-vs-artifact class, corrected same-diff rather than left for a cold agent to quote forward. ⚠ Read convention 11's ruled order, not mission numbering."
campaigns: [campaign_haussmann]                      # RATIFIED §7.7 2026-08-16 (Gate C, operator) — 6 phases / 27 missions; charter: how/campaigns/campaign_haussmann/campaign_haussmann.md
last_edited_by: agent_rosetta
_state_router_version: "1.0"
tags: [state, governance, router]
state_history: STATE_archive.md   # Clear Hearth graduation 2026-07-17 rides the vault's own immortal-spine (§Shifted-2026-07-17)
---
<!-- Router shape — split from monolithic STATE.md at M2.1 S2 2026-05-19. Historical session prose at STATE_archive.md. -->


# Operational State

Dynamic operational snapshot for cold-start orientation. Updated each session.

> **State router** (split from monolithic STATE.md at M2.1 S2 2026-05-19; pre-split SHA `1e337db`). For historical session prose (19 DEPRECATED-marker `## Last Session` blocks + retired Next Session Prompts) see [[STATE_archive.md|STATE_archive.md]]. Most-recent live session block + most-recent Next Session Prompt stay here.

## ⏭ QUEUED — Next Live Session (READ THIS FIRST)

> 🚀 **2026-09-04 — DEPLOYED. `R-97` IS LIVE, AND THE FOUR-SURFACE OVER-PROMISE CLASS IS CLOSED.**
> `deploy_record: 2026-09-04T16:09:21Z mode=prod tree=2a72efe`. **No override flags** — the alias-ancestry
> guard passed on its own terms (`7cef6e0` is an ancestor of `2a72efe`); live headers **4/4 by name AND
> value** on the alias. Content-probed, not header-probed (convention 16): `/` carries
> *"aDNA itself sends nothing"* **×1**, *"nothing leaves your machine"* **×0**, and `R-120`'s
> *"files stay on your machine"* **×1** ⇒ **the two lines that were in visible tension nine lines apart
> now agree.** `R-64` `/get-started` · `R-161` `/network` · `R-167` `/privacy` · **`R-97` `/`** — the class
> diagnosed at **P0.5** is live on **all four surfaces**.
>
> ⭐ **THE DEPLOY DID NOT NEED THE COMMIT THAT WAS BLOCKING IT.** The 2026-09-04 R-97 sitting halted
> because `b2e943b` (Milner's `/learn/course`, **1157 insertions, 13 files, ZERO test files**, ~4 new
> routes) landed mid-sitting un-CI'd, and framed the choice as *ship both or ship neither*. **It was
> neither**: R-97's fix was **already at `origin/main` and already CI-green** (`7475318` + `2a72efe`), and
> the blocking commit sits **above** it. ⛩ Operator ruled the scope: **deploy `2a72efe`, R-97 alone.**
> ⭐ **The scope was verified falsifiably, not asserted** — `/learn/course/` and its lesson slug both
> **404** on the live alias. Convention-16 re-probe: `/privacy/` `/commons/` `/network/`
> `/state-of-the-network/` `/community/proposals/` all **200**, `regulated-data` still present ⇒ no regression.
>
> ⛩ **GO #1 taken too — `2a72efe..c32a4b7` pushed, gitleaks clean.** Pushing publishes **source, not the
> site**, and it is the **only** mechanism by which CI can ever gate `b2e943b`. Run `33893251012` was that
> first look: **`failure`, and EXACTLY ONE STEP — `gate-49`, 2 of 24 assertions**, `doc-hub (/learn/)` in
> both themes. ⛔⛔ **THIS DESK'S PREDICTION WAS WRONG IN BOTH DIRECTIONS.** It named the route-sweeping
> gates (`gate-42` · `gate-4` · `gate-47` · `gate-31` · `gate-26`/`20` · `gate-33`) on the reasoning that
> **~30 specs sweep every route** and four new routes had never met them. **Not one fired** — the four new
> routes, two client islands and 1157 new lines **passed the whole sweeping suite on first contact.**
> ⭐⭐ **The red came from the opposite direction: a TEN-LINE edit to an OLD page.** `b2e943b` inserted a
> *"Take the intro course"* section into `site/src/pages/learn/index.astro` and renumbered two headings —
> and `gate-49:58` pins `{ id: 'doc-hub', path: '/learn/' }` as one of its **12 templates**. Real change,
> never re-baselined ⇒ **ADR-057 same-diff (convention 7)**, and the **`gate-49` TEMPLATES quirk arriving
> from the side nobody watches.** ⇒ ***The exposure of a diff is its intersection with the PINNED set, not
> its line count*** — a sweeping suite grows to fit new routes by construction; **a pinned baseline does
> not grow, it disagrees** — and the 1157-line figure that made this commit look dangerous was the least
> informative fact about it. ⛔ **NOT fixed here, and the restraint is the ruling**: re-baselining
> `doc-hub` **certifies that the new `/learn/` hub looks right**, a judgment about the **course lane's**
> copy (convention 4 — the builder never self-certifies, and this desk is not that lane's reviewer either),
> and the baselines are **in-container**. **Routed with a complete diagnosis. ⚠ `main` is RED until that
> lane acts** — cost named, not left to be discovered. ⚠ **`F-ab` did not fire**: `gate-39`, `gate-42 G42b`
> and all three `gate-47` assertions **passed** ⇒ one more green observation on the load-sensitive set,
> and a rate is exactly what `GR-5` needs.
>
> ⚠ **AN ATTRIBUTION IN THE HALT RECORD WAS FALSE, AND THE HALT WAS STILL RIGHT.** It called `b2e943b`
> *"another writer's … not this sitting's to ship."* `git log -1 --format='%an'` says **Stanley**, co-authored
> by Milner, *"under the operator-carried consent of 2026-09-03."* ⇒ **right on review state, wrong on
> authorization** — different objections, different remedies (review state is discharged by a push;
> authorization would have needed the operator). `F-u`'s family, **seventh sighting**, falsified by one
> `git log`. *A record that halts for the wrong reason teaches the wrong rule.*
>
> ⛔ **`DP8` HAS BEEN RULED SINCE 2026-08-23 AND THE CHARTER SAID `pending` FOR 12 DAYS.** Corrected
> strike-not-delete (SO-6): the operator co-ruled **ADR-053 (a)** and **ADR-059 (c)** at one gate, both
> carrying 4-field ratification blocks. **No decision was ever owed — do not re-convene DP8.** ⭐ It is the
> **inverse** of the stale-pointer finding: there *three agreeing indexes were one claim copied twice*;
> here **one index disagreed with the object, and the object won**. Found only by deriving the DP list
> against `what/decisions/` instead of reading the table.
>
> ⭐ **`P2.6` O0b AND `P5.1` AC-2 ARE THE SAME OPERATOR ACT — AND THEIR CONDITIONS CONFLICT.** Both run
> `artifacts/p2_5/ttfs_runbook_fresh_account.md` on a fresh macOS account. **P2.6 O0b** requires *"an
> unassisted **non-builder** runner"*; **P5.1 AC-2 STRUCK** *"by someone who did not build the system"* at
> its 2026-08-26 signature and put the run on **the operator with the CoI declared**. ⇒ **operator runs it
> ⇒ P5.1 satisfied, P2.6 NOT**; **non-builder runs it ⇒ BOTH**. ⛩ **Recommended: one non-builder runner,
> one session, artifact labelled for both consumers** — it closes the campaign's longest-standing ⛩
> (`P2.6` has been `in_progress` solely for this since 2026-08-19). If none is available, **rule
> explicitly** that P2.6 O0b takes the CoI-declared form too, rather than leaving it open on a condition
> that was quietly superseded next door.
>
> ⏭ **NEXT: `GR-5` on `F-ab`** at its own ⛩ convention-13 pre-build gate, carrying the pin ruling.
> `gate-39` / `gate-42 G42b` / three `gate-47` assertions are load-sensitive ⇒ **three gates are currently
> unreadable**, a standing tax on every remaining sitting. The sharp half is diagnosed: `netdiagram-svg`'s
> **7.9px pin came from a local measurement CI does not reproduce** (CI reads 7.4) ⇒ ***a ratchet whose
> tooth position is non-deterministic does not ratchet.*** ⛔ **Do not move 7.9 → 7.4** — that is moving a
> bar to pass a test. Two candidates for the ⛩: **(1) re-derive the pin in CI's own environment** (fixes
> the ratchet, costs a round-trip) · **(2) record the gate advisory with the reason on its face** (honest,
> cheap, loses the ratchet).
>
> ⚠ **Also owed, unchanged:** `P5.1` O1's five recruited cold readers (**kit already built and waiting** —
> `panel_kit_v2` · `recruitment_brief` · `contribution_run_protocol` · `ttfs_run_record`, all filed
> 2026-08-26; ⭐ `AC-P` is satisfiable and **now satisfied by this deploy**) · **ADR-056** still `proposed`
> past its own "P3 exit" gate, the **last** `proposed` ADR in the vault · **DP7** (recommend folding into
> DP9 rather than convening separately) · B1's Speed-Insights p75 and P3.3's `npm login`, both **waiting on
> the world, not on a decision** · the four outward acts (Vitruvius `/g/adna/` · Hopper 4.2.0 ack ·
> babbage's lease · two `proposed` upstream findings), **recommend batching into one sitting**.

> 🚀🚀 **2026-09-03 — DEPLOYED. THE GRANDE REVUE'S FIVE INCREMENTS ARE LIVE, AND `R-124` IS
> DISCHARGED.** `deploy_record: 2026-09-03T21:37:31Z mode=prod tree=7cef6e0`. **No override flags** —
> the alias-ancestry guard passed on its own terms (`a852423` is an ancestor of `7cef6e0`); live
> headers **4/4 by name AND value**. Probe **38 PASS / 0 FAIL** against `7cef6e0`, beside its
> **15 PASS / 22 FAIL** red run on `a852423` (`artifacts/gr_4/probe_{predeploy_red,postdeploy_green}.md`).
> ⛩ Two GOs in order — **push then deploy** — because `inject_build_stamp.mjs:83` stamps HEAD and
> nothing checks HEAD is public. Live now: two doctrine pages · `/commons`'s name note · `/network`'s
> local-models band · `/`'s *What's new* strip · `/privacy#regulated-data`.
>
> ⭐ **`R-124` → `gap → fixed`** (register **§21.1**). ⛩ Ruling 1's condition was *LIVE, never
> written*, and GR-4's close held the row open against a built-but-unshipped section for exactly that
> reason. It is **`fixed`, not `verified`** — `§regulated-data` **makes no claim to verify**, so
> growing it is how it breaks, not how it improves. Counts **189 / 174 / 0, unchanged**, derived
> after §21 was written: *a register whose counts move every time someone writes a paragraph is
> counting paragraphs.*
>
> ⛔⛔ **THE CARRIED "NEXT" POINTER WAS STALE IN THREE SURFACES AT ONCE — `P4.4b B3` CLOSED
> 2026-09-02 (`4bbbe01`).** `mission_haussmann_p4_4_ci_hardening.md` reads `status: completed`, both
> halves, AAR filed. ⭐⭐ **The finding is how it survived**: all three surfaces (campaign `CLAUDE.md`,
> this file, `MEMORY.md`) **agreed**, so re-reading them against each other confirmed it every time;
> it fell only when the mission file the pointer *names* was opened. ⇒ ***three agreeing indexes are
> not a corroboration; they are one claim copied twice.***
>
> ⚠⚠ **THE GREEN IS WEAKER THAN GR-1's AND SAYS SO.** The script was **modified between the red and
> the green** — the first post-deploy run read **33/4**, a **FALSE RED** (the campaign's second after
> GR-3's): Astro's scoped attribute made the strip-extraction return the empty string, and four
> assertions failed against nothing. ⛔ The cheap fix — revert to the whole-page match — would have
> **restored the vacuity scoping had removed an hour earlier** (those assertions were matching the
> *footer*). ⇒ **Cost stated, not absorbed: the four strip-content assertions have never been
> demonstrated to fail for the reason they assert** — `F-z` verbatim, in a probe written hours after
> this desk quoted it. *Catching a class once in a sitting is not immunity from it.*
>
> ⚠ **The build refused the changelog entry first**: `title` capped at **70**, draft **73** — the
> description had been measured and the title's length **printed and compared to nothing**, which is
> **GR-1's own *"a schema-limit check that measured one field and assumed its sibling"***. Both
> limits now **derived** from `src/content.config.ts`. ✅ **`F-aa` discharged** — the glossary's
> *"cheapest → most capable"* was backwards against §2.1's binding; control re-measured, `grep -rl
> cheapest site/dist/` → **0**.
>
> ⚠⚠ **CI ADDENDUM — the close cascade's push went RED on `gate-39`, on bytes that had gone green
> twice, and a rerun of the SAME commit passed. `F-ab` filed (register §21.2).** The deployed tree
> `7cef6e0` is **CI-green**; `1d6af75` is records-only and `git diff … -- site/src site/public` is
> **empty**. ⇒ **the campaign's third false red**, non-determinism **demonstrated** by rerun rather
> than argued. ⚠ Sharper half: `netdiagram-svg`'s **7.9px pin came from a local measurement CI does
> not reproduce** (it read 7.4) ⇒ ***a ratchet whose tooth position is non-deterministic does not
> ratchet.*** ⛔ **Not fixed** — the figure is campaign-protected, loosening the pin is moving a bar
> to pass a test, and this sitting already produced two of its own instrument defects.
>
> ⏭ **NEXT: the endgame, and it is HUMAN-GATED — nothing agent-reachable remains in the backbone.**
> `P5.1`'s three criteria each wait on a human act (five recruited cold readers · a fresh macOS
> account · the operator running the funnel as an outsider); `P5.2` waits on `P5.1`. ⭐ **`P5.1`'s
> `AC-P` is satisfiable for the first time** — its stamp check demands *ancestor-of-HEAD AND contains
> the closed missions' work*, and production carried none of GR-4's copy until today (**G-11**, which
> a panel run yesterday would have walked straight into). ⛔ Held: `P2.6` (⛩ TTFS run) · `P3.3`
> (⛩ operator `npm login`, not performable here). ⚠ Owed: B1's ⛩ Speed-Insights → transport → first
> p75 · **`R-97`** (ratified copy, own gate) · **`F-w`** → next `skill_template_release` · babbage's
> lease question + two `proposed` upstream findings · **⛩ Vitruvius's `/g/adna/` decision** and
> **Hopper's 4.2.0 ack**, each its own outward act.

> ⛩⛩ **2026-09-02(c) — `GR-4` (GRANDE REVUE **Lane D**, story coverage) IS OPEN, SIGNED, AND BUILDING.**
> `missions/mission_haussmann_gr_4_story_coverage.md` → **`in_progress`**;
> `artifacts/gr_4/ac_amendment_proposal.md` → **`accepted`**. Budget ⛩ ratified at the **AMENDED**
> **~255–400 kT / 2–3 sessions**. ~~queued / proposed / criteria and budget NOT ratified~~ *(struck
> same-diff at the signature — P1-7's lesson: a stale qualifier denying a signature its own
> frontmatter records is how a cold agent re-opens a passed gate.)*
>
> ⛩ **FOUR RULINGS.** **R-124 → MINIMAL DISCLAIMING POSTURE** (new `AC-8` + `V7`: `/privacy` states
> aDNA is a file-layout convention, nothing is transmitted, and HIPAA/GDPR/IRB obligations rest with
> the **operator and their own tooling** — **no compliance claim of any kind**; ⛔ the register row
> moves when the section is **LIVE**, never when written). **D3 → route to where the L0–L3 ladder
> actually lives** (branch (i); the false premise **corrected, not built around**). **D4 → `/commons`**,
> the surface the **measurement** selected and **neither disjunct named** ⇒ **CONSTRAINT-2 dissolved**
> (⚠ the strip's prose-corpus measurement is **still owed at O4** — *a constraint that stops binding is
> not a measurement that has been taken*). **Both admin items PERFORMED**: charter **31 · 44-59 ·
> 44-51** (`phase_count` HOLDS at 6), and the capture estate folded in **dangle-safe** — ⚠ derived
> **67 untracked = 20 cited + 47 uncited**, where the carried figure *"~59 uncited"* was **wrong in
> both numbers**; controls **0 untracked · 20 staged · 0 dangling**.
>
> ⚠⚠ **THE BAND MOVED BECAUSE A RULING HAS A PRICE, AND IT WAS QUOTED IN THE SAME ACT.** §7 costed
> R-124 as an ⛩ *disposition*; Ruling 1 makes it a **deliverable** ⇒ +25–40 kT for AC-8. ⭐ **SO#11's
> P4.1 lesson has a sibling**: *a budget ratified before the pass is costed against a spec nobody has
> read* — and ***a budget ratified before the OPERATOR'S RULINGS is costed against a scope nobody has
> chosen yet.*** Same fix: quote the delta at the gate.
>
> ⭐⭐ **THE PASS WAS EXTENDED OVER ITS OWN AMENDMENT AND FOUND A SEVENTH DEFECT.** `AC-8` and `V7`
> arrived **at** the signature ⇒ they were **not among the 63**, and a signature is exactly when a pass
> stops being re-run. The **21 new pairs** surfaced **DEFECT-6**: **AC-8 adds prose to `/privacy` with
> NO reading-level constraint while its sibling AC-4 carries one** (`/privacy` 9.43 vs target 12 ⇒
> headroom 2.57 — low risk, **but low risk is not an assertion**, and V3 already runs the whole census).
> ⚠ **P4.4b's finding INVERTED** — there an amendment *stranded* a clause; here an amendment **omitted
> one its siblings carried**. ⇒ ***a criteria set is not re-read when it is amended, and the amendment
> is exactly the edit nobody checks against the set.*** Coverage **84 · 7 defective · 77 clean**. This is the ratified Gate-1 order's
> **LAST lane**. Derived at open: CI **green ×5** (`33586055067`), `origin/main` == HEAD `7210d5e`,
> **unpushed 0**, prod alias **`a852423`** re-probed (convention 16 — it **confirmed**, second time in
> this campaign against three contradictions). **No deploy is owed or sought.**
>
> Pass **COMPLETE at 63/63 with coverage recorded** (`C(7,2)=21` + `7×6=42`, derived not typed), both
> directions: **6 defective · 57 clean · 7 non-pair findings**. **Tenth consecutive mission where it
> paid for itself**, and the **first where it paid in the RECON, before a criterion existed.**
>
> ⚠ **FIRST CONTENT-CLASS MISSION AT THIS GATE, AND THE PASS WAS RE-AIMED.** Nine prior passes examined
> **build** missions, whose characteristic defect is *a method that cannot move a test*. A content mission
> fails differently — **a criterion met by a claim moving UP**, **a criterion already green for a reason
> that builds nothing**, and **a criterion whose met-state is emptiness** that most limbs cannot tell from
> neglect. **All three are present.**
>
> ⭐⭐ **TWO OF THE REVUE'S SIX PREMISES ARE FALSE AND A THIRD IS MATERIALLY NARROWER.**
> **D1** — model routing is not *absent*: it occurs **once site-wide**, inside the **byte-vendored
> `.adna/CLAUDE.md`** published on the trust page **with its own sha256** ⇒ the obvious criterion is
> **ALREADY GREEN against zero work**, and is unfixable in place (Standing Rule 1; editing the published
> copy would trade a copy defect for a **trust defect on the one surface built to be checked** — `F-w`'s
> reasoning, second row). ⭐ **Convention 18's family caught in a criterion that did not exist yet** —
> the first time this class has been caught **before** its instrument was written.
> **D3** — *"`/network` owns the L0–L3 story"* is **FALSE**: **0** L0–L3 and **0** compute-tier on its
> live twin; it owns the *aDNA-computer* story. **D6** — *"'opening progressively' has 0 hits
> corpus-wide"* is **FALSE WHEN WRITTEN**: live and rendered on `/state-of-the-network` since `9e0fd06`,
> **2026-08-18, ten days before the revue measured it**. ⭐ Source carries 4 occurrences of which **2
> render and 2 are comments that never do**, so source and rendered greps **disagree in both directions
> — and neither returns zero**. The figure was **typed** (KW-14), and typed wrong.
>
> ⭐⭐ **DEFECT-1 (V→AC, fourth sighting): `AC-4` IS TESTED ONLY BY ITS CONSTRAINT, NEVER ITS
> SUBSTANCE.** Nothing in V1–V6 asserts the copy is **present**; `V3` measures reading level ⇒ the
> mission could **ship nothing** and V3 passes. ⭐ And **FKGL falls as prose gets shorter and simpler**,
> so the limb **moves in the reassuring direction when the criterion is met** — the signature of a limb
> that gets read as confirmation. **DEFECT-4 spends `F-z` FORWARD**: `V4` had two assertions and one
> mutation, which reds via the nav count ⇒ the entry-point assertion would **never have been demonstrated
> to fail** — GR-3's clause applied at **authoring** time instead of in a harness's fourteenth day.
>
> ⭐ **Headroom MEASURED, and it points where neither disjunct named:** `/` **9.96/10 ⇒ 0.04** ·
> `/about` 0.57 · `/community` 0.72 · `/get-started` 1.84 · **`/commons` 3.39**. ⚠ The target set is
> itself ambiguous — P2-2 names `/`, `/about`, `/commons`; the instrument's ratified `FIRST_CONTACT` is
> `["/", "/get-started", "/learn/what-is-adna", "/community"]`; **they share exactly one member.**
>
> ✅ **`R-124`'s routing is recorded AT ITS OWN DESTINATION** (`claim_register.md`), because a routing is
> only checkable if the destination records the arrival (`F-u`'s class, four sightings). ⛔ **No ruling
> taken** — *arriving at a gate is not being ruled at one.*
>
> ⚠ **Two index defects found and fixed same-diff:** `session_prompts_haussmann.md` had **no rows at all
> for GR-2 or GR-3** while GR-1's still read *"⬅ CURRENT"* — five days and three missions stale, **in the
> file the campaign `CLAUDE.md` names as the REMEDY for the index-vs-artifact class.** ⇒ *a warning about
> an index is not maintenance of one.* And at this sitting's open, **the clock**: `date` returns local
> (**2026-09-01 23:0x PDT**) while every session file is stamped **UTC**; a local stamp would have filed a
> session **sorting before two that already happened**. ⇒ **a timestamp is a measurement, and it has a
> zone the way a count has a command** — and it would have read exactly like compliance.
>
> ⏭ **NEXT: `O1`** (D1 + D2 — the two clean agent-reachable adds), then O2 (D4 → `/commons`) · O3 (D3,
> branch (i)) · O4 (D5 **+ the owed prose-corpus measurement**) · O5 (D6 + AC-8 + close cascade + AAR).
> ⛔ **No deploy authorized or owed**; a push is its own ⛩ GO. ~~THE SIGNATURE, carrying THREE questions~~ (proposal §6): **R-124's audience/scope call** ·
> **D3's home** (the false-premise fork) · **D4's receiving surface** (ruled on measured headroom); plus
> §8's **`mission_count` 30 → 31** with both session bands re-derived same-commit (`phase_count` **HOLDS
> at 6**). **Until then, no build and no copy.** ⛔ Held: **P5.1** with the humans. ⚠ Owed: B1's
> ⛩ Speed-Insights → transport → first p75 · ~59 uncited capture PNGs · babbage's lease question.

> ▶ **2026-08-28(d) — 🚀🚀 DEPLOYED. GR-1 + P4.4b B1/B2a ARE LIVE; probe 26 PASS / 0 FAIL.**
> `deploy_record: 2026-08-29T03:14:32Z mode=prod tree=d5ff043`
> (`url=https://adna-docs-483gwhhxc-science-stanleys-projects.vercel.app`, `token=SS_VERCEL_TOKEN`).
> **No override flags** — the alias-ancestry guard passed on its own terms (*live 51af717 is an
> ancestor of HEAD d5ff043*); live headers **4/4 by name AND value**. The alias now serves
> **`d5ff043`**, re-read at `/.well-known/adna-build.json`. Record:
> `artifacts/gr_1/probe_postdeploy_green.md`, beside the red one — **the same unmodified script that
> read 13 PASS / 13 FAIL against `51af717` four hours earlier**, which is what makes this green worth
> something: every assertion has now been shown capable of failing on this surface.
>
> ⚠ **TWO CARRIED FACTS WERE WRONG AT OPEN AND BOTH WERE DERIVED, NOT TRUSTED.** The record said
> *unpushed 39* and *next = ⛩ push GO then ⛩ deploy GO*. Derived: unpushed was **2**, and the push
> GO **was already spent** — `f2d7324` was on `origin/main`, checked with `git ls-remote` **at the
> remote**, not at a tracking ref, which can be stale by exactly this much. ⇒ the gate in front of the
> operator was **one act, not two**.
>
> ⭐⭐ **THE PUSH-BEFORE-DEPLOY RULE HAS A MECHANISM, AND IT WAS FOUND BY READING FOR ONE.** The two
> unpushed commits were **records only** — STATE, the probe, the session log, **zero shipped-surface
> bytes** — so "push first" looked purely procedural and the tempting move was to deploy and push
> after. But `inject_build_stamp.mjs:83` stamps `git rev-parse HEAD` and **nothing in the chain
> checks that HEAD is public.** Deploying at an unpushed `d5ff043` would have published
> `/.well-known/adna-build.json` naming a commit **no stranger could resolve — P1-3's exact defect
> class, reintroduced by the act of shipping P1-3's fix** — and left the next deploy's ancestry guard
> resolvable only from this checkout (F-s's shape). Verified at the object, not inferred from the
> push succeeding: `git branch -r --contains <stamped sha>` → **`origin/main`**. ⇒ *A convention
> whose mechanism you cannot state is one you will skip on the day it looks like ceremony.*
>
> ⭐ **CONVENTION 18 DECIDED WHETHER CI'S RED BLOCKS.** CI is red on `gate-33-freshness` (641/1, five
> consecutive red runs). The question is not *is the suite green* but **which surface the instrument
> ran against**: `deploy_adna.sh:157` builds **locally**, `:237` ships **`--prebuilt`** ⇒ **CI's
> artifact never reaches production**, and the gate's own claim — *dates are real on shipped pages* —
> is green on the surface that ships. The red is a statement about **CI's checkout**. Scoped out with
> the reason written down, rather than waved past.
>
> ⚠ **`F-x` ADDED — AND IT IS TWO DEBTS, NOT ONE.** (a) The cause: **unknown**. (b) **The error
> message names a remedy already applied** (`gates.yml:51` is `fetch-depth: 0`), so the gate converts
> a symptom into a confident wrong diagnosis — the campaign's own class, inside its own CI. ⭐ A lead,
> filed **as a hypothesis and explicitly not as a diagnosis**: the CI log shows `actions/checkout`
> writing `safe.directory` under a **temporarily overridden HOME** which it then restores, and
> `contentSource.ts:37` computes `isShallow` as `git(...) !== 'false'` — so **a git that fails for any
> reason at all is indistinguishable from a shallow clone.** That conflation is what lets the message
> be wrong, and it is fixable regardless of the cause. ⛔ Operator scoped this **out** of the deploy
> sitting; a fix authored on an unverified cause is the thing the prior session warned against.
> Register re-derived **22 total · 15 struck · 7 live**.
>
> ⏭ **NEXT = Lane D** (story coverage, the Gate-1 order's last lane) **or P4.4b B3** — `F-x`'s own
> sitting now competes with both. ⛔ Still held: **B2b** on ⊳ D-E (the Vitruvius scope-B reply is
> *staged*, not delivered) · the **Hopper reply** (separate ⛩ send GO) · **P5.1** with the humans.
> ⚠ **B1's vitals emitter is live but zero-network** — "the emitter ships" is not "vitals are
> collected". Owed: ⛩ Speed Insights enable → transport → first p75.

> ▶ **2026-08-28(c) — ✅ P4.4b B1 + B2a BUILT (`9c8d79b` · `f852060`); B2b stays HELD; NEXT = GR-1 Lane A.**
> **B1 / AC2 ✅ ON-BUILD**: `web-vitals` emitter wired in `BaseLayout` — same-origin, **zero-network
> by construction** (CSP `connect-src 'self'` · `output: 'static'` has no collector · gate-42 reds any
> failed request); **emission observed live** (TTFB + FCP on one page load → `window.__adnaVitals` +
> the `adna:vital` CustomEvent); `gate-50` red-proven **6/6**. `/privacy` gained §performance **in the
> same commit, before ship, per that page's own written commitment**; changelog entry 2026-08-28. The
> **owed-list is on the mission's face** (B1 row): ⛩ dashboard enable (Vercel Speed Insights, toolkit
> A8's p75 source) → transport wiring at its own gate (`/privacy` first, again) → first p75 reading
> (+ deploy + traffic accumulation). **B2a / AC3 ✅**: weekly `unlighthouse-sweep.yml` (Tue 07:43 UTC
> + `workflow_dispatch`) over the **CI-built artifact**, **no `continue-on-error`**, **joins
> `gates-${{ github.ref }}`** exactly as gates.yml's own comment contracts — queues, never cancels;
> `gate-51` contract gate red-proven **7/7**, incl. a **transcription-drift detector** binding the
> sweep's bar to gate-19's `perfMin`×100 same-diff (bars stay un-sourced — F-e; provenance is B2b's,
> AC4 ◐ OWED). **V3 demonstrated live**: EXIT=1 at bar 100 with per-route ERROR lines; EXIT=0 at the
> standing 90 (`/` = 1.0 · `/vaults/graph` = 0.91). ⭐ **Calibration finding**: Unlighthouse's CI
> default applied **mobile 4G network throttle (150 ms RTT / 1.6 Mbps) to a DESKTOP form factor**,
> scoring the site **0.78** where gate-19's fixture instrument (`--preset=desktop`, 40 ms / 10.24
> Mbps) reads 0.95–1.0 — *a bar transcribed from one instrument and enforced by a stricter hybrid is
> two instruments sharing one number*; the sweep is **pinned to the bar's own instrument**, measured
> not assumed (`unlighthouse.config.ts`). Suite **659 → 667** derived (gate-50 +4 · gate-51 +4); fast
> lane 514 → **522/1skip**. ⛔ **B2b's hold VERIFIED at both surfaces**: no reply delivered; Vitruvius's
> 08-27 notice says on its face the scope-B reply *"remains staged"* their side — **staged ≠
> delivered**, the ⛩ re-entry gate has not fired. ⛔ Nothing deployed (P4.4b is met on-build); the
> **first on-GitHub scheduled sweep run is owed** on the next ⛩ push GO, which now also carries the
> Hopper-redaction + this sitting's work (unpushed: **derive, don't quote** — 28 at this sitting's open, plus its commits). ⏭ **NEXT = GR-1 Lane A at
> its own conv-13 pre-build gate** (ratified Gate-1 order); then Lane D. P4.4b remainder = **B3**
> (+ B2b on the reply, at its own ⛩ gate). P5.1 with the humans (AC-3 → AC-2).
>
> ▶ **2026-08-28(b) — ⛩⛩ GRANDE REVUE GATE 1 SIGNED AS PROPOSED; Lanes B + C EXECUTED SAME SITTING.**
> The battle plan (`artifacts/grande_revue/battle_plan.md`) is **`accepted`** with a 4-field
> ratification block: order **B → P4.4b B1+B2a → GR-1 Lane A (own conv-13 gate first) → Lane D**;
> Lane C GO'd stage-and-deliver. **Lane B done**: P4.4's stale `status:` qualifier struck-and-corrected
> (P1-7) · **F-n struck** (discharged-in-fact by `6675442`, live-on-paper for 3 days — P1-8) · **F-m
> struck in the same commit as its fix** (P1-6: adr_index backfilled 047–059, tally 41 → **54** = 52
> accepted · 1 amended · 1 proposed per-file-field-derived; the index's drift-check glob matched its own
> filename, fixed) — **register re-derived: 20 total · 15 struck · 5 live (F-d F-e F-j F-k F-v)** ·
> R-124's deferral now gated (dated register annotation routing it to Lane D's scope gate, supersession
> condition on its face — P2-5) · the 2 untracked inbound memos committed · **Hopper's measurement
> CONFIRMED at the object** (1 tracked occurrence, bare host) and the mesh overlay literal **REDACTED at
> source** in `keystone_cohort_manifest.md` (fix-forward, D6.1 ours-to-rule; ⚠ the literal stays on the
> public repo until the next ⛩ GO'd push carries the redaction) · ⛔ the Hopper reply is **staged,
> awaiting its ⛩ send GO** (`coord_2026_08_28_rosetta_to_hopper_measured_confirmed_and_redacted_at_source.md`)
> · Venus's ADR-022 ack verified **already delivered 08-28** (Dynamo refit — the "2 unanswered" handoff
> count was stale by one). **Lane C done**: the Hestia data-ask memo (worldgenome correction per Gaia's
> field table restated verbatim · wga refresh · freshness advisory **56/74 genesis · 50 null · 18
> frozen, re-derived live 2026-08-28**) **DELIVERED** to `Home.aDNA/who/coordination/` (cp + cmp
> identical, untracked peer-side). ⏭ **NEXT = P4.4b `B1`** (field instrument, **wired AND emitting** —
> V4's amended limb) **then `B2a`** (the sweep, **failing loudly** — V3 needs a run that goes red);
> ⛔ **B2b stays HELD** on ⊳ D-E. After that: **GR-1 Lane A** opens at its own pre-build gate.
>
> ▶ **2026-08-28 — ⛩ GRANDE REVUE Gate 0 ACKNOWLEDGED: GO — Phase 1 (the revue) runs now.** Between the 08-27 block below and this one: **P4.4b was SIGNED 2026-08-26** (`artifacts/p4_4/ac_amendment_proposal_p4_4b.md` `accepted`; §4 ruled **(c)** — build B2a now, **HOLD B2b** until Vitruvius answers ⊳ D-E, B2b re-enters at its own ⛩ gate) and **B0 CLOSED** (`1816993` — gate-49, 12 templates × 2 themes generated **and** compared in-container, V1 7/7; suite **633 → 659** derived; AC1 ✅); **P5.1 was SIGNED, O0 CLOSED and HANDED OFF** (waits on humans only — 5 cold readers · fresh-account TTFS · operator-as-outsider; run order **AC-3 → AC-2**; ⛔ AC-3 halts before submitting); the **Dynamo P5 refit** ran and closed in a peer sitting (`df46d5c`); and **Operation GRANDE REVUE** (chief-of-staff order 08-27, executing *inside* campaign_haussmann on main) closed Phase 0 at Gate 0 (`how/campaigns/campaign_haussmann/artifacts/grande_revue/situation_report.md`). ⏭ **Live line of advance: Grande Revue Phase 1** — rubric reconstruction (⛩ ratified before scoring) → ten-dimension revue → **Mid-Campaign Review + battle plan at ⛩ Gate 1**. Agent lanes after Phase 1: P4.4b **B1** (web-vitals wired AND emitting) → **B2a** (the sweep, failing loudly — V3 needs a run that goes red).
>
> ▶ **2026-08-27 — ✅ HAUSSMANN P4.5b CLOSED, AND WITH IT THE WHOLE `P4.5` MISSION — DEPLOYED.** `deploy_record: 2026-08-27T01:31:40Z mode=prod tree=51af717`; headers **4/4 by value**; alias re-probed, **8 of 8 shipped surfaces present**. Commits `78f6bbe` · `51af717` (O3) on `3c50f8c` · `b4e22c8` · `00adddd` (pre-flight · O1 · O2). Suite **628 → 633/633** derived (`gate-48` **+5**, plus one previously-skipped test now running because `inject_redirects` was run per convention 6) · `html-validate` **0** · `gitleaks` **938 commits, no leaks** · **unpushed 0**. **AC-a ✅ AC-b ✅ AC-c ✅ AC-d ✅ AC-e ✅ · V1 ✅ V2 ✅ V3 ✅ V5 ✅ · ⚠ V4 PARTIAL.** The voice guide is `accepted` and **published at `/design-system#voice`** — its publication limb was re-read from *"named as owed"* to **required** once the freeze lifted, the *criterion-amended-around-a-temporary-condition* class closing in the healthy direction for once. Budget **≈390–470 kT vs the ratified ~280–400 kT band** — at/just over the top, inside SO#11's 2× trigger. ⚠ `executor_tier` declared `opus`→`fable`; **O2 and O3 both ran `opus`**, stated rather than absorbed.
>
> ⛔ **V4 IS PARTIAL AND SAYS SO: `/commons` scores 3.77 against a 4.0 gate.** The pooled mean of the five ranked surfaces is **4.13** and clears it — ⭐ **which is exactly why P4.1 ruled surfaces are scored separately and never averaged, and this is the first time that rule caught a real failure instead of a hypothetical one.** Neither weak dimension is a copy defect: Actionability **3.0** is gated by aDNALabs **ADR-025** (human-only until federation GA) and the counsel embargo, so raising it would mean **inviting an action that does not exist** — a claim moving *up*, which convention 1 forbids. ⇒ **routed to D-8 / federation GA, not fixed.** `ranker_derive.mjs` computes every mean (KW-14) and **exits non-zero when a surface is below gate**, so the record cannot claim a pass the numbers do not support.
>
> ⭐⭐ **THE GATE'S SHAPE WAS THE FINDING, AND MEASURING BEFORE BUILDING IS WHAT PRODUCED IT.** AC-b's glossary limb read literally — all 25 glossary terms × all 21 rewritten routes — reports **79 unlinked mentions**, top offenders **`/glossary` (20)** and **`/reference/specification` (19)**: the definition home itself, and the ratified spec mirror. **A 97 % violation rate is the signature of a rule measuring the wrong thing, not of a site that is 97 % broken.** Scoped to where the one-new-term law actually binds — the four first-contact surfaces, and the **20 of 25** terms a literal match can decide — it reports **6 mentions and 5 real defects**, all cheap. ⚠ An earlier draft's *single* finding was a **false positive** (*"at the start of each session"* is not the aDNA `session` entity): **a gate whose only finding is wrong is worse than no gate.** Every exclusion is named on `gate-48`'s face (V5) and **G48d pins the arithmetic**, because quietly moving an inconvenient term into the ambiguous list is the cheapest possible way to fake it green — which is why that is the red-test case that matters most. `reading_glossary_redtest.sh` **7/7** (5 mutations + 2 controls).
>
> ⭐⭐ **TWO OF THE FIVE DEFECTS WERE NOT COPY DEFECTS — AND FIXING ONE MADE A SECOND INSTRUMENT WORSE.** The twin emitter converted `<strong>` before `<a>` and flattened the content with `stripInline`, so `<strong><a href>Triad</a></strong>` emitted `**Triad**` **with the href discarded**: **8 glossary links in the HTML of `/learn/what-is-adna`, 6 in its twin.** The copy was already right and **the machine surface was lying about it** — this campaign's *content that exists below the point of confusion* shape, arriving in an emitter instead of a sentence, on the surface P3.1 built to be machine-readable. Recovering those links then pushed a fifty-word prose bullet over the census's `multi-link` threshold and **out of the reading corpus**. Measured site-wide: **90 lines carry ≥ 2 links and 30 of them are punctuated paragraphs**, mostly the glossary's own definitions. ⇒ **the corpus shrank every time the copy became more reachable, so the instrument would quietly have rewarded leaving terms unlinked. An instrument that degrades as its subject improves is measuring against the wrong axis.** Repaired with the block guard's **already-ratified** invariant (*prose is punctuated*) rather than a fourth formulation; self-test **14/14 → 18/18**, both directions; `/how` and `/learn` gained enough recovered prose to clear the thin-prose threshold.
>
> ⚠ **AND THE POST-DEPLOY PROBE FOR THIS MISSION BREACHED THIS MISSION'S OWN AMENDMENT.** It grepped **HTML** for *"AI persona"* and reported the homepage disclosure **ABSENT**. It is live — Astro split `AI` and `persona` across a source line break, so the literal match fails on HTML and succeeds on the twin. **That is convention 17's amendment, authored at this increment's own O1 pre-flight, breached three commits later by the desk that wrote it.** *Knowing a rule and applying it while wearing a different hat are separate acts.* (A second wrong instrument the same session: a health-term census reported HIPAA in **224 of 223** twins, because `grep -lic` prints `file:0` for every file — caught by the number being impossible, not by care. Honest answer: **HIPAA 1 twin, GDPR 1, PHI/IRB/de-identification 0**.)
>
> ✅ **F-s STAYS DISCHARGED, RE-VERIFIED AT THIS DEPLOY:** live `install.sh` serves lemur's **0.4.19** and `/repo/arch/adna.db` is **200** after our push+deploy — **both writers' work live simultaneously.** ⚠ **The alias was serving `4b43c63`, not the `6675442` the handoff claimed** — checked live rather than trusted, and it is convention 16 exactly: *a verification with no recurrence is a claim about the past wearing the grammar of the present.* Ancestry guard passed; **no `--bootstrap-stamp`** (consumed 08-25); push preceded deploy.
>
> ~~⏭ **NEXT: `P5.1`** (human evidence).~~ *(superseded 2026-08-28 — struck not deleted, SO-6: P5.1's gate was signed and its O0 closed 08-26; it now waits on humans and nothing in it waits on an agent. See the 08-28 block above.)* Both `depends_on` are satisfiable — **P4.5 is `completed`**; P4.4 stays `in_progress` for **P4.4b** alone. ⭐ **G-11's hard precondition is genuinely met for the first time**: freeze lifted, backlog deployed, and `/.well-known/adna-build.json` serving a real stamp, so every criterion can record the commit the participant actually saw. ⛩ **Re-read that stamp at P5.1's open rather than quoting `51af717` forward.** ⛩ Also still queued: **O4**, the three staged Vitruvius memos (operator-GO'd 08-25, each shown before sending) — **not an acceptance criterion**, which is why the close did not wait on it. ⛩ Open follow-ups: `/commons` Actionability re-scorable at D-8 · **R-124 still deferred** (needs an audience decision, not copy) · the published `#voice` section and `doctrine_site_voice.md` are kept in step **by hand**, no gate asserts they agree.

> ▶ **2026-08-24 — ✅ HAUSSMANN P4.4a CLOSED (A0 · A0v · ruling 2 · A1 · A2 · A3), AAR filed (SO#5), AC0 ✅ + V5 ✅. The deploy hazard has an ANCESTRY GUARD, the debt register went 16 live → 6 all routed, four gate classes shipped red-proven — and the last objective found that the row it was executing was wrong about the peer's tree.** Sessions `..._221214_haussmann_p4_4a_ac0` → `..._230056_..._a1_register` → `..._172016_..._a2_gates` → `..._181438_..._a3_vitruvius_aar`; commits `4a9bc09` · `ff4ad51` · `3cc659f` · `ffcc0f3` · `0c97af3` · `72fb15b` · this close. Suite **571 → 587/587** (derived from the run) · red-tests **7/7 · 6/6 · 7/7** + AC0's **7/7** (14 mutations + 6 controls, plus the alias guard's own matrix) · `alias_guard_redtest.sh` **13/13** · `token_census_redtest.sh` **10/10** · gitleaks **893 commits, no leaks** · `astro check` unchanged at F-j's re-measured 26-error baseline. ⛩ Budget **≈555–635 kT against the re-raised ~600–750 kT band — inside it**; SO#11 triggers no retrospective.
>
> ⭐⭐ **A3 SET OUT TO SEND AN ASK AND FOUND THE ROW ASKING FOR IT WAS WRONG ABOUT THE PEER'S TREE — three of `F-e`'s four load-bearing claims measured FALSE.** `lighthouse_profiles.json` **exists** (`WebForge.aDNA/what/lib/gates/`, 43,988 B) — the `find` that reported *"0 hits vault-wide"* searched **our** vault and never said so. Our federation pin `6096157` is **byte-current** against it (md5 `134c9647c4c348034db3fa32d65d9db1` at the pin, at their HEAD `14838774`, and in their working tree; `git log 6096157..HEAD -- <path>` **empty**). And **nothing of ours reads it** — `grep -rn lighthouse_profiles site/` → **0** — so gate-19's bars were never transcriptions at all: they are the CWV *Good band* over slim **desktop** fixtures (LH 13.4.0, no `configSettings`), against their live **mobile** class of Perf 95 / tbt 200 / a11y 95 / bp 95 / seo 100, fingerprint-bound. ⇒ **the bars are UN-SOURCED, a different defect the mirror would not have touched — and `Perf ≥ 90` is LOOSER than their 95**, the direction their `ratchet_law` reserves for an operator gate.
>
> ⭐ **The mirror is contradicted from BOTH ends, and one of them is our own file.** Our wrapper's line 24 names **gates** among what is *"consumed by reference, never copied"*; WebForge's `CLAUDE.md` says the bars *"are class-keyed data … **read them there and never transcribe them**"*; and our two live consumers (`lock_coverage_check.py`, `token_aa_check.py`) already **resolve the pinned path**. A mirror would have been a third mechanism that goes stale in silence. ⭐⭐ **AND A1'S RE-READ PASS HAD ALREADY RUN — it re-ran the row's own `find`, got 0, and confirmed the wrong thing.** ⇒ **Re-reading a row at the object means re-deriving its QUESTION, never re-running its COMMAND**; the command already encodes the assumptions. Second face: **`F-e` prescribed a mirror at the moment of diagnosis exactly as `F-u` prescribed a lease** — both diagnoses sound, both remedies wrong. **Two instances is a pattern: diagnosis and prescription are separate acts.**
>
> ⛩ **Ruling 3 HONOURED, not overturned.** The ask is **authored and STAGED, not delivered** (`coord_2026_08_24_rosetta_to_vitruvius_profiles_are_read_not_mirrored`) — it withdraws our premise, then asks **one** question in **two scopes**: does `adna_site` enter their fleet (cost named on its face: it means adopting `run_lighthouse.mjs`, not a number), or do we derive against a named class with every divergence pinned. *"Neither yet"* is pre-recorded as a real answer, and we raise their own *"do not apply one class's bars to another"* against our scope B rather than wait for them to. **Convention 4's RULE is untouched and no amendment is owed**; only its **evidence sentence** is corrected to name the surface it searched — conventions **16 and 17**, both authored by this campaign, both breached by that one sentence. ⛩ Operator ruled **no delivery this session**: all **three** memos to Vitruvius stay staged (`registry_tier_pattern_owed_back` 08-19 · `craft_floor_consumer_surface` 08-24 · this one).
>
> ⚠ **The live residue is named, not closed: our gate-19 bars are still UN-SOURCED, and P4.4b's AC4 does NOT close that by hashing a mirrored file** — a hash over a copy passes while the bars still come from the CWV band, reproducing *"the distinguishing claim was tested by nothing"* one layer in. AC4 amended to require hashing the bars' **source**, red-proven against a hand-edited bar; **B2 must not be built before Vitruvius answers**, or the provenance chain gets hashed to a class we were never assigned. Register: **19 rows, 13 struck, 6 live, every one routed** — `F-d` · `F-j` → A1b · `F-e` → the staged ask · `F-k` → the next `skill_template_release` · **`F-m` · `F-n` FENCED by `gate-41`, not fixed** (a ratchet is not a discharge; ⚠ **F-n must never be cleared by bumping a date** — `updated:` claims the content was *reviewed*).
>
> ⛔⛔ **BUILT, NOT DEPLOYED — the THIRD consecutive mission accumulating unshipped work.** Freeze re-verified at this session's open **and** close: `git cat-file -t` fatal on both `30c8163` and `f4fa9c5`. **P4.1 · P4.2 · P4.4a are all built-not-deployed.** ⚠ First prod deploy after the freeze lifts needs `--bootstrap-stamp=<that day>` **once** (`/.well-known/adna-build.json` 404 live = documented pre-bootstrap state, not a defect). ⛩ **NEXT IS AN OPERATOR ROUTING CALL AND IS NOT TAKEN HERE (SO#1):** convention 11 puts P4.4 → **P4.3**, but P4.4's remaining half (**P4.4b**) has **every criterion waiting on an actor outside the session** — Vitruvius's answer, lemur's push, the operator's dashboard. So: hold for P4.4b's unblock, or open **P4.3** (*accessibility beyond automation*), which already holds **O1's 12px rendered-typeset floor** (⛩ deferred to it) and P4.2's `aria-live` residue. **The mission file stays `in_progress` because P4.4b has not started** — one file holds both halves, so a `completed` there would claim P4.4b. **Read the qualifier, not just the field.**
>
> ▶ **2026-08-24 — ✅ HAUSSMANN P4.2 CLOSED: O0 ✅ O1 ✅ O2 ✅ O3 ✅, all six criteria met, AAR filed. The craft floor is DECLARED, GATING and now ENFORCED ON 13 LOCKS — and two of the census's own findings turned out to be false.** Session `session_stanley_20260824_190604_haussmann_p4_2_o2_o3`; commits `e2fca67` + this close. Suite **560 → 571/571** · html-validate **0** · `token_aa_check` **AA PASS** · lock census **PASS 60/60** (enforced **8 → 13**, gap **29 → 24**, rung1a **6 → 11**) · `craft_floor_redtest.sh` **11/11** (9 mutations + 2 controls) · component census **30/30** · hub depth **4/4** · axe **0** across 5 surfaces × 3 viewports × **both themes** · gitleaks **880 commits, no leaks**.
>
> ⭐⭐ **TWO CENSUS FINDINGS WERE FALSE, BOTH CAUGHT BY RE-VERIFYING AT THE OBJECT BEFORE BUILDING AGAINST THEM.** **B3/E4** — *"`aria-live` appears nowhere in `src/`"* is wrong: it has been at `vaults/index.astro:226` since **2026-07-11** (`5b9be4c`), six weeks before the census, wired to the search input and both chip groups, announcing the zero case. **F20/A2** — *"`JetBrains Mono Variable` reports `document.fonts` state `error` on every page"* had stood untested since 08-19; probed across **4 routes × both themes it is errors 0/0 every time**, and the observable that reads as the claim is `unloaded: 5`, the *correct* state for five subsets whose `unicode-range` matches no glyph — **Inter and Space Grotesk show the identical shape**, so the control was in the same FontFaceSet all along. ⇒ **A census that declares coverage does not exempt itself from convention 14.** ⛩ Operator ruled **strike + re-disposition, build nothing**. Both keep a narrower true residue: nothing *asserted* the live region stays wired, and **nothing watched font loading** — which is exactly how a false claim about it stood five days. `gate-38` G38c/G38d watch it now.
>
> ⭐ **`gate-39` found a real defect on its FIRST run, and the honest answer is that lock O1's floor is NOT met.** Measuring what the lock actually names — rendered size via `sqrt(|det(CTM)|)`, never `getComputedStyle` — `hero-graph-svg` paints **27/27 labels below 12 px at every width** (3.5 px at 320, never better than 7.1 at 1920); `netdiagram-svg` 7/8; `convergence-funnel` 8/8 at 320. Ships with a **dated baseline** that hard-fails any unlisted figure or regression and ratchets as each is fixed — but **O1 stays `gap`**, because a non-regression fence is not the rule and calling it one is the fake-enforcement this lock's own text warns about. Remedy is design work (0.28× scaling at 320 px) on a campaign-protected surface ⇒ **routed to P4.4**; `/design-system#diagram` states the floor is unmet rather than letting a reader assume.
>
> ⭐ **The component census's finding: the only token family with a gate was the only one that had not drifted.** Colour (gate-25) **0** findings across 30 components; **font-weight**, whose tokens exist and whose own comment says they *"replace the scattered literal 400/500/600/700 across components"*, had reached **2 of 15 files**. 26 literals → tokens across 13, with zero rendered change **proven** (all 27 `var()` references resolve) rather than asserted. **A migration announced in a comment is not a migration.** ⛩ Operator ruled **census (30), not sample (20)** — recorded as *exceeding* AC3, never as redefining it.
>
> ⭐ **The thin-hub budget is DERIVED — and the first draft of it was circular.** It invented `h2 ≥ 2, bodyLen ≥ 1200` and graded four pages against it. F13 never stated a threshold; it named a **counter-example** (*"/learn does the same job with a numbered path"*), so the site's own conformant hubs became the budget: floor **h2 4 / bodyLen 1932**, and the two groups do not overlap on either axis. All four brought to it with orientation content a reader needs — `/how` 0→4 & 659→2015 · `/patterns` 0→4 & 1507→2659 · `/use-cases` 1→4 & 1530→2454 · `/reference/specification` 0→4 & 927→2630, including the spec hub's **anchor-forwarding behaviour, which was real, implemented and undocumented**. **KW-14 applies to the yardstick, not only to the reading.**
>
> **Also shipped**: **A5/B4** — `scroll-padding-top` from a **measured** header (69 px at 5 viewports × 4 routes); removing `commons.astro`'s two page-local `scroll-margin-top` rules was **required, not tidying**, because scroll-padding and scroll-margin *compose*. **J1** — one `<h1>` first in the DOM, 226/226 already conformant, which is why the red test mattered. **I3** — documentation chrome contributes no headings to the outline. **`/design-system` → `DocumentationLayout`**, fixing the ranker's weakest Findability (**3.6**); `noindex` rides a new additive pass-through prop, and the Illustration-slots section survives intact.
>
> ⚠ **Nine instruments wrong before the subject this session** — sixth consecutive session, every one caught by its own output, not by review. Including a red test whose **control failed on port reuse**, a probe conflating *element not found* with *value collapsed*, a gate reading a figure's class off the wrong element, and **a suppressed build error** (`> /dev/null 2>&1`) that produced a stale `dist/` and surfaced as a nonsense Playwright error. ⭐ **What caught them was structure, not vigilance**: mutation-applied assertions, controls, coverage floors (`measured >= 200`, never `> 0`), and re-verifying at the object. The red test twice found defects **in itself** before it could test a gate.
>
> ⏭ **NEXT = P4.4** (`mission_haussmann_p4_4_ci_hardening.md`) — the ruled successor, and the holder of **F-u**, the deploy freeze's release condition. It now also inherits **O1's three figures** (numbers on their face, ratcheting) and the *"should the component census become a gate"* question. ⛔ **P4.2 is the SECOND mission accumulating unshipped work**; lemur's `30c8163` + `f4fa9c5` re-verified **absent** at open. ⛩ **The push GO is owed and not mine to fire — 8 commits unpushed.** ⛔⛔ **CORRECTED 2026-09-02 — THIS CLAIM WAS FALSE AND IT WAS MINE.** The fixtures **do** record their instrument: every one carries a top-level **`_provenance`** string (*"Lighthouse 13.4.0 **desktop** on /vaults/graph"*; two name `--preset=desktop` exactly), and the **raw archived runs** (`site/evidence/`, gitignored) carry hard `configSettings` `[D]`: **`formFactor: desktop` · `screenEmulation.mobile: false` · `rttMs: 40`** at LH **13.4.0**. ⇒ **the desktop claim is `[D]`, not `[I]`.** The error was checking `configSettings` alone, finding it absent, and concluding no record existed — **convention 16 breached one sitting after this campaign cited it**, by the desk that cited it. ⭐ **The residual is real but NARROWER**: the *committed* fixture carries no **machine-readable** instrument field, so **no gate can assert it** (a gate cannot assert prose), and the hard evidence sits in a **gitignored** directory CI never sees. *The defect was never "we cannot prove it" — it was "nothing re-checks it".* Closed by the 2026-09-02 re-baseline + `gate-53`'s **G53g**.

> ▶ **2026-08-24 — HAUSSMANN P4.2 `in_progress`: O0 ✅ + O1 ✅. The craft floor is DECLARED and GATING, and the markup debt is 4,444 → 0. ⛔ BUILT, NOT DEPLOYED — freeze stands.** Session `session_stanley_20260824_170854_haussmann_p4_2_o0_o1`; commits `61aff0e` · `e62a465` · `2d3f9ed`. Suite **560/560**, html-validate **0** across 226 pages, `token_aa_check` **AA PASS**, lock census **60/60**, gitleaks **877 commits no leaks**, injectors 495 routes / 222 Vary.
>
> ⛩ **THE ACs WERE AMENDED AND OPERATOR-SIGNED BEFORE ANY BUILD.** The convention-13 pass ran **30/30 pairs with its coverage recorded** and found **three of five criteria wrong at their premises** plus **two tested by nothing** (`artifacts/p4_2/ac_amendment_proposal.md`, `accepted`; six ACs now). ⭐ **This is the pass paying for itself on the one P4 mission with `human_gate: false`** — nothing in P4.2's own definition would ever have stopped to ask. AC1 typed **57 locks** against a floor of **60** and named *"the site surface"*, which is **WebForge's own row** (60/60, full, passing) ⇒ ours is a distinct consumer surface. AC2 said *"the 5 classes, ~964 → 0"* when the field was **11 classes / 4,444 errors** with the dominant class absent from the five — **the stated method could not make the stated test green**. AC3 said *"regenerated"* of a **hand-authored 506-line page with no generator anywhere** — ⚠ **and the campaign's own protective warning about `/design-system` was built on that same absent mechanism**; the real hazard there is a **manual rewrite**.
>
> ⭐⭐ **THE CENSUS IMMEDIATELY FOUND THREE DEFECTS THE 560-ASSERTION SUITE STRUCTURALLY CANNOT SEE**, because nothing had ever declared coverage — which is the KW-12 condition exactly. The header is **`position: sticky` with no `scroll-padding-top` anywhere in `src/`**, so every in-page anchor lands underneath it (`gate-31` passes because it asserts anchors *resolve* — a different claim). **`aria-live` appears nowhere in `src/`**, so filtering the registry changes the result set in silence for AT users; the `aria-pressed` half is fine, which is why it looked done. And the CSP self-validates against nothing. **All three are O2's first work.**
>
> ⭐ **THE `no-inline-style` DIAGNOSIS FALSIFIED ITS OWN HYPOTHESIS — and that was the more useful answer.** Shiki was **already 4.0.2** on both sides of P3.2's lockfile touch; Astro unchanged; same 8 code fences; `syntaxHighlight`/`shiki` have **never** appeared in `astro.config.mjs` history. ⇒ the 964→4,444 delta is **not a regression but an UNREPRODUCIBLE MEASUREMENT**: the 08-19 artifact records **neither its command nor its config**, so the run cannot be re-executed. Recorded as a standing property, not an event — calling it a regression is a claim the evidence does not support. Hence this mission's baseline artifact records both on its face.
>
> ⭐ **TWO EXCEPTIONS ARE REFUSALS TO TRADE REAL ACCESSIBILITY FOR A LINT NUMBER.** `role="list"` is Safari's list-semantics workaround and **all six triggering lists carry `list-style: none`** — removing it regresses VoiceOver. Seven `aria-label`s are the only accessible name their lists have. ⚠ Note the deliberate **asymmetry**: SidebarNav's `<ul aria-label>` **was** removed, because its parent `<nav>` already named the region. Same rule, opposite answers, because the question is whether *information is lost*, not whether the linter is quiet.
>
> ⚠⚠ **THE PARITY FIX BROKE AXE-0 ON ITS FIRST ATTEMPT, AND MY OWN INSTRUMENTS WERE WRONG BEFORE THE SUBJECT — TWICE.** `github-light`/`github-dark` both ship sub-AA token colours (`#e36209` on white **3.48:1**; `#6A737D` on `#24292e` **3.05:1** — the dark one **already live**, since the site rendered `github-dark` in *both* appearances). Shipped the `-high-contrast` variants; re-measured **58 pages × 7 pairs × 2 palettes = 0**. On the way: a contrast sweep of **one page** reported *"light failures: 0"* — true for that page, false for the site, because the offending colour only appears in markdown blocks that page lacks (**a palette is a property of the corpus, not of a page**); and an ad-hoc axe probe reported **71 phantom nav failures** because it drove the theme by class-toggling while the real gate is **dark-by-default and seeds `localStorage` before load**. ⭐ **What settled causation was a CONTROL, not a better probe** — revert *only* the Shiki change, rebuild, re-run the same gate; it passed. That took less time than the probing had.
>
> ⚠ **A ROT-HOOK PREDICATE FIRED FALSELY AND WAS CAUGHT BEFORE SHIPPING.** WebForge's `run_predicate` is a raw regex with **no comment stripping** while `resolve_rung` **does** strip — so the obvious "no hydration directives here" predicate matched a **comment** describing a component as an island. Untested it would have failed the census over a directive that does not exist. Two mechanisms in one checker disagreeing about whether a comment is code; staged to Vitruvius with two siblings (Playwright assertion anchors can only ever score rung 2; `check_aa.luminance` **rejects 3-digit hex** and crashes on Shiki's own `#fff`).
>
> ⚠ **The stale register row that CAUSED AC1's "57" is fixed** — live floor **60**, census **452/387/1 at R1**; and N2's *"the site's llms.txt lacks the honesty line"* was also stale (it has one, `llms.txt.ts:56`).
>
> 📨 **The CLOSE sweep caught four Hopper memos that were not there at open** — third session running where the close sweep is the one that finds something. Routed **not ruled** (no rulings at a wind-down); **one carries `ack_required: true`** (`r4_deletes_the_license_nothing_re_adds_it`) and owes its own sitting. The commit is the read-receipt.
>
> ⏭ ~~**NEXT = P4.2 O2**~~ **SUPERSEDED — P4.2 CLOSED 2026-08-24; see the entry at the top. ⚠ And note what this line got wrong: it said "the three census defects", and one of the three was not a defect at all.**  then `/design-system` refreshed **in place** (never regenerated; the Illustration-slots section survives) + the **declared-frame** 20-component sample. Then O3: the **diagram** rules only (AC4 narrowed — P4.1 O2 already published the illustration half), thin hubs (**4** instances), F20's font test, and the AAR. ⛔ **P4.2 is the SECOND mission accumulating unshipped work** behind **F-u**; lemur's `30c8163` + `f4fa9c5` re-verified **absent** at open. ⛩ **The push GO is owed and not mine to fire** — **5 commits unpushed**.

> ▶ **2026-08-24 — ✅ HAUSSMANN P4.1 COMPLETE at O3: all five criteria met, AAR filed (SO#5), mission `completed`. ⛔ STILL NOT DEPLOYED — the freeze stands.** Session `session_stanley_20260824_152000_haussmann_p4_1_o3`. Suite **unchanged at 560/560** — no `site/` source changed, which is the *correct* result for a record-and-documentation objective, not a reassuring one. **Freeze re-verified at open after `git fetch origin`**: lemur's `30c8163` + `f4fa9c5` **both still ABSENT**, HEAD `9608820` vs `origin/main` `0312855` — ⚠ and the scope of that negative is stated, because **a negative result is only as wide as the command that produced it**: it covers *this clone on this node* and says nothing about lemur's tree. **Convention-16 content probe 7/7 at 200** with three content assertions holding (`/vaults.json` at **80,997 B**, byte-identical to P3.2's record) — the F-s restore is still standing. **AC5 remains MET-ON-BUILD with deployment NAMED AS OWED**; `completed` is a statement about a mission, never about the live site.
>
> **AC4 → the staged Pygmalion ask** (`who/coordination/coord_2026_08_24_rosetta_to_pygmalion_style_atmosphere_class2_ask.md`, `status: staged` — ⛩ delivery is a separate outward act needing its own GO). The branch its own conditional selects, re-verified at the object per convention 12: VisualDNA's schema dir still holds **3** files, no `style_atmosphere` `[D]`. ⭐ **The ask is not "please invent a schema."** `extensions_registry.yaml` → `class_2_new_entity_type.entries: []` is **empty**, while `style_atmosphere` is the **worked example** both their `spec_modular_extension_protocol.md:45` *and* VDNA-ADR-004's own Context paragraph use to illustrate the Class-2 trigger — so it asks them to run **Step 1 of their own protocol on their own example**. ⭐⭐ **And it reports what we could see of their ability to act**: their machinery is itself pre-activation (spec `DRAFT`, registry *"promotes to ACTIVE at P5 close"*, `mission_p4` `STUB_NEXT_SESSION`, `mission_p5` `STUB_AWAITING_PILOT`) `[D]`, so the full run is **two missions out on their roadmap** — said on the memo's face rather than left for them to discover by trying (convention 15's reachability face). **One scope is performable today by their own precedent**: both existing registry entries were pre-populated at `PROPOSED` with `adr_path: null` *while the registry was and still is DRAFT*. Memo carries **both scopes with the choice named as theirs**, and pre-records *"neither yet"* as a real answer. Verified before believed: **5/5 paths resolve**, **6/6 line citations** read as quoted, and §6 states what we do **not** claim — there is **no `how/federation/visualdna/` wrapper here**, so aDNA is a *candidate* first consumer, not a wired one.
>
> **Persona ranker (V6 — the obligation convention 13 recorded as UNRUN and O3's): `/vaults` 4.03 · `/design-system` 4.10 · `/get-started` 4.37**, all ≥4.0, scored **separately, never averaged** (an average of 4.17 would have let `/vaults` hide behind `/get-started`). All 18 dimension means + 3 totals **re-derived programmatically, not typed** (KW-14). ⚠ **`/vaults` clears by 0.03** — one cell moved down anywhere puts it under the floor; recorded as the number rather than rounded. Conflict of interest **declared, not managed away** (`[D-syn]`; the builder scored the surfaces it built; independent re-rank offered, operator ruled the P2.2 precedent governs). ⭐ **THE RANKER'S FINDING: Delight is 3.6 on all three surfaces with the IDENTICAL persona vector `4·4·3·3·4`** — the signature of a lazily-scored dimension, treated as suspect before it was treated as a result. It is not laziness: **it is ADR-053's containment rule appearing in the measurement.** All three pages are structurally the same object for this dimension — one illustrated hero, restraint everywhere else — so the rule that makes the voice governable caps delight, and caps it *identically because it applies identically*. ⇒ a **designed trade** (option (b) would have pushed it lower by deleting nine live surfaces) that **bounds its own remedy**: `vault_card_mark` and `graph_frame` are the only sanctioned places it can move, so a future *"raise Delight"* reads as **"build a slot"**, never *"decorate a page"*. ⚠ The instrument **cannot separate "capped by design" from "under-delivered within the cap"** — that needs the human instrument at P5.1.
>
> ⚠⚠ **DEBT THIS CAMPAIGN SAID WAS "ROUTED TO P4.4" HAD NEVER REACHED P4.4'S REGISTER.** The convention-16 amendment states the prod-alias gap is *"Routed to **P4.4**"*; **there was no such row**, and no F-r or F-s row either — for four days, and **F-u is the freeze's release condition**, so the one row saying when deploys can resume did not exist. Landed at the close: **F-u** (the missing single-writer lease, with the design constraint that the vault's own §Single-Writer Lease governs shared *files* and does **not** port to an external alias with no `updated` field) and **F-r** (P3.4's, picked up because a known-unrouted row is worse than a small scope bleed). Register **17 → 19**, derived. ⇒ **"Routed" is a claim about the destination — verify it THERE, never in the prose that routed it.** ⭐ **F-t is WITHDRAWN as a duplicate of F-l** — same idiom, same variable, same mechanism, two sightings; it got a fresh ID because the second was written up *from the session rather than checked against the register*, and P4.4's own F-b precedent already rules the shape (*"recurrence is evidence for the allowlist, not a new row"*).
>
> ⚠ **SO#11 RETROSPECTIVE TRIGGERED**: **~590–930 kT across FOUR sessions** against a ratified **~250–400 kT across two** — **≈2.36× / ≈2.33×**, over threshold at both ends. Decomposed rather than absorbed: O1 and O2 each landed *within* their own session estimates (unplanned work displaced planned work); O0's overrun is **F-s**, incident response not scope drift; and **the 2-session figure was wrong at ratification**, because convention 13's pass ran *after* the budget and then forced an operator-signed AC amendment mid-mission. ⇒ **The remedy is not "estimate higher" — it is that convention 13 runs BEFORE a DP ratifies a budget**, which is what convention 13 already says. Also silently divergent for four sessions: `executor_tier: fable` while every session ran **opus**.
>
> ⚠ **A CONTROL FIRED AND THE CONTROL WAS THE DEFECT** (sixth instance this campaign). A luminance check flagged three `vaults__*__light.png` captures as dark-under-a-light-filename — the exact hazard an ad-hoc script produced at O2. They are fine: median **255**, p95 **255**, bottom strip **243.7**, identical to the confirmed-good light control; the low reading is `/vaults`'s **dark `hero_panel`**, dark in both themes by design, and `visual_capture.mjs:86` toggles `html.dark` correctly. ⭐ **A control that fires is a question, not a verdict** — asking what else could produce the reading saved 18 good captures. A second the same session: a table-integrity checker flagged `F-l` by splitting on `|` without honouring markdown's `\|` escape. ⭐⭐ **THE MISSION'S FINDING (from O2, carried here as its headline): a red test can reveal that code's STATED structure is not its ACTUAL structure** — two of three named predicates were **decorative**, so a mutation left the gate green *because the line it was aimed at could not move*. No green run reaches that class.
>
> ⏭ ~~**NEXT = P4.2**~~ **SUPERSEDED — P4.2 opened and reached O1 on 2026-08-24; see the entry above.** (`mission_haussmann_p4_2_craft_floor.md`, ~~`queued`~~ `in_progress`; `depends_on: [p4_1]` satisfied; `executor_tier: sonnet`; the **only P4 mission with `human_gate: false`**). **Two conditions carried in, neither blocking:** ⛔ **the deploy freeze applies to P4.2 too** — it touches `site/` heavily (57 locks, ~964 markup errors, design-system regeneration), will gate green and **will not ship**, so plan it knowing its output queues behind **F-u** and say so at its close rather than letting a second mission silently accumulate unshipped work. ⚠ **`/design-system` now carries O2's Illustration-slots section** (five-slot table, both scales, four contributor rules) — **ADR-053-governed content, not generated chrome**; a regeneration that overwrites it deletes the artifact AC5 was met on. It also scored the ranker's weakest Findability (**3.6** — no in-page TOC, no left nav on a 5,584 px page), which P4.2 is well placed to fix. ⛩ **OWED AND NOT MINE TO FIRE: the push GO** — `9608820` (O2) plus this close are unpushed; a push is an outward act needing its own per-action GO. ⚠ **STATE.md is now ~149 KB, past the 100 KB `skill_state_graduation` tripwire** — flagged as a standing watch item at operator ruling 2026-08-24; graduation gets its own sitting, not a ride on a close.

> ▶ **2026-08-24 — ⛩ HAUSSMANN P4.1 **O2 COMPLETE**: the `empty_state` slot, ADR-053's first NEW slot, built in BOTH the states its row names. ⛔ NOT DEPLOYED — the freeze stands.** Session `session_stanley_20260824_080010_haussmann_p4_1_o2_empty_state_slot`. Suite **560/560** (555 + 5 G35b), **9/9 mutations red-proven**, `token_aa_check` **AA PASS 0 below floor**, axe **0** across 3 surfaces × **both** themes, convention-16 re-probe green 6/6 at open. **Freeze re-verified at open** — `git fetch origin`: lemur's `30c8163` + `f4fa9c5` **still absent**, HEAD == `origin/main` == `0312855`. ⛩ Operator ruled *"live surface" = a real shipped route, build-verified*; **AC5 recorded MET-on-build, deployment NAMED AS OWED** (per P3.2's warning, said here rather than left to a reader to infer from a status field that cannot express it). **Shipped**: one hand-drawn `currentColor` SVG at two scales — inline beside every honest-absent line on the registry cards, and at block scale heading a **zero-result block that did not previously exist** (a filter matching nothing used to collapse every section, leaving only a ` — nothing matched` suffix on a count line above the fold); registered for contributors at `/design-system#illustration`; normative spec at `artifacts/p4_1/slot_spec_empty_state.md`. ⛩ **Four rulings taken BEFORE building, three of them defects found by re-reading the ruling against the data.** ⭐ **THE RULED TARGET SET WAS NOT THE SET THE SLOT IS ABOUT** — *"the 57 planned vault cards"*, but only **52** of those 57 have any empty field (3 are fully empty) while **12 NON-planned cards do**; tier-keying would mark 5 non-empty cards, miss 12 empty ones, and re-differentiate the tiers **ADR-052 §tiers.2** deliberately rendered equal. **The mark keys on the ABSENCE**, and that it lands on `in use` cards is the *visible* proof it is not a stage badge. ⭐ **AC5(b) NAMED A MECHANISM THAT CANNOT REACH ITS OWN TARGET** — `DocumentationLayout.heroImage`, on a page that uses BaseLayout + HomeHero, for a slot that is not a hero; ADR-053 names the **pattern**, AC5 named one **instance**. Ruled **both**, and the literal field is **exercised** on `/get-started` (credit verified at the generation ledger: `imagen-4.0-ultra-generate-001`, cycle 157) so it does not ship unexercised. ⭐⭐ **THE RED TEST FOUND A DEFECT IN MY OWN CODE THAT NO GREEN RUN COULD HAVE**: two of the three absence predicates were **decorative** — the surrounding ternary was the real guard, so a mutation setting `personaAbsent = true` left the gate green **because the line it was aimed at could not move**. Red-proving is usually sold as proof the fix was needed; here it proved **the code's stated structure was not its actual structure**. And when the restructure made an older mutation stop matching, the harness reported it as a **HARNESS BUG, NOT A PASS** — the whole reason the match-count assertion exists. ⚠ **FOUR more instruments wrong before the subject (fourth session running)**: my own new copy put **`DP8` on the only page in 225 carrying it**, caught by grepping the *built* output before gate-27/gate-35 ever ran · the new gate read **raw `vault_slug`** while the page emits the ADR-051 canonical slug, producing two failures **indistinguishable from the tier-keying regression it exists to catch** (the site was right both times) · an ad-hoc capture script set only Playwright's `colorScheme` and produced **a dark screenshot under a light filename** (this site's theme is a `.dark` class on `<html>`) · **two typed counts** (KW-14, 5th instance) whose derivation **corrected the predicate** — `heroImage`-prop pages = **9** because `/vaults/graph` renders bespoke; ADR-053's ten is right and the naive predicate was wrong, and the obvious shortcut (glob `assets/heroes/`) is the **wrong predicate even though it agrees at 10 today**. ⭐ **`listing: 'minimal'` EXCLUDED and that is the honest half** — those rows are not unwritten, they are **deliberately withheld**; marking a policy choice as an oversight is the class of defect this campaign exists to retire. ⭐ **A PRE-EXISTING contrast gap surfaced, not created**: `--color-text-muted` on `--color-bg-alt` was **untested** while **23 files** render it (40 container rules site-wide); added with its counted usage and red-proven (**1.21:1** under mutation, exits 1; live **5.36:1** light / **6.41:1** dark). **⏭ NEXT = P4.1 O3** — **AC4** (the VisualDNA `style_atmosphere` bundle **or** the staged Pygmalion ask; the schema does not exist, so this is an upstream ask, not a local fix), the **persona ranker ≥4.0** on the three surfaces O2 changed (convention-13 pass recorded it as **UNRUN and O3's**, so it cannot be inherited unstated), and the **AAR** (SO#5).

> ▶ **2026-08-23 — ⛩ HAUSSMANN P4.1 DP8 RULED (ADR-053 (a) + ADR-059 (c)), O0 CLOSED · ⛔ AND A LIVE PRODUCTION REGRESSION FOUND + RESTORED (F-s).** Session `session_stanley_20260823_192318_haussmann_p4_1_token_pipeline`. **F-s first, because it outranks the mission**: `adna.network` was serving a build predating **2026-08-18** — `/api/registry.v1.json` + `/vaults.json` **404** (P3.2 verified them **200 on the alias** the day before), `/state-of-the-network` **404 in all three forms** while the footer, the homepage `proofHref` and `llms.txt` all link it, P2.1's + P2.2's redirects un-fired, the P3.3 machine-door block absent. **Ten production deploys landed that day, none in any `deploy_log*` fleet-wide**; no git integration on the project; **cause UNKNOWN**. Restored under ⛩ operator GO via the sanctioned path — `deploy_record: 2026-08-24T02:44:59Z mode=prod tree=922519c`, **red-proven 10/10**. ⭐⭐ **`check_live_headers` passed 4/4 on the alias BOTH BEFORE AND AFTER** — the stale build carried the same four headers. **Headers were never what regressed; content was.** ⇒ **campaign convention 16**: a live check that runs once at deploy and is never asked again makes *“DEPLOYED + LIVE-VERIFIED”* **a claim about the past wearing the grammar of the present** — four such verifications were honest when they ran and went false with no event to mark it. Habit adopted; ⛔ no monitor built (convention 15 stands). ⚠ Found **by accident**, by a capture harness pointed at prod for an unrelated mission — and **its first casualty was this session's own evidence**: 30 green T0 captures, of the wrong build. ⛩ **DP8**: **ADR-053 → `accepted` (a)** — governed **slot-contained illustration program**, normative 5-slot table, containment rule (all other chrome stays Tokyo-Night type-and-colour, both themes), **credit NORMATIVE + UNMET**, pipeline **owed not claimed**; **ADR-059 → `accepted` (c)** — adopt WebForge's **validators**, **pin the emission divergence**, ⛔ derive no ceiling, regenerate no token value. ⭐ **The ADR's premise was FALSE and correcting it changed the ruling**: not *“one excellent hero”* but **TEN illustrated routes in ONE render language**; the instrument's definition is *“nothing **else** carries the style”* ⇒ DP8 governed an **existing ungoverned program**, 3 of 5 dossier requirements already met, and option (b) *“reduce to an accent”* silently meant **removing nine live surfaces**. ⚠ **`tokyo_night` is NOT our seed** — SS app canon, `appearances:[“dark”]`, dark primitives only, and the **anti_signature** in WebForge's own fixture; adopting it **deletes light mode**. The pattern register's *“convergence is natural”* is WITHDRAWN (memo to Vitruvius owed at O1). ⚠ **Convention-13 pass COMPLETE (16/16 AC×V + 4 AC×AC): 3 failures + 1 structural gap** — AC2's verification cannot see AC2 (gate-25 **excludes** the token files by construction) · AC2's record limb named the wrong ADR · **AC4 has no schema to instantiate** (`style_atmosphere` unexercised at GA, no schema file — a **Pygmalion ask**) · **O2's slot applications are covered by NO acceptance criterion**, so all four ACs could pass with **zero slots built**. ⛩ Operator ruled **amend the ACs before O1**. ⚠ **F-t — `SS_VERCEL_TOKEN` leaked into the transcript by MY OWN redaction idiom**: `${VAR:+SET}${VAR:-UNSET}` prints the value precisely when the var IS set; **`${VAR:+SET}` alone is safe**. Operator ruled *record, no rotation*. ⭐ **Fifth wrong instrument in two weeks, authored inside the session auditing wrong instruments** — and the first whose failure mode is **disclosure**, not a false green. ⏭ **NEXT = P4.1 O1** — AC amendment for sign-off, then the wrapper pin + the two validators, both red-tested by deliberate mutation before either is believed.
>
> ▶ **2026-08-22 — ✅ HAUSSMANN P3.4 COMPLETE + DEPLOYED + LIVE-VERIFIED (24/0) · ⛩ DP7 RULED GO · ADR-054 ACCEPTED · NEXT = P4.1 (halt at ⛩ DP8).** Session `session_stanley_20260822_165717_haussmann_p3_4_flux_integration`, commits `a9ed459` + `5c6b22d`. **Deployed** — `deploy_record: 2026-08-23T01:45:36Z mode=prod tree=5c6b22d`, headers **4/4 verified against the ALIAS**. **The live probe was red-proven BEFORE the deploy at 8 PASS / 15 FAIL** — all 7 controls green and one deliverable already holding, so it discriminates rather than merely failing; post-deploy **24 PASS / 0 FAIL**. Gates **554/554** (552 baseline + 2 for the new claim rows), axe **0** on 4 surfaces × both themes, 56 captures. ⛩ **DP7**: *"minimal aDNA branding"* means **what a client renders**, not what an unauthenticated fetch sees ⇒ **PR-2 MET**, method amended. **PR-1 also MET at an amended method** — its on-instance limb is **unsatisfiable by construction**: the instance serves the **SPA shell with 200 for EVERY path**, proven by a negative control (a route that cannot exist returns the same shell) and a nonce control (`/terms` differs from `/` by a per-response CSP nonce alone). ⇒ **a 200 on that instance is not evidence a route exists.** The venue's stock-Fluxer public face is an **upstream ceiling** (Fluxer ADR-000 forbids the fork) — recorded in ADR-054 and **deliberately unmentioned on the site**: silence, not spin. ⭐⭐ **THE FINDING — A GREEN TEST WAS GREEN *BECAUSE* A CLAIM WAS FALSE.** `/community` said the venue's terms + privacy were *"still being stood up"*; both went live 08-21. That sentence is claim **R-95**, stamped `verified` on **08-17** evidence, and **`gate-26` asserts `verified` quotes stay PRESENT in `dist/`** — so the suite **defended the stale sentence and would have gone red on the truth.** Not the register failing to notice: **the register enforcing the wrong thing.** Structural cause: **no fixture row carries a probe date**, so no row can express *true as of when*; `gate-21` covers committed counts only. ⇒ **ADR-054 clause 3 now requires any sentence describing an EXTERNAL surface to carry its probe date on its face.** ⛔ **No staleness checker built, deliberately** — convention 15 already ruled this shape (*the habit costs a sentence and cannot itself be wrong; the checker costs a sitting and can*), and **this session produced three wrong instruments of its own**, which is the argument for the habit rather than against it. ⭐ **A SECOND stale claim sat in the same paragraph, caught only by reading the document before linking it**: *"agents work in the repositories and on the public record, not in chat"* is falsified by **aDNALabs S224 ruling ②** (2026-08-21, *"Fluxer = human-visible agent chat"* — **verified at the object, not from a STATE line**) and **flatly contradicted by the venue's own code of conduct §3**, which this mission now links. Linking it unread would have shipped a one-click contradiction. The page now **states the rules and asserts nothing about who is inside** (behind the auth wall; not ours to claim). ⭐ **A THIRD copy** of the same description was live on `/canonical-properties` + `/state-of-the-network` from **one shared line** in `canonical_properties.json` — found by convention 7's *grep the RENDERED output for what the defect claimed*: the first sweep said done, the second found two pages I had not touched. **Built**: `/community` links the venue's **terms · privacy · its own code of conduct** directly — closing site-side the one PR-1 gap that reaches a person deciding whether to join, since **the CoC is reachable from the instance by NEITHER route** (absent from `legal.*`; `/guidelines` is the shell). Link text is venue-scoped because the governance table above names the project-wide **Contributor Covenant**, whose own preamble covers *"the community server"* too. ⛔ **NOT built, named rather than skipped: the ladder→channel mapping** — AC2 required rungs mapped *"where true"* and **no `[D]` is obtainable outside an approval-gated venue**; deferred **in public, on the changelog**. **Convention 13 ran COMPLETE (10/10 pairs, coverage recorded) and found 3 of the 4 acceptance criteria unexecutable as written** — AC1's method unreachable by construction; AC1's test would have demanded removal of a link the operator ordered live on 08-17; **AC4's NO-GO state (*"a venue is being prepared; not open yet"*) is factually false and would have made the site lie.** Two share one root: **AC1 and AC4 were written 08-16 and never re-read against the 08-17 ruling.** ⚠ **New debt F-r**: a site-wide grep for a struck claim now hits **the changelog that quotes it** — every absence assertion must name its surface. **F-p confirmed live** (gate-17 G15 red at 444-expected/0-found; its skip guard tests `config.json` **existence**, not the routes it asserts on) — remedy was convention 6's *run the step that produces what the gate asserts*; fix stays routed to **P4.4**. ⏭ **NEXT = P4.1** (`mission_haussmann_p4_1_token_pipeline.md`, halt at ⛩ **DP8**). ⏸ **P3.3 stays open at ⛩ O2** — *skipped, not finished*; needs an interactive operator `npm login` before any GO means anything. ⛩ **P2.6 O0b** still the only other Decade-1 leftover.

> ▶ **2026-08-21 — ⏸ HAUSSMANN P3.3: O0 ✅ + O1 ✅ · THE MCP SERVER IS BUILT AND VERIFIED · UNPUBLISHED, NOTHING LIVE · HALTED AT ⛩ O2.** Session `session_stanley_20260821_183043_haussmann_p3_3_mcp_o0_o1`. **⛩ THE GATE: `npm publish` is an operator act.** From `mcp/`: run `npm run smoke && npm run redtest` (both must exit 0), then `npm publish` — `prepublishOnly` rebuilds `dist/` so a stale build cannot ship. **BUILT**: package **`adna-mcp-server`** at `mcp/`, stdio, `bin` → `dist/index.js`, ESM, Node `>=20`, 4 tools (`search_docs` · `fetch_page` · `query_registry` · `lookup_spec_glossary`), runtime-fetch + per-process cache. Packed **8 files / 11.8 kB** `[D]`. **VERIFIED FROM A SEPARATE PROCESS** by the official MCP client: **26 smoke assertions green**, **24 red-test mutations every one caught**, and the smoke suite re-run against a decoy origin goes red on **18 of 26** and exits non-zero `[D]`. ⚠ **NOTHING IS LIVE**: `/.well-known/mcp.json` and `/mcp` re-probed after the build → **both still 404** `[D]`. O1 makes a package, not a surface. ⛔ **DEFECT 3 — the finding to carry forward.** The coherence pass had checked AC2↔AC3 and AC4's *count*; it never checked **AC1↔AC4**. `machine_eye` item 11 is probed as a **URL** (`/.well-known/mcp.json`) **and** as a **text search of the site** — an npx **stdio** server is invisible to both, so built exactly as AC1 words it the mission would report done against a checklist item that never moved. Cohort corroborates: MCP's D10=5 came from a **live `/mcp` endpoint**; the Mastra npx pattern we copy scored **4 and 3**. ⇒ Clause 5 grew a **discoverability limb** (`/.well-known/mcp.json` + an `llms.txt` section), landing **O3** and **CONDITIONAL ON O2** — a descriptor naming an unpublished package is a false claim on a machine surface. ⇒ **CONVENTION 13 HAS A NAMED GAP**: it catches contradictions *between* criteria; it does not ask whether each stated **method** reaches the surface its **test** probes. Three defects of that family in one mission → `idea_upstream_mission_ac_coherence_check` should gain the clause. **⛩ OPERATOR RULINGS (3)**: (1) package name **unscoped `adna-mcp-server`** — an explicit **AC2 amendment**, because `@adna` ownership is UNKNOWN (`npm whoami` → `ENEEDAUTH`, scope exists but package list `{}`) `[D]`; this unblocks the build and collapses the scope question into the single O2 gate. The authenticated `npm whoami`/`npm org ls adna` STATE records as owed is **still owed but no longer blocking**. (2) **runtime fetch, not a bundled snapshot** — a snapshot answers "74 vaults" long after that stops being true, wearing our authority. (3) scope **O0+O1, halt at O2**. ⚠ **SDK FACT A COLD AGENT NEEDS**: the official TypeScript SDK **split into scoped v2 packages on 2026-07-27** and **NEITHER LINE IS DEPRECATED** — `@modelcontextprotocol/server` **2.0.0** targets the **2026-07-28 spec** (Node `>=20`); `@modelcontextprotocol/sdk` **1.30.0** still points at `draft`. Built on **v2**. "Use the official SDK" is no longer an unambiguous instruction. **BUDGET RE-COSTED AND RE-RATIFIED** (ADR-016/SO#11): **~245–285 kT** total (O0 ~80 · O1 ~75 · O3 ~90–130); O2 costs no agent tokens. **DEBT ROUTED → P4.4**: **F-m** (`adr_index.md` is **twelve ADRs behind** — tally 41, highest row 046, while **047–058 exist as files**; verified 53 numbered ADR files on disk `[D]`) and **F-n** (nothing compares a vault's `MANIFEST.updated` to its `STATE.updated`; **this vault is 45 days behind**, per Ilmarinen's 12-vault sample where **8 are 34–52 days behind**). Derive the F-count with `grep -cE '^\| \*\*F-[a-z]\*\*'`, **never type it** — it is now **14** (a→n). ⚠ Also repaired: a **blank line had split that table**, so F-k and F-l were rendering as a header, not as rows. **📨 TWO MEMOS INTAKEN AT OPEN** (both `ack_required: false`, both cited by coord id): `coord_2026_08_21_berthier_to_rosetta_doctrine_draft_moved_and_your_hook` — their Lane E-1 forge-watch went blind in 15 minutes because **a discovery mechanism must distinguish *mentioned* from *dispositioned***, and the ledger recording the backlog discharged it; and `coord_2026_08_21_ilmarinen_to_hestia_rosetta_manifest_pull_was_staler` — the MANIFEST-pull warning behind F-n. **⚠ THE UNTRACKED SWEEP FOUND BOTH**; `git status` habits would not have. **NEXT**: ⛩ O2 (operator), then **P3.3 O3** — homepage sentence + the discoverability limb + conformance report + register rows **R-133+** + machine-eye full re-run + AAR + the **A2** pattern memo owed to Vitruvius.

> ▶ **2026-08-21 — ✅ P3.2 DEPLOYED · owed queue cleared · P3.3 OPEN at its coherence pass.** Session `session_stanley_20260821_172747_haussmann_p3_2_deploy_p3_3_open`. **⛩ PROD DEPLOY FIRED** — `deploy_record: 2026-08-22T00:29:33Z mode=prod tree=861e871`. **The registry JSON is LIVE**: `/vaults.json` **200** (80,997 B, 74 vaults + 14 edges) and `/api/registry.v1.json` **200** `cmp`-identical; the other three machine-eye paths stay 404 by the two-URL ruling; `/reference/registry-api` 200; `llms.txt` advertises both; `Dataset → DataDownload` points at the live endpoint; P3.1's twins unregressed. **Every row held unchanged from the local build** — delta packet re-stamped `probe_scope: live_alias_verified`, old LOCAL BUILD banner **struck not deleted**. **R-131 deliberately NOT moved to `verified`** — deploying a mechanism does not exercise a forward promise. Pushed `924f2d8..45adf02` then `..9cdd80b`. ⚠⚠ **TWO OPERATOR GOs FROM THE DECADE-2 ISS HAD NEVER BEEN CONSUMED** — the gate was re-submitted `23:51:27Z` (committed copy read `22:49:44Z`, decisions byte-identical) and **nobody had acted on either**. **📨 BOTH NOW DISCHARGED, delivered on operator GO, md5+cmp verified after the act:** **(1) Venus** — publish GO **GRANTED** and **⛔ NOT PERFORMABLE HERE**: `adna-installer-0.4.3.tar.gz` exists **nowhere in the workspace** (`dist/` holds only 0.4.1 and is gitignored), no `.minisig`, `MINISIGN_PUBKEY` is **pinned** so `release.sh` makes signing mandatory, and this node has **neither the key nor the `minisign` binary**. Asked them to deliver the tarball + sig, or say the cut must be re-run on the key-holding box. **adna.network/install.sh still serves v0.3.1** until they answer. **(2) Hopper** — ⛩ **the `.adna` pre-push gate IS OURS**, ruled by the operator; routed to **P4.4 as F-k**; ships in the **next `skill_template_release`** (operator-opened, no date pinned on purpose). ⚠ Their caveat we must not lose: **shipping v2 into the template does NOT deploy it** — one live installation fleet-wide, so a release note reading "the fleet is covered" would be false. **(3) Hestia R-129** unchanged — staged, pt19, owner-attested. ⭐⭐ **THE SESSION'S ONE LESSON, HIT TWICE IN UNRELATED LANES: an operator GO can be correctly granted for an act whose PREREQUISITE DOES NOT EXIST, and neither the gate nor the operator reading it can see that.** Venus's memo was right in every particular and still unactionable. **P3.3's AC2 is the same shape** — it says "published under the org's npm scope" and **no objective establishes the scope**: `@adna` exists on npm (200 where a control scope 404s) but is **empty**, npm search `adna` → **total 0**, `npm whoami` → `ENEEDAUTH`, so ownership is **UNKNOWN not ours**. ⛩ **OPERATOR OWES: run `npm whoami` / `npm org ls adna` authenticated and say whether `@adna` is ours.** Candidates `adna`, `adna-mcp-server`, `@adna/mcp-server` are all **unclaimed**. **NEXT = P3.3 O0** (`mission_haussmann_p3_3_mcp_server.md`, now `in_progress`) — design tool surface / transport / versioning / **runtime-fetch vs bundled-snapshot for the registry tool**, which does not need the npm answer. ⚠ **P3.3's coherence pass found a SECOND defect: AC3 is NOT independently deliverable** — it has the homepage name the server, but AC2 is `human_gate` and may be deferred, which would put an **uninstallable server on the highest-traffic page** (S1). Resolved: AC3 conditional, **both wordings agreed in-file now** rather than under deadline at O3. **Budget NOT re-ratified.** ✅ AC4's "13-item checklist" **checks out** — directive §B.2 has exactly 13 boxes; `machine_eye.md`'s 15 rows append two beyond it and 1–13 keep the numbering (recorded so nobody re-derives it and concludes the AC is wrong). 🩹 **Two self-caught defects, both in instruments/copy I had just written:** the live JSON-LD census walked `@graph` + `publisher` (P3.2's two known blind spots) but **not `distribution`**, so it called `DataDownload` absent — it is present; **third instance in two missions**. And the Hopper memo **typed** "nine inherited items" when the derived count is 12 → fixed in **both** copies and re-verified identical. ⚠ **F-l filed: `${VAR:+SET}${VAR:-UNSET}` DOES NOT REDACT** — `:-` prints the **value** when the var is set; it put `SS_VERCEL_TOKEN` in a transcript. Use `[ -n "$VAR" ] && echo SET || echo UNSET`. Token is the known throwaway (E4 c159); **not an incident**, but the idiom is in campaign memory as *the* redaction pattern. ✅ **The pre-push hook's no-op CONFIRMED in live operation** — it printed `gitleaks clean ✓` on today's push having scanned nothing; the manual `gitleaks detect --source .` found the one known DTCG false positive. **Open sweep was CLEAN** (first in four sessions) — still run it at open AND close.

> ▶ **2026-08-21 — ✅ HAUSSMANN WIND-DOWN after P3.1 · conventions 13–15 landed · Decade-2 status ISS OPEN · NEXT = P3.2.** Session `session_stanley_20260821_153445_haussmann_winddown_p3_1`. **No deploy** — live is correct at `tree=f053431`. 📨 **THE UNTRACKED SWEEP CAUGHT THREE MORE UNREAD MEMOS, one dated the same day.** Second consecutive session where it was load-bearing (yesterday's caught Hopper's, which arrived *after* that session's own opening sweep came back clean). **Four peer memos in two days, all found this way, zero by any other means — the sweep is the ONLY channel by which this vault learns a peer wrote to it, and mid-session arrival is normal.** ⚠ **Ilmarinen (Forgejo.aDNA): a FALSE CLAIM IS LIVE** — `/vaults/forgejo/` publishes `status: genesis` for a service running since **2026-08-08**. Registered **R-129 (S2)**; **cannot be fixed here** (pt19 — `vaults.json` is Hestia's), staged as `coord_2026_08_21_rosetta_to_hestia_forgejo_row_stale.md`. ⚠ **Venus ×2: yesterday's v0.4.1 publish request IS IN OUR TREE AND WRONG** — target moved to **v0.4.3** (signed, MIT LICENSE in-tarball, `07ae6371…`); `adna.network/install.sh` still serves **v0.3.1**; **publish GO owed**. Their cautions: `installer/DEPLOYMENT.md` is **stale and will break the deploy if followed**, and `.ps1` **must** stay `text/plain`. 🧭 **CONVENTIONS 13–15 ADDED** (the fold-in the operator asked for, so lessons live where a cold agent reads them, not only in AARs): **13 — read a mission's ACs against each other BEFORE a DP ratifies its budget** (P4.5a *and* P3.1 both shipped internally-inconsistent specs; two consecutive missions, one class); **14 — an instrument is not believed until demonstrated to fail, and must assert it reached its target** (`check_live_headers.mjs` read *Vercel's login page* and printed `OK — no drift`; since **P0.2** it had never verified adna.network); **15 — the claim register cannot see staleness, and outbound memos must date their own pins** (Ilmarinen's *"a stale row and a broken row look identical from the outside"* + Venus's F-S395-02, where **both copies agree perfectly and both are wrong**). **Routed → P4.4: F-f** (header check compares NAMES not VALUES) · **F-g** (`stripHtmlComments()`'s 2nd root is INERT — adapter copies *after* `astro:build:done`) · **F-h** (**⚠ P0.2's header evidence needs re-reading against the alias**). **P4.4 now carries 8 inherited items and has become the campaign's debt sink** — may deserve its own scoping pass. **2 upstream proposals filed** (operator-approved): `idea_upstream_mission_ac_coherence_check` · `idea_upstream_verification_instrument_discipline` — **the second ANSWERS Hopper's §4**, which flagged the near-identical ADR-011 A4 clause as standard-shaped and ours to call. 🖥 **ISS OPEN** — `how/gates/haussmann_decade2_sitrep.html` (`phase_exit`, rosetta, 4 sections: DELIVERED · SHIPPED · REMAINING · OWED), receiver on **:8768**, round-trip smoke-proven, output → `haussmann_decade2_sitrep.output.json`. **⚠ GOTCHA: port 8765 is the HQ DASHBOARD (uvicorn), not a gate receiver** — the first render baked `:8765/gate` and would have posted to someone else's service; the POST path is **`/save`** and `receiver_url` must be the **base origin only** (round_trip.js appends). **Gap review (5):** **G-1 the charter's `updated:` frontmatter has become a changelog** (→ `skill_state_graduation`, reported not fixed) · **G-2 charter mission tables have NO status column** (partly fixed: derived progress line added, labelled as ageing) · G-3 tier drift fixed · G-4/G-5 → conventions 15 + the sweep evidence. **4 DECISIONS AWAIT THE OPERATOR:** `.adna/` template pre-push gate (**ISS composite** — Hopper §2; Rule 1 bars Hopper, so it is ours via `skill_template_release`) · **v0.4.3 publish** (ISS §4) · ADR-056 at the P3 exit · ⛩ O0b. **NEXT = P3.2** (scope already shrank).

> ▶ **2026-08-21 — ✅ HAUSSMANN P3.1 COMPLETE + SHIPPED + PROVEN LIVE · NEXT = P3.2.** Session `session_stanley_20260820_221213_haussmann_p3_1`, commits `47f1a69..f053431`. **Deployed** — `deploy_record: 2026-08-21T19:21:07Z mode=prod tree=f053431`, headers **4/4 verified against the alias** (see the instrument finding below — that phrase now means something it did not mean yesterday). Machine-eye **2/3/4/12 all PASS live**: twins **10/10** (baseline 0/10) · negotiation **5/5** with differing ETags + `Vary: Accept` (baseline: byte-identical body, *literal same ETag*) · corpus **940,718 B / 221 sections** (baseline 2,476 B) · `llms` in page HTML **2/page** (baseline **0**). Negative control holds — un-twinned routes still serve HTML. Suite **521 → 541 zero xfail**; axe **0** on 6 surfaces × both themes; **13 gate assertions red-proven by mutation**. 🧭 **THE FINDING THAT RESHAPED THE MISSION: the acceptance criteria contradicted each other.** AC1 named the method (*"from the content collections"*); AC4 required machine-eye item 3 to pass — and **all ten of item 3's probes target bespoke `.astro` pages with no markdown source**. Executed as written: 120 twins, "done", 10/10 still 404. **This is P4.5a's finding one mission later** — there the mission body contradicted its frontmatter, here the frontmatter contradicted itself, four lines apart. **Two consecutive missions, one class: a mission's ACs are not self-checking, and nothing in the close cascade reads them against each other.** ⛩ Operator ruled full three-tier coverage (114 A + 75 B + 32 C = **221**) and an in-file budget re-declaration (~140–200 → ~190–270 kT; actual **≈250 kT**). ⚡⚡ **THE DEPLOY CHAIN'S OWN VERIFICATION HAD NEVER VERIFIED ANYTHING.** `check_live_headers.mjs` follows redirects and checks header **names**, so on a Deployment-Protection-gated URL it lands on `vercel.com/sso-api` — whose login page sets the same four names — and prints `OK — no drift`. It read **Vercel's CSP** and called it ours. Worse: `deploy_adna.sh` pointed it at the per-deployment `*.vercel.app` URL, **which is gated on prod as well as preview** — so since **P0.2** built it, the step has never once verified `adna.network`. Both fixed (assert `res.ok` + same-origin; prod verifies the alias), red-tested. **⚠ P0.2's header evidence needs re-reading against the alias — flagged, not touched, not its lane.** **Still open → P4.4**: the checker compares header **names, not values**, so correct-name/wrong-value drift passes on prod today; plus `stripHtmlComments()`'s second root is **inert** (the adapter copies *after* `astro:build:done`, so its "cannot be defeated by hook ordering" comment describes a mechanism that isn't the one working). **Other lessons:** *a live probe that returns 7/10 is telling you about the site, not the probe* — the 3 failures were `/get-started/.md`, the URL an agent **naturally builds** from a canonical slash-terminated URL + llms.txt's own wording, so the fix was a second route class (442 total), not a rounded-up claim · **three of my own new gate assertions were wrong** (a 300 B floor failed a genuinely sparse vault at 273 B; a whole-body `^>` sweep failed the spec's legitimate blockquotes) — read the failure, don't reach for the content · **MDX comments leak into twins** (24 files carried ADR/campaign ids invisible in HTML — the H13 class re-entering through the mirror) · **gate-27 never scanned `.md` at all**, so 221 public files arrived unlinted; model adopted = *a derived surface inherits the allowances of what it derives from*, with the corpus **header** judged strictly as itself because a union there would exempt authored text · **ADR-056 stays `proposed`** and **anchor 4 is NOT claimed** — the registry endpoint is P3.2's, the re-score is P5.2's with fresh scorers; the builder never self-certifies. **Pattern A1 staged to Vitruvius** (non-blocking). **NEXT = P3.2**, whose scope already **shrank**: `Organization`+`sameAs` shipped unremarked at P1.2, so what remains is `Dataset` on the registry, the versioned JSON endpoint, schema-dts, and the three zero-JSON-LD pages.

> ▶ **2026-08-20 — ✅ HAUSSMANN P4.5a COMPLETE + SHIPPED + PROVEN LIVE (26/0) · DECADE 2 IS UNDER WAY · NEXT = P3.5.** Session `session_stanley_20260819_213829_haussmann_p4_5a_copy_increment`, commits `615b2c8..e4f0d65`, **pushed `a8cc707..e4f0d65`, gitleaks clean**. **Deployed** — `deploy_record: 2026-08-20T20:05:44Z mode=prod tree=bb00464` (221 pages, headers **4/4 live no drift**). **The live probe was red-proven BEFORE the deploy at 12 PASS / 14 FAIL** — every failure a P4.5a deliverable, and 12 assertions already holding, so it discriminates rather than merely failing. Post-deploy **26 PASS / 0 FAIL**. Suite **487 → 495 zero xfail**; axe **0** on 6 surfaces × both themes; every new assertion **red-proven by mutation**. 🧭 **THE FINDING THAT SHAPED THE MISSION: P4.5a had no executable spec.** ⊳ D-A split P4.5 at DP6, but the split lived only in two frontmatter *comment strings*, the charter's P4 table, and `p2_replan.md` — the mission body, objectives, session prompt and **all four acceptance criteria were still 100% P4.5b voice-rewrite content**. Executed as written it would have run against criteria it cannot satisfy. **DP6 ratified a budget for a mission whose spec nobody had checked existed.** Authored in-file (not a new file — `mission_count: 27` sits in ratified §7.7 text). ⚡ **THE SAME-DIFF LAW HAS A MIRROR: A GATE CAN BE COUPLED TO THE DEFECT.** `gate-23` **required** the exact sentence ⊳ D-C ruled must be cut (`.hero-trust-links` must contain *"opening progressively"*). The obvious repair — delete the failing test — would have silently removed its **second half, the regression guard on R-14**, a resolved FALSE claim. **Inverted, not deleted**; R-14's guard split into its own test so neither can take the other down. *Same-diff says update the gate that hardcodes what you changed; this adds — check what else that gate was holding up.* 🔍 **TWO OF OUR OWN ROWS WERE WRONG, both caught by re-probing at execution instead of quoting forward.** **R-111 NARROWED** from *"`/canonical-properties` + `/about`"* to `/canonical-properties` **alone** — the disclosure had already shipped on `/about` and `/state-of-the-network`; the row overstated its own gap for two missions, the mirror-image of the defect R-111 was filed to fix, same root cause: **never re-probed**. And the fixture pins `R-111` as a *shipped* sentence while the register's R-111 is the *unshipped* half — two faces of one finding, and nothing said so until now. ✂️ **THE HERO CUT WAS FOUR SURFACES, NOT ONE**: *"Lattice Protocol"* is hardcoded in **both** arms of `HomeHero`'s `graphLed` branch, so it rendered ×1 on `/`, `/network`, `/commons` **and `/vaults`** — three the ruling did not name, one of them where the homepage's own primary CTA lands. Operator ruled all four, term-free replacement, spec link kept, **reversible at D-8**. Survives on exactly 2 deep pages by design — a self-explaining TAUGHT-AS-DESIGN tutorial, and the **byte-exact vendored `.adna` file whose whole claim is that it was NOT edited**. 🐛 **R-126 — EVERY CHANGELOG ENTRY HAD BEEN RENDERING ONE DAY EARLY**, found while writing P4.5a's own entry. `toLocaleDateString` with **no `timeZone`**: a UTC-midnight frontmatter date rendered in the *build machine's* zone, so **the displayed date was a function of where the build ran** — CI in UTC correct, this laptop wrong, from identical source — while the same `<time>` element's `datetime` attribute was always right, so the machine-readable and human-readable dates disagreed **on one line** and only the wrong one was visible. Third defect this campaign whose root cause is a value read in a different context than it was written in. *A timestamp with no zone is not a time.* ⛩ **R-124 DEFERRED, and that is a deliverable**: §8.5 frames it *"routing, not policy"*, but probing found **no destination to route to**, so any fix must *author* a clinical posture — a positioning claim on ratified ADR-048/P0.1 ground. Deferred with reasoning (§9.3), not quietly fixed. 📋 Register **140 rows / 126 unique ids** (`R-11`…`R-126`, 0 gaps), **re-derived twice** — R-126 invalidated the first derivation inside the same commit, the third occurrence of that failure in two missions. **⏭ NEXT = P3.5** (D9 repair: R-122 CTA target + R-123 licence — a planning-time probe already re-verified both live and is logged in the session file, so P3.5 inherits evidence, not a to-do). **P4.5 stays `in_progress`** until P4.5b (SO#5). **P2.6 still `in_progress` behind ⛩ O0b** — unchanged, and it does not block Decade 2.

> ▶ **2026-08-19 — ⛩ HAUSSMANN DP6 RATIFIED: DECADE 2 IS OPEN · NINE ⊳ RULED (NOT THE EIGHT ANNOUNCED) · 12 MISSIONS DISCHARGED · ⛩ O0b STILL OUTSTANDING.** Session `session_stanley_20260819_190213_haussmann_p2_6_dp6`, commits `38874cc..` (**local and UNPUSHED with session 1's twelve — outward acts are operator-gated**). **Four operator rulings taken at planning**: DP6 fires **in-chat** rather than as an ISS surface (precedent: Gate C, DP5, P2.4's four rulings) · **cascade then halt**, P3.5 deliberately NOT authored here (re-plan §6.4 asks for a fresh session) · **O0b stays outstanding** · **push at session close**. 🏛 **`p2_replan.md` → `accepted`**, §7.7 stamped with all four fields; **12 missions discharged `queued-provisional` → `queued`**; `mission_count` **holds at 27**; `calibrated_sessions` set (empty since genesis); ADR-057 `proposed` → `accepted`; charter phase-map, decision table and two campaign conventions rewritten. **Zero `site/` files — diff-verified across the whole mission.** 🔍 **NINE sub-decisions, not eight.** **D-I sat unlettered inside §2's P3.2 prose** and would have ridden the master signature silently; found by grepping the document for `⊳` rather than trusting §3's heading — **§1.8's own finding recurring one level up: the index believed over the artifact**. It was lettered, put to the operator, and ruled. ⚡ **D-A is the one that mattered, and it went further than its own recommendation.** The draft read *"P4.5 takes the copy rows early as a first increment"* while §4's order left P4.5 at **position 10 of 12** — read literally, option (iii) wearing option (ii)'s label, leaving an **S2 self-contradiction in the homepage's 30-second zone** (*"nothing leaves your machine"* / *"shared in the open"*, two sentences apart) live through **eleven missions**. Surfaced as its own question; ruled to the **front of the whole decade**. P4.5 splits: **P4.5a** (R-111 · R-120 · R-121 · R-124 + the ⊳ D-C hero cut) runs **FIRST**, **P4.5b** (the voice rewrite) still runs **LAST** — which is what the sequencing law actually protects. **The charter's own rule that "any violation needs written justification at a gate" is now satisfied in writing**, not assumed. 🔢 **`calibrated_sessions` moved THREE times in one session and the record says so**: drafted `35-40` → the O0b ruling made P2.6 three sessions not two → `36-41` → the **round-3 D-A split added a session** → **`37-42`**, stamped. Both halves counted, never carried (`grep -c haussmann` on session history → 17, minus the one that *is* P2.6 s1 → **16** Decade-1 `[D]`). The operator's quoted value was `36-41`, derived one ruling earlier; **the ruling selected a *principle*** — *"stamp the number that survives being counted today"* — and the deviation is flagged in-field for one-line reversal. The Decade-2 total is now **machine-derived from the twelve mission files by script**: **~2,380–3,650 kT / ~18–23 sessions**. At 37–42 the range lands back **inside** the charter's 37–50, which the 36–41 correction had briefly falsified. ⚠ **TWO STANDING RULES FOUND UNFOLLOWABLE AND MARKED AS SUCH RATHER THAN LEFT STANDING**: campaign convention 4 (*"read gate bars from `lighthouse_profiles.json`, never transcribe"*) — the file is **0 hits vault-wide**, so **every gate-19 bar is a transcription**, the exact thing it forbids (mirror owed at P4.4, ⊳ D-E); and *"claim next open mission in phase order"* — **since D-A, phase order is not claim order**, and an agent obeying it would open Decade 2 on the wrong mission. 📜 **ADR-057 held three statements about itself, one contradicting the other two** — frontmatter `proposed`, its own Status section *"ratified at Gate C"*, the signed §7.7 *"adopted with the charter"*: **the campaign was measuring itself under a regime whose own status field denied it had been accepted.** ⊳ D-B ruled Gate C covered it; **Date stamped 2026-08-16 (the act), annotated 08-19 (the correction)**. 🐛 Two field defects fixed in passing: P3.1's budget carried a **mojibake CJK char** (`llms-full真corpus`), and P4.5's split field **led with the superseded figure** — the first `~N–N kT` a grep finds must be the operative one. **NEXT = ⛩ O0b** (operator: a fresh macOS account + an unassisted non-builder runner; runbook `ready_for_operator`) **then P4.5a**. P2.6 stays `in_progress` — **no AAR, no `completed` (SO#5)**; D3 unscored, no 12-dimension composite, R-34/R-63 undischarged.

> ▶ **2026-08-19 — 📐 HAUSSMANN P2.6 SESSION 1 COMPLETE: RE-SCORED 50.5 → 63.2 ON ELEVEN DIMENSIONS · DECADE-2 RE-PLAN AT `proposed` · ⛩ DP6 + O0b BOTH OPEN.** Session `session_stanley_20260819_165937_haussmann_p2_6_midscore`, commits `3b8e90d..5b4f56e` (**11, local and UNPUSHED** — outward acts are operator-gated). **Two operator rulings taken up front**: proceed to DP6 with **D3 WITHHELD** rather than stall on the operator-gated TTFS run, and score on **instrument v1.0 unchanged** so the deltas mean something. 📊 **Reconciled 55.6 of 88 → 63.2/100** (baseline recomputed on the same eleven: **44.4 of 88 → 50.5**; Δ **+11.2 weighted / +12.7 normalized**). **No 12-dimension composite is published** — D3 carries 12 weight points and is not scored, not estimated, not carried forward. **D6/D7/D8 each moved 2 → 3**: the credibility stratum the charter named as the binding constraint, which is the campaign thesis surviving contact with measurement. **D9 moved 0** — the one dimension nine missions never touched, and it now fails for a *different* reason than at baseline. Calibration cleared at variance **≤1 on 11 of 11**, both reviewers independently quoting the **identical binding anchor on eight** dimensions. 🔍 **Four findings worth the record.** (1) **R-111** — adjudicated at P1.2 §7.5, assigned an id, **never given a table row**, and the fix never shipped: `/canonical-properties` files the Wilhelm Foundation as "not ours" with **zero** operator-affiliation disclosure. It survived three missions because it is an **absence** and not an identifier — same-diff is structurally blind to both. (2) **487 gate assertions green while 8 claim rows are open, with zero overlap** — prose contradictions, absences, and **off-site state** (no gate probes the repos the primary CTA points at) are all invisible to the suite. (3) **~0.8 points of the delta are the instrument, not the site**: both scorers re-read D2's conjunctive anchor *"search present and scoped"* the opposite way from the baseline, on a registry search that `git log -S` dates to **Storyweave P3 M3.1 — a previous campaign**. Annotated, not banked; the filed **v1.1 anchor fix must precede P5.2**. (4) **Three derived figures published wrong before being caught** (register §8.3, §8.6, re-plan §4) — the rule became mechanical: **count last**. 🆕 **Five new register rows R-120…R-124** from three fresh cold reads, every one re-probed `[D]` before registering: the homepage promising *"nothing leaves your machine"* and *"shared in the open"* **two sentences apart** (S2, 30-second zone) · an invented "Before and after" anecdote (S3, recorded as **weaker** than R-118) · the *"Contribute on GitHub"* CTA pointing at the repo where CONTRIBUTING/CoC **404** (S2) · **that other repo being unlicensed** while the badge reads MIT (S2, **legal edge, `#needs-human`**) · zero clinical posture on `/privacy`+`/security` while `/` says *rare* ×15 (S3). Held back deliberately: *"Lattice Protocol"* is in the hero and defined nowhere — **the counsel embargo forbids defining it**, so the question is whether the hero should name it. 📋 **`artifacts/p2_6/p2_replan.md` at `status: proposed`** — 8 measured-state findings, 12 mission dispositions (**P3.1 + P3.2 shrink**, their premises died; **P3.5/P4.2/P4.4/P5.2 grow**; net +60/+50 kT; **P3.5 moves FIRST**), and **8 `⊳` sub-decisions** drafted for the operator to stamp. **`mission_count` stays 27** unless the operator takes D-A(i), which would amend ratified §7.7 text. 🕰 **III cycle 166 written** — the **first of the entire campaign** and the first since 2026-06-10; the ~10-week dormancy is stated in the record rather than papered over. ✅ **Zero `site/` changes**, diff-verified across all eleven commits. **NEXT = ⛩ DP6 (a decision) + ⛩ O0b (a machine act — fresh macOS account, unassisted non-builder runner); they are independent and either can go first.**


> ▶ **2026-08-19 — 🚀 HAUSSMANN P2.5 SHIPPED + PROVEN LIVE (52/0) · ✅ P2.5 CLOSED 3-of-4 · O2/O3 RE-HOMED TO P2.6 · CONTEXT REFRESHED.** Session `session_stanley_20260819_224752_haussmann_p2_5_winddown`, commits `db4b34f` → (this session), **pushed `31369cb..db4b34f`, gitleaks clean**. **Deployed** — `deploy_record: 2026-08-19T22:54:11Z mode=prod tree=db4b34f` (221 pages, headers **4/4 live no drift**, redirects **42/42 widened**, installer routes 51). **The live probe was red-proven BEFORE the deploy at 3 PASS / 33 FAIL** — every failure a P2.5 deliverable, and *not* uniformly red (three assertions already held), so it discriminates rather than merely failing. Post-deploy **52 PASS / 0 FAIL**. Its strongest assertion **re-hashes each vendored file from the served HTML**: a build-time gate proves what we built, only a live fetch proves what a reader receives — all four match their published SHA-256. ✅ **P2.5 CLOSED at O1 under operator ruling, with the record stating what did NOT ship**: acceptance criteria **3 of 4 met** (zero-install path · first-success + troubleshooting + uninstall · cost statement), criterion **4 CARRIED** — the TTFS instrument + clean-machine run was **never measured**, and **R-34/R-63 remain undischarged**. 🔀 **O2/O3 re-homed to P2.6 as O0b/O0c**, *not* to a new mission file: the charter's `mission_count: 27` sits inside the **ratified §7.7 statement**, and amending ratified text is the operator's act, not an agent's. P2.6 is the better home anyway — it is the mission that *scores* D3, whose baseline 3 is explicitly provisional *"no TTFS run"*, so the measurement now happens inside the mission that scores it. P2.6 budget raised ~200–300 → **~300–450 kT / 2 sessions** (SO#11). 📚 **Context refreshed on all four fronts**: `context_claim_register` no longer reads *"8 are false"* in the present tense (they were resolved 8/8 at P1.1; the file now separates the 2026-08-16 baseline from the 112-row current state and explains the addendum convention) · `context_website_assessment` gained a *"read the above as history, not status"* block that names what has since been fixed **and refuses to quote an improved score before P2.6 measures one** · `context_web_quality_toolkit` graduated the P2 instruments (verbatim publication with a **verified** pin · the prose-level defect same-diff cannot see · allowlist-vs-baseline · the live probe) and its carry-lines went 11 → **15**, heading count derived not typed · campaign `CLAUDE.md` §6/§7 gained three standing gotchas. 🧰 **TTFS kit authored and owed upstream discharged** (`artifacts/p2_5/ttfs_instrument_kit.md`, A6) — offered to Vitruvius as a **staged, undelivered** memo, and labelled **authored-but-never-exercised**, because an instrument shipped as "proven" on the strength of careful writing is the self-certifying-gate class WebForge's own register warns about. 📋 **O2 is no longer hardware-blocked**: `artifacts/p2_5/ttfs_runbook_fresh_account.md` uses a **fresh macOS user account** (the design always permitted "VM *or* fresh user account"), states plainly where that is weaker than a VM (Homebrew/Node are machine-wide → it measures the *"prerequisites already installed"* evaluator), and leaves account creation + the run as the operator gate they always were. ⚠ **Credential hygiene lapse, self-reported**: an env-presence check written `${VAR:+SET}${VAR:-unset}` printed the **SS_VERCEL_TOKEN value into the transcript** — `${VAR:+SET}` alone is the correct redaction idiom and was already a recorded lesson. Operator has standing de-prioritization on rotating this specific token (throwaway test account, 2026-06-07), so it is logged rather than escalated; the idiom error is the durable part. **NEXT = P2.6 (⛩ DP6)** — its O0b needs the operator to create the account and run it.

> ▶ **2026-08-19 — ✅ HAUSSMANN P2.5 O1 BUILT: THE ZERO-INSTALL PATH IS LIVE IN THE TREE · SUITE 472→487/487 · NOT DEPLOYED, NOT PUSHED.** Session `session_stanley_20260819_202459_haussmann_p2_5_o1_zero_install`. **⛩ The 4-question pick was taken, all four as recommended**: variant **A now + B at O3** · invented transcript **deleted now, labelled gap** · cost statement **as drafted** · first-success definition **accepted as O2's stop-condition**. **Built**: `/get-started/what-your-agent-reads/` — a hub + one page per vendored file, four files pulled from `.adna` at a **verified** pinned commit and rendered **verbatim**, on the P2.3 spec-split shape (generated projection · committed output · drift gate **gate-36**, 9 assertions). `/get-started/` gained the cost statement above the first command, a published **first-success** definition (five runnable assertions + the behavioural half), troubleshooting, and uninstall. 🔍 **THREE FINDINGS TAKEN BEFORE BUILDING — TWO WOULD HAVE SHIPPED A DEFECT.** (1) **The obvious vendor source was a data leak**: O0 said "the workspace `CLAUDE.md` router", and *this node's* `~/aDNA/CLAUDE.md` names five vaults it marks **local-only, NO remote**; the image's `template_workspace_claude.md` greps **zero** for all five and is the honest source anyway. (2) **`install_truth.json`'s `template_sha` is not a usable pin** — it records `fd32fc7`, and `git cat-file -t fd32fc7` in `.adna` returns **not a valid object name**; frozen by that generator's own idempotency guard, then orphaned when `.adna`'s origin was repointed. It renders **nowhere**, so it is latent — but the tour's entire claim is *"these bytes came from that commit"*, so the generator derives and **verifies its own** pin and **refuses to emit** against one that will not resolve. (3) **R-118's error was asserted twice more in prose** (→ **R-119**, S3): deleting the code block alone would have left the same false mechanism standing in two sentences — **the same-diff hazard class no route grep finds**, since the defect is in prose, not a route, slug, or count. ✏️ **AND A CORRECTION TO OUR OWN FINDING**: R-118 said the onboarding interview *"does not fire here"* — reading the vendored files showed that **overshoots**. It does fire, *after* the fork and offered by it (`template_workspace_claude.md:102`, `skill_project_fork.md:216`); the page's error was **agency and order**, not the interview's existence. Register corrected — a claim of ours that outruns its evidence is the same defect as one of the site's. **Verified** `[D]`: suite **487/487 zero xfail** (was 472) · drift gate **red-proven** (byte mutation → exit 1, clean → exit 0) · all four files **byte-exact through the full render pipeline** · the five published assertions **run and pass as written** · **axe 0** in both themes × 3 surfaces × 3 viewports. **Two gate failures diagnosed, neither a regression**: gate-30's redirect tests need `inject_redirects.mjs` (runs in `deploy_adna.sh`, not in `npx astro build`), and gate-27's 7 leaks were all the standard's own words on tour pages → **allowlist** (reviewed, dated, token-scoped), **never the baseline**. ⚠ **One instrument false-positive, corrected not believed**: the first private-vault assertion swept all of `dist` and failed on **nine legitimate pages** — those vaults have public registry records by design under **pt19**; their names are not the secret, a wrong *source* is. Re-scoped. **NEXT: ⛩ O2 needs a clean machine** (separate gate) — it produces the real transcript as a by-product; **do not author a timing claim before that run** (R-34/R-63). **5 commits unpushed; a push is its own per-action GO. Nothing deployed this session.**

> ▶ **2026-08-19 — 🚀 HAUSSMANN P2.4 SHIPPED + PROVEN LIVE (37/0) · ✅ P2.5 O0 DESIGNED — ⛩ HALTED AT THE PICK.** Session `session_stanley_20260819_123105_haussmann_p2_4_deploy_p2_5_design`, commits `87fe0c8` → `9096680`. **P2.4 deployed** — `deploy_record: 2026-08-19T19:37:04Z mode=prod tree=d42ee68` (216 pages, headers **4/4 live no drift**, redirects **42/42 widened**). **The live probe was red-proven BEFORE the deploy**, against production carrying the defect: **17 PASS / 17 FAIL**, every failure a P2.4 deliverable — and *not* uniformly red, so it discriminates rather than merely failing. Post-deploy: **37 PASS / 0 FAIL**. Empty-derivation guard mutation-tested: **all four empty forms refused**. 🔴 **What production had actually been serving**: `/commons/` read **`"member records last synced ."`** — empty date, dangling full stop — with **WilhelmAI at 0 of 3** declared relationships and **RareArchive at 0 of 1**, one sentence before that page promises honest activity is *"exactly this: the dates above and the relationships each vault declares."* It now renders the date, `umbrellas RareArchive · federates Astro, III`, and `under the WilhelmAI umbrella` — **verified by eye as well as by script**. ⚠ **ONE PROBE CORRECTION, AND IT WAS A CORRECTION OF KIND, NOT OF CONVENIENCE**: the first live run came back **36/1**, red on 11 raw `data-slug` values. That is the recorded deferred generator item — but the assertion was testing **a layer nobody reads**: `HomeHero.astro` canonicalizes those on read, and the SVG is `role="img"`, non-interactive to AT. The hard contract is the emitted **href** — **0 of 109 non-canonical**. Split: href stays hard; the generator item prints under **KNOWN-OPEN** with its samples and its reason. Named, not silenced, not green. LESSON: *a probe that fails on the wrong layer trains you to ignore its reds.* 📋 **Changelog backfill surfaced its own finding**: the `2026-08-19` entry covered **P2.1 + P2.2 only** — **P2.3 shipped to production earlier today with no entry at all.** The deploy script's cadence prompt is non-blocking, printed its nudge, and was skipped, which is exactly the decay its own comment says it exists to prevent. *Asking is not the same as being answered.* Extended to cover the spec pagination, the dated pages, the tier-first registry, and the `/commons` drop — the last named plainly and at length. **Counts derived, not typed**: **114** pages carry a rendered `Last updated` in `dist` (STATE and the P2.3 record both said **113**; the derived figure shipped); the four dateless prose pages are **stated in the entry** rather than left for "114 pages carry a date" to read as *all* of them. The content-collection schema **rejected my first description at 213 chars** — caught at the layer that should catch it. 🔎 **F-P2.5-1 — `/get-started/` renders invented terminal output as if real** `[D]`: the `$ claude` block's strings exist **nowhere in `.adna/`** (searched in full, zero hits; only occurrence in the repo is the marketing page), and it depicts an onboarding interview that **does not fire on a fresh clone** (`skill_onboarding.md` gates on a forked project directory; `skill_project_fork.md` runs instead). Registered **R-118** (`unsupported → fix at O1`, S3). It sits three inches above the one line the refusing cold-read reader said he trusted. ✅ **P2.5 O0 DESIGNED**: three variants costed; **recommended A now (annotated real-file tour) · B at O3 (real recorded session, from O2's run) · C not at all** — A and B answer *different* objections and C would cost the site's no-JS/axe-0 commitments for motion the static transcript already carries. **"First success" defined observably** (five runnable assertions + the behavioural half); **cost statement drafted** retiring the *"any tool can support"* contradiction and the absent uninstall; **TTFS kit sketched reusable** (clock starts at the entry URL, not the first command — reading the page is where the refusal happened; an empty friction log reads as **suspect, not excellent**). **⏭ Next: ⛩ THE P2.5 PICK — 4 questions** (variant · the invented transcript's disposition · cost-statement copy · first-success as O2's stop-condition; recommendations at `artifacts/p2_5/design_zero_install_path.md` §7). **O1 does not start until it lands.** Then O2 needs a ⛩ clean machine. **⛩ NOT PUSHED** — 3 new commits; a push is a separate per-action GO. **Open:** 77-vs-74 (Hestia) · the 740 mechanism (deferred) · `build_graph_svg.mjs` raw `data-slug` · P0.4 Aspasia · Vitruvius owed-back note staged-not-delivered · **C01 rotation before 2026-08-27 (8 days)**. Token: ~95 kT.
>
> ▶ **2026-08-19 — ⛩ FOUR RULINGS SIGNED · ✅ HAUSSMANN P2.4 COMPLETE (O0–O3) · SUITE 460→472 · 4 MEMOS DELIVERED — NOT DEPLOYED.** Session `session_stanley_20260819_105538_haussmann_p2_4_build`. **The operator answered the split pick and three carried items**: **(1) variant A, tier-first** · **(2) the 740 mechanism explicitly deferred** · **(3) DP-16 shape A conditioned** · **(4) GO on both outward acts**. **§tiers.6 (77-vs-74) deliberately NOT put** — it needs Hestia's B7 data pass and stays *stated, not decided*; `pt19` holds, the count stays a true unregenerated **74**. **ADR-052 → `accepted`** with a new **§tiers.7** recording `scales at 10×` **UNMET** plus the three mechanisms that would meet it and a revisit trigger (~150 rows or P3.2) — *unmet-and-known beats met-on-paper*. **DP-16 ruled in the Keystone manifest**: `Inference` now appears in **both** columns — control-plane for its serving lanes, **data-bearing §8 for its human chat surfaces** — because the OpenWebUI instance ran four months unregistered *precisely because the label said there was nothing to register*, and shape A without the declared row would have left that condition in place deliberately. **P2.4 BUILT**: `/vaults` is tier-first — in use 7 · chartered 10 · planned 57, each with its meaning; **all three tiers render the same card on purpose** (a density gradient over a self-declared field reads as a *ranking*, and the thing it ranks by is uncorroborated — §tiers.2); tier badge on every card **and** the detail page; **`RegistryCard` had to move too — the homepage was rendering `vault.status` RAW**, so `genesis`/`pending` reached a public surface and the two surfaces would have described one vault with two words; the **74-framing is reconciled** (hero states the split, stat strip shows 7/10/57); **"These stages are self-declared" ships as body text**, not a tooltip. **No-JS anchors retargeted** — the jump-chips pointed at `#class-*` sections that no longer exist, and `VaultClassFacet` **built its own href**, so a caller could pass a correct tier slug and still emit a dead anchor → renamed **`VaultFacetChip`**, takes the full target id; 3/3 resolve, 0 dangling. 🔴 **THE UNPLANNED FINDING — the "cosmetic" carry-over was a LIVE SILENT DROP.** The 13 mixed-case links were logged as *"they 301, so they work"*. They did not: `commons.astro` joined the subnetworks overlay to the registry with **raw** ids while P2.1 had canonicalized the registry **and its edge endpoints**, so the join missed. **Probed production before touching anything**: WilhelmAI rendered **0 of its 3** declared relationships, RareArchive **0 of 1**, and the freshness line read **"member records last synced ."** — empty list, dangling full stop — one sentence before the page promises *"honest activity, today, is exactly this: the dates above and the relationships each vault declares."* It hid because **every dropped field had an honest-absent path**, so a lookup failure rendered identically to a fact. LESSON: *an honest-absent affordance is where a silent drop hides best, because the page looks considerate rather than broken* — and **when a boundary is normalized, every consumer that joins across it is a suspect**. Non-canonical vault links **13 → 0** of 442 emitted (the last 11 were the hero graph's keyboard/AT nav parsing raw `data-slug` — the accessible path paying a redirect the mouse path didn't). **Suite 460 → 472 green, zero xfail**; 12 new assertions (**gate-30** ×2 + new **gate-35** ×10), **every one red-proven**. ⚠ **FOUR OF MY OWN INSTRUMENTS WERE WRONG FIRST, all caught before being believed**: gate-30's link check split the query string *after* the trailing slash and reported the graph page's `?focus=` deep-links as broken routes; gate-35's overclaiming check scanned the whole page and failed on **"flagship"** inside `Harness.aDNA`'s own tagline — pt19 data, a vault describing itself, not this site awarding a rank (**scope, not the rule, was wrong**); the tier-badge regex assumed `class="…">` and **matched nothing** through Astro's scoped-style attributes, caught *only* by the floor assertion; and the overlay-join guard **was satisfiable by a comment** — it grepped the source for `canonicalVaultSlug`, which the docblock above the join mentions, so the mutation test **passed with the fix fully removed**. Rewritten to assert built output. LESSON: *a gate a comment can make pass is not a gate.* **axe 0** × 4 surfaces × 3 viewports × **both themes** (24 captures, `evidence/captures_p2_4/`), parsed on `axeViolations` with a missing-key guard. **Claim register R-114…R-117** (§7.7) — **R-117 is the claim the page STOPPED making**: *"most tended by a named agent"*, true at 61/74 personas and still wrong across a 57/74-planned set (anti-pattern 7.5), **guarded against return** because the R-28/R-62 family already recurred twice when a fix went to a component instead of the claim. **⛩ 4 MEMOS DELIVERED under GO** — Venus (`invite/v1`, Gangway Phase A's exit gate) · Pythia (DP-16, **corrected at delivery**: its headline said "not ruled" and it *is* ruled) · Pandora (cohort row + roster-wide finding) · Hestia (77-vs-74 + the dual-clock ask). Owed-back pattern note to Vitruvius **staged, not delivered**. **⛩ NOT DEPLOYED — a deploy is a separate per-action GO.** **⏭ Next: ⛩ deploy GO for P2.4** (fetch + diff `deploy_log` FIRST, then `npx astro build` → `deploy_adna.sh prod`) → then **P2.5 onboarding paths**, then **⛩ DP6 at P2.6**. **Open:** 77-vs-74 (Hestia) · the 740 mechanism (deferred, recorded) · `build_graph_svg.mjs` still writes raw `data-slug` (normalized on read; generator belongs with the `vaults_graph.svg` currency pass) · P0.4 Aspasia ack. Token: ~155 kT vs 250–350 est.
>
> > ▶ **2026-08-19 — 🚀 HAUSSMANN P2.3 SHIPPED + PROVEN LIVE (124/0) · 3 OWED REPLIES CLEARED · P2.4 O0+O1 DONE — ⛩ HALTED AT THE PICK.** Session `session_stanley_20260819_101805_haussmann_p2_3_deploy_memos_p2_4`, commits `ef5a5dd` → `6e4f30a`. **P2.3 finally shipped** — deployed `deploy_record: 2026-08-19T17:19:00Z mode=prod tree=97561c0` (216 pages, 4/4 headers verified live, **42/42 redirects widened**), pushed `301daef..ef5a5dd` gitleaks clean. **Production before-state captured FIRST so the change is provable**: `/reference/specification/full/` and all **20 numbered section URLs were 404 and are now 200**; the spec hub was **163,169 bytes as one page**; RSS was **1 item**, now ≥4; date coverage 0 → **114 pages**. Probe **124 assertions, 0 failures**, written in `artifacts/p2_3/` not a scratchpad (P2.2's instrument evaporated from one), with its **empty-derivation guard red-proven** — against an empty `dist/` it throws rather than printing green, which is exactly what P2.1's probe did. ⚠ **TWO INSTRUMENT DEFECTS, BOTH MINE, BOTH CAUGHT BEFORE THEY WERE BELIEVED**: the probe first reported **58 redirect failures that were not real** — the adapter config's `src` is a **regex**, and unescaped backslashes meant it was fetching the literal string `/vaults/aDNA\.aDNA`; then it **asserted a contract nobody wrote**, demanding a date on every page when P2.3's contract is 113 content pages and the freshness layer is an opt-in Props field listings decline by design. LESSON: *an instrument reporting the system broken is exactly as suspect as one reporting it healthy.* 🔎 **FINDING (P2.3 follow-up)**: four **substantive prose** pages carry no freshness signal — `/learn/what-is-adna/` (1,446w), **`/provenance-audit/` (1,282w)**, `/get-started/`, `/community/`; hand-authored `.astro` pages that never pass `source?:`. `/provenance-audit/` is the sharp one — *the page that tells readers how to check this site's claims, carrying no date of its own.* **STAGE 2 — the manifest pass grew from 1 row to 10.** Pandora reported row 6 stale; **re-reading every cohort vault's HEAD before editing hers found all ten stale** — the roster described the cohort as seeded 2026-06-22, and three graphs are no longer stubs (**Inference 165 files · Container 139 · Forgejo 116**). *Fixing only the row someone complained about would have left eight known-wrong rows in a file whose first line calls itself "the authoritative register."* Structural fix: **`Fork method` is a historical fact that does not age** (a graph forked from the lean stub is still forked from it at 139 files) — the column that lied was the file count, so **`State` is now its own column**. **⛩ DP-16 AUTHORED, NOT RATIFIED**: an OpenWebUI instance has persisted prompt/response payloads on this node **since 2026-04-10**, inside a graph the manifest classes control-plane. Recommendation = Pythia's **shape A** (narrow ADR-000 §3 to the serving lanes) **with one condition from her own evidence** — the human surface must become a **declared §8 row, not a footnote**, because that instance survived four months unregistered *precisely because the graph's label said there was nothing to register*. Classification **unchanged** pending signature. **Three replies staged** (delivery = separate outward act): **Venus** — `adna.network.invite/v1` **no standard-side objection, gate may close**, one real defect (**the code alphabet excludes `L` and three strings say it does not, including the refusal message a phone-relaying human hits**), declined a vault entity type for the invite, taking the code-is-not-the-object pattern upstream as a candidate awaiting a 2nd instance; **Pythia** — the DP-16 recommendation + our symmetric failure (~15 inbound memos sat **untracked in git**, delivered but never arriving); **Pandora** — row 6 + the roster-wide finding. **R-108 CLOSED, verified not taken on report** — `stanley.science` serves "Head of AI", **"Lead AI Architect" returns 0 across four URLs**; S4 cleared, P2.6 re-check discharged early. **Correction appended to the delivered 08-18 memo** (it cited SS's CI-08-5 as open; closed 2026-06-22 under D4 — we read a peer's item at the version *we* held); body left as sent, *a delivered memo is a record not a draft*. **STAGE 3 — P2.4 O0+O1.** **ADR-052 §tiers complete at `proposed`**, and two of the genesis decision space's four items **did not survive the data**: **`card_present` disqualified as a tier input** (all 7 `active` vaults have a card → the sketched split yields an **empty bucket**; it discriminates inside `genesis` 7/49, i.e. it measures **documentation, not lifecycle** — tiering on it gives a badge that *claims maturity and measures paperwork*, the narrated-vs-derived error **inverted** and harder to catch because the number really is derived); and **every status here is self-declared with nothing to corroborate it** (`github_url` **1/74**, `docs_site_url` **0/74**, `last_synced` 24/74 with **18 frozen at one date**) → vocabulary describes **declared stage**: **in use 7 · chartered 10 · planned 57**, no `flagship`, no `mature`, "self-declared" shipped plainly. **Dual clock recorded NOT DERIVABLE**, not faked from `last_synced` (that would say 18 vaults changed on one day — a true fact about a bulk sync sold as a false one about vaults). **77-vs-74 STATED not decided** (a `sync:vaults` would publish `Bearly`·`RareGraph`·`StrongerWithScience`, two *data-bearing local-only NO remote*) — DP4-class, Hestia memo staged with a sharpened ask: **a timestamp beats a tagline** (readable vs **checkable**). **O1: three variants, headless-verified 23/0 — and none of them scales.** A 18,896 px · B 19,327 px · **C 17,974 px at 740**; C was *designed* to be the scalable one and beats A by **5%** — noise, not architecture, because at 10× the planned tier is ~570 rows and 570 dense rows at ~30px is ~17,000px by itself. **Density cuts cost per row and does nothing about row count** → `scales at 10×` recorded **UNMET**, and **the pick is now two decisions, not one** (grouping **and** the 740 mechanism; deferring the latter is legitimate — at 74 all three are normal pages). Pre-screen `[D-syn]`, **conflict declared** (builder scored its own comps): no variant clears 4.0, **A and C tie** as P2.1's did. **⛩ NEXT = THE OPERATOR PICK** — (1) grouping A/B/C · (2) the 740 mechanism or an explicit defer · (3) DP-16 · (4) delivery GO for **four staged memos** · (5) 77-vs-74. Then P2.4 **O2–O3**, which also own the **13 mixed-case vault links** (`commons.astro:196` bypasses the P2.1 accessor; gate-30 has a `subnetworks.json` blind spot). Token: ~340 kT vs 300–420 est.
>
> ▶ **2026-08-18 — 🚀 HAUSSMANN P2.2 SHIPPED (121/0 live) · ✅ P2.3 COMPLETE (5/5 objectives, undeployed) · CI HAD BEEN RED FOR TWO DAYS AND IS FIXED.** Session `session_stanley_20260818_212246_haussmann_p2_2_deploy_p2_3_docs`, commits `0a472f3` → `29313e8`. **P2.2 finally shipped** — pushed `356b33b..301daef`, deployed `deploy_record: 2026-08-19T04:24:08Z mode=prod tree=301daef`, pre-flight fetch-and-compare clean. Production **before-state captured first** so the change is provable, not asserted: `/provenance-audit/` was **404** and is now 200; `/compliance/` and `/adopters/*` were 200 and now 301. Live probe **121 assertions, 0 failures** (42 redirects × both slash forms + all 37 destinations at 200); nav **7** with `nav-more` absent from the page; `/reference` titled "The Standard"; `/install.html` still 200; 4/4 headers. **P2.3 ran end-to-end in the same session, all five objectives.** **O0** — the 29/11 broken-link figure was re-measured rather than trusted (P2.1/P2.2 had moved routes underneath it); **26 stale targets rewritten across 5 files**, four to real pages, four to the public repo *each verified 200 before being linked*, and `/patterns/content-as-code` — a page that never existed — to spec §14. Bare code-span mentions of the same filenames are **prose about the upstream file and were deliberately left alone**; a blanket sweep would have corrupted 6 correct sentences. **gate-31** is purpose-built rather than lychee, deviation recorded with its reason: URL correctness here lives in `dist/` **and** the adapter's redirect config, so a generic crawler reports the 13 mixed-case vault links as broken when they 301 correctly — it classifies those as hops and prints them (P2.4's lane). External links get a **scheduled non-blocking** workflow instead; all 31 currently reachable. **O1** — ⛩ operator ruled **hub + sections + full**: `/reference/specification/` is now a contents page, 20 numbered sections each have a URL with prev/next, and the whole document stays at `/full/`. **Default URL 74,067 → 2,244 px at 375px (33×).** Generated, never hand-split — `specification.mdx` is *itself* a projection, so splitting the file would be reverted by the next transform (the `subnetworks.yaml` clobber class). **Two traps avoided by measuring**: the split is on *numbered* headings because `## SITREP` lives inside a fenced code block in §8 and a heading-level split would **cut through the fence**; and the anchor map is derived from *rendered* ids because rehype strips the dot, making §1.4 `#14-normative-language` collide head-on with §14's own `#14-content-as-code-pipelines`. **O2** — **113 pages** now carry a last-updated date and an edit link. Dates come from **git, not frontmatter** (only 3 of 92 files declare one) and the helper **refuses to guess**: a shallow clone makes every file resolve to one commit, so it omits rather than print a confident wrong date; both workflows now set `fetch-depth: 0`. Spec pages point at the **upstream standard** with the label "Edit the standard", because an edit to the generated copy would be overwritten. **O3** — changelog **1 → 4 entries**, RSS carries them, valid XML; every claim probed live before it was written, and **this session's own work is deliberately absent until it deploys**. Cadence rule wired into `deploy_adna.sh`, non-blocking on purpose. **O4** — F11 fixed (`AGENTS.md — AGENTS.` and two more degenerates); the `excerpt()` docblock had *predicted this exact bug* and filed the consolidation as a follow-up — now closed. **Suite 446 → 460, zero xfail; axe 0 across 8 surfaces × 2 themes; 36 T0 captures.** **14 new assertions across 4 gates, every one red-proven.** ⚠ **THE BIG UNPLANNED FINDING: the standing-watch CI had been failing for three consecutive runs** (`b9d510a`, `356b33b`, `301daef` — every run since P2.1's redirect work) **and nobody had noticed.** `gate-30` asserts the redirect widening, but the widening is done by `inject_redirects.mjs`, which runs **only inside `deploy_adna.sh`**; CI ran `astro build` → `test:gates` with no injection, so it failed every time while the same suite passed locally — because local sessions had deployed first and left an injected `config.json` on disk. `gates.yml` now assembles the real deploy artifact before testing, proven by reproducing the CI sequence from a clean tree. ⚠ **A REGRESSION THIS SESSION INTRODUCED, caught by evidence**: the 20 section pages went h1→h3 (axe `heading-order`, both themes); fixed by promoting body headings one level, **skipping fenced blocks**. It was nearly missed because the capture-report parser looked for a `violations` key that does not exist (it is `axeViolations`), summed nothing, and printed a clean **0** over a real violation — the **fourth instrument-lies-green incident of the session**, and the only one that would have shipped a defect. LESSON: *the verification instrument is as likely to be wrong as the thing it verifies, and a red result deserves the same scrutiny as a green one.* ⚠ **Self-inflicted, recovered**: `node -e "import('./transform-content.mjs')"` as a parse check **executed** the script and regenerated 37 content files over committed work; fully restored via `git checkout HEAD --`, one intentional change re-applied, verified by diff. The ESM syntax check is `node --check`. **⛩ NEXT = A DEPLOY GATE for P2.3** — fetch + diff `deploy_log` FIRST, then `npx astro build` → `deploy_adna.sh prod`; a push GO is per-action and does not carry forward. HEAD is **6 commits ahead of origin**. Then **P2.4** (registry redesign), which also owns the 13 mixed-case vault links and the **77-vs-74 registry admission ruling**. **Owed, still unanswered:** Pythia's **DP-16** ruling and Venus's `adna.network.invite/v1` standard-side review (**Gangway Phase A's exit gate is waiting on it**). New follow-up: the `src/content/docs/` files have **drifted from `transform-content.mjs`** — it is no longer authoritative for hand-edited projections, and that needs an explicit ruling. Token: ~250 kT.
>
> ▶ **2026-08-18 — ✅ HAUSSMANN P2.2 COMPLETE: ADR-049 Option A IMPLEMENTED (nav 7 · 11 redirects · 446/446) — BUILT + GATED, NOT DEPLOYED.** Session `session_stanley_20260818_183026_haussmann_p2_2_ia_implementation`. **The mission is done end-to-end**: DP5 was signed last session and the site was still unchanged; O2 implemented it and O3 proved it. **Nav is 7 with no disclosure** — `Standard(/reference) · Learn · Vaults · Network · Commons · Use Cases · Community`; verified in built markup (7 links, `nav-more` absent from the page entirely). `/reference` is **retitled "The Standard"** so the click does not land on a differently-named page — a `/reference`→`/standard` rename was outside the ratified 11-redirect budget, so the label moved to the page instead of the page to the label. **The four copies of the audience link set are one**: the "For you" group deleted, `home.ts` `audiences` **derived from `personas`** rather than deleted (deleting it would have broken gate-13's import; deriving keeps the test deriving, KW-8/FR-K), `pathCards` gone with its page, `Breadcrumb` `terminalRoutes` down to one row. The persona double-listing ended, dissolving the `navigation.ts:10-13` ordering constraint. **11 redirects**, all widened to both slash forms (42/42). **`/compliance` → `/provenance-audit`** is a real page move paying **ADR-048's owed rename**, and it closes the charter's **Enterprise Architect routing gap** — that page was reachable from neither disclosure surface and now sits in Guides, crumbing under HOW. **⚠ THE RATIFIED ADR CARRIED A FALSE PREMISE, AND THE GUARD DIFF CAUGHT IT.** ADR-049 called the `/adopters/*` prose "near-paraphrase" and Option A "redirect-only, zero content rewritten". Measured on disk it is not: each adopter doc carries a **`## Typical Ontology Extensions` table — 13 unique entity-type rows** absent from its twin, plus a self-reference block and unique glossary links; the four segment landings are **curated reading-path decks** (~1,690w — a 3-week course structure, a procurement checklist, a 60-minute quickstart), not narratives. Under the operator's fold-before-retire ruling this became **9 folds, not 4**; five destinations grew (483→690, 467→762, 441→917, 474→826, 536→644 words). Redirect count, nav, and audience surface are **exactly as ratified** — only the cost line changes, to **zero content *discarded*; 7 destinations gained folded sections**. ADR-049 carries an implementation note; the charter DP5 row is corrected. *DP5 settled which branch survives — a judgment call, and it held. It could not settle what is in the files.* **VERIFIED**: build clean 195 pages · **446/446 gates incl. `@audit`** (444 at P2.1 + 2 new gate-30 tests) · re-crawl of 194 built pages = **0 orphans · 0 duplicate `<title>` · 10/10 ≤2-click** · 36 T0 captures · 11/11 redirects correct in `.vercel/output/config.json`. **The new assertions were proven by making them fail** — both mutations applied, observed failing, reverted (`gate-7:68` had been asserting against `/adopters/solo-developer`, **a route that never existed**, passing vacuously its whole life). **One trap avoided**: the redirect assertions were first drafted into gate-13, which runs against `npm run preview` — per P2.1 doctrine §3.2 preview does not serve the adapter layer at all, so they would have failed in a way indistinguishable from a real bug; they live in gate-30 reading the emitted config. **4 FINDINGS OUT**: (1) **13 mixed-case vault links are still emitted in markup** — `commons.astro:196` renders `/vaults/${row.slug}/` from `subnetworks.json`'s raw values, bypassing P2.1's accessor; they 301 so they work, but every click is a hop, and **gate-30's accessor test has a `subnetworks.json` blind spot** → P2.4 · (2) duplicate `<h1>` "aDNA" on `/glossary/glossary-adna/` + `/vaults/adna/` → P2.3 · (3) `/patterns/content-as-code/` still dangles → P2.3 · (4) stray gitignored `dist/` + `node_modules/` inside `site/src/pages/` (Astro skips both; debris, flagged not touched). **The B1 crawl instrument had evaporated** — it ran from a session scratchpad, so an inventory four missions cite could not be regenerated; **re-authored at `artifacts/p2_2/crawl_local_inventory.mjs`**, in the campaign dir, pointed at the local build (crawling production would have measured the OLD IA and called it healthy). **⛩ NEXT = A DEPLOY GATE** — fetch+diff `deploy_log` FIRST, then `npx astro build` → `deploy_adna.sh`; a push GO is per-action and does not carry forward. Then **P2.3**. **Owed, unanswered:** Pythia's **DP-16** ruling (`ack_required` — an OpenWebUI instance has persisted conversations on this node since 2026-04-10, colliding with the control-plane classification held in THIS vault's `keystone_cohort_manifest.md`; her 08-07 question memo sat undelivered 11 days and both memos were **untracked in git**, invisible until this session committed them) and Venus's `adna.network.invite/v1` standard-side review (Gangway Phase A exit gate waits on it). Token: ~105 kT.
>
> ▶ **2026-08-18 — 🚀 HAUSSMANN P2.1 SHIPPED + PROVEN LIVE (162/0) · ADR-051 ACCEPTED · P2.2 O0+O1 DONE — ⛩ HALTED AT DP5.** Session `session_stanley_20260818_164055_haussmann_p2_1_deploy_p2_2_open`, commits `7b9956e` + `5112884`. **P2.1 finally shipped** — it had been completed-but-undeployed, two commits local-only while production still 404'd. Pushed `070f104..b9d510a` (gitleaks clean), deployed `deploy_record: 2026-08-18T23:42:11Z mode=prod tree=b9d510a`; `inject_redirects` **widened 31 of 31 routes — its first run ever against production**; headers 4/4 no drift. **THE OWED PROBE RAN: 162 assertions, 0 failures** — 31 legacy sources × *both* slash forms (62), all 74 canonical vault URLs at 200, the 24 vault redirects followed end-to-end to a live page, `/org-context-graphs` in both forms. The headline repair is confirmed: `/org-context-graphs/` **with** the trailing slash was a 404 and is now a 301, and trailing-slash is the shape every canonical URL here emits. **⚠ THE INSTRUMENT REPRODUCED THE BUG IT WAS TESTING FOR** — the probe's first draft reported a green "64 PASS, 0 FAIL" while testing *nothing at all* in its canonical third: it guessed field `slug` where the registry carries `vault_slug`, got an empty array, and iterated it happily. That is P2.1's own silent-drop class recurring **inside the verification instrument within a day**. It now derives from the build snapshot and **throws** on an empty derivation. A claim also needed retracting: `/vaults/ScienceStanley` was never a real URL (its card already declares a canonical slug) — the broken class was the 24 cards declaring a *non-lowercase* slug. **ADR-051 → accepted** (operator, at the deploy ⛩ — the earlier of its two permitted gates; ratification followed the probe, not the intent). **Doctrine §3.2 added** (the P2.1 follow-up that had no owner): local preview does not serve the adapter layer — `astro preview` cannot test redirects **at all**; the trap is that the failure is indistinguishable from a real bug, and the control that disambiguates is to probe a redirect known to work in production. **P2.2 OPENED**: ADR-049 authored with exact derived redirect counts (A=11 · B=12 · C=18); **B cut as dominated** (more redirects *and* a rewrite, no home for `open-source-project`, insider vocabulary) with the reasoning recorded not the option dropped. Comps at `artifacts/p2_2/ia_comps.html` headless-verified — each pane renders exactly the nav count it claims (Current 8 + load-bearing More · A 7 · C 6), zero console errors. **Ranker A 4.03 · C 4.17, both ≥4.0** — and the record **declines to name a winner**: 0.14 from a single synthetic rater is one persona moving one cell, and the two are near-perfect mirrors (A Relevance 4.4/Actionability 3.4; C Actionability 4.8/Relevance 3.4). **Conflict declared**: the agent that built the comps also scored them — against convention 4 — so the score is a `[D-syn]` **pre-screen**, not a verdict. **4 FINDINGS → O2**: `gate-7-interaction.spec.ts:68` asserts against `/adopters/solo-developer`, **a route that has never existed** — `page.goto` doesn't throw on a 404, so it has been **passing vacuously** its whole life · 2 nav-orphaned routes · `question-test` at 3 routes · the audience link set in **4 copies**, personas nav-listed **twice**. Charter's `nav ≤8` row corrected to **≤7** (operator ruling). **⛩ DP5 RATIFIED same-session — Option A** (consolidate to `/use-cases/`; nav 7; 11 redirects; **zero content rewritten**). The ranker had cleared both and **declined to separate them**, so the call was judgment: A is reversible and generates evidence cheaply, and **P2.6 is already the recalibration point** where C gets reconsidered on data — C is deferred, not rejected. Cost accepted openly: A keeps an identity taxonomy (7.7 only partly answered) and a later C means a second redirect generation. ADR-049 `accepted`; charter DP5 row + status board updated; **next substantive DP = ⛩ DP6 at P2.6**. **Pushed** `b9d510a..19950c5`, gitleaks clean. **⏭ Next: P2.2 O2** — implement A (nav 7 · fold `/adopters/*` → `/use-cases/*` · retire 4 segment landings · `/compliance/`→`/provenance-audit/` **owed by ADR-048** · kill 4 duplicate titles · collapse the **4 copies** of the audience link set · fix the vacuous gate-7 assertion), **same-diff in the same commit** (`gate-13-nav-surfacing` is the primary blocker — its test *name* hardcodes "8-item desktop row"; `Footer.astro` is NOT nav-derived and needs its own edit), then O3 (re-crawl · T0 captures · AAR). Deploy is a separate ⛩. **Flagged, deferred by operator ruling:** **a concurrent lane committed `cac43e1` into this tree mid-session** — an inbound Venus memo, `ack_required: true`, standard-side review of `adna.network.invite/v1` **before Gangway Phase A's exit gate closes** (medium severity, not this mission's lane, unanswered). Token: ~110 kT vs 200–300 est.
>
> ▶ **2026-08-18 — ⛩ P1→P2 GATE SIGNED · P2 OPEN · ✅ P2.1 URL NORMALIZATION COMPLETE (not deployed).** Session `session_stanley_20260818_150215_haussmann_p2_1_url_normalization`, commits `070f104` + `10de74c`. **The gate:** signed by the operator with the record stating what the signature covers — P1 closed 4/4 on *missions*, **3-of-4 on *exit conditions***, D6/D7 re-score **deferred to P2.6, recorded as deferred not satisfied**. Next substantive DP = **⛩ DP5 at P2.2**. **Also cleared, both found by reading the tree rather than the record:** an **overdue Vitruvius ack** (his 08-16 reply carried `ack_required: true` and had been held on this vault's own non-empty lease — the lease cleared at P1's close) — answered with the craft-floor graduation cite **and its true weight**: an *agent-authored* mission-close ruling whose artifact is still `status: draft` with the operator countermand window open, explicitly **not** labelled ratified; §A8 got an owner + a dated slot (after Decade 1, via `skill_template_release`, since it touches a base-template skill) with the honest caveat that no view on the merits is formed yet. And **four governance artifacts authored 08-17 that had never been committed** + ~15 untracked inbound peer memos. **Pushed** `a37b40a..070f104`, gitleaks clean. **P2.1 (ADR-051, proposed):** root cause was **not data** — `card.vault_slug || slugOf(slug)` let a vault_card beat the canonicalizer, and 24 cards declare the raw name. Law decided by **census**: both candidate laws are collision-free across all 74, so incumbency decided it (50 of 74 already route drop-suffix; keeping the suffix breaks 50 to fix 24). **The registry was deliberately NOT regenerated** — `Home.aDNA` now carries **77** vaults to the committed **74**, so a `sync:vaults` would have silently published `Bearly` · `RareGraph` · `StrongerWithScience` to a public page, two of them *data-bearing, local-only, NO remote*. That is a **DP4-class admission ruling**, not a side effect of a casing fix → flagged to **P2.4 / Hestia's B7**. Instead: generator fixed *and* a new read-side accessor `site/src/data/vaults.ts` (11 importers repointed), so `vaults.json` is byte-untouched (pt19 absolute) and a future card **cannot** reintroduce a mixed-case URL. **TWO FINDINGS:** (1) the site's **two shipped redirects were half-broken in production** — `/org-context-graphs` 301s, `/org-context-graphs/` **404s**, and trailing-slash is the shape every canonical URL here uses; Astro's `redirects:` **cannot** express the slash form (it normalises keys) and Vercel matches redirects *before* its filesystem phase → repair = `inject_redirects.mjs`, widening each `$` to `/?$`, carrying no list of its own so nothing can drift, wired into `deploy_adna.sh`; (2) the **Wayback CDX sweep returns ZERO project URLs** — all 47 captures across both hosts belong to **prior owners** (an evolution-biology group; a portfolio template) → ruling recorded: **prior-owner URLs are not redirected**, because pointing `/the-team` at aDNA content claims another org's URL space. **Same-diff (ADR-057) cost two SILENT-DROP sites** — canonicalization would have removed 4 of 6 NetworkDiagram nodes and 6 of 8 homepage registry cards **with no error**, because a missed lookup was filtered away rather than raised; only one had a gate. Curated slice now **throws** on a miss. **gate-30** added (6 assertions, red-proven ×3, all derived from the build snapshot — never a typed list). Suite **444 → 450 green, zero xfail**. LESSON: *a silent-drop lookup is a same-diff hazard no route grep finds* — same-diff looks for hardcoded **routes**; these were hardcoded **keys**, which do not look like URLs. **⛩ NOT DEPLOYED** — and verification is honest about its limit: `astro preview` cannot test redirects at all (they live only in the adapter config, never in `dist/`), proven by the control that the pre-existing *production* redirect also 404s locally; config-level + regex simulation pass, **live probe owed at the deploy ⛩**. **⏭ Next: ⛩ deploy GO for P2.1** (fetch + diff `deploy_log.txt` first) → then **P2.2 IA consolidation (⛩ DP5)**. **Not done, flagged:** the P2.1 commit `10de74c` is **unpushed** — the earlier push GO was consumed by the P1 record and an outward act is per-action (Git-Ops rule 3). Token: ~135 kT vs 120–200 est.
>
> ▶ **2026-08-18 — ✅ HAUSSMANN P1 CLOSED 4/4 — P1.2 RE-RANKED (4.22, CRITERION MET), DEPLOYED + LIVE-VERIFIED. NEXT = THE ⛩ P1→P2 PHASE GATE.** Close session (`session_stanley_20260818_143557_haussmann_p1_2_close`, commits `84dd3bd` + close cascade) executed the operator's three rulings: **re-rank first → one deploy → close P1 and halt**. **The owed measurement ran.** P1.2's `verification_method` demanded ranker ≥4.0; O3 measured **3.61** and never re-scored after its own remediation. Re-run: **4.11 pre-fix → 4.22 post-fix — MET**, and met *before* the fixes as well as after. **Actionability, the dimension the remediation targeted, moved 2.50 → 4.00**; Credibility + Tone held 4.50. ⚠ **Finding before the finding: the 3.61 was NOT REPRODUCIBLE** — it logged neither its persona set nor 3 of its 6 dimensions, and 2 it *did* name (Credibility, Tone) are not in the canonical `skill_decadal_aar` six. Only 3 rows are a true comparison; the aggregate delta is *indicative, not measured*. This run records its instrument in full (`artifacts/p1_2/rerank_record.md`; register **§7.6**) so the next is comparable. **The re-rank caught 2 S2s (no S1), both fixed before the deploy + gate-pinned + RED-PROVEN:** **R-112** `/about` — the closing band re-asserted *"a real Founding Architect, a real anchor partner, real public-good work"* **fifty lines below the band this same mission rebuilt to stop asserting it (R-62)** — the R-28 family's *second* recurrence, on the sibling surface never adjudicated; the trailing *"Real stewards grow the network"* asserted stewards the page elsewhere says don't exist → **cut, not softened**. **R-113** `/canonical-properties` — dead-ended at the footer while its twin, built in the same mission on the same scaffold, closed with five exits; **both reviewer lenses found it independently**. Suite **442 → 444 green, zero xfail**. **⛩ DEPLOYED** `deploy_record: 2026-08-18T21:45:51Z mode=prod tree=84dd3bd` (4/4 headers at deploy *and* re-checked on the apex) → **live-verified on adna.network**: both surfaces 200 + both footer-linked, disclosure sentence above the stat strip, `og:site_name`="aDNA Network", 5/5 load-bearing P1.2 sentences intact, 3/3 retired phrasings absent, `/install.html` still 200 (the 08-16 collision class). **Title-alignment memo DELIVERED** to ScienceStanley + WilhelmAI under GO. **Charter corrected:** DP2/DP3/DP4 all read `pending` while their missions were closed — status labels lag truth. DP7 annotated with its P1.1 early-fire deviation. **P1 exit-gate evidence recorded honestly: 3 of 4 conditions met, D6/D7 re-score DEFERRED to P2.6 by the instrument's own cadence** — not claimed as met. LESSONS (both generalize): **a rebuilt band does not rebuild the page — grep the claim family, not the component**; and **paired pages need paired reviews** — when two surfaces ship together on one scaffold, a fix to one is a hypothesis about both. Plus: **record the instrument with the score, always** — an unrecorded measurement decays into a claim. **⏭ Next: ⛩ the P1→P2 phase gate (operator, SO-1)** → then P2.1 URL normalization. **Not done, flagged:** HEAD is now **14 commits ahead of `origin/main`** — pushing is an outward act needing its own GO (Git-Ops rule 3). Token: ~65 kT actual vs 80–140 est.
>
> ▶ *(superseded same-day by the banner above)* **2026-08-18 — 🏛 HAUSSMANN P1.2 CODE-COMPLETE + VERIFIED — ⛩ AWAITING DEPLOY GO (nothing shipped).** Single session (`session_stanley_20260818_125835_haussmann_p1_2_state_of_network`), six commits `462ac2e` → `eff6670`. **All five acceptance criteria met.** **`/state-of-the-network/`** ships the §8.3 move — the four strata (what runs · operator-operated · not ours · planned), three file-sourced dates, and the sentence the campaign turns on: *"All of them run on one computer, operated by one person"* (**H5, the trust stratum's only S1, resolved; anti-pattern 7.3 inverted**), plus *"It is not evidence of adoption, and this site will not present it as any"* — a forward commitment, not a confession. **The fold carries the disclosure SENTENCE, not a link to it** (a band that only links is the footnote §7.3 rules out), and that same edit is the **Berthier proof-of-life re-placement** — one move, two criteria. **`/canonical-properties/`** ships the §7.1 clone-site defense (every property + probe date; `adna.dev` named as retired; *"aDNA runs no social accounts"*). **Named humans (H12)**: Stanley Bishop + three affiliations + one outbound link, **O1 consent satisfied in advance by operator ruling** — incl. the ship-and-stage call on the Wilhelm title conflict (stanley.science still says "Lead AI Architect"; memo staged) and the **"UCLA Anderson GSB" → "Anderson Venture Accelerator" correction** (GSB is Stanford's naming, zero vault support). **R-62's structural debt — the one §6 deferred to this mission — DISCHARGED.** Identity coherence: **13 hardcoded `adna.dev` fallbacks → 0**, `og:site_name` reconciled, `sameAs` created, `PUBLISHER_URL` off the GitHub repo, publisher attached to the two builders that emitted none. **Suite 407 → 442, zero xfail; axe 0 × 4 surfaces × both themes; 13/13 properties re-probed logged-out; the rare-archive sole-contributor claim re-verified against the GitHub API; R-58 byte-intact; 4/4 new gates RED-PROVEN.** Register **97 → 111 rows, zero FALSE, zero unsupported**. ⚠ **TWO INDEPENDENT REVIEWS FORCED 11 CLAIM CORRECTIONS** (register §7.5): a hostile cold-read and a ranker **converged on the same two S1s** — the verification checklist verified nothing (all three checks are artifacts a clone copies; check 3 was additionally *false* because the O2a `sameAs` narrowing made it so), and *"Every entry was opened from outside"* was false for 4 of 13. Also: a wrong data path **inside "How to check this page"**, and the largest omission — `/about` names the operator as Head of AI at the Wilhelm Foundation while the other page called the Foundation independent. **Ranker 3.61 vs ≥4.0 — NOT MET** (Credibility 4.50 · Tone 4.50 validated the thesis; Actionability 2.50 was the miss; remediation targets it but was not re-scored — **re-rank owed**). GOTCHA (new): **a Playwright gate cannot import site source that transitively imports JSON** — data moved to `canonical_properties.json` with the `.ts` as a typed wrapper + a build-time drift assertion. LESSON: **the author of a checkability claim is structurally the worst auditor of it.** **⏭ Next: ⛩ operator deploy GO** (`site/scripts/deploy_adna.sh prod`; **fetch + diff `deploy_log.txt` first** — a parallel lane committed `ab841c2` into this tree mid-session) → then P1 closes and **P2 opens**. Token: ~430 kT actual vs 200–300 est.

> ▶ **2026-08-17 — 🚀 HAUSSMANN P1.1 CLOSED: CLAIM PURGE — ZERO FALSE, ZERO XFAIL, DEPLOYED.** Single session (`session_stanley_20260817_185706_haussmann_p1_1_claim_purge`, commit `0f7cca0`). **Hero ships ADR-048 candidate-A VERBATIM** (definition-as-hero + audience sub + NOT-line; lyric demoted INTACT to a movement band; the embargo pair is now the fold's only protocol phrasing — gate-23 re-anchored + guards the FALSE gloss's return). **All 8 FALSE resolved** (R-14/20/23/23b/46/47/61/84/90): quantifiers down · **/community funnel REAL** — issue templates SHIPPED to `aDNA-Network/aDNA` (`fd32fc7..d4742db`, ⛩ GO; questions-link → community.adna.network per operator) · **⚠ Fluxer link-GO fired EARLY by operator OVERRIDE** (P0.4/ADR-054 prerequisites unmet — live probe: `legal.*` null + unbranded; deviation recorded ADR-054 §Status; honest-state + dated + human-only framing shipped, R-95/R-96 register rows guard it) · R-84 truth-copy ("attributed and dated") · **R-90 verified-links projection gate** (`verified_links.json`, empty=healthy; dead Videos URL 0-in-dist; Hestia memo staged w/ 3 data rows). 19 unsupported adjudicated (memo §2). **Hostile cold-read (fresh agent, 204pp, live channel probes): 18 findings ALL dispositioned** — 14 fixed · **SECURITY.md SHIPPED (`b64b81e`) + private vulnerability reporting ENABLED** (the security-disclosure channel was DEAD and is now real — worst find) · 2 → Hestia memo (pt19) · 3 adjudicated-keep. **Suite 407/407 green ZERO xfail** (was 405+9xf; gate-26 fixture 21→14, R-94/R-95 honesty guards added). **DEPLOYED prod** `2026-08-18T03:01:11Z tree=0f7cca0` (4/4 headers script-verified) → **live-verified on adna.network**: new hero + movement band + zero retired phrases + Fluxer disclosure + attributed-and-dated. GOTCHA (new, KW-14 class): **`subnetworks.yaml` is the SOURCE, json is projected** — a json-only edit clobbers at next `sync:vaults`; trace data files to their generator before editing. Still pending: P0.4 Aspasia ack (now MORE urgent — the site links the policy-naked instance) · evidence-retention ⛩ · `VERCEL_TOKEN_ADNA` shell-export. **⏭ Next: P1.2 state-of-network** (last P1 mission; ADR-048 dependency satisfied) — then P2. Token note: ~460 kT actual vs 150–250 est (hostile burn-down unscoped; AAR follow-up 4).

> ▶ **2026-08-16 — 🚀 HAUSSMANN P1 WAVE: P0.1 CLOSED at DP2 (panel waived) · P1.3 ✅ REGISTRY TRUTH · P1.4 ✅ MOBILE INTEGRITY — ALL LIVE.** Operator waived the O4 human panel in-chat → **ADR-048 ACCEPTED at DP2** (deviation recorded in §Status; retro-validation duty → P5.1) → **P1.1 + P1.2 UNBLOCK**. **P1.3 ✅** (`c61a544`+`df0b30b`+`0fa7b3d`) — projection sanitizer v2 (sentence-granular, fed by gate-27's own `leak_patterns.json` — the projection can no longer emit what the gate forbids), persona placeholders → null (F15 "— —" titles fixed), shared public-label util (raw enums out of built HTML incl. attributes), ⛩ **DP4 RULED minimal-card ×3** (aiLP-Dataroom · CakeHealth · PercySleep; suppression at the generator; ADR-052 §admission), **operator-GO'd projection regen** (55 notes sanitized, 28→honest-null, ZERO other fields; counts hold 74/14), graph svg 68-era→74 (59-orphan label consistent sitewide), **LEAK BASELINE RETIRED — 86 rows/563 occ → 0; empty-state promoted to a HARD gate, red-proven**; Hestia backfill memo **DELIVERED** (29 vaults await taglines — the honest cost of sanitization is sparseness). **P1.4 ✅** (`3e53637`) — F1 docs dead-column (base grid lacked its named areas — article sat in an implicit column; gate-9-invisible by construction), F2 /network reflow (minmax + soft-wrap, WCAG 1.4.10), F3 portrait diagram twin (labels ≥13px at 320), F12 copy-button `:global` overlay; **gate-29 reflow guard red-proven** (10 assertions); `visual_capture.mjs` now `reducedMotion`-deterministic (armed-animation captures were shooting blanks — part of the F3 evidence was the instrument). Suite **404→414 green, axe 0 both themes** (9 claim xf remain = P1.1's lane). **DEPLOYED prod** via `deploy_adna.sh`: record `2026-08-18T01:33:57Z mode=prod tree=3e53637` (UTC; operator-frame 08-16 evening), **4/4 headers + leak-free registry + minimal cards + graph counts live-verified on adna.network**. Unplanned find (structural fix): the gate suite silently adopted a **foreign port-4321 dev server** (`reuseExistingServer: true` — 304 bogus failures against stanley.science) → fail-loud + `GATE_PORT` env. Still pending: P0.4 Aspasia ack · `VERCEL_TOKEN_ADNA` unbrokered · ⛩ evidence-retention ruling (72M capture sets on disk, cited subset committed). **⚠ CONCURRENT-DEPLOY COLLISION, CAUGHT + REPAIRED at close:** a parallel session shipped the **installer (ADR-058)** to origin+prod (12 commits, `f38f93e..7c7eedb`, prod 19:54Z tree `e8b4540`) while this wave ran — the 01:33Z deploy above was built pre-installer and **knocked the installer routes to 404**; caught at push-time divergence, repaired by rebase (only conflict: the append-only deploy log, unioned) → suite 414 green on the merged tree → **redeploy `2026-08-18T01:39:25Z tree=62e7388`** → live-verified installer 200×3 + registry/mobile fixes + 4/4 headers. Lesson (the unrecorded-deploy class, sequel): **fetch-and-compare deploy_log BEFORE any prod deploy** — a deploy built behind origin un-ships the other lane's work; candidate guard for `deploy_adna.sh`. **⏭ Next: P1.1 claim purge** (ships the ADR-048 hero verbatim; clears the 9 xf) **or P1.2 state-of-network — both now open.** Session `session_stanley_20260816_204351_haussmann_p1_wave` (history/2026-08).

> ▶ **2026-08-16 — 🚀 HAUSSMANN P0 WAVE: 3/5 missions closed; PRODUCTION HARDENED + DEPLOYED.** All five P0 missions ran (operator-ordered, session `haussmann_p0_wave`): **P0.2 ✅** — header-drift root-caused (prebuilt ignores root `vercel.json`) + fixed via WebForge's `inject_headers.mjs` (byte-identical adoption) inside the new sole-sanctioned `site/scripts/deploy_adna.sh`; **adna.network now serves 4/4 configured security headers; MDN Observatory C/50 → B+/80**; ADR-050 **RATIFIED (c)** at ⛩ DP3; deploys RECORDED (preview `2026-08-17T01:38Z`, **prod `2026-08-17T01:54Z`**, tree `d88b6ff`) — **the deploy freeze is LIFTED**; drift checker red-proven; Vitruvius token-migration ACK delivered (pending-token caveat: `VERCEL_TOKEN_ADNA` unbrokered). **P0.5 ✅** — the editorial gate: suite **371 → 404** (394 ✓ + 10 expected-failures = the 9 FALSE-claim rows [xf-until-P1.1] + the 86-row leak baseline [expires P1.3]; 0 real failures), 4 red-tests proven incl. the unexpected-pass ratchet; **5,748 dev comments stripped per build incl. the deployed artifact**. **P0.3 ✅** — `how/federation/webforge/` wrapper live (pin @ WebForge `6096157a`), craft-floor **Tier-2 graduation ACCEPTED** (coverage declaration → P4.2). **P0.1 🟡 active** — candidates + unanimous 3/3 synthetic pre-screen + ⛩ operator picked **A (definition-as-hero)**; ADR-048 updated (`proposed`); **awaiting the O4 human panel** (kit ready: `artifacts/p0_1/panel_kit.md`) → DP2. **P0.4 🟡 active** — prerequisite register built (PR-1/2/3 × owners); **awaiting Aspasia's ack** (delivery verified 17:11). *(Out-of-wave note: Hestia's imagen-4.0 memo was **already resolved in a parallel session** — all 8 runners ported off the retiring family at `9534691` + lease closed `07423ad`, 2026-08-16 18:00; no action remains here.)*

> ▶ **2026-08-16 — ✅ HAUSSMANN GENESIS COMPLETE + CHARTER RATIFIED (Gate C §7.7, operator).** The full arc ran in one day: Phase A orientation (4 artifacts, `a1b586f`) → Phase B full assessment (12 evidence packets + 2-scorer baseline **51.6/100** vs MCP ≈83; 8 FALSE claims; H1–H15 resolved; `d58ea13`+`df3827c`) → ⛩ Gate B confirmed → Phase C genesis package (charter + 27 missions + ADR-048…057 `proposed` + 5 context graduations + 4 memos; cold-read test FAIL→FIXED; `30a9c3b`) → ⛩ **Gate C RATIFIED + both outward acts GO'd** (origin push; 4-memo delivery). **campaign_haussmann is ACTIVE**; next executable step = mission **P0.1** (or any P0 mission) via `missions/session_prompts_haussmann.md` — the activation gate in the campaign CLAUDE.md is now satisfied. Deploy-freeze request stands until P0.2 lands. Genesis session: `session_stanley_20260816_094350_haussmann_genesis` (history/2026-08/).

> ▶ *(superseded same-day by the banner above)* **2026-08-16 — 🏗 OPERATION HAUSSMANN GENESIS — IN FLIGHT.** Operator issued two directives (verbatim copies: `how/campaigns/campaign_haussmann/directives/`): a cowork directive to **design the campaign that rebuilds adna.network** (this session designs the campaign, never the website — zero `site/` edits) governed by the **VITRUVIUS review instrument** (D1–D12, S1–S4, provenance tags `[D]/[I]/[R]/[A]`, hypotheses H1–H15). Plan of record operator-approved 2026-08-16 (`~/.claude/plans/please-read-the-claude-md-fuzzy-sedgewick.md`); rulings: Gate A folded into plan approval · Phase B full fan-out · Gate C in-chat §7.7 · Flux assessment outside-only. Arc: **Phase A** orientation artifacts (committed) → **Phase B** full situation assessment (site + community.adna.network + cohort MCP/Mastra + craft dossier + claim register + VITRUVIUS baseline) → ⛩ **Gate B** (operator) → **Phase C** genesis package (charter `campaign_haussmann` + ~25 missions + 10 ADR stubs + 5 context artifacts + 4 staged memos) → ⛩ **Gate C** ratification. Key pre-findings: Berthier 08-11 wave memo absorbed-not-relitigated; Fluxer STATE stale-wrong (community live on third-party metal; ADR-025 human-only); WebForge substantive but **no `how/federation/webforge/` wrapper** (straggler); deployed site sends only HSTS vs `vercel.json` headers (deploy drift); llms-full.txt is an index not a corpus; 24/74 mixed-case vault URLs. Session `session_stanley_20260816_094350_haussmann_genesis`. **⏭ Next:** if this session is interrupted, resume from the session file's Activity Log + the plan of record.

> ▶ **2026-07-24 — ✅ REGISTRY REGEN 73→74 (RareAnthropic.aDNA) — the flagged Refit carry-forward, DONE.** Cold-start with **no active campaign** (Palimpsest + Refit both closed 2026-07-24) → operator elected the registry data-currency regen. **Full loop, operator-GO'd:** **(A)** committed Home.aDNA's deliberately-held **RareAnthropic** registration **locally** (`faf5eac`, `vault_count 73→74`; the row landed 2026-07-22 under Operation Portolan with the derived-projection regen deferred to Hestia — finalized here under GO; **NO push**, Home stays local-by-default) → **(B)** `npm run sync:vaults` regen `vaults.json` **73→74 / 14 edges** (sha `59058a4`→`536e9d62`; **org_graph 3→4**; RareAnthropic projected; **idempotent 2×**); subnetworks.json date-churn `git restore`d; `vaults_graph.mmd` +rareanthropic node (orphans 58→59); `.svg` left for a dedicated graph-currency pass (pre-existing 68-era stale) → **(C)** G20 `claim_trace_manifest` fixture **73→74** (edges stay 14) → **(D)** `npx astro build` (203pp) + **`npm run test:gates` 371/371 green** (gate-20 + gate-21 at 74) → **(E)** single-file commit `498f985`. Mirrors **Refit M2** (`75c6d42`). **✅ SHIPPED + DEPLOYED + PUSHED (wind-down 2026-07-24):** deployed live to **adna.network** via `npx astro build` → `VERCEL_TOKEN=$SS_VERCEL_TOKEN vercel --prebuilt --prod` (`dpl_2qAgWuYA9D38F9g8Xyw8EhU7J8eB`, `readyState READY`/`target production`; home renders **74**, HTTP 200 live-verified); aDNA.aDNA `main` **pushed → origin/main** (`498f985` + close-out `4d78a0d` + wind-down; gitleaks pre-push clean). **Home stays local — never pushed** (Rule 4). Session AAR appended. **⏭ Next:** no active campaign; pre-existing follow-ups only (`vaults_graph.svg` 68→74 currency · `install_truth` regen at next deploy · `.adna` doc-name-leak pass). *(Clock note: `generated_at` rolled to UTC `2026-07-25`; human records use operator-frame 2026-07-24.)* Session `session_stanley_20260724_200611_registry_regen_73_74`.

> *(QUEUED banners 2026-07-24 → 2026-07-11 — Palimpsest v8.9 · Refit · Distillery v8.8 · Cleanroom v8.7 · Storyweave P3–P5, all COMPLETED — archive-shifted → [[STATE_archive]] §Shifted-2026-08-03; never deleted.)*

> *(QUEUED banners 2026-07-08 → 2026-07-05 — Storyweave P0–P1.6 · Ouroboros · Meridian · Concord · Fleet-Re-Seed, all closed — archive-shifted → [[STATE_archive]] §Shifted-2026-07-17 [Clear Hearth W-B slice 3]; never deleted. Kept live: the 07-11+ Storyweave P3→P5 + v8.7/v8.8 release arc.)*

> *(Older QUEUED banners archive-shifted → [[STATE_archive]] §Shifted-2026-07-06; never deleted per SO-6.)*

## 🌐 adna.network — LIVE on Cloudflare (2026-05-31)

**The aDNA site is live at https://adna.network** (canonical public face; adna.dev abandoned). **First executed ADR-031 Cloudflare migration** — API-driven, single operator-present session (operator hands-on = 1 token + 1 nameserver change). CF zone `667a2d5e…` (account `e048a126…`; NS `keanu`/`sierra.ns.cloudflare.com`); apex + www → Vercel `adna-docs` (`prj_SBKnZf…`) via **CNAME flattening**, DNS-only; **Mailgun email preserved** (MX×2 / SPF / `smtp._domainkey` DKIM / strict `_dmarc p=reject`). Canonical flipped: `site/astro.config.mjs` `site:` → adna.network + Vercel `SITE_URL` env + **prebuilt** redeploy (`dpl_AxbEXjuF…`; local build required — `prebuild` reaches `../scripts`); live `<link rel=canonical>` + 159-URL sitemap emit adna.network. **CF token onboarded to Home.aDNA broker** (Keychain `CLOUDFLARE_API_TOKEN` + zshrc + inventory C47; 1P backup = operator Touch-ID one-liner pending). **Skill graduated to SiteForge** — `skill_cloudflare_dns_cutover` v1.0.0 (ADR-031 successor-ownership relocated per operator directive; SiteForge owns build+deploy+DNS). **Operator override:** M5.7 O6 domain-cutover executed early — DNS/canonical only; **renamed no content** (honors the aDNALabs "nothing renames before the brief" broadcast). WARN: `SS_VERCEL_TOKEN` leaked again in a Vercel-CLI error (recurring incident) -> rotate. Seeded turnkey for next in-vault session: `worldgeno.me` (wga.aDNA M02, unblocked) + `stanley.science` (ScienceStanley M14). Doctrine: [[adr_031_cloudflare_dns_site_publishing_standard]].

## Current Phase

> *(Current-Phase rows 2026-07-01 → 2026-07-03 — the Champollion G0→G5 ladder + STR Track-C close — archive-shifted → [[STATE_archive]] §Shifted-2026-07-17 [Clear Hearth W-B slice 3]; never deleted.)*

> *(Current-Phase activity older than 2026-07-01 archive-shifted → [[STATE_archive]] §Shifted-2026-07-02 (Champollion M1.5); 48 bullets, never deleted per SO-6. This router keeps the recent live arc (Champollion 2026-07-02 + STR close 2026-07-01); older bullets archive-shifted, trim to the next diet.)*

## Active Campaigns

### `campaign_v8_9_release` (Operation Palimpsest — ✅ **COMPLETED 2026-07-24**; v8.9 SHIPPED [commit c8e5427 + tag v8.9]; P0→P1→P2→P3 all done — **DO NOT re-open**)

> *(Body archive-shifted → [[STATE_archive]] §Shifted-2026-08-03; the ruling above is the live record.)*

### `campaign_refit` (Operation Refit — ✅ **COMPLETED 2026-07-24**; G1 07-21 · G2 07-24 · **G3 07-24** [DP10: accept + push all + deliver 5 memos]; 6/6 missions, 21/21 A–E rows, no normative change — **DO NOT re-open**)

> *(Body archive-shifted → [[STATE_archive]] §Shifted-2026-08-03; the ruling above is the live record.)*

### `campaign_v8_8_release` (Operation Distillery — ✅ **COMPLETED 2026-07-14**; v8.8 SHIPPED, `a32724b` + tag `v8.8`)

> *(Body archive-shifted → [[STATE_archive]] §Shifted-2026-08-03; the ruling above is the live record.)*

> *(Completed-campaign entries — Cleanroom v8.7 · Meridian · operation_adna · feedback_loop · keystone · looking_glass · STR · network_audit · v2_infrastructure · Completed Mini-Campaigns — archive-shifted → [[STATE_archive]] §Shifted-2026-07-17; never deleted. Kept live: ACTIVE Distillery + Pending/seeded below.)*

### Pending Campaigns (seeded but not yet open)

#### `campaign_obsidian_deployment_stabilization` (NEW successor — seeded 2026-05-13 at M-LWX-03 S2 Phase L; **ABSORBED by `campaign_adna_serious_tool_readiness` 2026-05-17**)

`how/campaigns/campaign_obsidian_deployment_stabilization/` — implementation-focused successor to `campaign_lattice_workspace_ux`. Owner: Rosetta. Strategic intent: make the Obsidian deployment of every aDNA vault stable, standard, and self-stabilizing. **ABSORBED into `campaign_adna_serious_tool_readiness` Phase 3** (Forge Ecosystem Hardening) 2026-05-17 — 8 tracks T1-T8 distributed across v8 missions M3.1-M3.4. Stub directory preserved for audit (per Standing Order #6 archive-not-delete); status `planned` stays in stub frontmatter; effective status `absorbed_by: campaign_adna_serious_tool_readiness`. 7 backlog files F-S2-1..8 (in `aDNA.aDNA/how/backlog/`) source the v8 Phase 3 work directly.

#### `campaign_validation_node_adna_lwx_outputs` (NEW successor — seeded 2026-05-13 at M-LWX-03 S2 Phase L; lives in `lattice-labs/`)

`lattice-labs/how/campaigns/campaign_validation_node_adna_lwx_outputs/` — validation-focused successor; dispatched to Carly + Herb. Owner: Berthier (lattice-labs). Strategic intent: validate M-LWX-01/02/03 outputs on operator-owned machines via Carly+Herb dispatch. Phase 1 narrow: M-VNAL-01 covers outstanding O4 (wikilinks), O5 (cross-vault links), O6 (marketplace), O3-extended (full vault tables). Phase 2+ broader: recurring "Carly+Herb validate-all-aDNA-features" pattern — the FIRST instance of an explicit validation-dispatch campaign for aDNA work. Coord memo at `lattice-labs/who/coordination/coord_2026_05_13_carly_herb_node_adna_validation_dispatch.md`. Status: `planned`; opens when Carly + Herb each acknowledge the coord memo.

#### `campaign_adna_v3_ecosystem_compliance` (planned successor — seeded 2026-05-08)

`how/campaigns/campaign_adna_v3_ecosystem_compliance/` — applies v7.0 changes per-vault to the 19 active aDNA ecosystem vaults. Strategic intent: bring the lattice into full v7.0 compliance after the standard codifies it. Preliminary phase structure: P0 planning + P1 audit + P2 bulk skill upgrade + P3 git remote setup + GitHub naming standardization + P4 airlock adoption + workspace router resync + P5 final ecosystem audit + AAR. Preliminary mission outline: M01-EC (per-vault audit) → M02-EC (bulk skill upgrade) → M03-EC (git remote setup) → M04-EC (GitHub repo rename) → M05-EC (airlock adoption) → M06-EC (workspace router resync) → M07-EC (final audit + AAR). Estimated 12–20 sessions (recalibrated by M01-EC). Persona: Rosetta continues. **Opens at v2 P3 phase gate** (post-M03 flatten + M08a/M08b shipped); M11 of v2 finalizes the mission tree before this campaign opens.

> *(Operation Rosetta [absorbed by STR] + its phase table archive-shifted → [[STATE_archive]] §Shifted-2026-07-17; never deleted.)*

## Phase 7 Progress → archived
> *(Operation Rosetta Phase-7 100-cycle III loop + Persona Ranker Summary (done 2026-04-26; ranker 5.00) archive-shifted → [[STATE_archive]] §Shifted-2026-07-02, Champollion M1.5. Historical; live handoff = ⏭ QUEUED above.)*

## What's Working → archived
> *(the Phase-7-era site snapshot (117pp / 47 gates / "5 reviewer personas" — superseded: site 179pp, 16 personas, 281+ gates) archive-shifted → [[STATE_archive]] §Shifted-2026-07-02, Champollion M1.5. Historical; live handoff = ⏭ QUEUED above.)*

## Active Blockers

None.


## Next Steps → archived
> *(the 2026-06-24 keystone-DP2 Next-Steps snapshot (superseded by the ⏭ QUEUED banner above) archive-shifted → [[STATE_archive]] §Shifted-2026-07-02, Champollion M1.5. Historical; live handoff = ⏭ QUEUED above.)*

## Pending Manual Actions

- **ADR-010 Wilhelm co-sign** (carry) — gates the `/commons` un-embargo; first inclusion = the E5-close coordinated deploy (c169).
- **Hestia: vault-card public fields** — ack `coord_2026_06_10_rosetta_to_hestia_vault_card_public_fields.md` (Harness `display_name` split + optional taglines) → Rosetta regen + next deploy.
- **Hero eyeball (2 promoted candidates, 2026-06-10)** — `/get-started` doc-hero (`hero_get_started.png` ← `helix_r1_H2_v2`) + `/vaults/graph` band (`hero_vaults_graph.png` ← `sec_network_r1_N2_v2`); both LIVE; swap-and-redeploy is cheap if either misses.
- **GitHub social preview**: Upload `aDNABanner.png` (repo root; new banner from M3.2 S3 close 2026-05-22) at github.com/**aDNA-Network**/aDNA.aDNA > Settings > Social preview *(repo slug updated 2026-06-10 — org migration; old slug redirects)*. Supersedes prior recommendation to upload `site/public/images/og-default.png`; new banner is the canonical first-contact visual across all 3 surfaces (Astro hero + README + social preview).
- **Vercel Git integration**: Connect repo at vercel.com > adna-docs > Settings > Git for auto-deploy (currently manual via `vercel --prod`)
- **Google Search Console**: Register adna-docs.vercel.app, obtain verification code, add `<meta name="google-site-verification" content="...">` to `SEOHead.astro`
- **Bing Webmaster Tools**: Register adna-docs.vercel.app, obtain verification code, add `<meta name="msvalidate.01" content="...">` to `SEOHead.astro`
- **Delete M05 S2 scratch GitHub repo**: `github.com/ScienceStanley/m05-test` (private; ~10KB; verification scratch from 2026-05-18). Delete via GitHub UI > Settings > Delete, or refresh gh auth scope with `gh auth refresh -h github.com -s delete_repo` then `gh repo delete ScienceStanley/m05-test --yes`. Repo lingers because gh token at S2 only had `repo` scope (not `delete_repo`).
> *(Manual-action rows already ✅ RESOLVED (2026-04 → 2026-06: upstream PR #8 · `.adna/` README flatten-residue + banner · four Vercel production deploys) archive-shifted → [[STATE_archive]] §Shifted-2026-08-03; never deleted.)*


> *(Historical session log — ~20 `## Last Session (…)` blocks (2026-05→06-30) + the accumulated `## Next Session Prompt` stack (superseded 2026-06-27) — archive-shifted → [[STATE_archive]] §Shifted-2026-07-02 (Champollion M1.5). Never deleted (SO-6); the live handoff is the ⏭ QUEUED banner at the top.)*
