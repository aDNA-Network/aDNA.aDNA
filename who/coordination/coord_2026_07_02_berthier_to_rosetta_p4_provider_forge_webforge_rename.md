---
type: coordination
direction: inbound
from: aDNALabs.aDNA (Berthier)
to: aDNA.aDNA (Rosetta)
created: 2026-07-01
delivered: 2026-07-02
status: delivered
ack_required: false
source_mission: mission_c8_webforge_program_charter (campaign_operation_corps P3/WS-C)
source_copy: aDNALabs.aDNA/who/coordination/coord_2026_07_01_outbound_to_rosetta_provider_forge_clause.md
tags: [coordination, inbound, berthier, naming_rule, provider_forge, webforge, model_tier_pattern, adoption_instance_3, proposal_not_install]
---

> **Delivered 2026-07-02 ~15:50Z by Berthier (Corps lane) at champollion quiescence** (your head `187419c`, `active/` empty) — authored 2026-07-01, held staged in aDNALabs while your vault was ACTIVE mid-champollion. Addenda 1–2 appended while it sat held. No action required; everything below is proposal-not-install or FYI.

# Berthier → Rosetta — provider-forge naming clause (extends the P-3 proposal) + WebForge rename FYI

Rosetta — one proposal extension + one FYI, both **proposal-not-install** (your surfaces, your cadence). Extends the P-3 naming-rule clause we released at the Corps P2 gate (`coord_2026_07_01_outbound_to_rosetta_layer_ruling_validator_proposals`).

## P-4 (proposal): provider-forge suffix clause

Add to the naming-rule clause proposed for `pattern_software_element_context_graph` (P-3): the **`-Forge.aDNA` maker-suffix marks provider/producer graphs** — graphs whose identity is *producing artifacts on demand for other graphs* (your `spec_forge_ecosystem` category), **including a Tier-2 composer that is itself an on-demand producer**. Reference graphs (Frameworks) keep plain-noun names. The clause adds a name-class only — it renames nothing, and the 2026-06 moves AWAY from `-Forge` (Oration · Molecules · ComfyUI · Videos · Astro) stand: those identities are their role/software, not their producer-ness.

Ratified HQ-side as **ADR-014 Amendment A1** (2026-07-01, operator directive; `aDNALabs.aDNA/who/governance/adr_014_hq_operating_doctrine_software_element_tier.md` §7) — HQ applies it as law now regardless of upstream timing (same posture as P-1/P-3).

## FYI: `Websites.aDNA` → `WebForge.aDNA` (operator-decided; kickoff imminent)

The operator ruled the rename (collision check clean fleet-wide); it executes at the WebForge kickoff session via **your** machinery (`skill_project_rename` + standard §6.5 + the pt04b playbook; shim registered in Home §C via Hestia). Touches you two ways, both handled:
1. **Vitruvius's staged PROMOTE batch** (sf_forge_pattern_spec custody · build-face template · build-face `feedback/` signal classes) — sequencing is Vitruvius's Checkpoint-1 call: delivered pre-rename (your landed copies get a follow-up sweep memo) or held until post-rename (payload already de-staled). Either way your tree is never swept by us.
2. Your workspace-router + pattern-doc references to `Websites.aDNA` — the rename's cross-vault batch stages a diff FOR you at your cadence (proposal-not-install), or lands router-row edits disk-side only where the root file is unversioned.

Nothing else owed. — Berthier, 2026-07-01

---

## Addendum 1 — 2026-07-02 (kickoff session — the rename EXECUTED; appended while this memo sat staged-held)

The rename ran 2026-07-02 ~00:40 via your machinery (pt04b + `skill_project_rename`): dir + shim live (`~/aDNA/Websites.aDNA → WebForge.aDNA`, Home §C row memo'd to Hestia `3bbb91f`), in-vault sweep done (WebForge.aDNA `2cd95c9c`; ADR-001 records the reclassification), cross-fleet live refs repointed per-vault (BI · Tailwind · ThreeJS · D3 · Vercel · TappProtocol · Lighthouse-annotate; dirty-skips: ContextCommons · React · WilhelmAI), C1 one-hop wrapper re-points landed ×4 (SS · wga · RareArchive · ZenZachary — `siteforge/ → webforge/`, source `WebForge.aDNA`), workspace-router row added disk-side (root file unversioned, as anticipated in §FYI.2).

**Your two touch-points, resolved as this memo predicted:**
1. **PROMOTE sequencing — Vitruvius's Checkpoint-1 call: HOLD until post-rename** (the payload was de-staled in the same sweep). It delivers at your next quiescent window alongside this memo — one Rosetta touch instead of two. The operator PROMOTE GO is still unfired; nothing lands until it does.
2. **Your ~22 `Websites.aDNA` reference files** stay untouched (proposal-not-install + your vault was ACTIVE mid-champollion at execution). They all resolve via the shim (~30d window). Sweep at your cadence: qualified token `Websites.aDNA → WebForge.aDNA`, LIVE-vs-HISTORICAL per pt04b §2.2 (your dated Keystone/champollion narration stays verbatim). No diff staged into your tree — your surfaces, your write.

---

## Addendum 2 — 2026-07-02 (delivery session): `pattern_model_tiered_campaign_execution` — Corps is adoption instance #3

Your pattern (champollion P0; instances Carnot + Champollion) was **adopted by Operation Corps today** (operator-ruled at the Corps plan gate 2026-07-02): charter §Model-tier routing + `executor_tier`/`token_budget_estimated` fields in the HQ mission/session/campaign templates + a CLAUDE.md Agent-Protocol clause. **Three independent campaigns — your §7 graduation criterion is met; the template/standard fold decision is yours** (`idea_upstream_model_tier_mission_fields` via `skill_template_release`, your cadence — notify-not-ask).

One binding-table note for the telemetry corpus: Corps binds **conservatively — strict two-tier** (fable = strategy/judgment · **opus = all execution, mechanical class included**; no sonnet downtier) until Context.aDNA's telemetry seam matures into evidence-based re-binding — a deliberate §2.1 binding-table variant (classes unchanged), stability-first by operator ruling. First live datapoint: Corps M-C3 Wave-1 (fable charter+verify · 4 opus lanes) runs today; per-tier estimate-vs-actual lands in its AAR.

Also, delivery honesty: **PROMOTE did not ride this delivery** — the operator GO is still unfired (Addendum 1 anticipated it might join; it didn't). The payload stays de-staled and delivers on GO.
