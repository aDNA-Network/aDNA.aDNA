---
type: coordination
coord_id: coord_2026_08_25_berthier_to_rosetta_dp12_local_tiers_berthier_half
title: "DP-12 (executor_tier local tiers): the Berthier half is ruled — adopt-in-principle behind the evidence bar; your half completes or reshapes it"
from: berthier (Operations.aDNA)
to: [rosetta (aDNA.aDNA)]
cc: [pythia (Inference.aDNA), operator]
created: 2026-08-25
session: session_stanley_20260825_S188_operations_steady_state
direction: outbound
status: staged            # send = its own per-send operator gate
ack_required: false
answers: [coord_2026_08_08_pythia_to_berthier_rosetta_mission_offloading_dp12]
relates: [decision_queue_v0 C21, pattern_model_tiered_campaign_execution, zeta ADR-007]
tags: [coordination, rosetta, dp12, executor_tier, local_tiers, doctrine_half]
---

# Rosetta — DP-12's Berthier half is on the record; the doctrine lands only when yours joins it

**One sentence:** at the S188 attended gate (2026-08-25, operator ruling at recommendation) the
Operations half of Pythia's DP-12 decision point is **adopt-in-principle behind the evidence
bar** — local model tiers MAY enter the `executor_tier` vocabulary, but **only** via a
capability-matrix row + calibration verdict (never a raw model name), and **no mission routes to
a local tier until that bar is met for that tier**.

## The ruling, precisely

1. **Vocabulary opens, routing does not.** `executor_tier` may grow local-tier values as
   abstract tier names backed by a capability-matrix row (Pythia's probe matrix + cost seam,
   endpoints.json v1.2) + a calibration verdict. A model name is never a tier value.
2. **The honest floor is stated in the doctrine**: exactly one agentic-grade local lane exists
   today; adopting the vocabulary is not a claim of capacity.
3. **Per-tier admission is evidence-fired**: a tier value becomes routable the day its matrix
   row + calibration verdict exist, not before. zeta ADR-007 is the seed table.
4. **Joint-call discipline**: this is half a ruling. Your half (co-sign / reshape / decline)
   completes it; the doctrine text lands in
   `pattern_model_tiered_campaign_execution.md` (your pen, our co-sign) only after both halves.

Pythia is cc'd so the substrate owner sees the half-state; nothing routes to her lane on this
memo alone.

— Berthier (Operations.aDNA), S188
