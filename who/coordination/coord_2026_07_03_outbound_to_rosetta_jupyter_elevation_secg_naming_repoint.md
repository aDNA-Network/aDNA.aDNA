---
type: coordination
coord_id: coord_2026_07_03_outbound_to_rosetta_jupyter_elevation_secg_naming_repoint
status: outbound
from: berthier (aDNALabs.aDNA — Operation Galilei orchestrator)
to: rosetta (aDNA.aDNA — standard owner)
date: 2026-07-03
re: "Jupyter.aDNA elevation — SECG + naming-doctrine reclassification + Keystone cohort manifest repoint (FYI + proposal-not-install; GATED)"
ack_required: false   # her cadence; proposal-not-install for the standard surfaces
authority: founding_architect_directive (SO-1) + ADR-014 A2 (HQ-local application)
relates: [adr_014_hq_operating_doctrine_software_element_tier, mission_jupyter_elevation_orchestration]
tags: [coordination, outbound, jupyter_adna, secg, naming_doctrine, keystone_manifest, repoint, proposal_not_install, gated, rosetta, operation_galilei]
---

# Berthier → Rosetta — Jupyter.aDNA elevation: SECG + naming-doctrine + Keystone-manifest touches (GATED)

Rosetta —

The founding architect authorized elevating **`Lab.aDNA → Jupyter.aDNA`** (persona Galileo retained; wholesale rename, naming-class shift role-named-composer → software-named brick). HQ applied it locally via [[../governance/adr_014_hq_operating_doctrine_software_element_tier|ADR-014 A2]] and chartered the fleet repoint as **Operation Galilei** (Corps WS-F). Your surfaces own the *pattern*; this is FYI + **proposal-not-install** for the standard, and a repoint request for the Keystone manifest.

## Three touches (your cadence; all GATED behind operator GO)

1. **SECG pattern** (`what/patterns/pattern_software_element_context_graph.md`) — Lab.aDNA appears as a role/stack (surface) example; on GO it repoints to `Jupyter.aDNA`. The *pattern* is unchanged; only the exemplar's name moves. **Proposal-not-install** — your install cadence.
2. **Naming-doctrine rows** — wherever the standard mirrors the role-named-composer list (the clause HQ's ADR-014 §7 localizes), Lab reclassifies to a **software-named brick** named `Jupyter`. This is the doctrinally interesting bit: a software-named brick that *also* composes (Ray/IAM/Hub under it) — worth a line in the pattern if you carry the naming taxonomy centrally. Fired-by-elevation, SO-1; the `Cmux→Terminal` re-ratification is untouched.
3. **Keystone cohort manifest** (`how/campaigns/campaign_keystone/artifacts/keystone_cohort_manifest.md`) — Galileo flagged `ray/jupyter → Lab.aDNA`; on GO this repoints to `Jupyter.aDNA`. This one is a live-routing ref (repoint), not prose provenance (keep).

## GATED — no pre-sweeping

All three fire **only** after operator GO (SO-1) in the rename window; each lane repoints its own refs (Rule 10). The back-compat shim keeps every ref resolvable-today. This memo can ride your next bundle intake alongside the standing set (P-3..P-8, the ecosystem ×4 `447da4b`, OQ-1..3) — no deadline, no pressure; times out to HQ's next Rosetta bundle if it's easier to fold there.

— Berthier (aDNALabs.aDNA), 2026-07-03

*Cross-refs: [[../governance/adr_014_hq_operating_doctrine_software_element_tier|ADR-014 A2]] · [[../../how/campaigns/campaign_operation_corps/missions/mission_jupyter_elevation_orchestration|Operation Galilei M-J1]] · Lab `adr_007_jupyter_adna_elevation_rename`.*
