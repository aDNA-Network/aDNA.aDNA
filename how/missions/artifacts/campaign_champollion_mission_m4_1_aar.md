---
type: aar
artifact_id: campaign_champollion_mission_m4_1_aar
title: "AAR — Champollion M4.1 (newcomer stress-test: the real first hour vs the v8.3 public image)"
campaign_id: campaign_champollion
mission_id: mission_champollion_m4_1_newcomer_stress_test
executor_tier: opus
token_budget_estimated: "45 kT"
token_budget_actual: "~46 kT (opus builder lane ~33 self-reported + fable bookends ~13) — +2%, inside band w/ the G3 D4 Mode-B bookend allowance"
created: 2026-07-02
updated: 2026-07-02
last_edited_by: agent_rosetta
status: active
tags: [aar, champollion, p4, m4_1, newcomer, stress_test, first_contact, mode_b]
---

# AAR — Mission M4.1: Newcomer stress-test

**Session**: `session_stanley_20260702T183653Z_champollion_p4_sweep` · **Executor: opus subagent** at-tier, **fable-orchestrated** (Mode B) · verification-class (with real execution surface) · Ring 1.

## Five lines

- **Worked**: the live-walk discipline — a real scratch clone (`e4372a6`, 1.45s/35M/404 files) + six honest legs produced 10 evidence-anchored rows; the Mode-B split held cleanly (opus walked + narrated; fable re-verified every load-bearing claim against the clone and finalized routes; one builder count corrected 2→3 at census — the P3 records≠adoptions lesson recurring on schedule).
- **Didn't**: the FIRST opus dispatch died at spawn (account session limit, 0 tokens/0 tools, 380ms) — retry succeeded; zero work lost, but spawn-death is a new Mode-B failure mode the runner design (R1) must expect; also the builder narrated "two beginner tutorials" where census says three.
- **Finding** (marquee): **F-CHM-207 is worse than filed** — the old workshop URL 301-redirects to frozen `adna-legacy` @ `74cb761` instead of 404ing, so the taught flow *silently succeeds* with a stale pre-v8 vault; plus the image ships **no learning on-ramp at all** (F-CHM-210): mechanics delight (0 blockers), learning path loses the newcomer (3 majors).
- **Change**: fix-list fully routed with no silent drops — (b)=4 rows annotated as riders into the M4.2/M4.3/M4.4 briefs; (c)=5+1 rows consolidated into ONE fold stub `idea_image_newcomer_currency_fixes` (`fold_batch: champollion_m6_1_rc`, F-CHM-211); (d)=1 declined with reason (N-09 legacy-root prompt is intentional migration UX); **(a)=NONE** — every fix had a better-owned venue (N-10 fable-ruled to M4.3, which runs next-but-one in this same sweep).
- **Follow-up**: M4.3 re-walk must kill the silent-success failure mode end-to-end (not just swap the URL); M4.2 persona pass walks the site AS the image-holding newcomer; RC re-censuses all fold-stub counts (narrated-count class now confirmed shipping publicly).

## Estimate vs actual

45 est → ~46 actual (+2%). The G2 class-split calibration **HELD again** (second consecutive phase): the brief's note that "the walk itself is the work, not a re-derivable judgment" priced the execution surface correctly; the builder came in −7 kT under lane and the bookends consumed the difference (re-verification + routing + rider authoring across 4 files). No retrospective trigger.

## Evidence chain

[[../../campaigns/campaign_champollion/artifacts/newcomer_stress_test|newcomer_stress_test]] (artifact: evidence block + as-lived hour + friction log + finalized routing) · [[../../campaigns/campaign_champollion/artifacts/findings_ledger|findings_ledger]] F-CHM-207 (annotated) / F-CHM-210 / F-CHM-211 · [[../../backlog/idea_image_newcomer_currency_fixes|fold stub]] · rider blocks in the three P4 briefs.
