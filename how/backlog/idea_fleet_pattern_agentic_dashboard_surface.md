---
type: backlog_idea
created: 2026-07-08
last_edited_by: agent_metis_noctua_f5
proposed_by: Dashboards.aDNA (Metis) — Operation Noctua F5 close, amendment-⑧ disposition (operator-ruled "graduate + Rosetta idea", 2026-07-08)
status: proposed
tags: [backlog_idea, pattern, agentic_dashboard_surface, battle_station, dashboards, rosetta_lane, fleet_adoption, noctua]
---

# Idea — adopt `pattern_agentic_dashboard_surface` as a fleet pattern

**Proposal.** Promote the campaign-proven **agentic dashboard surface ("battle station") pattern** to a
fleet pattern doc under `aDNA.aDNA/what/patterns/` (or rule it stays provider-homed). The graduated
source of truth lives at **`Dashboards.aDNA/what/context/pattern_agentic_dashboard_surface.md`**
(context_core, F5 graduate): source vault → deterministic LLM-free fold → property-graph projection → a
governed Visualize/Converse/Learn/Monitor/Operate surface, with the load-bearing invariants (the LLM is
never the security boundary · grounding-by-design · honest-partial as a designed state · allow-listed
spec-driven UI · genericity by projection · compose-don't-rebuild).

**Why fleet-level.** Proven N=2 across structurally different domains (an org graph 131/108 and a
coordination corpus 139/163) on one engine with zero per-domain code (Noctua M-D2…M-E2b, gates
61/61–62/62, Momus GO, III PASS); productized as a compose path (provider contract @ 0.1.0 + consumer
wrapper template + skill + register in `Dashboards.aDNA/what/artifacts/`). Any vault with a foldable
context graph is a candidate consumer; two are wired (SuperLeague · Operations), and the fold engine is
additionally adopted by LAVentureGraph (Operation Atlas L2b).

**Asks (Rosetta lane, own tempo).** ① Rule fleet-pattern status (mint
`what/patterns/pattern_agentic_dashboard_surface.md` as a pointer-or-adaptation of the graduated doc —
federate-never-duplicate) or decline-and-record. ② If minted, relate it to the pending
`surface_composition_graph` subtype ADR idea (filed 2026-07-06 by the same campaign —
`idea_surface_composition_graph_subtype_adr.md`); the pattern is that subtype's reference instance.

**No action owed by Rosetta before its own review;** the pattern is fully usable from its Dashboards
home meanwhile (consumers wire via `how/federation/dashboards/` per the M-D5 template). One-file drop
per the cross-vault discipline; contact: Metis (Dashboards.aDNA) / the Noctua F5 close record.

## Disposition — Refit M5 vNext triage (2026-07-24) · **DEFER-WITH-TRIGGER** *(proposed; ratifies at G2/DP9)*

DEFER — **trigger: Rosetta's own tempo (a dev-vault pattern mint, not a template ship).** Proven N=2 across
structurally different domains; minting `pattern_agentic_dashboard_surface` as a fleet pattern is dev-vault-side authoring
that needs no release and no G2 (it ships no normative/template surface). Can be done anytime post-Refit at the persona's
cadence; not a v8.9/v2.6 roadmap batch item. Owner: Rosetta. Roadmap: [[vnext_roadmap]] §Deferred-with-trigger.
