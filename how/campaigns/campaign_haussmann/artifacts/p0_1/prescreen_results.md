---
type: artifact
artifact_type: prescreen_results
campaign_id: campaign_haussmann
mission: mission_haussmann_p0_1_positioning
title: "P0.1 O2 — SYNTHETIC pre-screen results (3 blind readers × 4 candidates)"
created: 2026-08-16
updated: 2026-08-16
status: active
last_edited_by: agent_rosetta
provenance: "[D-syn] — disclosed synthetic agent readers, blind (randomized order, no rationale shown); a stand-in for the O4 human panel, never a substitute"
tags: [haussmann, p0_1, prescreen, synthetic]
---

# Pre-screen results — SYNTHETIC (3 blind agent readers)

Candidates presented as anonymous mocks in per-reader randomized order. De-mapped:

| Reader (persona) | Rank (best→worst) | Ship pick | Decisive quote |
|---|---|---|---|
| Senior engineer | **A** > C > D > B | **A** | "the only one that answers all three first-screen questions… its lone flaw ('vaults') is a one-word polish" |
| Clinician-researcher | **A** > C > B > D | **A** | "'(agentic DNA)' in the very first clause kills the genomics reading before it can form" |
| OSS contributor | C > **A** > D > B | **A** | "the only text that answers all four skeptic questions before I scroll… I'd steal [C]'s annotated tree for the block directly beneath it" |

**Verdict: A unanimous on ship** (3/3), C the consistent runner-up (its tree out-communicates — fold it under A's lead; the R2 install block already sits there). **B is hazardous copy**: two readers independently misread "the network where that shared context lives" as a *hosted* destination fighting "owned by you" — this phrasing class is now on the P4.5 avoid-list. **D confirmed the doctrine's own warning**: the split layout forced restarts ("whiplash… which half is the point").

## Revisions folded into A's final draft (per the flags)

1. "vaults" glossed at first use → "…the registry of **workspaces — 'vaults' —** that run it" (all 3 flagged it).
2. Opener acknowledged as dense ("40-word freight train") — kept two-sentence, colon-broken; the human panel adjudicates whether it needs a further split.
3. The annotated `what/ how/ who/` tree stays directly beneath the lead (already on-page; C's strength retained).

## A — final draft (the O3 stimulus, pending operator pick)

> **Lead**: "aDNA (agentic DNA) is an open standard for organizing a project's files so AI agents — and the people working with them — always know where things live: three folders, plain Markdown, versioned in git. This site is the standard, its docs, and the registry of workspaces — 'vaults' — that run it."
> **Audience sub**: "For teams working with agentic coding tools on real projects."
> **NOT-line**: "Not a product or service — no server, no signup, nothing leaves your machine."
> **Lyric**: demoted *intact* to the movement band immediately below the fold.
> **Protocol line (embargo-safe)**: "federation runs on the Lattice Protocol — opening progressively, spec first" (post-lift variant staged in ADR-048).

Limitations: n=3, synthetic, text-only mocks (no rendered composition). The O4 human panel (≥5, ≥80% bar) remains the gate; these results only inform the operator's O3 direction pick.
