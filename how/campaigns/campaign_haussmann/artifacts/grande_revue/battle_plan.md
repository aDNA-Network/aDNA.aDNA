---
type: artifact
artifact_id: grande_revue_battle_plan
campaign: campaign_haussmann
operation: operation_grande_revue
title: "GRANDE REVUE — the battle plan: Phase 2/3 decomposition proposed at Gate 1"
created: 2026-08-28
updated: 2026-08-28
status: proposed          # ⛩ Gate 1 signs (or amends) this before anything in it executes
last_edited_by: agent_rosetta
session: session_stanley_20260828_135704_haussmann_grande_revue_p1
grounded_in: "[[mid_campaign_review]] (all finding IDs below refer to its §2)"
tags: [artifact, grande_revue, battle_plan, gate_1, phase_2]
---

# GRANDE REVUE — battle plan (proposed at ⛩ Gate 1)

> **Nothing below executes on this document's authority.** Gate 1 ratifies the shape; each new
> mission then gets its own convention-13 pre-build pass BEFORE its budget is ratified (the
> campaign's standing law, seven missions running). Lanes are split on **reachability** — the
> P4.4a/b line — so nothing waits on an actor it does not depend on.

## Lane A — GR-1: trust-path repairs (new mission, agent-reachable, `site/` build)

One mission, one theme: **the surfaces that let a reader verify are the surfaces that break.**

| Obj | Fixes | Findings |
|---|---|---|
| A1 | CSP/font: prefer `assetsInlineLimit: 0` for fonts (all fonts ship as files under `font-src 'self'` — CSP-preserving) over adding `data:`; red-test = a live-shaped probe (headers applied) goes red pre-fix, green post | P1-1 |
| A2 | `llms.txt.ts:71` — strike "federating on the Lattice Protocol" for embargo-safe phrasing; sweep ALL non-HTML surfaces (llms.txt, llms-full, twins, RSS) for R-14-class residue — **the purge lesson: page-by-page is not surface-by-surface** | P1-2, P2-3 (the skill-mirror residuals promise needs an upstream call — the mirror is verbatim-vendored; fixing display copy vs asking `.adna` upstream is a gate question) |
| A3 | Twin emitter: escape/preserve `<name>`-style placeholders; fix homepage-twin graph/How-it-Works garble; re-run twin-integrity checks | P1-4 |
| A4 | /get-started: re-pin the provenance links to a public commit whose bytes match (byte-identity already measured true) or push the release-sync commit (⛩ outward act, template-release lane); scope-clause the "nothing is sent" sentence; "except the last two" fix | P1-3, P2-1, P3 |
| A5 | Micro-fixes: /reference v3/v2 label; "how does my vault get listed" sentence (⚠ not on `/` — FKGL margin 0.04) | P3s |

Estimated band (to be re-derived at the mission's own gate): ~120–200 kT / 1–2 sessions.

## Lane B — campaign-graph corrections (this-vault, same-diff class, cheap)

- B1 Strike-and-correct P4.4's stale `status:` qualifier (cite the signature + B0). — P1-7
- B2 Strike F-n's register row; re-derive the live count (7 → 6). Same commit as B1 (one file). — P1-8
- B3 R-124: register row with a named destination, or ⛩ operator routing at Gate 1. — P2-5
- B4 Commit the 4 untracked inbound memos (audit trail); draft replies to the 2 `ack_required`
  memos — **each shown before sending** (outward acts). — P2-6
- B5 `adr_index.md` catch-up: index adr_047–059 (13 entries, derived from disk). — P1-6

Estimated: ~30–60 kT, can ride this session's close or the next sitting.

## Lane C — data asks (staged memos; Hestia-owned, pt19 — never local edits)

- C1 `worldgenome` row correction exactly per the delivered memo's field table. — P1-5
- C2 `wga` row refresh + advisory `terminal`/`jupyter` status in the same sync pass. — P2-4
- Both staged as one memo to Hestia; delivery is an ⛩ outward act.

## Lane D — story coverage (content; Gate-decided scope, candor-framed)

- D1 **Model routing** — the only "runs now" story (this vault's own live doctrine; self-reference
  win). Home: patterns/mission-decomposition or a concepts entry. — D9(d)
- D2 Token-budget doctrine layer (SO-11/ADR-016) on design-a-mission / mission-decomposition. — D9(a)
- D3 Local models — *planned*-framed section on `/network` (owns L0–L3). — D9(c)
- D4 Ancient-DNA one-liner near first contact — ⚠ FKGL margin means placement is a design
  decision (hero micro-copy or /about), not a paragraph. — P2-2
- D5 "Latest" strip for returning members (nav capped at 7 by ADR-049 ⇒ homepage strip, not a nav
  item). — P2-7
- D6 Movement-3: **hold under embargo** (correct compliance state); revisit at D-8. — D9(e)

## Instrument repairs (routed, not built at this gate — conventions 15/16)

- gate-42's structural blind spot (no CSP in local preview): note on the gate's face now; a
  headers-applied local probe is A1's red-test, which retires the blind spot for this defect
  class without authoring a standing instrument at a review's tail.
- Register quote re-pins R-101/R-104 + R-104 fixture coverage. — P3
- P5.2 pre-req reminder: VITRUVIUS v1.1 anchor fix unchanged by all of this.

## Sequencing put to the commander (the Gate 1 question)

P4.4b **B1 → B2a** hold a ratified budget and are the campaign's standing next agent increments;
Grande Revue lanes A–D are new scope. Recommended order: **B (this sitting or next, trivial) →
P4.4b B1+B2a (ratified, waiting) → GR-1 Lane A (after its own gate) → Lane D (content, after
Lane A so the trust path is sound before new copy lands on it)**. Lane C rides whenever the memo
is GO'd. P5.1 stays with the humans throughout; nothing here touches it.
