---
type: evidence_packet
created: 2026-08-16
updated: 2026-08-16
status: active
last_edited_by: agent_rosetta
tags: [evidence, machine_eye, campaign_haussmann, D10, agent-readability, B4]
packet: B4
dimension: D10 (Machine legibility and agent-readiness)
target: https://adna.network
method: "Direct verification — curl (headers, bodies, ETags), saved raw artifacts, and small Python scripts for text-extraction/JSON-LD parsing. No browser rendering used; this is a deliberate agent-eye/no-JS test of what a non-visual client sees. One SYNTHETIC sub-pass (item 15) uses a local LLM (disclosed) as an independent second reader — clearly labeled, not treated as ground truth."
instrument: directives/OPERATION_VITRUVIUS_review_instrument.md v1.0, §D10
fetched: 2026-08-16
provenance_key: "[D] = read directly from a live fetch/response during this session, 2026-08-16. [SYNTHETIC] = output of a disclosed local LLM reading extracted page text, not a real human and not the auditing model."
raw_evidence_dir: how/campaigns/campaign_haussmann/evidence/machine_eye/raw/
---

# Evidence Packet B4 — Machine-Eye Pass (D10 Agent-Readability Audit)

**Target:** `https://adna.network` · **Fetched:** 2026-08-16 · **Method:** curl/script-first, no browser. Raw responses saved under `raw/` in this directory (26 files: `llms.txt`, `llms-full.txt`, `sitemap-index.xml`, `sitemap-0.xml`, `robots.txt`, `rss.xml`, `md_twins.txt`, `content_negotiation.txt`, `uri_casing.txt`, per-page `.html` + extracted `_text.txt`, `graph_svg_full.txt`).

## Verdict table

| # | Item | Verdict | Evidence | Prov |
|---|---|---|---|---|
| 1 | `/llms.txt` | **PASS — present, curated, current** | HTTP 200, 1,464 B, `text/plain`. 9 "Key pages" links each carry a hand-written description (e.g. "the five-minute conceptual tour") — not an auto-dump. Claims "74 vaults" — cross-checked against sitemap (74 `/vaults/*/` URLs), the `/vaults` registry page (74 `vault-card-meta` elements), and `llms-full.txt`'s own taxonomy table (sums to 74). All four counts agree. | `[D]` |
| 2 | `/llms-full.txt` | **CONFIRMED: index, not full-corpus — and the name overclaims** | HTTP 200, 2,018 B (~2 KB). Content = route list (14 links) + vault-taxonomy table + edge-type legend. Zero page prose is inlined. Well under the ~100 KB+ a real full-corpus artifact would run; a "-full" name promises deep-ingestion content it doesn't deliver. | `[D]` |
| 3 | `.md` twins | **ABSENT — 10/10 404** | Tested `/learn/what-is-adna.md`, `/reference/specification.md`, `/get-started.md`, `/vaults.md`, `/network.md`, `/glossary.md`, plus trailing-slash-before-`.md` variants of 4 of those. All 10 → `HTTP 404`, `text/html` (the site's HTML 404 page, not a JSON/plain 404 — confirms no markdown-twin route exists at all, vs. e.g. MCP's convention where every doc path resolves as `.md`). | `[D]` |
| 4 | Content negotiation | **ABSENT** | `curl -H "Accept: text/markdown"` on `/learn/what-is-adna`, `/reference/specification`, `/get-started` → identical `HTTP 200`, identical `content-type: text/html`, **byte-identical body** (sizes: 60,481 / 169,581 / 43,787 — unchanged). Confirmed at header level: `etag` on `/get-started` is the literal same value (`"e0728af0...`) for both the default and the `Accept: text/markdown` request — Vercel is serving one cached static object regardless of `Accept`. No `Vary` header, no `Link: rel=alternate` header either. | `[D]` |
| 5 | `sitemap-index.xml` + `sitemap-0.xml` | **PASS — complete and current** | `sitemap-index.xml` (200, `application/xml`, 183 B) correctly points to one child, `sitemap-0.xml` (200, 13,635 B, 202 `<loc>` entries). 74 of the 202 are `/vaults/*/` detail pages — exact match with the registry. Spot-checked 5 non-vault URLs (`/glossary/glossary-triad/`, `/vaults/III.aDNA/`, `/how/workshops/lattice-design/`, `/learn/tutorials/first-claude-md/`, `/reference/quality-rubric/`) → all 5 resolve `200`. | `[D]` |
| 6 | `robots.txt` | **Default permissive posture, not a deliberate AI-crawler policy** | Verbatim (72 B): `User-agent: *` / `Allow: /` / (blank) / `Sitemap: https://adna.network/sitemap-index.xml`. No named user-agent blocks or allows for `GPTBot`, `CCBot`, `ClaudeBot`, `anthropic-ai`, `Google-Extended`, `PerplexityBot`, etc., and no `Content-Signal` directive (contrast: the cohort's D10 reference site states `Content-Signal: ai-train=yes, search=yes, ai-input=yes` explicitly). Effectively AI-friendly by omission, but there is no evidence of an authored stance — it's the Astro/Vercel default. | `[D]` |
| 7 | `/rss.xml` | **Valid feed, but stale content** | HTTP 200, `application/xml`, well-formed RSS 2.0, 1 `<item>`: "v0.1.0 — Site scaffold," `pubDate: Tue, 14 Apr 2026`. The `/changelog` page itself corroborates exactly one entry too (dated Apr 13 — one day off from the RSS `pubDate`, minor). Today is 2026-08-16 → ~124 days / ~4 months with zero new entries, despite the site having visibly grown enormously since (74-vault registry, full content collections, the graph page) — a maintenance gap in the changelog pipeline, not a feed bug. | `[D]` |
| 8 | Registry as JSON/API | **ABSENT — 4/4 404** | `/vaults.json`, `/api/vaults`, `/vaults/index.json`, `/data/vaults.json` → all `HTTP 404`, `text/html`. Matches the known build-side fact that `site/src/data/vaults.json` exists only at build time and is never exposed as a served route — an agent's only path to the vault list is scraping `/vaults/` HTML or parsing the 74 `/vaults/*/` slugs out of `sitemap-0.xml` (slugs only, no metadata). | `[D]` |
| 9 | JSON-LD | **Present but shallow — one block per page, no `Dataset`, no `sameAs`** | Exactly 1 `<script type="application/ld+json">` per page checked. Home → `WebSite` with `publisher: Organization{name, url}` — **no `sameAs`**. `/learn/what-is-adna` → `TechArticle` (confirmed: docs do get `TechArticle`). `/vaults` → `CollectionPage` with `isPartOf: WebSite`. `/vaults/III.aDNA` (a vault-entity page) → generic `WebPage`, not `Dataset` or `SoftwareSourceCode` despite vaults being structured, typed, governed entities. No `Dataset` type found anywhere on the 6 pages checked. | `[D]` |
| 10 | Stable URIs | **Mixed — trailing slash is forgiving; casing is a hard failure with zero recovery** | Casing: `/vaults/III.aDNA` → 200; `/vaults/iii.adna` (lowercased) → hard `404`, no redirect, no `Location` header. Same pattern reversed: `/vaults/terminal` → 200; `/vaults/Terminal` → hard `404`. Trailing slash: `/vaults/III.aDNA` and `/vaults/III.aDNA/` **both** → 200 with byte-identical bodies (16,433 B each), no redirect either direction (so both forms permanently work, but neither canonicalizes to the other). Independently counted the sitemap's 74 vault slugs: **24 mixed-case** (e.g. `aDNA.aDNA`, `Astro.aDNA`, `CakeHealth.aDNA`, `III.aDNA`) vs **50 all-lowercase** (e.g. `terminal`, `apscheduler`) — no visible rule for which vaults get which casing, so an agent cannot reliably guess a slug it hasn't already seen verbatim. | `[D]` |
| 11 | MCP server | **ABSENT — no server, no endpoint, only one incidental mention** | Zero hits for "mcp" (case-insensitive) in `llms.txt`, `llms-full.txt`, `/reference/specification`, `/reference/tool-setup`, `/community`. The only sitewide hit across 8 fetched pages is on `/vaults`, describing the **Warp.aDNA** vault's own tooling ("config-overlay + MCP (not a fork...)") — a fact about one listed vault, not a capability adna.network exposes over its own docs/registry. | `[D]` |
| 12 | Copy-as-context affordance | **ABSENT (page content); PRESENT but different (install CTA); llms.txt itself is undiscoverable from the HTML** | The only "copy" control on `/get-started` and `/learn/what-is-adna` is `aria-label="Copy install command"` (`.cta-copy` inside a `.try-claude-cta`/`.cta-install` block) — copies `npm install -g @anthropic-ai/claude-code` (or the `git clone…` one-liner) to clipboard, **not** the page content. No "copy page as markdown/context/prompt" control exists. A "Try this in Claude Code" CTA block is present — it invites the reader to open Claude Code and work the tutorial *with* an agent, which is adjacent but not the same affordance. Separately: literal string "llms" appears **zero times** across all 8 saved HTML pages (home, get-started, learn, vaults, community, compliance, vault-to-site, changelog) — the `llms.txt`/`llms-full.txt` files exist and work, but nothing on the site's own rendered pages links to them; an agent has to already know the convention. | `[D]` |
| 13 | Self-conformance | **Explicitly claimed and specific — but narrative, not structural** | `/how/publishing/vault-to-site/` states verbatim: *"This document describes how aDNA.aDNA vault content becomes a live documentation website... You are reading a file that was produced by the same vault this pipeline publishes — the process described here built the site."* That same page also discloses the pipeline **"strips vault frontmatter (`type`, `created`, `tags`, etc.) and replaces it with Astro-specific fields"** — i.e. the site owns up, in its own prose, to not carrying the vault's own frontmatter onto the live pages. The homepage's registry preview separately surfaces aDNA.aDNA's own listing as "Self-referential aDNA documentation — teaches the standard by using the standard." So: the conformance claim is real, specific, and findable — but it lives in prose on one deep page (not the homepage, not a persistent marker), and it isn't backed by anything a machine could verify structurally (no `source_vault_path` meta tag, no frontmatter passthrough, no JSON-LD field tying a rendered page back to its source `.md`). An agent must read and trust prose, not check a field. | `[D]` |
| 14 | Text-extraction reads | **High fidelity on `/` and `/get-started`; partial on `/vaults/graph` — plus a citable internal count mismatch** | `/`: 1,072 words extracted cleanly — hero, stat strip, ASCII directory tree, `CLAUDE.md` excerpt, and a mission-YAML excerpt all survive as coherent text. `/get-started`: 744 words, same fidelity — full tutorial, code blocks, CLI transcript all read as intact prose/code. `/vaults/graph`: the graph is a genuine inline, server-rendered `<svg id="vaultsGraph" role="img" aria-labelledby="...-title ...-desc">` (not `<canvas>`, not JS-only) whose own `<desc>` states **"the accompanying vault list is the keyboard-navigable twin."** That twin (519 words) *does* carry: the full vault roster grouped by class, and the edge-type legend with per-type counts. It does *not* carry the enumerated edges themselves (which specific vault points to which, with direction/type) — that topology exists only in the SVG's `<path>` geometry with no per-edge accessible name. **Opportunistic finding:** the graph page's lead paragraph says "74 vaults... 59 vaults carry no cited relationship," while the SVG's own `<title>`/`<desc>` says **"68 vaults... 15 of 68 vaults are joined... the remaining 53 vaults carry no cited relationship."** 74 vs. 68 and 59 vs. 53 — the hero copy reuses the sitewide constant (74) while the graph itself actually renders a smaller set (68); both halves are internally consistent on their own (74−59=15=68−53) but disagree with each other on the same page. | `[D]` |
| 15 | Independent machine reader | **SYNTHETIC — see transcripts below.** Model correctly identified the product category, audience, and negative space, and derived the correct next action (the `git clone` command) directly from page text on both passes, with no fabricated facts; mild terminology drift ("framework" for what the site calls a "standard & network"). | See transcripts | `[SYNTHETIC]` |

## SYNTHETIC reader transcripts

Both passes used `mcp__local-llm__local_chat`, model **`local-qwen3.6-35b-a3b`** (local inference, disclosed synthetic — a different model family from the agent conducting this audit, run against the plain-text extraction of the live HTML with no additional framing beyond the prompt below).

### Pass 1 — homepage (`/`)

**Prompt:** *"You've been given the text of a website's homepage. In 3 sentences: What is this? Who is it for? What is it NOT? Then: what would you do first if you wanted to try it?"*

**Input:** `home_text.txt` (1,072 words / 6,846 chars — full page, under the ~3,000-word trim budget so used whole).

**Verbatim response:**
> It is an open-source, MIT-licensed specification and federated network that structures project knowledge into navigable graphs, enabling both humans and AI agents to organize, share, and govern contextual workflows. It is designed for developers, researchers, enterprises, and public-interest organizations who need AI-human collaborative knowledge management without vendor lock-in or centralized control. It is not a proprietary documentation platform, a prompt-engineering workaround, or a closed system that restricts data ownership or requires special formats. First, I would clone the public GitHub repository, run the `claude` command, and take the five-minute tour to explore the `what/`, `how/`, and `who/` directory structure before experimenting with my own vault.

*(tokens in/out: 1,581/2,056, finish: stop)*

### Pass 2 — `/get-started`

**Prompt:** same as Pass 1.

**Input:** `get_started_text.txt` (744 words / 4,602 chars — full page).

**Verbatim response:**
> aDNA is a local, privacy-first AI development framework that scaffolds and manages AI-agent-ready project workspaces entirely on your machine. It is designed for developers and technical teams seeking structured, version-controlled environments where both humans and AI tools can seamlessly navigate project context and governance. It is not a cloud-hosted SaaS platform, a data-collection service, or a rigid copy-paste template; instead, it operates as a live, version-controlled local workspace that explicitly sends zero data externally.
>
> To try it, I would install Git and Claude Code, then execute the provided one-command setup (`git clone https://github.com/aDNA-Network/aDNA.git ~/aDNA && cd ~/aDNA && claude`) to instantly generate a local workspace and begin the AI-guided onboarding interview.

*(tokens in/out: 1,131/2,164, finish: stop)*

**Reading note on both passes `[SYNTHETIC]`:** no hallucinated numbers or invented features in either transcript. Pass 2's "framework" and "explicitly sends zero data externally" mildly overclaim relative to the source text — the page says "aDNA" is a workspace/standard (not a "framework"), and "nothing is sent anywhere" is stated about the local-clone step specifically, not as a blanket privacy guarantee (the same site elsewhere describes federation/sharing as opt-in). Both passes correctly refused to invent a UI they weren't shown and correctly extracted the literal install command as the first action — a low bar, but real: it confirms the CTA and command are legible enough for a small, unrelated model to act on with zero scaffolding.

## Closing assessment — D10 anchor score

Per `directives/OPERATION_VITRUVIUS_review_instrument.md` §D10 anchors:

| Score | Condition | Met here? |
|---|---|---|
| 0 | JS-only rendering; no machine artifacts | No — site is fully server-rendered; ruled out |
| 2 | Server-rendered HTML; sitemap only | Cleared and exceeded |
| 3 | llms.txt + sitemap + RSS + clean extraction | **Cleared** — all four present and working (RSS is stale-content but not broken; extraction is clean on 2/3 sampled pages, partial on the 3rd) |
| 4 | As 3, plus markdown twins, JSON-LD, machine-readable registry, documented agent entry point | **Not cleared** — 3 of these 4 bullets hard-fail (no `.md` twins, no registry JSON/API, `llms.txt` not linked/advertised anywhere in the site's own HTML), and the 4th (JSON-LD) is only partially met (present but shallow: no `Dataset`, no `sameAs`, no per-vault structured type) |
| 5 | As 4, plus MCP server, copy-as-context, stable resolvable URIs, demonstrated self-conformance | Far out of reach — no MCP server, no copy-as-context control, URIs are only half-stable (case-sensitive, no redirect/normalization), self-conformance is asserted in prose on one deep page rather than structurally demonstrated |

**Score: 3 / 5.** The site clears anchor 3 cleanly — `llms.txt` is genuinely curated and current, the sitemap is complete and matches the live registry exactly, RSS validates, and plain-text extraction preserves meaning well on ordinary content pages. But it does not reach anchor 4: three of that anchor's four specific requirements fail outright (no markdown twins, no machine-readable registry endpoint, and — a sharper problem than a missing nice-to-have — the one genuinely good artifact the site does ship, `llms.txt`, is not discoverable from anywhere inside the site's own HTML, so an agent has to already know a convention rather than being told). The `llms-full.txt` name is a specific, fixable overclaim (2 KB index wearing a "full corpus" label). And the most interesting finding is the instrument's own named failure mode, *self-exemption*, showing up in a genuinely nuanced, partial form: the site's own publishing-pipeline page candidly documents that vault frontmatter is stripped before pages go live — so the standard's flagship instance narrates its self-conformance rather than exhibiting it structurally, and even the one place that narration lives (`/how/publishing/vault-to-site`) contains a page-internal arithmetic mismatch (74 vs. 68 vaults) between its own hero copy and what its own graph actually renders. Given the archetype's own framing — for a project whose thesis *is* agent-navigable context, D10 gaps are read as evidence against the product claim, not merely a missing feature — a 3 sits right at "present and functional" without yet being "self-conformant," which is the more exacting bar this specific site sets for itself.
