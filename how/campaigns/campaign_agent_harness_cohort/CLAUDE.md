---
type: governance
subtype: campaign
campaign_id: campaign_agent_harness_cohort
created: 2026-09-07
updated: 2026-09-07
last_edited_by: agent_fable_polyglot
status: active
tags: [governance, campaign, agent_harness, codex, claude_code, software_element_graph, polyglot]
---

# CLAUDE.md — campaign_agent_harness_cohort (Operation Polyglot)

Governance for the **agent-harness cohort** — the chartering campaign that seeded
`Codex.aDNA` (Cassiodorus) and `ClaudeCode.aDNA` (Achates) as software-element context
graphs (ADR-039) for the two vendor agent CLIs the network operates with. Sibling in
method to Operation Dynamo (`campaign_dynamo/`) and Operation Keystone.

## Orientation

1. Master: [[campaign_agent_harness_cohort|campaign_agent_harness_cohort.md]].
2. The cohort manifest is the artifact of record:
   [[artifacts/agent_harness_cohort_manifest|artifacts/agent_harness_cohort_manifest.md]].
3. Active work now lives **in the seeded graphs** — each carries its own genesis campaign
   with an M00 charter-and-design mission (fable tier). This campaign holds the charter,
   not the ladder.
4. Derive the active state from the two graphs' `STATE.md` files, never from this file.

## Standing orders (campaign-scoped)

- **Seams ratified at each graph's P0** (⛩ operator-ruled 2026-09-06) — four seam memos
  staged to Harness · Terminal · RareAnthropic · Obsidian; forks did not wait on acks.
- **Router rows STAGED `#needs-human`** in each graph's `who/coordination/`; never
  self-inserted.
- **No harness config mutation** (`~/.codex/`, `~/.claude/`, settings cascades) until each
  graph's P0+ rules it — the stubs describe, they do not operate.
- **Asymmetry is recorded, not papered over** (§Asymmetry in the manifest): ClaudeCode has
  a vendor Org-Graph peer and 57 template context copies; Codex has neither.
- Gates are human gates · archive never delete · federate never duplicate.
