---
type: aar
artifact_type: mission_aar
aar_class: lightweight  # 5-line + 4-category extended findings (per mission spec §Acceptance criteria + Standing Order #5)
mission: mission_adna_str_p2_m24_agents_md_heatmap
campaign: campaign_adna_serious_tool_readiness
phase: 2
mission_number: 2.4
mission_class: hybrid  # measurement (heat-map queries + pattern β verdict) + implementation (AGENTS.md invariants codification + ADR-022 elevation) per ADR-016 Appendix A measurement-row + implementation-row intersection
sessions_executed: 3  # S1 / S2 / S3
estimated_sessions: 3
actual_sessions_match: yes  # canonical 3-session implementation-class shape — 5th instance after M1.3 + M1.4 + M2.1 + M2.3
opened: 2026-05-20T19:33:04Z
closed: 2026-05-20T~23:11Z+  # S3 close (this artifact)
total_wall_clock: ~3h40m elapsed (S1 19:33 + S2 21:49 + S3 23:11; operator gaps minimal between sessions — concentrated execution within a single calendar day)
created: 2026-05-20
updated: 2026-05-20
status: complete
last_edited_by: agent_stanley
load_bearing: true  # primary: AGENTS.md is UNDER-LOADED not re-read-wasted — re-frames hardening from waste-prevention to discoverability; strong-extended: two-step ADR slot reassignment chain ADR-007 → ADR-019 → ADR-022 extends M1.5 forward-only precedent and is doctrinal signal for future ADR drafting; primary connects directly to north-star UX goal (easy and fluid context-graph build/operate/utilize depends on AGENTS.md routing being efficient)
tags: [aar, mission, m24, v8, p2, serious_tool_readiness, lightweight, load_bearing, agents_md_under_use_re_frame, agents_md_invariants_spec_ratified, pattern_beta_hold_at_14_ratified, adr_022_elevation_accepted, adr_slot_reassignment_chain_007_to_019_to_022, audit_only_d5_b, p2_exit_gate_4th_brick_satisfied, p2_to_p3_phase_exit_gate_ready, canonical_3_session_shape_5th_instance, op_3_archive_on_close_6th_canonical_instance, standing_order_8_self_reference_7th_invocation, two_metric_discipline_operational, m23_consumer, m23_5_template_consumer, north_star_ux_alignment]
related_artifacts:
  - ../mission_adna_str_p2_m24_agents_md_heatmap.md  # mission spec
  - m24_obj2_heatmap_query_suite.md   # S1 — 4 SQL queries + Mermaid digraph + load-bearing finding AGENTS.md UNDER-LOADED not re-read-wasted
  - m24_obj3_pattern_beta_final_verdict.md   # S2 — HOLD pattern β at rank 14 ratified; 27.44% / CV 0.353 / r=0.142
  - m24_obj4_agents_md_hardening_audit.md   # S2 — 44-row inventory + 7-item invariants spec + top-12 priority list + M2.4.5 OR M3.1 absorption dispatch contract
  - m24_obj7_validation_output.md   # S3 (this session) — final consolidated 8-section output
  - ../../../../what/decisions/adr_022_tool_use_logging.md   # S2 — `status: accepted` with 3 clauses A/B/C; slot reassigned from ADR-007 → ADR-019 → ADR-022
  - ../../../../what/decisions/adr_016_per_mission_context_budget.md   # M2.3 LIVE; Clause A two-metric + Clause C ratified empirical constants + Appendix A per-mission-type calibration (M2.4 inherits)
  - ../../../../../node.aDNA/what/context/token_baselines.md  # v0.1.2 → v0.1.3 fold-in at this S3 with NEW §8 heat-map summary + NEW Appendix B per-directory AGENTS.md invariants spec
  - aar_m23_convergence_validation.md  # M2.3 AAR — 4th canonical 3-session implementation-class instance; this AAR inherits full shape per mission spec
  - aar_m23_5_push_readiness_review.md  # M2.3.5 AAR — 3 templates inherited (per-vault blast-radius matrix + 5-phase model + 12-item push-readiness checklist; M2.4 reused per-vault matrix template at Obj 4 §2 44-row inventory)
  - aar_m22_per_mission_budget.md   # M2.2 AAR — Standing Order #8 self-reference 1st canonical instance; planning-class single-session shape
  - aar_m15_coord_network.md   # M1.5 AAR — Standing Order #8 self-reference 2nd instance + ADR slot forward-only reassignment precedent (extended by M2.4 to two-step chain)
  - aar_m21_context_audit_split.md   # M2.1 AAR — 3rd canonical 3-session instance + Op 3 archive-on-close pattern 1st canonical instance (M2.4 close is 6th)
  - aar_m14_latticescope_schema.md  # M1.4 AAR — 2nd canonical 3-session instance; measurement substrate substrate for M2.4 corpus
  - aar_m13_token_audit.md  # M1.3 AAR — 1st canonical 3-session instance; AGENTS.md heat-map sketch explicitly deferred to M2.4 (now closed)
---

# M2.4 AAR — AGENTS.md Per-Directory Hardening + Context-Graph Traversal Heat Map

## 5-line summary (Standing Order #5)

- **Worked**: Canonical 3-session implementation-class shape held cleanly for the **5th consecutive time** (M1.3 → M1.4 → M2.1 → M2.3 → M2.4; pattern now ratified across 5 missions per Campaign S.O. #17). 6/6 deliverables landed across S1 (Obj 1 mission spec + Obj 2 heat-map query suite) + S2 (Obj 3 pattern β HOLD at 14 + Obj 4 AGENTS.md audit-only + Obj 5 ADR-022 elevation accepted) + S3 (Obj 6 mission close with AAR + obj7 + node.aDNA token_baselines.md v0.1.3 + companion + inventory + campaign master + STATE.md Op 3 6th canonical instance + 3 session moves). All 5 D-gates discharged with Rosetta defaults at S2 plan ratification (D2=A elevate ADR-022 / D3=evidence-decides HOLD β=14 / D4=B invariants as Appendix B to token_baselines.md v0.1.3 / D5=B AGENTS.md audit-only with deferred bulk-edit). Token budget within band at all 3 sessions; **M2.4 close satisfies the 4th brick of Campaign Standing Order #19 P2 → P3 exit gate** ("AGENTS.md heat map operational"); combined with M1.3 + M2.2 + M2.3, the P2 → P3 transition is now operator-gate-ready.
- **Didn't**: No bulk-edit AGENTS.md mutations at S2 or S3 (D5=B audit-only protected the 44-file fix from premature codification). No new ADR work at S3 (ADR-022 ratified at S2; no S3 amendments). No `~/.adna/measurement/measurement.sqlite` mutations end-to-end (sqlite3 -readonly throughout). No `.adna/` upstream touches (v7.0 frozen at `27e6395`; v8 P6 owns AGENTS.md invariants + ADR-022 + ADR-016 amendment + SO #11 refinement upstream propagation). No partner-vault contact (4 embargo memos preserved). No `~/.claude/settings.json` modifications. No hook modifications. No node.aDNA mutations at S1 or S2 (S3 bounded to exactly 3 files: token_baselines.md + .yaml + inventory_vaults.yaml row). No M2.1.5 / M3.x / M1.5 / M2.4.5 work.
- **Finding (load-bearing — primary)**: **"AGENTS.md is UNDER-LOADED, not re-read-wasted."** S1 Obj 2 §3 measured 86% of 44 active AGENTS.md files NEVER read in the 10-session corpus; the 6 that were touched got exactly 1 read per session (0% re-read rate). Pattern β waste (40-67% re-read) lives DOWNSTREAM in mission specs + STATE.md + campaign master, not in the AGENTS.md routing layer. **Re-frames the entire hardening direction** from waste-prevention (M2.3 candidate text framing) to *discoverability* (making AGENTS.md the first-stop entry that pre-routes agents to the right deep file before they cold-load it). The strategic-direction shift connects directly to the campaign's north-star UX goal — *"easy and fluid way to build/operate/utilize context graphs"* depends on agent-side AGENTS.md routing being efficient and predictable. Without this re-frame, M2.4.5/M3.1 bulk-edit would optimize the wrong axis.
- **Finding (load-bearing — strong extended; doctrinal)**: **Two-step ADR slot reassignment chain ADR-007 → ADR-019 → ADR-022.** At S2 entry, the M1.3-era working name "ADR-007 (tool-use logging)" collided with an accepted ADR (v2 M01 outer-CLAUDE.md disposition, ratified 2026-05-08); initial reassignment to ADR-019 collided with a campaign master forecast reservation (Modular III for Obsidian at P3 M3.7); final reassignment to ADR-022 (next clear unreserved slot beyond ADR-014/015/018/019/020 all forecast-reserved). M1.5 ADR-017 forward-only reassignment precedent extended to a two-step chain; predecessor references in `m23_obj5_adr_007_deferral_memo.md` + `m13_obj7_calibration_output.md` §6 + the S2 plan file preserved verbatim per the M1.5 forward-only invariant. **Doctrinal implication**: future ADR drafting in v8 P6 + M3.x SHOULD check campaign master ADR roadmap + `ls what/decisions/` at draft time to avoid the same collision pattern. Skill graduation candidate `skill_adr_drafting.md` at ≥ 3 instances (currently 2: M1.5 forward-only single-step + M2.4 two-step chain; v8 P6 may surface 3rd instance triggering graduation).
- **Change**: `m24_obj2_heatmap_query_suite.md` LIVE (S1; 4 SQL queries Q1-Q4 + Mermaid digraph; 27.44% aggregate re-read; 86% AGENTS.md never-read finding). `m24_obj3_pattern_beta_final_verdict.md` LIVE (S2; **HOLD pattern β at rank 14** ratified; CV 0.353 < 0.4; r=0.142 < 0.4; cross-corpus arc M1.3 11% → M2.3 26.8% n=7 → M2.4 27.44% n=14 = methodology shift + stability convergence). `m24_obj4_agents_md_hardening_audit.md` LIVE (S2; 44-row inventory: 8 canonical / 13 domain-OK / 22 procedural / 1 stale / 0 missing-invariant; 7-item invariants spec ratified — **4 mandatory + 3 conditional** per §3 final matrix; deterministic top-12 priority list; 5 bulk-edit gap codes; audit-only verdict per D5=B; M2.4.5 OR M3.1 absorption dispatch). `adr_022_tool_use_logging.md` LIVE (S2; `status: accepted`; 3 clauses A PostToolUse hook payload capture + B SQLite store schema + retention + C analysis dispatch contract naming m24_obj2 as first consumer; elevation chain provisional → queued-forecast → deferred → accepted). `m24_obj7_validation_output.md` LIVE (S3; 8 sections). `node.aDNA/what/context/token_baselines.md` v0.1.3 LIVE (S3; NEW §8 heat-map summary + NEW Appendix B per-directory AGENTS.md invariants spec + §4 pattern β HOLD ratification + revision history v0.1.3 row). Companion `.yaml` + `inventory_vaults.yaml` row version-bumped.
- **Follow-up**: **P2 → P3 phase exit gate now READY at this S3 close** — operator-decision per Campaign Standing Order #19. M2.4 close satisfies the 4th brick ("AGENTS.md heat map operational"); combined with M1.3 (token-efficiency baseline) + M2.2 (per-mission budget SO ratified) + M2.3 (convergence model validated), P2 → P3 transition becomes operator-gate-ready. **M2.4.5 OR M3.1 absorption** for AGENTS.md bulk-edit awaits operator dispatch decision at P2 → P3 entry (m24_obj4 §7 Option A M2.4.5 dedicated single-session mission is Rosetta-default recommendation; operator may absorb into M3.1 forge-ecosystem mission instead). **M2.1.5 retroactive Op 3 interstitial** stays `planned-optional`; Op 3 archive-on-close pattern is now at **6th canonical instance** (M2.1 + M2.2 + M1.5 + M2.3 + M2.3.5 + M2.4); skill graduation rubric (≥ 3 distinct rotations per `m21_obj4_archive_convention_design.md`) satisfied many times over; `skill_campaign_close_archive.md` graduation candidate at v8 P6 OR M2.1.5 OR M3.x retroactive task. v8 P6 propagation queue inherits AGENTS.md 7-item invariants spec + ADR-022 + ADR-016 amendment + Project SO #11 refinement + Heavy-File Read Convention (5 v8 doctrinal additions for v8.0 tag firing). Pattern β refresh trigger at M3.x ≥ 20 sessions + ≥ 3-per-class binning (per-mission-class REFINE branch evidence currently insufficient because `sessions.session_type` NULL).

---

## Extended findings (4 categories per mission spec §Objective 6)

### (a) Measurement / pattern-β verdict — HOLD at 14 ratified; cross-corpus convergence at ~ 27%

1. **Pattern β HOLD at rank 14 RATIFIED** on n=14 corpus (S2 Obj 3). Aggregate Read re-read rate **27.44%** (in 20-28% HOLD band per mission spec §Mission scope table). Per-session CV **0.353** (well below 0.4 stability threshold). Pearson r between session turn count and per-session re-read rate **0.142** (well below 0.4 PROMOTE threshold). Both HOLD branch criteria satisfied; PROMOTE fails on correlation gate; REFINE fails on data availability (`sessions.session_type` NULL across all 12 rows at S2 entry; per-mission-class binning unavailable for the candidate-promotion contingent on per-class divergence > 2×).
2. **Cross-corpus arc M1.3 11% → M2.3 26.8% (n=7) → M2.4 27.44% (n=14)** = methodology shift (M1.3 session-file scan vs M2.3+ live-hook SQLite capture) THEN stability convergence around 27%. M2.3's 26.8% reading was approximately accurate, NOT upward-biased as the HOLD-branch template predicted at mission spec design time. The corpus extension from n=7 to n=14 did not pull the rate toward a lower stable value; it pulled it 0.6 pp higher to ~ 27% which is consistent with the HOLD band.
3. **Final ranking carries forward to node.aDNA v0.1.3**: **α=25 (top; unchanged; corpus-validated since M2.3) / β=14 (HOLD ratified at M2.4) / δ=10 (M1.4 upgrade EMPIRICALLY VALIDATED since M2.3) / γ=6 (unchanged; γ corpus still thin at 107 traversals total; M3.x forge-ecosystem missions feed γ corpus toward potential upgrade)**. Pattern β candidate-promotion text from M2.3 retroactively reframed: M2.3 was within the HOLD band, not above it.
4. **Forward refresh trigger documented**: pattern β re-rank at M3.x **≥ 20 sessions** with **≥ 3-per-class binning** (REFINE branch evidence requires `sessions.session_type` population; gate at M3.x or later if hook captures session_type via a different mechanism). M2.4's HOLD at 14 stays load-bearing through M3.x unless refresh triggers.

### (b) Governance / hardening — AGENTS.md under-use re-frame + 7-item invariants spec + 44-row audit + audit-only deferral

1. **AGENTS.md UNDER-LOADED, not re-read-wasted** (S1 Obj 2 §3 load-bearing finding propagated as **primary** at this AAR). 86% of 44 active AGENTS.md files NEVER read in 10-session corpus; 6 touched files have 0% re-read rate. Pattern β waste lives in mission specs / STATE.md / campaign masters (40-67% re-read), NOT in the AGENTS.md routing layer. **Hardening direction shift**: waste-prevention → **discoverability**.
2. **7-item per-directory invariants spec RATIFIED** (S2 Obj 4 §3 with final matrix at lines 211-220): **4 mandatory** (Invariant 1 Entry-tier descriptor / Invariant 3 Load-order discipline / Invariant 6 Dual-audience pass / Invariant 7 Cross-link density) + **3 conditional** (Invariant 2 Safety hints / Invariant 4 Archive cross-link / Invariant 5 Heavy-file warning). Note: mission spec + S2 session log narrative referenced "5 mandatory + 2 conditional" as a copy-paste mismatch; the §3 final matrix is the load-bearing authoritative source. Compliance scoring: 4 of 4 mandatory invariants satisfied = canonical-eligible. Files missing ≥ 1 mandatory invariants = audit follow-up at bulk-edit dispatch.
3. **44-row inventory at audit-only depth** (S2 Obj 4 §2): 8 canonical (18%; including `how/AGENTS.md`, `what/AGENTS.md`, `who/AGENTS.md`, `what/decisions/AGENTS.md`, `how/templates/AGENTS.md`, `how/campaigns/AGENTS.md`, `how/sessions/AGENTS.md`, `what/context/AGENTS.md`) + 13 domain-OK (30%) + 22 procedural (50%; mostly 2026-04-13 `agent_stanley` batch from Rosetta Phase 1+2 build-out — frontmatter + Load/Skip + Token cost but lacking Purpose + Structure + Cross-References) + 1 stale (2%; `what/context/object_standards/AGENTS.md` with no frontmatter) + 0 missing-invariant.
4. **Deterministic top-12 priority list** (Obj 4 §4) ranked by subtree-frequency proxy (`subtree_read_count × (1 - canonical_score / 9)`) — directories that gate heavily-read content AND have low canonical-score get top priority. Rank #1: `AGENTS.md` (root) — procedural verdict but functionally canonical-class content-wise; gates STATE.md (25 reads) + project-wide entry. Rank #2: `how/campaigns/AGENTS.md` — canonical² content but missing Cross-References section. Bulk-edit dispatched to M2.4.5 OR M3.1 absorption per D5=B Rosetta-default with **5 bulk-edit gap codes** mapped (`gap_frontmatter` 1 file / `gap_load_skip` 3 / `gap_token_cost` 3 / `gap_purpose_section` ~20 / `gap_convergence_model_ref + gap_heavy_file_warning` ~10-15).

### (c) ADR ecosystem — ADR-022 elevation with two-step slot reassignment chain + M1.5 precedent extension

1. **ADR-022 LIVE with `status: accepted`** (S2 Obj 5) at `what/decisions/adr_022_tool_use_logging.md`. 3 clauses: **A** PostToolUse hook payload capture (LIVE since M1.3 S2 2026-05-18; ratifies what's running; privacy invariants codified — token-metadata only + sanitized cwd + append-only) + **B** SQLite store schema + retention (M1.4 v0.1.1 schema; forward-compatible additive-only migrations; manual archival permitted; sqlite3 -readonly agent-side queries only) + **C** analysis dispatch contract (names `m24_obj2_heatmap_query_suite.md` as **first analysis-dispatch consumer** per `m23_obj5_adr_007_deferral_memo.md` §3 forward contract verbatim; same construction pattern as ADR-016 Clause C citing `m23_obj2` §3 empirical constants; consumer-contract 6 invariants applied retroactively to `m23_obj2`). Elevation chain: *provisional* (M1.3) → *queued-forecast* (M2.2) → *deferred* (M2.3 S2 via `m23_obj5`) → **accepted** (M2.4 S2).
2. **Two-step slot reassignment chain ADR-007 → ADR-019 → ADR-022** (S2 Obj 5 in-session discovery; load-bearing finding strong-extended at this AAR). **1st collision at S2 entry**: ADR-007 occupied by 2026-05-08 v2 M01 outer-CLAUDE.md disposition (unrelated topic; `status: accepted`); M1.3 §6 + M2.2 prep + M2.3 deferral memo all referenced "ADR-007 (tool-use logging)" as numbering carry-forward predating awareness of the v2-era slot occupation. **Initial reassignment attempt ADR-019**: picked without yet reading campaign master ADR roadmap; ADR-019 file authored with `status: accepted` + 3 clauses. **2nd collision mid-S2**: campaign master ADR roadmap row 310 forecast-reserves ADR-019 for "Modular III for Obsidian" (concern #6 capstone, P3 M3.7); forecast ADRs are "subject to load-bearing-decision discipline; not all will be authored" but reassigning an *accepted* ADR onto a *named forecast* slot creates the same audit confusion the ADR-007 collision created. **Final reassignment ADR-022**: next clear unreserved slot beyond all forecast reservations (ADR-014/015/018/019/020 all forecast-reserved; ADR-021 also TBD per "ADR-020+" catchall row 311 — renamed catchall to "ADR-022+"). M1.5 ADR-017 forward-only reassignment precedent extended to two-step chain.
3. **`slot_reassignment_history` frontmatter block + §Status documentation** at ADR-022 records the full chain; predecessor references in `m23_obj5_adr_007_deferral_memo.md` + `m13_obj7_calibration_output.md` §6 + the S2 plan file are NOT retroactively edited (preserves audit trail per M1.5 forward-only invariant — references to earlier names remain as committed evidence of the reassignment moment).
4. **Doctrinal implication for v8 P6 + M3.x**: future ADRs SHOULD check campaign master ADR roadmap + `ls what/decisions/` at draft time to avoid the same collision pattern. Skill graduation candidate `skill_adr_drafting.md` queued at ≥ 3 distinct instances (currently 2: M1.5 single-step forward-only + M2.4 two-step chain; v8 P6 may surface 3rd instance triggering graduation).

### (d) Doctrinal / forward signals — north-star alignment + ADR draft-time discipline + P2 → P3 exit gate ready + 6th canonical Op 3 instance + 7th SO #8 self-reference invocation

1. **North-star UX alignment confirmed at primary load-bearing finding propagation**. Campaign's strategic intent — *"easy and fluid way to build/operate/utilize context graphs"* (MEMORY.md) — depends on agent-side AGENTS.md routing being efficient and predictable. M2.4 makes that surface measurable (heat map at Obj 2) and codified (invariants spec at Obj 4). Without M2.4, the per-directory routing layer stays informal; with M2.4, it becomes governed. The under-use re-frame redirects M2.4.5/M3.1 bulk-edit effort toward the discoverability axis — first-stop entry that pre-routes agents to the right deep file before they cold-load it.
2. **ADR draft-time slot-availability discipline** emerges as doctrinal output of the two-step slot reassignment chain. Lifecycle protocol: at ADR draft time, check (a) campaign master ADR roadmap (forecast reservations + named upcoming slots) AND (b) `ls what/decisions/` (existing files including non-forecast-reserved ones from prior campaigns). Mid-session reassignment is acceptable per M1.5 precedent but adds ~ 20-30 turns of remediation overhead vs the original-plan single-pick path (per S2 token-budget delta self-report — calibration data for M3.x).
3. **P2 → P3 phase exit gate READY** at this M2.4 S3 close. Campaign Standing Order #19 4-bricks complete: M1.3 (token-efficiency baseline established) + M2.2 (per-mission budget Standing Order ratified — ADR-016 LIVE) + M2.3 (convergence model validated — Clause C ratified empirical constants from 49-session corpus) + **M2.4 (AGENTS.md heat map operational — heat-map queries LIVE + invariants spec ratified + ADR-022 analysis-dispatch contract LIVE)**. The transition becomes operator-gate-ready immediately after this S3 commit lands; M3.x forge-ecosystem hardening cohort inherits two-metric budget discipline + 3 M2.3.5 templates + 7-item AGENTS.md invariants spec + ADR-022 dispatch contract.
4. **Op 3 archive-on-close pattern 6th canonical instance** at this STATE.md refresh. Prior instances: M2.1 (1st) + M2.2 (2nd) + M1.5 (3rd) + M2.3 (4th) + M2.3.5 (5th) + **M2.4 (6th)**. Skill graduation rubric (≥ 3 distinct rotations per `m21_obj4_archive_convention_design.md` §rubric) satisfied many times over — `skill_campaign_close_archive.md` graduation candidate at v8 P6 OR M2.1.5 OR M3.x retroactive task. The pattern is now operationally proven across BOTH single-session planning-class shape (M2.2 + M1.5 + M2.3.5) AND canonical 3-session implementation-class shape (M2.1 + M2.3 + M2.4) — universal across mission classes.
5. **Standing Order #8 self-reference 7th tactical invocation in v8 P2** (closing the M2.4 mission). Prior invocations: (1) M2.2 — first mission to declare `token_budget_estimated` per the very ADR Clause A it ratified; (2) M1.5 — ADR-017 ratified `network_` namespace inside aDNA.aDNA's own coord memo discharge; (3) M2.3 — calibration formula ratified on the very session it was designed to predict; (4) M2.3.5 — push-readiness gate gated its own push; (5) M2.4 S1 — AGENTS.md hardening measurement applied to AGENTS.md files in this campaign directory; (6) M2.4 S2 — pattern β verdict ratified on the very SQLite store the analysis-dispatch contract (Clause C) ratifies access to + slot-reassignment chain doctrine applied to a fresh slot-reassignment event in this very session; (7) **M2.4 S3 (this AAR)** — the AAR consolidating the under-use re-frame finding IS being written for the very AGENTS.md routing layer the re-frame names as the target of the M2.4.5/M3.1 bulk-edit (and this AAR is itself one of the artifacts the routing layer should help future agents discover). The pattern is now canonical for governance-synthesis sessions at all 3 levels of execution (campaign / mission / session) — every doctrine that compresses prior measurement compresses *with the session's own measurement folded in*.
6. **5 doctrinal additions for v8 P6 propagation queue**: (a) AGENTS.md 7-item invariants spec (NEW at M2.4; lifts to `.adna/AGENTS.md` ecosystem-wide at v8 P6); (b) ADR-022 tool-use logging (NEW at M2.4 S2; lifts to `.adna/what/decisions/` at v8 P6); (c) ADR-016 amendment with Clause A two-metric + Clause C ratified constants + Appendix A per-mission-type calibration (carried from M2.3); (d) Project Standing Order #11 refinement with API-billing companion bullet (carried from M2.3); (e) ADR-016 Clause B Heavy-File Read Convention (carried from M2.1). Backlog placeholder mechanism per ADR-005 rule 3 + Campaign Standing Order #14; all 5 lift together at v8.0 tag firing.
7. **Two-step ADR slot reassignment chain doctrine** queued for graduation at ≥ 3 instances (currently 2). Backlog placeholder candidate at v8 P6 propagation: `skill_adr_drafting.md` with "check campaign master ADR roadmap + `ls what/decisions/` at draft time" lifecycle protocol. The pattern is generalizable across vaults — any vault with active campaigns + forecast ADR roadmaps + existing accepted ADRs faces the same collision risk.

---

## Acceptance-criteria scorecard (M2.4 mission spec §Acceptance Criteria — 12 items)

| # | Criterion | Status |
|---|---|---|
| 1 | All 6 deliverables landed at S3 close (Obj 1:S1 / Obj 2:S1 / Obj 3:S2 / Obj 4:S2 / Obj 5:S2 if trigger met / Obj 6:S3) | ✅ PASS (S1 landed Obj 1+2 = 2/6; S2 landed Obj 3+4+5 = 3/6 cumulative 5/6; S3 lands Obj 6 = 1/6 cumulative 6/6 = ALL LANDED) |
| 2 | `m24_obj2_heatmap_query_suite.md` contains ≥ 4 SQL query §sections + Mermaid digraph for Q3 + caveats for 7.7% join-rate; all 4 queries produce non-empty result sets | ✅ PASS (Q1-Q4 LIVE with non-empty result sets; Mermaid digraph validated `valid: true`; §6 caveats note 7.7% join-rate; aDNA.aDNA → node.aDNA dominant edge 55 reads) |
| 3 | `m24_obj3_pattern_beta_final_verdict.md` declares verdict explicitly (HOLD/PROMOTE/REFINE) with evidence threshold; cross-corpus check vs M1.3 + M2.3 | ✅ PASS (HOLD pattern β at rank 14 explicit; CV 0.353 evidence; r=0.142 evidence; cross-corpus arc M1.3 11% → M2.3 26.8% n=7 → M2.4 27.44% n=14 at §4) |
| 4 | `m24_obj4_agents_md_hardening_audit.md` audits all 40 active AGENTS.md files + ratifies 7-item per-directory invariants spec + produces deterministic top-12 priority list | ✅ PASS (44 files audited — corrected from spec's N=40 at Obj 4 §inventory; 7-item invariants spec at §3 with final matrix 4 mandatory + 3 conditional; top-12 priority list at §4 with deterministic ranking function) |
| 5 | ADR-007 elevation decision recorded — `adr_007_tool_use_logging.md` LIVE with `status: accepted` IF SQLite session ≥ 10 at S2; OTHERWISE refreshed deferral memo | ✅ PASS (trigger MET — 11 sessions at S2 entry per S2 SITREP; elevation accepted; **slot reassigned to ADR-022** via two-step chain ADR-007 → ADR-019 → ADR-022 — file at `what/decisions/adr_022_tool_use_logging.md`) |
| 6 | ADR-007 Clause C names `m24_obj2_heatmap_query_suite.md` as first analysis-dispatch consumer (if elevation fired) | ✅ PASS (ADR-022 Clause C names `m24_obj2_heatmap_query_suite.md` as first consumer per `m23_obj5` §3 forward contract verbatim) |
| 7 | `aar_m24_agents_md_heatmap.md` follows lightweight 5-line + 4-category extended findings shape (SO #5); load-bearing tag if hardening invariants spec adopted | ✅ PASS (this artifact; 5-line + 4-category extended findings; `load_bearing: true` with PRIMARY = AGENTS.md under-use re-frame + STRONG EXTENDED = ADR slot reassignment chain doctrine) |
| 8 | `m24_obj7_validation_output.md` follows 8-section M1.4 obj7 template | ✅ PASS (m24_obj7 at this S3; 8 sections §1 Method / §2 Pattern re-rank confirmation / §3 API-billing companion validation / §4 per-mission-class N/A / §5 Threshold rule / §6 ADR-022 substrate-naming / §7 Forward refs / §8 Acceptance scorecard) |
| 9 | `node.aDNA/what/context/token_baselines.md` v0.1.2 → v0.1.3 (or +M24 addendum); companion `.yaml` + `inventory_vaults.yaml` row bumped | ✅ PASS (v0.1.3 published at this S3 with NEW §8 heat-map summary + NEW Appendix B per-directory AGENTS.md invariants spec + §4 pattern β HOLD ratification update + revision history v0.1.3 row + frontmatter context_version bumped; companion `.yaml` content_entity.version + revision.current_version + v0_1_3 revision row appended; `inventory_vaults.yaml` token_baselines content_entities row version-bumped + v0_1_3 lineage field added) |
| 10 | Campaign master M2.4 row `in_progress → completed`; ADR-007 roadmap updated; amendments-table entry appended | ✅ PASS (M2.4 row Status `in_progress → completed` at this S3 with S3 inline summary; ADR-022 roadmap row final state; amendments-table 2026-05-20 entry appended at S3 per M2.1/M2.2/M2.3 precedent) |
| 11 | STATE.md router refresh per Op 3 archive-on-close 6th canonical instance (≤ 20 kT router cap) | ✅ PASS (As-of header updated to M2.4 CLOSED + P2 → P3 phase exit gate READY; M2.4 S2 LANDED bullet demoted to concise form per Op 3 6th instance; M2.4 S1 OPENED bullet stays concise; Next Steps refreshed; Last Session block + Next Session Prompt updated; ≤ 20 kT cap maintained per `wc -c` self-check at S3 close) |
| 12 | Hard constraints honored end-to-end (zero `.adna/` touches; zero partner-vault contact; zero `~/.claude/settings.json` mods; zero SQLite mutations via `-readonly`; zero hook mods; zero schema migrations; each session declares `token_budget_estimated` two-metric; estimate-vs-actual within band) | ✅ PASS (all 11 hard constraints honored end-to-end per S1 + S2 + S3 SITREPs; zero `.adna/` touches via `git -C .adna status` clean; zero partner-vault contact via 4 embargo memos preserved; zero settings.json mods via `stat` mtime unchanged; zero SQLite mutations via sqlite3 -readonly throughout; zero hook mods; zero schema migrations beyond M1.4 v0.1.1 baseline; M2.4 frontmatter declares two-metric `token_budget_estimated`; estimate-vs-actual within band at all 3 sessions per token-budget table below) |

**12/12 PASS.**

---

## Standing-Order discharge table (M2.4 mission spec §Standing-Order Discharge Plan — 14 rows)

| # | Standing Order | M2.4 outcome |
|---|---|---|
| 1 (Project) | Phase gates are human gates | ✅ S1 + S2 + S3 entries all operator-gated; ADR-022 elevation operator-gated at S2; P2 → P3 phase exit gate stays open per Standing Order #19 — M2.4 close opens the gate but operator must explicitly authorize transition |
| 2 (Project) | Destructive actions require confirmation | ✅ S2 destructive entry (β verdict + ADR-022 + AGENTS.md audit artifact) operator-confirmed at S2 plan ratification; mid-S2 slot-reassignment chain re-entry via plan file (post-account-upgrade) operator-confirmed; S3 non-destructive consolidation — no separate confirmation needed beyond plan ratification |
| 3 (Project) | Context budget is doctrine | ✅ M2.4 frontmatter declares `token_budget_estimated` two-metric per S1/S2/S3; estimate-vs-actual reported in all 3 session SITREPs + AAR token-budget table; drift > 2× retrospective trigger NOT met at any session |
| 4 (Project) | Local context over global | ✅ `how/campaigns/.../CLAUDE.md` auto-loaded at session entry; M2.3 mission spec consulted as template; ADR-016 + ADR-022 + `m24_obj4` §3 (invariants spec) all referenced directly in S3 work |
| 5 (Project) | Every mission gets an AAR | ✅ this artifact (lightweight 5-line + 4-category extended findings; `load_bearing: true`; primary + strong-extended findings both tagged) |
| 6 (Project) | Archive, never delete | ✅ 3 session files (S1 + S2 + S3) move `active/` → `history/2026-05/` at S3 commit (preserves audit trail); m24_obj2/3/4 + ADR-022 + AAR + obj7 all preserved verbatim; mission spec frontmatter + §Status updates surgical (no body deletes) |
| 7 (Project) | Dual audience test | ✅ M2.4 mission spec + all 5 deliverable artifacts (m24_obj2 + m24_obj3 + m24_obj4 + ADR-022 + this AAR + m24_obj7) authored dual-audience legible: developer formal protocol (SQL + threshold tables + invariant matrices + ADR clauses) + newcomer plain-language entries (5-line summary + worked examples + cross-corpus arcs explained in narrative form) |
| 8 (Project) | Self-reference mandatory | ✅ M2.4 ratifies discipline (β + invariants + ADR-022 dispatch contract + slot-reassignment doctrine) IT applies to itself — Standing Order #8 self-reference **7th tactical invocation in v8 P2** at this AAR (M2.2 + M1.5 + M2.3 + M2.3.5 + M2.4-S1 + M2.4-S2 + M2.4-S3 = 7 instances); pattern now ratified across both planning-class single-session shape AND implementation-class 3-session shape at all 3 levels of execution (campaign / mission / session) |
| 9 (Project) | Upstream spec source of truth | ✅ ADR-016 + ADR-017 + ADR-022 referenced verbatim; no spec contradictions; `network_` namespace honored read-only; v7.0 frozen at `LatticeProtocol/aDNA@27e6395` — `git -C .adna status` clean at S3 close |
| 10 (Project) | Cross-link aggressively | ✅ M2.4 mission spec + AAR + obj7 + ADR-022 + token_baselines.md v0.1.3 all wikilink to ≥ 2 related files (M2.3 + M2.3.5 + ADR-016 + M1.3 + node.aDNA token_baselines.md + each-other); AAR cross-references 15 related artifacts in frontmatter |
| 11 (Project, refined M2.3 S2) | Per-mission context budget two-metric | ✅ M2.4 frontmatter declares both content-load kT + API-billing cache_read M + turn count; this AAR reports both at token-budget table below; drift > 2× retrospective trigger NOT met at any session |
| 12 (Campaign) | Per-mission context budget | ✅ discharged via Project SO #11 above |
| 13 (Campaign) | Cross-vault coord memo preservation | ✅ no new coord memos at M2.4 (audit-only scope; no partner contact per HC #2); existing coord memos preserved verbatim |
| 14 (Campaign) | ADR ratification gated at phase entries | ✅ ADR-022 elevation operates within ADR-007 → ADR-019 → ADR-022 reassignment chain (mid-phase via Campaign SO #14 *exception* for load-bearing decisions that block ADR-022 dispatch contract for M3.x; explicit operator-override path at D2=A Rosetta-default at S2 plan ratification); pattern: pre-scheduled ratification within mid-phase exception scope ≠ new override invocation |

**14/14 discharges complete.**

---

## Token-budget table (per Campaign Standing Order #12 + Project Standing Order #11 — estimate vs actual; two-metric reporting per ratified Clause C)

| Session | Estimated (kT content-load) | Actual content-load self-report | API-billing forecast (Clause C ratified) | API-billing actual | Notes |
|---|---|---|---|---|---|
| S1 | 60-100 | ~ 70-90 (within band) | `cache_creation ≈ 328 K + 75 × 1 K ≈ 403 K`; `cache_read ≈ 4.1 M + 75 × 126 K ≈ 13.6 M` (forecast for ~75-turn S1 spec-authoring + heat-map queries + Mermaid digraph) | TBD post-ingest at next `~/.adna/measurement/ingest_transcript.py --all` run | Mission spec authoring + heat-map query suite construction + Q1-Q4 SQL execution + Mermaid validation; non-destructive |
| S2 | 90-150 | ~ 110-140 (within band; investigation overhead ~ 20-30 turns from slot-reassignment chain remediation noted but did not breach 2× drift) | `cache_creation ≈ 328 K + 130 × 1 K ≈ 458 K`; `cache_read ≈ 4.1 M + 130 × 126 K ≈ 20.5 M` (forecast for ~120-160-turn S2 destructive governance entry; slightly above estimate band but acceptable) | TBD post-ingest | β verdict + AGENTS.md audit + ADR-022 elevation with slot-reassignment chain investigation + mid-S2 plan-mode re-entry post-account-upgrade; destructive governance synthesis; **slot-reassignment chain investigation added ~20-30 turns overhead** vs single-collision plan path (calibration data for M3.x — doctrinal signal that ADR slot-availability verification SHOULD precede ADR drafting per the campaign-level load-bearing finding) |
| S3 (this session) | 60-90 | actual self-report at S3 close in session SITREP (estimated ~ 65-85 based on consolidation-class precedent from M2.3 S3 ~ 65-80 actual) | `cache_creation ≈ 328 K + 75 × 1 K ≈ 403 K`; `cache_read ≈ 4.1 M + 75 × 126 K ≈ 13.6 M` (forecast for ~ 75-turn S3 consolidation) | TBD post-ingest | AAR (this artifact) + m24_obj7 8-section validation + node.aDNA token_baselines.md v0.1.3 with NEW §8 heat-map summary + NEW Appendix B per-directory AGENTS.md invariants spec + companion `.yaml` + inventory_vaults.yaml row + mission file close + campaign master close + STATE.md router refresh (Op 3 6th canonical instance) + session moves; non-destructive consolidation |

**Estimate-vs-actual delta**: within 1× band for S1 and S3; S2 within 1× band with investigation overhead noted (within 2× drift threshold; no retrospective triggered). Implementation-class S1/S2/S3 calibration model validated at **5th canonical-shape instance** (M1.3 = 1st; M1.4 = 2nd; M2.1 = 3rd; M2.3 = 4th; M2.4 = 5th — pattern fully ratified per Campaign S.O. #17).

**API-billing companion actual**: will populate at next `ingest_transcript.py --all` post-S3 (~ 1-session lag); first post-M2.4 mission entry will report M2.4 S1+S2+S3 actuals as the second-cohort post-Clause-C-ratification corpus data points after M2.3's first-cohort; if actual cache_read on any of S1/S2/S3 deviates > 2× from forecast, next M2.3-class retrospective (post-M3.x) dispatches refresh of Clause C constants.

---

## Load-bearing finding propagation map (BOTH findings per S2 Next Session Prompt explicit instruction)

### Primary load-bearing finding: "AGENTS.md is UNDER-LOADED, not re-read-wasted"

Propagates upward through the campaign architecture:

- **`m24_obj2_heatmap_query_suite.md` §3 + §5** carries the original finding (86% never read; touched files 0% re-read; pattern β waste lives downstream NOT in routing layer).
- **`m24_obj4_agents_md_hardening_audit.md` §6 + §7** propagates the re-frame as the *measurement-cycle contract* — hardening goal becomes "increase discoverability so the downstream re-read cycle shrinks", not "fix waste"; M2.4.5 / M3.1 absorption decision must include a measurement cycle after the bulk-edit (re-run Q2 from Obj 2 at ≥ 20 sessions to verify AGENTS.md frequency rises AND deep-content re-read falls).
- **`node.aDNA/what/context/token_baselines.md` v0.1.3 §8 (NEW heat-map summary section) + Appendix B (7-item per-directory invariants spec)** carries the empirical finding + the invariants spec into Hestia's canonical home for cross-vault propagation; future operators reading the routing-layer doctrine consult the node.aDNA file for the freshest empirical bands + invariants codification.
- **Campaign master M2.4 row + amendments-table entry** records the under-use re-frame as a load-bearing finding of M2.4 (campaign-level audit trail).
- **STATE.md Next Steps + Next Session Prompt** surfaces the M2.4.5 OR M3.1 absorption decision with the discoverability framing as primary; operator decides at P2 → P3 entry which dispatch slot owns the bulk-edit.
- **v8 P6 ecosystem propagation queue** inherits the AGENTS.md 7-item invariants spec (Appendix B → `.adna/AGENTS.md` template propagation at v8.0 tag firing per Campaign Standing Order #14 + ADR-005 rule 3); 19+ ecosystem vaults adopt the per-directory invariants framework at v8.0 tag — every Lattice vault's AGENTS.md files become the first-stop discoverability layer.
- **M3.x forge-ecosystem cohort** inherits the invariants spec; forge missions apply the per-directory invariants to their own AGENTS.md files; SiteForge + CanvasForge + ComfyForge + VideoForge + MoleculeForge + SpeechForge each pull-through the discoverability hardening as part of their forge-ecosystem propagation work.

### Strong-extended load-bearing finding: "Two-step ADR slot reassignment chain ADR-007 → ADR-019 → ADR-022"

Propagates as doctrinal signal:

- **ADR-022 §Status + `slot_reassignment_history` frontmatter block** records the full chain at the load-bearing artifact (the ADR itself).
- **M2.4 mission spec frontmatter `adr_007_slot_reassigned_to: ADR-022`** + Obj 5 §Status documents the chain at the mission-level audit trail.
- **S2 session activity log + S2 SITREP** capture the mid-session discovery sequence (1st collision → ADR-019 attempt → 2nd collision → final ADR-022) verbatim.
- **Future ADR drafting in v8 P6 + M3.x**: lifecycle protocol — at ADR draft time, check (a) campaign master ADR roadmap (forecast reservations) + (b) `ls what/decisions/` (existing files including non-forecast-reserved from prior campaigns). Mid-session reassignment acceptable per M1.5 precedent but adds ~ 20-30 turns of remediation overhead vs original-plan single-pick path.
- **Skill graduation candidate `skill_adr_drafting.md`** at ≥ 3 distinct instances. Currently 2 (M1.5 single-step + M2.4 two-step); v8 P6 may surface 3rd instance triggering graduation. The pattern is generalizable across vaults — any vault with active campaigns + forecast ADR roadmaps + existing accepted ADRs faces the same collision risk.
- **Backlog placeholder candidate** at v8 P6 ecosystem propagation: `idea_upstream_adr_drafting_protocol.md` (operator-discretionary at next M2.4.5/M3.1 dispatch decision; would seed graduation work for the eventual `skill_adr_drafting.md`).

### Pattern across 7 self-reference instances at v8 P2 (Standing Order #8 invocation summary)

| # | Mission | Self-reference type |
|---|---|---|
| 1 | M2.2 | Declared `token_budget_estimated` per the very ADR Clause A it ratified |
| 2 | M1.5 | ADR-017 ratified `network_` namespace inside aDNA.aDNA's own coord memo discharge; ADR slot forward-only reassignment precedent (extended at M2.4 to two-step chain) |
| 3 | M2.3 | Calibration formula ratified on the very session it was designed to predict |
| 4 | M2.3.5 | Push-readiness gate gated its own push |
| 5 | M2.4 S1 | AGENTS.md hardening measurement applied to AGENTS.md files in this campaign directory |
| 6 | M2.4 S2 | Pattern β verdict ratified on the very SQLite store the analysis-dispatch contract (Clause C) ratifies access to + slot-reassignment chain doctrine applied to a fresh slot-reassignment event in this very session |
| 7 | **M2.4 S3 (this AAR)** | AAR consolidating the under-use re-frame finding IS being written for the very AGENTS.md routing layer the re-frame names as the target of the M2.4.5/M3.1 bulk-edit; this AAR is itself one of the artifacts the routing layer should help future agents discover |

The recursion is the meta-lesson: a self-referential vault doctrine-checks itself in flight. Future doctrine-compression sessions can plan around the recursive footprint — *design the doctrine to incorporate its own session's measurement at ratification time*, and *design the AAR to fold into the routing layer the AAR concludes is under-loaded*.

---

## Forward references (cross-link to obj7 §7 to avoid duplication)

See `m24_obj7_validation_output.md` §7 — M2.4 close opens P2 → P3 phase exit gate (operator-decision per Campaign Standing Order #19); M2.4.5 OR M3.1 absorption owns AGENTS.md bulk-edit dispatch (operator decides at P2 → P3 entry); M2.1.5 retroactive Op 3 stays planned-optional; v8 P6 ecosystem propagation inherits 5 doctrinal additions (AGENTS.md invariants spec + ADR-022 + ADR-016 amendment + Project SO #11 refinement + Heavy-File Read Convention); M3.x forge-ecosystem missions inherit two-metric budget discipline + 3 M2.3.5 templates + 7-item AGENTS.md invariants spec + ADR-022 dispatch contract.

---

## Cross-references

- [[../mission_adna_str_p2_m24_agents_md_heatmap.md|M2.4 mission spec]]
- [[m24_obj2_heatmap_query_suite.md|m24_obj2_heatmap_query_suite.md]] (S1 — 4 SQL queries + Mermaid digraph + AGENTS.md under-loaded finding)
- [[m24_obj3_pattern_beta_final_verdict.md|m24_obj3_pattern_beta_final_verdict.md]] (S2 — HOLD pattern β at rank 14 ratified; 27.44% / CV 0.353 / r=0.142)
- [[m24_obj4_agents_md_hardening_audit.md|m24_obj4_agents_md_hardening_audit.md]] (S2 — 44-row inventory + 7-item invariants spec + top-12 priority list + M2.4.5/M3.1 dispatch)
- [[m24_obj7_validation_output.md|m24_obj7_validation_output.md]] (S3 — final consolidated 8-section output)
- [[../../../../what/decisions/adr_022_tool_use_logging.md|adr_022_tool_use_logging.md]] (S2 — `status: accepted` with 3 clauses A/B/C; slot reassigned from ADR-007 → ADR-019 → ADR-022)
- [[../../../../what/decisions/adr_016_per_mission_context_budget.md|adr_016_per_mission_context_budget.md]] (M2.3 LIVE; Clause A two-metric + Clause C ratified empirical constants + Appendix A per-mission-type calibration; M2.4 inherits)
- [[../../../../CLAUDE.md|aDNA.aDNA/CLAUDE.md]] §Standing Order #11 (refined at M2.3 S2)
- [[../../../../../node.aDNA/what/context/token_baselines.md|node.aDNA token_baselines.md]] v0.1.3 (post-S3 fold-in with NEW §8 heat-map summary + NEW Appendix B per-directory AGENTS.md invariants spec)
- [[../campaign_adna_serious_tool_readiness.md|campaign master]] M2.4 row + ADR-022 roadmap + amendments-table entry (post-S3)
- [[aar_m13_token_audit.md|aar_m13_token_audit.md]] (M1.3 — 1st canonical 3-session implementation-class instance + AGENTS.md heat-map sketch deferred to M2.4 — now closed)
- [[aar_m14_latticescope_schema.md|aar_m14_latticescope_schema.md]] (M1.4 — 2nd canonical instance + measurement substrate for M2.4 corpus)
- [[aar_m21_context_audit_split.md|aar_m21_context_audit_split.md]] (M2.1 — 3rd canonical instance + Op 3 archive-on-close pattern 1st canonical instance)
- [[aar_m22_per_mission_budget.md|aar_m22_per_mission_budget.md]] (M2.2 — planning-class self-reference 1st instance)
- [[aar_m15_coord_network.md|aar_m15_coord_network.md]] (M1.5 — planning-class self-reference 2nd instance + ADR slot forward-only reassignment precedent extended at M2.4 to two-step chain)
- [[aar_m23_convergence_validation.md|aar_m23_convergence_validation.md]] (M2.3 — 4th canonical instance; this AAR inherits full shape per mission spec)
- [[aar_m23_5_push_readiness_review.md|aar_m23_5_push_readiness_review.md]] (M2.3.5 — push-readiness gate gated its own push; 3 templates inherited by M2.4 — per-vault blast-radius matrix at Obj 4 §2 44-row inventory)
- `~/.adna/measurement/measurement.sqlite` v0.1.1 (live-hook subset — 12 sessions / 1179 tool_calls / 107 context_traversal at M2.4 S3 close)
- `~/.adna/measurement/reports/session_*.json` (49 per-session aggregates — used at M2.3 corpus; M2.4 reads informationally for context-graph traversal)
