---
type: artifact
artifact_class: coordination_ledger
campaign_id: campaign_operation_adna
title: "Operation aDNA — cross-vault seam ledger"
created: 2026-06-18
updated: 2026-06-24
status: active
last_edited_by: agent_rosetta
tags: [coordination_ledger, program, cross_vault, seams, keystone, pt19]
---

# Coordination Ledger — Operation aDNA

> The single registry of **cross-vault seams** the program tracks. Each child campaign depends on work
> owned by *other vaults*; a seam is where that ownership hands off. The program does not do the work on
> the far side of a seam — it tracks the seam, names its owner, and records the handshake that closes it.
> When a seam moves, update **here** (not a child's file). Keystone-relevant seams (★) are preconditions
> of the coordinated public launch ([[campaign_operation_adna]] §Program gates, DP2).

> **★ KEYSTONE LAUNCHED ✅ 2026-06-24** — DP2 fired; `adna.network` is live with the full improved site,
> landed joined-up with the shipped Hearthstone v8.0 base. The five ★ keystone-precondition seams below are
> **CLOSED → shipped**. Remaining open seams (non-keystone): the doctrine-relocation carve-out + vault-card
> public-fields. Detail: [[dp2_keystone_launch_runbook]] §2 launch-complete snapshot + [[campaign_operation_adna]] DP2.

## Seams

| Seam | Owner (persona / vault) | What it gates | Status | The handshake needed |
|------|-------------------------|---------------|--------|----------------------|
| **★ pt19 — vault-data regen** | Hestia / `Home.aDNA` (Operation Production Tidy) | Track A data-coupled units (`/vaults` · `/network` · `/vaults/graph` vault-name correctness, home `NetworkDiagram` hardcodes); a keystone precondition (site must show keeper-set names the day it launches) | **registered, waiting** ([[coord_2026_06_18_wadna_pt19_dependency]]; pt19 = Production Tidy P7) — **now the headline remaining keystone gate** (2026-06-22: Hearthstone v8.0 shipped ✓; WEBSITE closed ✓; C-1 stage-2 staged ✓; Tier-2 v2.3 port ✓). **2026-06-22 verify-park:** pt19 **confirmed NOT landed** against `Home.aDNA/STATE.md` (~1wk out, behind pt17/Network P6); staged payload re-verified green (163/281) & **parked at the keystone gate** — still awaiting the pt19 ping. **2026-06-22b verify-hold+tidy:** re-confirmed pt19 still `pending` at source (`campaign_production_tidy.md` P7 row; the day's `inventory_vaults.yaml` touches = Free Harbor origin-recordings, not pt19); payload re-verified green (163 / **281/281**); still parked. **2026-06-23 — pt19 LANDED ✅ + LAUNCH PREP EXECUTED:** a fresh source read found pt19 done (PT charter P7 + Home STATE, commits `d00c401`→`6d584c2`); operator chose launch-prep-hold-at-GO. Ran runbook §2 steps 1-6: a duplicate `note:` key in Home `inventory_vaults.yaml` (Molecules.aDNA) blocked the strict-parse regen → operator-authorized source fix (committed in Home's repo, [[coord_2026_06_23_rosetta_to_hestia_inventory_dupkey_fix]]); regen clean (**54 vaults**); keeper-set names verified CLEAN; **edge regression 17→8 fixed** (propagated the PT rename cascade through `network_edges.yaml`, restored 8→**14**) + repointed 2 hardcoded homepage slugs; build **177**/0 err (new baseline, +14 vault-detail) + gates **281/281**. **✅ LAUNCHED + CLOSED 2026-06-24** — operator gave DP2 GO; the committed pt19 regen (54 vaults, keeper-set names) shipped as-is to `adna.network` (honored — no `sync:vaults` at deploy); live-verified **0/12 pre-rename slugs**. **pt19 seam CLOSED** | When the coordinated regen lands (inventory_vaults.yaml reconciled to keeper-set + card overlays), **pt19 pings WEBSITE.aDNA** → site re-runs `build_vaults_data.mjs`, verifies axis-J names, unblocks the sequenced units. **No `sync:vaults`, no hand-edit before then.** |
| **★ skill_template_release / aDNALabs** | Berthier / `aDNALabs.aDNA` | Track B's outward release — Hearthstone P5 ships the v8.0 base image to `.adna/` + public `aDNA-Network/aDNA` *through* this gate-fired skill (Standing Rule 1: never hand-edit `.adna/`); a keystone precondition | **✅ v8.0 SHIPPED 2026-06-19** — `skill_template_release` pushed `v8.0` @ `adae20c`; `.adna/` synced @ `ad72979`; 13/13 post-push smoke GREEN. **Keystone condition #2 = GREEN.** | Closed: Hearthstone P5 invoked `skill_template_release` (SCOPE C); operator approved the public push (Hearthstone DP5); the Hearthstone campaign is **completed**. v8.0 coordinated with STR's v8.0 (no double-bump). |
| **Lab ADR-006 bootstrap-offer** | Galileo / `Lab.aDNA` | The Step-0 router "offer to bootstrap" experience — fold the Lab ADR-006 bootstrap-offer into Hearthstone's Step-0 "offer to bootstrap Home" flow so the two offers compose rather than collide | **resolved** — folded into the Hearthstone P2 Step-0 router (Lab ADR-006 D2 opt-in offer; 2026-06-19, concurrent session) | Closed: Hearthstone P2 added the Step-0 "offer to bootstrap Home" flow with the Lab opt-in offer folded in — one coherent bootstrap-offer surface. |
| **CanvasForge → Canvas repoint** | Mondrian / `Canvas.aDNA` | Hearthstone's exemplar bundle (`template_node_adna_exemplar/`) — it still references the merged-away **CanvasForge** (→ Canvas.aDNA at PT pt09, 2026-06-17); the topology/gallery exemplar must resolve to Canvas.aDNA before release | **resolved** — Hearthstone P3 repointed the exemplar → Canvas.aDNA + documented the `canvas_std` prereq (commit `3d0c3f9`; Decision 3 = optional-degrade, 2026-06-19) | Closed at Hearthstone P3 exit (operator Decision 4). |
| **Network / W-NET federation** | Venus / `Network.aDNA` | Track D's full social/federation layer (profiles, follows, cross-node feeds, membership/federation model); the site **builds toward** it, does not ship it before the substrate exists | **deferred** (Venus-gated; gated at PT P5.5 / Network P6) | Track-D MVP (public-good showcase + "connect to a subnetwork" affordances + the registry as first social surface) ships on `vaults.json` + federation_refs *now*; the full layer is co-designed with Venus when the membership substrate lands. See [[narrative_ethos_public_good]] §5. |
| **doctrine-relocation** | Hestia / `Home.aDNA` | The 4 workspace doctrines now home at `aDNA.aDNA/what/doctrine/` — cross-vault sweep done (19 pointers / 13 vaults; ref-sweep 0 broken) | **acked; one carve-out pending** ([[coord_2026_06_14_hestia_to_rosetta_doctrine_relocation]]) | Doctrines' new home **acked**. Carve-out: `Context.aDNA` was mid-session at sweep time — repoint its `adr_010` → `aDNA.aDNA/what/doctrine/doctrine_credential_handling.md` **when Context.aDNA (Prometheus) next quiesces**. Deferred-pending-Context-quiesce; non-blocking. |
| **vault-card public-fields** | Rosetta → Hestia / `Home.aDNA` | The public projection of vault cards (Harness display split → "Harness" with RareHarness as flagship; per-vault public `tagline`); ties to pt19 (both are Home vault-card edits the site regen consumes) | **open; awaiting Hestia ack** ([[coord_2026_06_10_rosetta_to_hestia_vault_card_public_fields]]) | On Hestia's edits landing in `inventory_vaults.yaml` / cards: ping Rosetta → site regen + commit → rides the next deploy. **Reconcile with pt19** — both are Home-side vault-card edits; fold the regen into the single pt19 handshake rather than a separate `sync:vaults`. |
| **★ aDNA.aDNA public inspectability (C-1)** | operator · Berthier / `aDNALabs.aDNA` | WEBSITE **C-1** — the site's "real, inspectable vault" proof-links have **no public URL to resolve to**: `aDNA-Network/aDNA.aDNA` → 404 (private), the public image `aDNA-Network/aDNA` → 200 but gitignores `*.aDNA/` (subtree → 404). A keystone precondition — proof-links must resolve the day the site launches (keystone condition #1). | **stage-1 SHIPPED — D1 deployed-live to adna.network 2026-06-20** (repoint to resolving public-image `.adna/` paths; 5/5 proof-links live-200; operator declined publishing `aDNA.aDNA`). **Stage-2 STAGED commit-only 2026-06-22** (session `…_keystone_c1_stage2`): proofs upgraded fixed→exemplary (+ `template_home_claude.md` + the exemplar bundle, both live-200 in the public repo) and the `14→16` entity-count reconciled site-wide (16 files); build 163 / gates 281/281. **✅ SHIPPED + CLOSED 2026-06-24** at the keystone (DP2) — all 7 proof-links live-**200-unauth** against `aDNA-Network/aDNA`. *(WEBSITE.aDNA closed 2026-06-21, Decision 6 — stage-2 was program-owned.)* **C-1 seam CLOSED.** | **Stage-1 (D1, now):** repoint proofs at resolving paths in the public image `aDNA-Network/aDNA` (`install_truth.json.verified_paths`: root `CLAUDE.md`, `how/skills/skill_onboarding.md`, `skill_project_fork.md`, `how/templates/template_workspace_claude.md`); **reconcile the prose** so the "inspectable" claim stays literally true (links point at the standard template image, not the private dev vault); G6 link-check enforces 200-unauth. **Stage-2 (keystone):** upgrade proofs to point at Hearthstone's published polished base ([[campaign_hearthstone]]) — *fixed → exemplary*. NB: `aDNA.aDNA` stays private; this seam closes without a visibility change. |
| **★ WEBSITE improved-site deploy (keystone launch payload)** | Rosetta · operator / `aDNA.aDNA/site/` | The full improved site — D2–D4 + cycles c162–c165 — is **committed not deployed** (only D1 is live); the keystone joined launch ships it all at once. A keystone deliverable now that [[campaign_website_adna]] has **closed** (2026-06-21, Decision 6). | **✅ SHIPPED LIVE + CLOSED 2026-06-24** — `vercel --prebuilt --prod` deployed the full improved site to `adna.network` (`dpl_AYKPbywF`); `/commons` 200 (was 404); live Lighthouse **BP 92→100** (Perf 98 · A11y 100 · SEO 100); gates 281/281; `origin/main` pushed + standing-watch active. **Improved-site-deploy seam CLOSED.** | **At keystone (DP2):** `vercel --prebuilt --prod` ships the full improved site + clears `/commons` 404 + live BP 92→100 + MENU-1 + C-1 stage-2 + the 14→16 / v2.3 entity-count currency reconciliation (staged 2026-06-22) + the Tier-2 spec-mirror v2.3 port, then run the on-live cold-3sec verdict. Residual register = [[CAMPAIGN-REPORT]] §residual backlog. Honor pt19 (verify vault-name correctness after the regen) at deploy. |
| **★ standard-currency / spec-mirror v2.3 port** | Rosetta / `aDNA.aDNA/site/` | The site must read **v2.3 / 16 types** the day it launches (keystone condition #1, axis-J). Surfaced by the 2026-06-22 claims-traceable pass: the normative spec mirror `/reference/specification` + ~8 prose version footers still cite v2.2. | **✅ DONE + SHIPPED LIVE** (in-vault DONE 2026-06-22; deployed to `adna.network` at DP2 2026-06-24) (session `…_tier2_v23_specmirror`) — ported `specification.mdx` (version strings + §5.1 `inventory` / §5.2 `identity` rows + ERD) + flipped the 8 prose footers (`open-standard` ×2, `design-rationale`, 5 `adna-vs-*`); left `governance-model` illustrative examples. Build 163 / gates **281/281**. **Finding:** the v2.3 §5 prose did **not** exist to mirror — the dev standard was still v2.2 and the released `.adna/` carried only a title-bump (§5 body un-ported) — so the §5 rows were **authored** (grounded in ADR-035) and the **dev standard `what/docs/adna_standard.md` was also ported to v2.3** (operator scope B). | Done — site reads coherent **v2.3 / 16** the day it launches; deploys with the payload at DP2. **Carry:** the public `aDNA-Network/aDNA` image's `adna_standard.md` §5 body still shows 14 types → closes at the next gated `skill_template_release` (the dev-standard delta is now staged for that Hearthstone "catch-up release"). **Now ASSEMBLED + SHIPPED as the combined v8.1** (2026-06-23): folded with the Cornerstone Obsidian parity + fork-reseed fix into one release ([[adr_038_combined_v81_release]]) — assembled + tagged `v8.1` at `/tmp/adna_v81_release` (commit `32b3793`; verified green), **shipped — operator pushed it LIVE (public `aDNA-Network/aDNA` main `32b3793`, tag `v8.1`); ship-now, decoupled** from the keystone (ADR-038 D5). Detail: [[keystone_credibility_traceability_20260622]] §Tier-2 + [[adr_038_combined_v81_release]]. |

★ = keystone precondition (DP2).

## Post-launch seam triage (2026-06-24)

With the keystone shipped, the **two remaining non-keystone seams** were re-triaged
(`session_stanley_20260625T013000Z_post_launch_consolidation`). Both stay **open + non-blocking** — neither
gates DP4 (which is gated on Track C/STR + Track D/commons terminalizing, not on these): *[Annotation 2026-07-03, Champollion M6.2: superseded-by-events — Track C reached terminal 2026-07-01 (reopen-reconciliation closeout) and the program doc's DP4-readiness note (2026-07-01) gates DP4 only on Track D; Track D discharged READY by [[dp4_dossier]] §2. Line kept as historical record.]*

- **vault-card public-fields** — re-confirmed **open, awaiting Hestia ack** (memo still `status: open`, no ack
  on record). The pt19-sequencing constraint that previously bound this ("fold into the single pt19 handshake;
  no `sync:vaults` before pt19") is now **lifted** — pt19 landed 2026-06-23 and the launch shipped — so when
  Hestia lands the public-field edits (Harness display split → "Harness" / RareHarness-flagship; per-vault public
  `tagline`) and pings Rosetta, the regen is a **clean standalone future deploy**, no longer entangled with pt19.
  *(Live `/vaults` already shows keeper-set "Harness" / "RareHarness" from the pt19 regen; the per-vault public
  taglines remain the un-acked delta.)*
- **doctrine-relocation carve-out** — re-confirmed **deferred-pending-Prometheus-quiesce, non-blocking** (memo
  `status: acknowledged`; the single open item is the Context.aDNA `adr_010` → `aDNA.aDNA/.../doctrine_credential_handling.md`
  repoint, to be done when Context.aDNA next quiesces). Unchanged; re-check at the next Context.aDNA quiesce.

> **Standing-watch (in-vault, not a cross-vault seam — recorded here for the launch-readiness picture): FIXED 2026-06-24.**
> The CI floor had **never run green** — every run hung ~30 min in `playwright install --with-deps` (apt phase) and was
> killed before the build/tests ran. **Fixed** by running the job in the official Playwright container
> `mcr.microsoft.com/playwright:v1.59.1-noble` (browsers + deps preinstalled; no install step) — **first green run 3m42s**
> (`4a10458`). The initial fast/audit shard was a misdiagnosis (the whole suite runs in ~3.5 min once the browsers are
> present), reverted to a single job. Site was live-green throughout. Tracked in `STATE.md` §Active Campaigns.

## Open memos — `who/coordination/` (program-relevant), with disposition

| Memo | Disposition |
|------|-------------|
| [[coord_2026_06_18_wadna_pt19_dependency]] | **pt19 LANDED 2026-06-23 + launch prep executed** — the pt19-lands signal arrived (detected at source); site regen + axis-J keeper-set verify + the `network_edges.yaml` rename-cascade fix done **commit-only**. See the pt19 seam (★). |
| [[coord_2026_06_23_rosetta_to_hestia_inventory_dupkey_fix]] | **Open / outbound (FYI)** — operator-authorized cross-vault fix: removed a duplicate `note:` key in Home `inventory_vaults.yaml` (Molecules.aDNA) that blocked the keystone strict-parse regen; committed in Home's repo. No action needed; optional upstream = a dup-key guard in Home's generators / health check. |
| [[coord_2026_06_18_hestia_to_rosetta_hearthstone_charter_handoff]] | **Open / handoff** — Hearthstone chartered by Hestia, handed to Rosetta for P0 execution. Disposition: Track B activation (its own DP1); the program tracks it as Track B. |
| [[coord_2026_06_14_hestia_to_rosetta_doctrine_relocation]] | **Acked, one carve-out open** — doctrines' new home acked; Context.aDNA `adr_010` repoint deferred-pending-Prometheus-quiesce. Tracked as the doctrine-relocation seam. |
| [[coord_2026_06_10_rosetta_to_hestia_vault_card_public_fields]] | **Open / outbound** — awaiting Hestia ack + vault-card edits + regen handshake. Tracked as the vault-card public-fields seam; fold its regen into the pt19 handshake. |
| [[coord_2026_06_22_rosetta_to_berthier_f1_catchup_release]] | **Folded into the combined v8.1 — ASSEMBLED + SHIPPED** — F1 §5 body-completion is now combined with the Cornerstone Obsidian parity + fork-reseed fix into one **v8.1** release ([[adr_038_combined_v81_release]]). **Assembled + tagged `v8.1`** at `/tmp/adna_v81_release` (commit `32b3793`; verified green — HC 0-warn, F1 4/4, gitignore-safe, clone+fork smoke) 2026-06-23, **shipped — operator pushed it LIVE (public `aDNA-Network/aDNA` main `32b3793`, tag `v8.1`)** (the push *is* the release). **Ship-now, decoupled** from the keystone (ADR-038 D5 supersedes the launch-coupling recommendation); drive consolidated under the operator (ADR-038 D7). Under the ★ standard-currency + ★ skill_template_release seams. |

> Other `who/coordination/` memos (the v7 fan-out, SiteForge-interactive-subsystem genesis, Noether/LP
> check-ins, etc.) are **not program-relevant** and stay in their own lanes; this ledger carries only the
> seams Operation aDNA's tracks depend on. New cross-vault seams are added here as they surface.
