---
type: home_page
cssclasses: [dashboard, homepage]
node: {{node_hostname}}
operator: {{operator}}
persona: {{persona}}
governance: CLAUDE.md
data_source: what/inventory/inventory_vaults.yaml
vault_count: {{vault_count}}
healthy_count: {{healthy_count}}
drift_count: {{drift_count}}
blocked_count: {{blocked_count}}
last_full_health_check: {{last_health_check}}
feature: who/assets/banners/{{banner_image}}
updated: {{interview_date}}
last_edited_by: agent_{{persona_lower}}
tags: [home, lattice_home, gallery, {{persona_lower}}, node_adna, exemplar]
---

<!-- ════════════════════════════════════════════════════════════════════════
  EXEMPLAR HOME — premium themed superset of the base `.adna/HOME.md`, elevated to the
  Operation-Prytaneion bar (landing layer + dashboard band + Bases gallery + disclosure folds).
  Materialized by skill_node_bootstrap_interview.md Step 9 (or skill_project_fork --exemplar-home);
  every placeholder is catalogued in ./SUBSTITUTIONS.md (referenced here by bare name to keep this
  comment free of live placeholders). The base inventory vars (node_hostname, operator, persona,
  vaults_table, named_projects_table, vault_count, …) are SHARED with `.adna/HOME.md` so the Step-9
  substitution fills both identically. The theming vars (banner_image, persona_greeting, the
  accent-hex trio, the count trio) are exemplar-only.

  STRUCTURE (resting view ≈1.5 viewports; everything else one click down):
    banner → landing strip + adna.network CTA + marketplace link → dashboard band (5-second status)
    → identity line → greeting → §Gallery (Bases) → §Topology (link) → 4 disclosure folds.
  §GALLERY ENGINE = Bases (Prytaneion M3.2; DataviewJS retired node-wide). Pairs with: the
  persona_lower-named accent.css (styles the classes below) + what/inventory/curation_gallery.base
  (the §Gallery cards view) + who/curation/ (its source) + what/canvas/topology.canvas.
═════════════════════════════════════════════════════════════════════════ -->

<div class="banner-{{persona_lower}}"><img src="who/assets/banners/{{banner_image}}" alt="{{banner_title}}" onerror="this.style.display='none'"><div class="banner-text banner-text-fallback">{{banner_title}}</div></div>

<div class="landing-strip">
<strong>The aDNA network</strong> — give any project a knowledge architecture that humans <em>and</em> AI agents can navigate. This node is one cell; the network is the living graph of them all.
</div>

```meta-bind-button
label: Visit adna.network  →
icon: globe
id: visit-adna-network
style: primary
action:
  type: open
  link: https://adna.network
```

<span class="landing-secondary">↗ &nbsp;browse the <a href="https://adna.network">context-graph marketplace</a> — <em>coming soon, via the Lighthouse network</em></span>

<div class="dashboard-band">
<span class="db-num"><strong>{{vault_count}}</strong> vaults</span><span class="db-ok">&#10003;&nbsp;{{drift_count}} drift</span><span class="db-ok">&#10003;&nbsp;{{blocked_count}} blocked</span><span class="db-stamp">last full check&nbsp; {{last_health_check}}</span>
</div>

`{{node_hostname}}` · `{{operator}}` · {{persona}} · `{{workspace_root}}` &nbsp;·&nbsp; [[CLAUDE]] · [[MANIFEST]] · [[STATE]] · [[CHANGELOG]]

<div class="{{persona_lower}}-greeting"><em>{{persona_greeting}}</em></div>

---

## Gallery

The portal — one curated card per vault; click a card to open that system. Grouped by archetype, drawn from `who/curation/*.yaml` (Bases-rendered).

![[what/inventory/curation_gallery.base#Gallery]]

<span class="gallery-launch">⤢ &nbsp;Open Full Gallery (full-screen browser viewer) → run&nbsp; <code>python what/code/gallery_viewer.py</code></span>

<!-- §GALLERY ENGINE = Bases (M3.2 operator decision). The cards view lives in
     what/inventory/curation_gallery.base (#Gallery), reading who/curation/*.yaml — the curation
     single source of truth (rebuild: `python what/code/build_curation_cards.py`). The .base file
     carries NO placeholders (node-agnostic) and ships verbatim. DataviewJS was retired node-wide
     (Prytaneion M3.2 Finding A: JS queries default off); the dormant `.curation-card` CSS in the
     accent stylesheet remains usable on any node that re-enables Dataview JS, but Bases is the
     sole shipped engine. -->

## Topology

The spatial twin of the gallery — an Advanced Canvas map of the {{vault_count}}-vault fleet by category over the WHO/WHAT/HOW triad; every vault title is a hotspot into its curation card. Generated by `what/code/build_topology_canvas.py` from `inventory_vaults.yaml`; regenerates as the fleet grows.

> 🗺 **[[what/canvas/topology.canvas|Open the node topology canvas →]]**

---

> [!info]- Node detail — machine · health · tools · memberships
> - **Machine** {{machine_class}} (`{{node_hostname}}`) &nbsp;·&nbsp; **Workspace** `{{workspace_root}}` &nbsp;·&nbsp; **Last inventory refresh** {{last_inventory_refresh}}
> - **Health** — last full check {{last_health_check}}: **{{drift_count}} drift / {{blocked_count}} blocked** across {{healthy_count}} of {{vault_count}} vaults.{{health_detail_note}}
> - **Detail** → [[what/inventory/inventory_system|inventory_system]] (tools + env-var names) · [[what/inventory/inventory_memberships|inventory_memberships]] (LP-network + federation)

<!-- CALLOUT-FOLD PROFILE (M3.4): the all-vaults index is a `> [!abstract]-` disclosure fold,
     NOT free divs/table. The vaults_table var (bare name here — M6.1/F-M6.1-C: a literal token in
     this comment gets substituted INTO the comment) supplies ONLY the body lines, and each emitted
     line MUST be `>`-prefixed (one `> - **<Category> (n)** · [Name](../Name.aDNA/) · …` per aDNA
     class) so it renders INSIDE the callout. A raw <div class="vault-grid"> or a blank-line-bearing
     markdown table breaks the callout open — do NOT use the base/legacy "styled grid" profile here.
     The `../Name.aDNA/` links are fleet-specific: they flow from inventory, never hand-pasted.
     Re-render via skill_inventory_refresh. (Interview wiring for this `>`-prefixed profile is
     proposed upstream — see SUBSTITUTIONS.md + idea_upstream_node_exemplar_template.md.) -->
> [!abstract]- All {{vault_count}} vaults — full text index (regenerated from `inventory_vaults.yaml`, {{last_inventory_refresh}})
{{vaults_table}}

<!-- CALLOUT-FOLD PROFILE (M3.4): same `>`-prefixed-body rule. The named_projects_table var (bare
     name — F-M6.1-C) supplies `> - **<group>** · [name](../path/) · …` lines for named projects +
     external deps + archived (the interview appends external_dep:/archived: groups when inventory
     carries them), callout-body only. Renders `> No named projects on this node yet.` when the
     inventory has none. -->
> [!note]- Non-vault projects & external deps
{{named_projects_table}}

> [!tip]- Operator utilities — personae · node-skills · inventory · maintenance
> - **Personae** · each `.aDNA` vault carries its own persona ({{persona}} is this node's) — browse the §Gallery cards above to reach them.
> - **Node-skills** ({{persona}}'s domain) · [[how/skills/skill_node_health_check|health_check]] · [[how/skills/skill_update_all_vaults|update_all_vaults]] · [[how/skills/skill_inventory_refresh|inventory_refresh]] · [[how/skills/skill_node_credentials_audit|credentials_audit]]
> - **Inventory sources** · [[what/inventory/inventory_vaults|vaults]] · [[what/inventory/inventory_system|system]] · [[what/inventory/inventory_memberships|memberships]]
> - **Maintenance** — on inventory change: (1) `skill_inventory_refresh` rebuilds `inventory_vaults.{md,yaml}`; (2) `python what/code/build_curation_cards.py` regenerates `who/curation/` (the §Gallery reads it directly); (3) commit inventory + cards together. Gallery engine = **Bases** (`curation_gallery.base`), no JavaScript. (Use `python`, not `python3` — the generators need PyYAML.)

---

> *The lattice-home gallery for this node. Role expansion: [[what/decisions/adr_001_node_vault_role_expansion|ADR-001]] &nbsp;·&nbsp; identity + live state: [[MANIFEST]] · [[STATE]].*
