---
type: state_history
created: YYYY-MM-DD
updated: YYYY-MM-DD
status: append_only
last_edited_by: agent_<username>
canonical_router: <Vault>.aDNA/STATE.md
tags: [state_history, immortal_spine, historical_audit]
---

# STATE History — `<Vault>.aDNA/STATE.md`

> **Immortal-spine for this vault's `STATE.md` history.** Aged sections move here — **verbatim, never deleted**
> (Standing Order 6 — archive, never delete) — when they leave the live router. The live router is
> `<Vault>.aDNA/STATE.md`, which carries a `state_history:` frontmatter pointer back to this file.
>
> **Append-only by convention:** new graduation sections land at the **top** under a dated header; existing
> sections are **never edited** (their headings are preserved so cross-references still resolve here). Produced by
> [[skill_state_graduation]] — see that skill for the keep-set rule, the loss-gate, and the two supremacy caveats
> (esp.: **if this vault already has an archive file under a different name — e.g. `STATE_archive.md` — append to
> it and delete this seed; do not run two history files**).

<!--
  SEED USAGE
  1. Fork this file to `<Vault>.aDNA/STATE_history.md` (or, if an archive convention already exists, DELETE this
     seed and append to the existing file instead — skill_state_graduation caveat 5).
  2. Substitute <Vault> and <username>; set created/updated.
  3. Add `state_history: STATE_history.md` to the live STATE.md frontmatter.
  4. Each graduation appends a new dated section BELOW this header, newest first.
-->

---

## Graduated YYYY-MM-DD — <reason (e.g. "Operation <Name> close" / ">100 KB tripwire")>

> Archive-shifted from the router `STATE.md` at <trigger>. Blocks below are **verbatim, never deleted** (SO-6);
> headings preserved so anchors resolve here. Live handoff = `STATE.md` ⏭ QUEUED banner.

<!-- graduated content, byte-for-byte, goes here -->
