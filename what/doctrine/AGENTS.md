---
type: directory_index
created: 2026-06-14
updated: 2026-09-10
last_edited_by: agent_rosetta
tags: [directory_index, doctrine]
doctrine_count: 9   # ⛔ DERIVE, never carry: `ls doctrine_*.md | wc -l` must equal the row count below
---

# AGENTS.md — what/doctrine/

**Workspace-canonical doctrines.** These **9** doctrines bind **by reference** across every `.aDNA` vault on the node (consumer vaults cite them, they carry no copy). Relocated here from the workspace root on 2026-06-14 (Operation Production Tidy P2, ledger §A — clean-root: no loose governance files at `~/aDNA/`).

> ⚠ **Corrected 2026-09-10 while adding the drop-box row, and the correction is the finding.** This index listed **6** rows over a directory holding **8** files — `doctrine_site_voice.md` and `doctrine_web_quality_assessment.md` had never been indexed — and its opening sentence said *"these four"* while the tables below it listed six. ⇒ ***a directory index that is typed rather than derived drifts from its own directory, and the sentence introducing it drifts from the table beneath it.*** Found only because a new row had to be placed. The `doctrine_count:` key above exists so the next drift is one command away from visible; **derive it, never carry it.**

**Safety quartet** — destructive-op and secret-handling discipline:

| Doctrine | Scope |
|---|---|
| `doctrine_credential_handling.md` | credential broker discipline (NAMES-ONLY; Keychain+1P / age backends) — broker = `Home.aDNA` (Hestia) |
| `doctrine_secret_scanning.md` | pre-commit gitleaks + allow-list discipline |
| `doctrine_key_rotation.md` | rotation triggers + procedure (post-leak / doctrine-trigger) |
| `doctrine_safe_mutations.md` | safe destructive-op discipline (ratified `aDNALabs.aDNA/who/governance/adr_011_safe_mutations.md`) |

**Authoring / rendering conventions** (also home here — non-safety authoring doctrines; bind by reference the same way):

| Doctrine | Scope |
|---|---|
| `doctrine_visual_inspection.md` | headless-first visual-inspection tier ladder (T0 `visual_capture.mjs` → T1 `@playwright/mcp` → T2 Chrome MCP) |
| `doctrine_state_conventions.md` | STATE.md posture keys (`mission:` + the `phase:`/`campaigns:` siblings) + the `P<n>[/<count>]` phase-display grammar + the `+adna-normalize-phase` render convention (v8.9) |
| `doctrine_site_voice.md` | the public site's written voice — register, claim discipline, what the surface may assert *(indexed 2026-09-10; on disk and unlisted before that)* |
| `doctrine_web_quality_assessment.md` | how a rendered web surface is assessed and re-scored — the gate/capture/Lighthouse posture *(indexed 2026-09-10; on disk and unlisted before that)* |

**Operational conventions** — cross-vault conduct:

| Doctrine | Scope |
|---|---|
| `doctrine_coordination_dropbox.md` | the coordination drop-box — `who/coordination/inbox/`, the three delivery branches (`F-HOLD-01`), receipts + the stamp-then-RESYNC cure, sweep coverage laws, the **discovery** clause (incl. the `-uall` enumeration trap, reproduced in-tree), and the **replies-owed** law: derive the debt from the correspondence, never read it off a sender-controlled field. Graduated 2026-09-10 from 30 per-vault inbox READMEs |

**Canonical path:** `aDNA.aDNA/what/doctrine/doctrine_*.md` (cite workspace-root-relative, or `~/aDNA/aDNA.aDNA/what/doctrine/...`). **Standard-touch:** the doctrine-home convention is filed upstream as `idea_upstream_doctrine_home` (Production Tidy §H, P8). The operational owner of the credential doctrine remains `Home.aDNA` (Rule 6); these files are the standard's reference home.
